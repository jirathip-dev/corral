SCOPE CORRECTION — serialization fence, 2026-09-01

This file amends .brief.md only where stated. Continue the original #328/#329/#330 contract otherwise.

Observed collision
- Your current uncommitted diff now includes src/main.rs.
- Active #332 owns src/main.rs for Herdr issue-scope discovery/cache wiring.
- Two live lanes may not edit the same file. This is an orchestrator reservation correction, not a product-scope change.

Required action now
1. Finish the current tool call, then inspect `git diff -- src/main.rs`.
2. Remove ONLY your own uncommitted src/main.rs delta by restoring that file to this branch HEAD: `git restore --source=HEAD -- src/main.rs`.
3. Prove `git diff -- src/main.rs` is empty.
4. Continue #328/#329/#330 implementation, focused tests, and evidence in the already-reserved files. Do not edit src/main.rs, src/adapters/gh_plane.rs, or src/api/issues.rs while #332 is active.
5. Design the structured exchange ledger so its module/adapter/API seams and tests are complete without committing a competing main.rs wiring hunk. Do not move the wiring into another #332-owned file or add a private-registry/sidecar workaround.
6. You may commit/push the non-main implementation as a clearly marked partial phase only if tests genuinely pass at that phase. Do not claim #330 production wiring complete.
7. Stop at `SERIALIZATION HOLD — MAIN WIRING PENDING` after the non-overlapping implementation and gates. In .report.md, include the exact minimal src/main.rs wiring still required, but do not edit that file.
8. After #332 merges to integration, the orchestrator will rebase this branch and issue a phase-2 brief for the minimal production wiring, rerun gates, exact-head review, and PR.

Forbidden
- Do not reset/clean the worktree.
- Do not discard any file except your own uncommitted src/main.rs delta named above.
- Do not create a PR or mutate GitHub.
- Do not ask #332 to absorb #330 wiring; ownership remains separate and serialized.

Report the restore proof and continue immediately.