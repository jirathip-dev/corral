# #512 — iOS installed display name is exactly `Corral`

Lane: `impl512-display-name` · branch `impl512-display-name` · base `origin/integration` @
`37203a49320cd7e3dbe669f062163e280314501b`

- Implementation head (all code this report describes): `cc2dc9a2753ac05e29ed95a020c9ff6b124515be`
- Report commit: the direct child of `cc2dc9a` on this branch (report-only; `git log --oneline -1`
  gives the exact final head). Nothing is pushed; no PR was opened; no merge was performed.
- Evidence dir: `/Users/jirathip/.config/fleet-operations/run/corral/evidence/impl512-display-name/`

## What changed (old → new)

| file:line (new) | old value | new value |
| --- | --- | --- |
| `ios/project.yml:84` (was `:81`; +3 comment lines) | `CFBundleDisplayName: "Corral: Agent Fleet"` | `CFBundleDisplayName: "Corral"` |
| `ios/FleetNotifier/Info.plist:8` (generated by XcodeGen) | `<string>Corral: Agent Fleet</string>` | `<string>Corral</string>` |

The owner's private screenshot (`/Users/jirathip/.hermes/cache/images/img_8ce69d4d2853.jpg`) was
inspected privately and shows the Home Screen rendering the truncating label `Corral:Agent…`;
no screenshot bytes were copied into this repo, a PR, a log, or any artifact.

Generated files changed by regeneration: **only `ios/FleetNotifier/Info.plist`**.
`ios/FleetNotifier.xcodeproj/project.pbxproj` is **byte-identical** across regeneration
(SHA-256 `8724cfe44d68e017ef2bd7da64a250de2d3f8e63f165b2b5ae54babfcbbc4509` = `pre-regen-shas.txt` vs
`post-regen-shas.txt`), and a second regeneration at the committed head is idempotent
(`xcodegen-rerun.log`, `head-spec-drift.log`).

## Built product (not a source grep)

`xcodebuild … build -configuration Debug -destination 'generic/platform=iOS Simulator'` built
`/tmp/impl512-dd/Build/Products/Debug-iphonesimulator/FleetNotifier.app`; `plutil -extract … raw`
on the **BUILT** `Info.plist`:

    CFBundleDisplayName = Corral            (exact; extract exit 0)
    CFBundleIdentifier  = com.corral.fleetnotifier   (preserved)
    CFBundleIcons.CFBundlePrimaryIcon.CFBundleIconName = AppIcon   (preserved)
    CFBundleIcons.CFBundleAlternateIcons = Black, Grey, Palomino   (preserved)

## New packaging regression check (must bite)

`ios/tools/herd-art/check-display-name.py` (stdlib only, source mode is Linux/CI-safe) fails closed on:
the generated `ios/FleetNotifier/Info.plist` not being exactly `Corral`; the `ios/project.yml` scalar
not being exactly `Corral` (so a spec-only edit fails before regeneration); a missing/unreadable/duplicate
spec key; an unreadable/missing/non-string plist key. `--bundle <app>` extends the same exact-value
assertion to the BUILT product's `Info.plist` (plus the `com.corral.fleetnotifier` identity).
`ios/tools/herd-art/test-display-name.py` is its `test-native-art.py`-style mutation battery: 23 real-CLI
checks (18 source-only, +5 built-product probes with `--app`) run against disposable copies only,
requiring each mutation to exit 1 with a guard-owned `display-name FAIL:` line and the restored tree to
exit 0 — `test-display-name.log` → `PASS: 23 real CLI checks, disposable mutations only` (exit 0).
It is wired into `.github/workflows/ios-art.yml` (source mode) so it runs per push/PR.

RED/GREEN demonstration (raw exit codes):

- live generated-plist truncation → `display-name FAIL: … found 'Corral: Agent Fleet'`, **exit 1**
  (`live-red-plist.log`); restored byte-identically (`shasum -c` OK) → **exit 0** (`live-green-plist.log`)
- live spec-only truncation (`ios/project.yml` only) → **exit 1** (`live-red-spec.log`);
  restored byte-identically (`shasum -c` OK) → **exit 0** (`live-green-spec.log`)
- battery mutations: truncating / empty / lowercase / trailing-space / non-string / missing / unparseable
  plist, spec truncation / lowercase / trailing space / missing key / duplicate key / missing spec, built
  product truncation / missing key / wrong bundle id — each **exit 1** with the guard's own FAIL line;
  pristine, `--app` bundle-pristine and both restored cases **exit 0** (`display-name-proofs*/results.json`).

## Commands, named logs, raw exits

| command (exact) | log | raw exit |
| --- | --- | --- |
| `( cd ios && xcodegen generate --spec project.yml )` | `xcodegen.log` | 0 |
| `git diff --stat` (post-regen) | `regenerated-diff.log`, `git-diff-stat-final.log` | 0 |
| `flock /tmp/corral-heavy-gate.lock xcodebuild -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier build -configuration Debug -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/impl512-dd CODE_SIGNING_ALLOWED=NO` | `build.log` | 0 |
| `plutil -p "$APP/Info.plist"` + `plutil -extract CFBundleDisplayName raw` | `built-info-plist.log` | 0 / 0 |
| `python3 ios/tools/herd-art/test-native-art.py` (as literally listed; requires `--app`/`--output`) | `native-art-test-noargs.log` | 2 (usage error) |
| `flock /tmp/corral-heavy-gate.lock python3 ios/tools/herd-art/test-native-art.py --app "$APP" --output "$E/native-art-proofs"` (documented args) | `native-art-test.log` | 0 (37 checks) |
| `python3 ios/tools/herd-art/check-native-art.py --help` | `native-art-help.log` | 0 |
| `python3 ios/tools/herd-art/check-native-art.py` | `check-native-art-source.log` | 0 |
| `python3 ios/tools/herd-art/check-native-art.py --bundle "$APP"` | `check-native-art-bundle.log` | 0 |
| `python3 ios/tools/herd-art/app-icons.py --check` | `app-icons-check.log` | 0 |
| `python3 ios/tools/herd-art/test-app-icons.py --output "$E/app-icon-proofs"` | `app-icons-battery.log` | 0 (36 checks) |
| `python3 ios/check-release-demo.py` | `check-release-demo.log` | 0 |
| `python3 ios/tools/herd-art/check-display-name.py` | `check-display-name-source.log` | 0 |
| `python3 ios/tools/herd-art/check-display-name.py --bundle "$APP"` | `check-display-name-bundle.log` | 0 |
| `python3 ios/tools/herd-art/test-display-name.py --output "$E/display-name-proofs-source"` | `test-display-name-source.log` | 0 (18 checks) |
| `python3 ios/tools/herd-art/test-display-name.py --app "$APP" --output "$E/display-name-proofs"` | `test-display-name.log` | 0 (23 checks) |
| `flock /tmp/corral-heavy-gate.lock xcodebuild -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier test -configuration Debug -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/impl512-dd -only-testing:FleetNotifierTests` | `ios-tests.log` | 0 — `Executed 505 tests, with 0 failures`, `** TEST SUCCEEDED **` |
| `( cd ios && xcodegen generate --spec project.yml ) && git diff --exit-code -- ios/` | `head-spec-drift.log` | 0 |
| committed-head re-runs: `check-display-name.py`, `test-display-name.py` | `head-check-display-name.log`, `head-test-display-name.log` | 0 / 0 (18 checks) |

Simulator note: the requested destination UDID was resolved by this host's Hermes terminal harness to its
task-owned simulator — the UDID **actually used** is `9B52BE04-B7FC-4AD0-B974-3105A1113971`
(device `Hermes task 95181-cf8f8dbefb47`), recorded in `ios-tests.log:2` (`hermes-sim-task: running on
9B52BE04-…`).

## Preservation / non-regression

- Bundle id `com.corral.fleetnotifier`, `CODE_SIGN_STYLE`/team/entitlements, `Assets.xcassets`, app data,
  pairing state: untouched — `git diff --name-only` contains no entitlements, asset, or `.xcodeproj`
  path, and the built product still carries the approved icon declarations (above) with
  `check-native-art.py --bundle` exit 0.
- Agent notification titles (`agent · repo`, e.g. `orch-herdr-fleet`) and the per-`(host, agent)`
  thread identifier are untouched (`ios/FleetNotifier/Notifications/` has no diff; the title comes from
  the daemon payload, not the app name). No source code sets the notification group application label;
  iOS renders it from the installed display name, so new notifications follow `Corral`.

## Not verified (explicitly not claimed)

- Rendered Home Screen label **on a physical device** and the notification-group application label
  **on a device**: physical acceptance, conductor-owned. Only the built `Info.plist` value those
  surfaces render from was verified.
- Release/TestFlight/App Store Connect acceptance: not claimed (`fastlane/Fastfile` keeps the ASC app
  record name `Corral: Agent Fleet` — that is the App Store Connect app name, not the installed display
  name, and is release-owned; deliberately unchanged).
- CI was not dispatched/pushed (orchestrator owns publishing); the new CI steps' commands were run
  locally and passed.
- Anti-slop Swift lint (advisory in `ios.yml`) not run: no Swift source changed.

## Docs

`docs: updated .github/workflows/ios-art.yml, docs/DEVELOPING.md, ios/tools/herd-art/README.md`
(CI wiring + the two documented invocations and the guard's contract).
