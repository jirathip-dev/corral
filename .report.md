# #545 bounded background sync feasibility — 2026-09-15

## Inventory at the pinned base (before any app change)

Base: `2efa3577086a794ad064898d8af9d69b33a62325`, integration after both
#547 (`eafebfb`) and #548 (`2efa357`). The brief incorrectly calls `2efa357`
the #547 merge; it is #548's merge, containing #547. The base itself is correct.
Issue body and all comments were read with the brief's two exact `gh issue view`
commands. No PR/merge, other issue, daemon, notification implementation, service,
credential or release work is authorized or performed here.

| Candidate already in flight at background entry | Finite? | Existing apply/persist path | Safe/useful without streaming under current contract? |
| --- | --- | --- | --- |
| Active `/host-key` attempt (AppModel.swift:1255-1331; CorraldClient.swift:123-130) | Individual GET yes, timeout 15 s; owner is a `while true` retry ladder, not finite. GET is unsigned, contrary to the brief's candidate wording. | Key match calls `verifyHostKey`, stamps last successful connection, then `startLive`; mismatch fails closed. No new board snapshot is fetched by the key response. | Not independently reusable: success starts live work; failure retries. Background disconnect closes/discards the speculative inbox and re-arms trust. Retaining the owner would violate no retries/no SSE and would not supply new board data after inbox disposal. |
| Coordinator host preflight (HostStreamCoordinator.swift:377-466) | Same finite attempt inside unbounded owner; one per peer. | Verification drains inbox and calls `openStream`, whose connected callbacks can trigger grants/token work. | Not an app-wide finite work item; retaining the whole operation admits per-host fanout and new streaming. Current `stopAll` cancels it. |
| `refreshGrants()` (AppModel.swift:2528-2659) | One signed `/grants-read` yes; outer profile iteration can launch additional hosts. Already-started requests are in `lifecycleTasks`, cancelled at background. | In-memory grants plus `HostProfileStore.applyGrants` and active `DeviceMeta` mirror (grant set and expiry). | Can finish without a stream technically, but not board metadata/cursor progress or first authoritative board frame; persists permission/device state outside this experiment's board-only allowance. Retaining the outer operation permits new multi-host work. |
| Active pull `refreshFleet()` (AppModel.swift:3075-3133) | Yes, identifiable single coalesced `/snapshot`; not an automatic foreground snapshot in this base. The active task is a local handle, NOT in `lifecycleTasks`; scene teardown does not cancel it. | `applyRefresh` -> `persistBoardMetadata` -> `reconnectIfNeeded`. Cursor is updated by accepted application; per-profile cursor+epoch is persisted at teardown. | Closest useful candidate in foreground, but background re-arms `awaitingHostVerification`; both apply (FleetStore.swift:537) and reconnect (:896) reject it. Finishing alone cannot update the retained pinned-host board. Keeping verification or inbox alive requires a different scoped lifecycle design, not merely wrapping `stopLive` in an assertion. |
| Coordinator pull refresh (HostStreamCoordinator.swift:623-678) | One finite GET per host, launched concurrently by `refreshAll`. | Applies/persists per-host metadata; if still verifying it can launch a new preflight. | `stopAll` cancels owned refresh tasks. Retaining the wrapper entails fanout and possible new ladder/stream; no safe reuse unchanged. |
| Device-token upload (AppModel.swift:974-993; AppDelegate.swift:208-267, read only) | Individual signed registration yes. | Coordinator upload tracks enrolled token in memory; active delegate uses `IdentityLifecycle.launch`, token dedupe state and an upload-fact default. | Notification ownership belongs to #546; remote token enrollment is not board metadata sync. Not a board return-latency mechanism. No token upload is run for this lane. |

The finite snapshot candidate is real and potentially useful after a separately
reviewed redesign; this report does NOT claim finite work is mathematically
impossible. No candidate qualifies for **unchanged reuse** under all constraints.
The bounded feasibility choice is to measure the closest candidate, keep the
existing production trust/teardown behavior, and stop before speculative product
background-task code or physical adoption.

## GO / NO-GO

**NO-GO — keep immediate disconnect; no background assertion is shipped.**
No physical iPhone is available (`xcrun devicectl list devices`, exit 0,
`No devices found.`, `/tmp/g545-devices.log`). Repeatable short-switch benefit
cannot be established here. Independent review and physical acceptance remain
owner/orchestrator gates, not completed work.

Retain the change only with repeatable short-switch benefit and no
security/lifecycle regression. Here “change” means a future background-completion
behavior, not the characterization test or empty device-results protocol.
The five-second application cap, one assertion per app, injected OS refusal,
expiry/exactly-once ending and retained-resource generation design are deliberately
NOT implemented or claimed. No idle keepalive, new background request or production
behavior change is introduced just to satisfy the issue.

## Scope and deterministic evidence

- One test in the existing `ForegroundReconnectTests` class, using its existing
  pinned-profile URLProtocol/gate fixture and the same production
  `handleScenePhaseChange(.background)` seam; `ScenePhaseLifecycleTests` is run
  alongside it. No duplicate provider/class/file or production lifecycle fork.
- The new test's foreground positive control fetches/applies revision 8. It then
  holds revision 9 already on the wire, enters background, releases completion,
  and checks retained revision/title, cursor+epoch, pending key posture, no new
  stream or preflight and cleared refresh indicator. This is characterization of
  existing code, expected GREEN at base, not a claimed bug fix or speedup.
- `ios/check-release-demo.py`: digest/comment-only change; app digest recomputed
  unchanged, test digest re-pinned. No checker weakening.
- `docs/evidence/issue-545/device-protocol.md`: exact owner-only future physical
  comparison, controls, sample counts, separate visual/authoritative endpoints,
  host timing/load, lifetime checks and empty results templates.
- Existing assertions changed: NONE (assertion ledger is empty).
- No app Swift files, generated project, notification files or other fenced
  surfaces changed. Inactive behavior and privacy are byte-unchanged.

## Verification commands and raw results

Working directory for all implementation gates:
`/Users/jirathip/.herdr/worktrees/corral/impl545-bg-sync`.
No tracked `AGENTS.md`, `CLAUDE.md`, `justfile` or `Justfile` exists at this
base; the brief and `ios/README.md` supply the native runner. No recipe invented.
All xcodebuild commands use the requested iPhone 16 simulator
`59DDC0C5-891E-4EC0-91AF-4F50DF68D793`, iOS 26.5. No physical execution.

The commands below were redirected to named logs, not piped through a display
filter. `/tmp/g545-deadline.py` supervises the lane-owned child process group:
900 s for G1; 1800 s for the G2-G4 sequence; 600 s for the advisory scan.
Raw child exits are in the status logs. The process tool reported the full-gate
wrapper as exited while PID 19400 and its xcodebuild child were still running;
that status was NOT treated as completion. A bounded status-file wait obtained
the actual final raw exits. No gate was re-run based on the misleading status.

G1 (`/tmp/g545-focused.sh`):

```sh
flock /tmp/n.lock env HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 \
  xcodebuild test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier \
  -destination "platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793" \
  -derivedDataPath /tmp/g545-dd \
  -only-testing:FleetNotifierTests/ScenePhaseLifecycleTests \
  -only-testing:FleetNotifierTests/ForegroundReconnectTests > /tmp/g545-focused.log 2>&1
```

Raw exit 0 (`/tmp/g545-focused-status.log`: `FOCUSED_EXIT=0`, `CHILD_EXIT=0`).
Actual output: `Executed 21 tests, with 0 failures (0 unexpected)`;
`** TEST SUCCEEDED **`. The new finite-completion test passed (0.030 seconds,
NOT a latency benchmark). The existing extended class replaces the brief's
`<YourNewClass>` placeholder; a redundant new class was not needed.

G2-G4 (`/tmp/g545-validation.sh`, status `/tmp/g545-validation-status.log`):

```sh
flock /tmp/n.lock env HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 \
  xcodebuild test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier \
  -destination "platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793" \
  -derivedDataPath /tmp/g545-dd -only-testing:FleetNotifierTests \
  > /tmp/g545-full.log 2>&1; echo FULL_EXIT=$?
python3 ios/check-release-demo.py > /tmp/g545-check-release.log 2>&1; echo CHECK_RELEASE_EXIT=$?
python3 ios/check-release-demo.py --self-test > /tmp/g545-self-test.log 2>&1; echo SELF_TEST_EXIT=$?
flock /tmp/n.lock env HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 \
  xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -configuration Debug \
  -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/g545-debug-dd \
  > /tmp/g545-debug.log 2>&1; echo DEBUG_EXIT=$?
flock /tmp/n.lock env HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 \
  xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -configuration Release \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/g545-release-dd CODE_SIGNING_ALLOWED=NO \
  > /tmp/g545-release.log 2>&1; echo RELEASE_EXIT=$?
python3 ios/check-release-demo.py --binary \
  /tmp/g545-release-dd/Build/Products/Release-iphonesimulator/FleetNotifier.app/FleetNotifier \
  > /tmp/g545-binary.log 2>&1; echo BINARY_EXIT=$?
```

Actual raw results: `FULL_EXIT=0`, `CHECK_RELEASE_EXIT=0`, `SELF_TEST_EXIT=0`,
`DEBUG_EXIT=0`, `RELEASE_EXIT=0`, `BINARY_EXIT=0`, supervisor `CHILD_EXIT=0`.
Full suite: `Executed 631 tests, with 0 failures (0 unexpected) in 146.702
(146.951) seconds`; `** TEST SUCCEEDED **`. The brief's 181-test base count
is stale, not a filter or skipped-test target. Both builds report
`** BUILD SUCCEEDED **`. Source and self-test report
`release-demo check: PASS (Debug source preserved; Release boundary verified)`.
Binary check additionally reports `bundle PASS: Mach-O ...; 6 product files`.
No claim that Debug alone proves the Release boundary.

G5 (after source commit, so intended source edits cannot fake generator drift):

```sh
(cd ios && xcodegen generate --spec project.yml) > /tmp/g545-xcodegen.log 2>&1
 echo XCODEGEN_EXIT=$?
git diff --exit-code -- ios/ > /tmp/g545-xcodegen-diff.log 2>&1
 echo XCODEGEN_DIFF_EXIT=$?
```

Raw `XCODEGEN_EXIT=0`, `XCODEGEN_DIFF_EXIT=0`; generated project unchanged.

G6 (advisory, not falsely labelled a clean lint pass):

```sh
swift run --package-path ios/tools/anti-slop-swift anti-slop \
  ios/FleetNotifier ios/FleetNotifierTests > /tmp/g545-aslop-head.log 2>&1
```

Raw child exit **1**: `[anti-slop] 24 violations across 53 files (17 rules)`.
The exact identity/count comparison against the pinned base runs in G8's scratch
worktree with the original base test bytes and byte-identical production files.
The scanner/tool sources themselves are unchanged. Its results are recorded
with the probe outcome below; no baseline/suppression/security assertion was
weakened to silence findings.

G7:

```sh
git diff --check > /tmp/g545-diffcheck.log 2>&1; echo DIFFCHECK_EXIT=$?
```

Raw `DIFFCHECK_EXIT=0`. Closeout checks also returned 0:
`git diff --check` (`/tmp/g545-final-diffcheck.log`),
`git diff --check 2efa357 HEAD` (`/tmp/g545-range-diffcheck.log`), and
`git diff --exit-code f72f23de5b491b44869db87f6d069a20b9b806a6 -- ios/`
(`/tmp/g545-tested-source-diff.log`). The tested iOS source is unchanged.

## Discrimination and deferred assertion experiment

G8 ran from the committed test/source head
`f72f23de5b491b44869db87f6d069a20b9b806a6` in a NEW detached
`/tmp/g545-scratch` created by `git worktree add`. Only evidence/report changes
may follow that source head. Driver: `docs/evidence/issue-545/probe-background.py`.
It breaks the existing apply and reconnect guards independently, requires
compiled XCTest assertion failures, restores source bytes with printed SHA-256
before/after, and finishes with the pristine focused classes on the untouched
implementation tree. No product assertion provider/expiry guard was added, so
there is no invented mutation proof for that deferred design.

Invocation (1800 s supervising deadline; each child test also capped at 900 s):

```sh
bash /tmp/g545-probes.sh > /tmp/g545-probes.log 2>&1; echo PROBES_EXIT=$?
# g545-probes.sh runs:
flock /tmp/n.lock python3 docs/evidence/issue-545/probe-background.py
```

Raw driver exit 0; outer child exit 0. `PROBE_BATTERY=PASS`.
Each mutation used this exact command from `/tmp/g545-scratch` while the driver
held `/tmp/n.lock` across mutation, run and restore:

```sh
env HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 \
  xcodebuild test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier \
  -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' \
  -derivedDataPath /tmp/g545-probe-dd \
  -only-testing:FleetNotifierTests/ForegroundReconnectTests/testFiniteSnapshotCompletionAcrossBackgroundHasNoRetainedBoardBenefit
```

| Probe | Intentional break | Raw exit / actual assertion result | Log |
| --- | --- | --- | --- |
| M1 | Remove only `apply(withoutDiff:)`'s `!awaitingHostVerification` guard | 65; 1 test executed, 2 failures: revision 9 != 8; title revision 9 != revision 8 | `/tmp/g545-m1-apply.log` |
| M2 | Remove only `reconnectIfNeeded`'s `!awaitingHostVerification` guard | 65; 1 test executed, 3 failures: unbounded stream exists, connecting != disconnected, events request count 2 != 1 | `/tmp/g545-m2-reconnect.log` |
| Pristine GREEN | Unmodified implementation tree, G1's exact two selectors and `/tmp/g545-dd` | 0; 21 tests, 0 failures; `** TEST SUCCEEDED **` | `/tmp/g545-pristine-green.log` |

These are intentional mutant failures, not reviewer FAIL verdicts or baseline
failures. No compile error, missing-test selector, skipped test or crash is used
as discrimination. M1 proves the held response actually reaches application
when the guard is broken; M2 proves mere completion would restart unbounded
transport without its separate guard. This is why keeping a task handle or
adding time alone is not a demonstrated board-sync improvement.

`shasum -a 256` before AND after each mutation:
`FleetStore.swift = fa5d3907aee207a0a26e7db22a55d2f759caed376f616db769686547ea01c902`.
Both restores printed `byte-identical`; scratch `git diff --exit-code` was 0.
The temporary base-test substitution for G6 also restored byte-identically:
`2d1cfded2067fa9022e1b77afd0e28e66919f5e09c47e0a2a74d4ac7285f7498`.

G6 base invocation from scratch (reuses the identical scanner package/build,
not a different lint rule set):

```sh
swift run --package-path /Users/jirathip/.herdr/worktrees/corral/impl545-bg-sync/ios/tools/anti-slop-swift \
  anti-slop ios/FleetNotifier ios/FleetNotifierTests > /tmp/g545-aslop-base.log 2>&1
```

Base exit 1; head exit 1; identity multiset comparison (path, rule, diagnostic
text; line-number independent, multiplicity retained): **ADDED = [], REMOVED = []**.
Both total 24 findings. Per-rule base = head: `no-force-unwrap` 18,
`no-swallowed-errors` 1, `no-shape-in-symbol-names` 4, `no-force-try` 1.
Machine-readable comparison: `/tmp/g545-aslop-identity.json`. No added finding.
A committed-head audit repeated the exact head scanner command into
`/tmp/g545-aslop-committed.log`: raw exit 1, again 24 findings, added/removed
identity multisets empty against base. No app/test/checker bytes changed after
that committed-head audit; only the report and verification manifest followed.

## Structural discovery receipts

The first four outlines returned raw exit 0; exact commands (worktree root):

```sh
ast-grep outline ios/FleetNotifier/App/AppModel.swift --view expanded
ast-grep outline ios/FleetNotifier/App/FleetStore.swift --view expanded
ast-grep outline ios/FleetNotifier/Profiles/HostStreamCoordinator.swift --view expanded
ast-grep outline ios/FleetNotifierTests/FleetNotifierTests.swift --match 'ScenePhaseLifecycleTests|Reconnect|Epoch|KeyContinuity|Refresh' --view expanded
ast-grep run --lang swift --pattern '$OBJ.fetchSnapshot()' ios/FleetNotifier/App/AppModel.swift ios/FleetNotifier/Profiles/HostStreamCoordinator.swift
ast-grep run --lang swift --pattern 'fetchHostKey' ios/FleetNotifier/Network/CorraldClient.swift
ast-grep outline ios/FleetNotifier/Notifications/AppDelegate.swift --view expanded
ast-grep outline ios/FleetNotifierTests/FleetNotifierTests.swift --match 'DriveRequestGate|DeterministicDriveScript|DeterministicDriveURLProtocol' --view expanded
ast-grep run --lang swift --pattern '$OBJ.handleScenePhaseChange($ARG)' ios/FleetNotifier/App/FleetNotifierApp.swift
```

The first four maps are `/tmp/g545-{app,store,coordinator,test}-outline.log`.
They located `startLive` 2684, `stopLive` 2842, scene handler 2911, refresh 3075;
`disconnect` 1071; coordinator `stopAll` 549 and `refreshAll` 624;
scene test class 13639 and reconnect class 14671. Focused snapshot-call query
returned AppModel lines 3112/3970 and coordinator 636; host-key declaration at
CorraldClient 123. Scene call query exited 0, exactly one match:
`FleetNotifierApp.swift:153: model.handleScenePhaseChange(phase)`
(`/tmp/g545-scene-wiring.log`). No repository-wide structural scan was needed.
The Notifications file was READ ONLY for candidate ownership inventory.

## Evidence inventory, limitations and handoff

- Committed: this report, physical protocol, scratch probe driver, the new test
  in the existing test file, and digest-only checker update. Log fingerprints and
  selected raw receipts are in `docs/evidence/issue-545/verification.json`.
- Local raw gate logs: every `/tmp/g545-*.log` named above. Native test bundles are
  under `/tmp/g545-dd/Logs/Test/`; mutation bundles under
  `/tmp/g545-probe-dd/Logs/Test/`. Debug/Release apps live in the requested
  `/tmp/g545-debug-dd` and `/tmp/g545-release-dd`. The pristine scratch worktree
  is left for audit, not merged, pushed or used as the product build.
- Existing simulator native-row/timing tests ran with the selected classes and
  full suite. Their controlled fixture timings are not physical return latency.
  No physical results were entered into the protocol and no speedup is claimed.
- The new test covers a VERIFIED PINNED active host and a same-epoch finite pull.
  It does not prove every legacy/unpinned asynchronous path cancels at background.
  In particular the active snapshot local task is not owned by `stopLive`; the
  pinned-host trust gate, not cancellation of that request, protects this result.
  This is explicitly not a claim of universal background-task cleanup.
- Existing suites cover inactive retention, actual-background stream/ladder
  teardown, key mismatch, host removal, epoch reset, old stream generation,
  rapid foreground return, slow-key buffering expiry and independent healthy
  peers. They are not OS background-assertion lifetime/refusal/expiry tests.
- Deferred: any assertion provider/design, completion-owned persistence and
  generation changes, OS refusal/expiration/five-second cap proof, real app
  switch and Wi-Fi/cellular measurements, CPU/network measurements on iPhone,
  independent review and owner adoption. None is presumed successful.
- Worktree hygiene exception: `.hermes-context.md` was UNTRACKED on entry and is
  the user/orchestrator-provided instruction file. It is preserved untouched,
  not committed, deleted or hidden via a shared ignore/config change outside
  the file fence. Therefore raw `git status --porcelain` cannot be claimed empty;
  after delivery it should contain only `?? .hermes-context.md`. Tracked work is
  committed. This is an explicit deviation from the empty-status requirement.
- No PR, merge, deployment, notification dispatch, host/service/credential change
  or other issue work. Only the authorized feature branch is to be pushed.
