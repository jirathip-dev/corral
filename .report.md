# .report.md — corral #361: iOS recents — one continuous raw-output rail (V2)

Worktree: `/Users/jirathip/.herdr/worktrees/corral/g361-ios-recents-rail`
Branch: `g361-ios-recents-rail` (exact base `ff655b3951f0fb0b328241fb0a93602dd02589d3`
= origin/integration)
Issue: https://github.com/jirathip-dev/corral/issues/361 (AMENDED 09-02 by Guy — DESIGN LOCK;
#271 V2 speaker-rail look + #316 RATIONALE Variant 1)
PR: https://github.com/jirathip-dev/corral/pull/366
Initial head: `645b4186f9d9c84c9c3db88c7a3cc5e795dc2c05` (reviewed; PASS except R1 below)
R1 head: `753bee3` (continuous spine fix; + this report commit)

## What changed (initial implementation, 6 files, ios/ only)

1. `RecentOutputModel.swift` — divider-only rows dropped from the row model BEFORE adjacent
   tool/system merging (`tailRows`), so zero divider rows exist in the model or rendering;
   new rail API (`railRows`, `RailRow.showsTransitionMarker`, `marker(for:)`:
   Assistant=circle, You=diamond, Tool=square; system/unknown = nil); dead timestamp /
   tool-summary / disclosure helpers removed.
2. `FleetViews.swift` — `RecentRailRowView` replaces `RecentBlockRow`: no divider rules, no
   role labels, no user bubble, no tool disclosure cards, no code/diff card containers;
   transition markers in the left gutter carry the locked role tokens (accent circle,
   working-blue #58A6FF diamond, gold #D29922 square); code/diff keeps inline syntax colors
   + numbered gutters; accessible roles kept via row labels; live tail, auto-scroll, live
   indicator, deep link, offline/refresh unchanged.
3. `FleetNotifierTests.swift` — `RecentRailModelTests` (zero divider rows incl. legacy-lines
   path, divider never rides inside merged content, full chronological order, transition-only
   markers, no-marker continuation/system/unknown rows, locked markers, id uniqueness) +
   slice-scoped wiring tests (railRows callsite, renderer marker gate, chrome absence).
4. `DemoFleet.swift` / `README.md` — divider-only demo block kept as rail-drop evidence;
   docs sync.
5. `check-release-demo.py` — `APPROVED_RELEASE_SOURCE_DIGEST` +
   `APPROVED_TEST_SOURCE_DIGEST` re-pinned (dated comments) for the #361 source set.

## Gates (initial head 645b418 — real runs)

- Focused (RecentRailModelTests/RecentOutputTailModelTests/ReadOnlySurfaceWiringTests/
  DemoSeedTests): Executed 26 tests, 0 failures.
- Full iOS suite: Executed 109 tests, 0 failures.
- anti-slop: 24 pre-existing violations, ZERO new (region-verified).
- Release build (Release/iphonesimulator) EXIT 0; `check-release-demo.py --binary` PASS;
  `--self-test` PASS.
- RED/GREEN probes (5): divider reintro → 9 failures; per-row markers → 2; view bypass →
  RED; bypass + decoy → still RED (slice-scoped); renderer marker-gate drop → RED. All
  restores byte-identical (sha-verified).
- Evidence: `/tmp/g361-rail-evidence2-390x844.png` (native iPhone 16 393x852, scaled) —
  pixel/vision verified: zero divider rows, zero role text, markers at transitions only,
  no cards.

## R1 section (reviewer finding + fix)

Fresh independent review of `645b418` PASSED everything except one design-lock gap:

- FINDING (verified live): the amended AC's CONTINUOUS RAIL/SPINE was absent — only
  disconnected transition markers rendered (no spine primitive; markers float in the gutter;
  no test pinned a spine). Per #271 V2 and RATIONALE V1, markers must sit ON a continuous
  vertical spine spanning the sheet.

FIX (iOS recents renderer only, everything else unchanged):
- `FleetViews.swift`: new `RecentRailSpine` primitive — one `Rectangle` (1.5pt,
  `RecentOutputPalette.railLine` = #30363d, the #316 evidence gutter line token) with
  `maxHeight: .infinity`, attached as a single `.background` BEHIND the whole row stack in
  `tailStream` (3.75pt leading inset centers it under the 9pt gutter markers), so the rail
  is one uninterrupted line over the full content — never per-row segments. Transition
  markers sit ON the spine; continuation/system/unknown rows ride it with no marker; hollow
  markers get a bg-circle mask so the rail appears to touch each node.
- Pinning tests (`ReadOnlySurfaceWiringTests`, slice-scoped):
  - sheet slice must contain `RecentRailSpine()` (the sheet rides ONE spine);
  - `testRailSpineIsOneContinuousSpan`: the primitive slice must contain `Rectangle()`,
    `.frame(width: 1.5)`, `maxHeight: .infinity`, `RecentOutputPalette.railLine` — a
    regression to per-row segments or a missing spine fails here.
- Digest pins re-pinned for the R1 source set.

GATES (R1 head, real runs under /tmp/corral-heavy-gate.lock):
- Focused: 27 tests, 0 failures; full iOS suite: 110 tests, 0 failures.
- anti-slop: zero new diagnostics (24 pre-existing).
- Release build EXIT 0; `check-release-demo.py --binary` PASS; `--self-test` PASS.
- RED probe (R1): removing the `RecentRailSpine()` background from `tailStream` → sheet
  wiring test RED; byte-identical restore → GREEN.
- Refreshed 390x844 evidence (synthetic-only): `/tmp/g361-rail-evidence3-390x844.png`
  (native `/tmp/g361-rail-evidence3-raw.png`) — vision-verified continuous TEAL-TINTED
  spine in the far-left gutter, unbroken from the first content marker to the last row,
  all markers (teal circle / blue diamond / gold square) centered ON the spine,
  continuation/system/unknown rows ride it with no marker, zero divider rows, zero role
  text, no cards.

No daemon/schema/egui/docs-root changes in either round. Not verified in-lane: hosted CI,
pull gesture, physical device, 390x844-native device geometry (host sim is iPhone 16).
