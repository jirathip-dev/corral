# Corral #379 — iOS Settings cleanup + How to connect

Branch: g379-ios-settings-connect
Worktree: /Users/jirathip/.herdr/worktrees/corral/g379-ios-settings-connect
Exact base: bea75dd0f5bf1331f16e8eeaf85343b1e4b14e09
Implementation head before this report: 455e65776f1fff26f6ada38e809bce976f60bb70
Lane date: 2026-09-04

This report replaces the inherited #376 report. The implementation stayed in the #379 iOS Settings/connect scope; no issue, PR, merge, TestFlight, or daemon mutation was performed.

## What changed

- ios/FleetNotifier/UI/FleetViews.swift
  - Added the Settings Device identity read-out: Key ID, Key storage/Keychain state, Read-only signed device, paired state, and Remove device action.
  - Removed the Settings grants list, device-name row, standalone Reset section, and stale capability names from the Settings surface.
  - Kept the Connection host/token/register flow and Notifications global toggle unchanged.
  - Added the Settings-header `?` Help entry and shared HowToConnectSheet.
  - Added the five numbered connection steps, healthz check, README Setup link, and disabled-until-set Copy host control.
  - Added release-active one-shot auto-presentation of the shared sheet when the device is unpaired on first launch.
  - Added DEBUG-only marker-driven simulator evidence for the unpaired auto-present, Settings Device section, and connect-sheet states. No Liquid Glass/translucency was added, per the scope adjudication.

- ios/FleetNotifier/App/FleetNotifierApp.swift
  - Added DEBUG-only `-corralDemoConnectEvidence` launch dispatch.
  - The evidence route resets local identity before launch so the real unpaired auto-present path is exercised.

- ios/FleetNotifierTests/FleetNotifierTests.swift
  - Updated Settings source-wiring pins for the post-cut Device surface.
  - Added SettingsConnectWiringTests for grants/stale-capability absence, Device identity rows/order, the release-active `?` entry, one-shot unpaired auto-present, five numbered steps, Copy host, and README Setup link.
  - Missing-opener line accesses use XCTUnwrap so mutation probes fail as XCTest failures rather than crashing.

- ios/check-release-demo.py
  - Re-pinned the Release source and test-source SHA-256 markers after the #379 source/test changes, with dated #379 comments.

- .report.md
  - Replaced the inherited #376 report with this #379 report.

No README or Xcode project source-list change was needed. The existing README Setup link target from #376 was consumed, not edited.

## RED/GREEN discrimination

Mutation driver: `bash /tmp/g379-run-probes.sh > /tmp/g379-probes-delivered.log 2>&1; echo PROBES_DELIVERED_EXIT=$?`

The driver ran against a disposable `/tmp/g379-scratch` worktree at implementation HEAD 455e65776f1fff26f6ada38e809bce976f60bb70. The implementation worktree was never mutated. Every probe restored `ios/FleetNotifier/UI/FleetViews.swift` byte-identically.

- P1 — resurrect Grants row: RED, `PROBE_P1_EXIT=65`; restored byte-identical.
- P2 — resurrect Reset section: RED, `PROBE_P2_EXIT=65`; restored byte-identical.
- P3 — remove Read-only signed device label: RED, `PROBE_P3_EXIT=65`; restored byte-identical.
- P4 — remove paired/registration State row: RED, `PROBE_P4_EXIT=65`; restored byte-identical.
- P5 — remove Settings-header `?` entry: RED, `PROBE_P5_EXIT=65`; restored byte-identical.
- P6 — remove unpaired-launch auto-present: RED, `PROBE_P6_EXIT=65`; restored byte-identical.
- P7 — remove README Setup link: RED, `PROBE_P7_EXIT=65`; restored byte-identical.
- P8 — remove Copy host control: RED, `PROBE_P8_EXIT=65`; restored byte-identical.
- Pristine final battery: GREEN, `FINALGREEN_EXIT=0`, 4 tests with 0 failures.
- Driver completion: `PROBES_DELIVERED_EXIT=0`, `ALL_PROBES_DONE`.

The final scratch worktree was removed after verification.

## Gates

All Xcode and simulator gates below were serialized with `flock /tmp/corral-heavy-gate.lock`. The simulator-backed commands used `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1`.

Focused Settings/connect gate (final delivered tree):

`HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination "platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793" -derivedDataPath /tmp/g379-focused-final-dd -only-testing:FleetNotifierTests/SettingsConnectWiringTests -only-testing:FleetNotifierTests/SettingsAccessWiringTests -only-testing:FleetNotifierTests/ThemeWiringTests`

- Raw exit: `FOCUSED_EXIT=0`.
- Result: 13 tests, 0 failures; `** TEST SUCCEEDED **`.
- Log: `/tmp/g379-focused-final.log`.

Debug compile gate (final delivered tree):

`HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /tmp/g379-final-debug-dd`

- Raw exit: `DEBUG_BUILD_EXIT=0`.
- Result: `** BUILD SUCCEEDED **`.
- Log: `/tmp/g379-delivered-debug-build.log`.

Full iOS simulator suite (final delivered tree):

`HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination "platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793" -derivedDataPath /tmp/g379-final-debug-dd -only-testing:FleetNotifierTests`

- Raw exit: `FULL_SUITE_EXIT=0`.
- Result: 181 tests, 0 failures; `** TEST SUCCEEDED **`.
- Log: `/tmp/g379-delivered-fullsuite.log`.

Release source check and self-test:

- `python3 ios/check-release-demo.py` → raw exit 0; `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- `python3 ios/check-release-demo.py --self-test` → raw exit 0; `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- Logs: `/tmp/g379-final-source-check.log`, `/tmp/g379-final-self-test.log`.

Release build:

`HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -configuration Release -destination "generic/platform=iOS Simulator" CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/g379-final-release-dd`

- Raw exit: `RELEASE_BUILD_EXIT=0`.
- Result: `** BUILD SUCCEEDED **`.
- Log: `/tmp/g379-final-release-build.log`.

Release binary check:

`python3 ios/check-release-demo.py --binary /tmp/g379-final-release-dd/Build/Products/Release-iphonesimulator/FleetNotifier.app/FleetNotifier`

- Raw exit: `BINARY_CHECK_EXIT=0`.
- Result: `release-demo check: PASS (Debug source preserved; Release boundary verified)` and `release-demo check: inspected /tmp/g379-final-release-dd/Build/Products/Release-iphonesimulator/FleetNotifier.app/FleetNotifier`.
- Log: `/tmp/g379-final-binary-check.log`.

Anti-slop:

`swift run --package-path ios/tools/anti-slop-swift anti-slop ios/FleetNotifier ios/FleetNotifierTests`

- Exact base `bea75dd0f5bf1331f16e8eeaf85343b1e4b14e09`: raw exit 1, 24 advisory violations.
- Final delivered HEAD: raw exit 1, 24 advisory violations.
- Programmatic identity comparison: `BASE_COUNT= 24`, `HEAD_FINAL_COUNT= 24`, `ADDED= []`, `REMOVED= []`, `IDENTITIES_EQUAL= True`.
- Logs: `/tmp/g379-aslop-base-final.log`, `/tmp/g379-aslop-delivered-head-final.log`.
- The nonzero status is the unchanged pre-existing advisory baseline; the #379 delta is zero.

Additional static check:

- `git diff --check` → raw exit 0.

## 390x844 synthetic evidence

Evidence inventory: `/tmp/g379-evidence/README.md`

Capture command: `flock /tmp/corral-heavy-gate.lock bash /tmp/g379-capture.sh`

Simulator: iPhone 16, UDID `59DDC0C5-891E-4EC0-91AF-4F50DF68D793`, booted. Native frames are 1179x2556 pixels (@3x); the three review frames were downscaled to exactly 390x844 with `sips`.

- `/tmp/g379-evidence/390x844-phase-1-connect-autopresent.png`
  - SHA-256 `8f3adfffc048be0fe8dc3d63e562774fc6f3b23d6fec6cfe51accf80fff9d267`.
  - Shows the How to connect sheet auto-presented after the evidence launch reset the local identity; the host state is `Not set — type it in Settings → Connection`.
  - Visible content includes numbered steps 1 and 2, healthz text, README Setup link, and the scrollable sheet continuation. The lower step-2 content continues below the 844-pixel viewport by design.

- `/tmp/g379-evidence/390x844-phase-2-settings-device-no-grants.png`
  - SHA-256 `8c7ae50b97dfeec832cc250a5663b14106684c0aa053c05556309a1488ef4419`.
  - Shows Settings with Key ID, Key storage = Keychain, Read-only signed device, State = Not paired, Remove device, and the removal footer. No grants list is visible.

- `/tmp/g379-evidence/390x844-phase-3-connect-sheet.png`
  - SHA-256 `9d3ebe8a1d839cf71cee60686a96e5d066a20ac9af34d5d19909032efd32f2aa`.
  - Shows the shared How to connect sheet with the synthetic host `https://fleet.example.ts.net`, the README Setup link, and an enabled Copy host control.

The capture log is `/tmp/g379-capture2.log`; the first attempt at `/tmp/g379-capture.log` encountered a shutdown simulator (simctl code 405), after which the simulator was booted and the capture completed successfully. The evidence route's phase-4 completion marker was observed.

## Non-claims and boundaries

- No hosted CI, fresh exact-head review, PR, integration merge, or issue closure was performed; those belong to the orchestrator after push.
- No live daemon, real host, physical device, TestFlight install, real network pairing, or remote host revocation is claimed. The Remove device button invokes the existing local identity reset and its UI explicitly says nothing is sent to the host.
- The iPhone 16 simulator is the honest host available here; no native 390x844 simulator exists on this host. The requested 390x844 artifact is a deterministic downscale of the 1179x2556 simulator capture.
- simctl cannot inject user taps or scroll gestures. The DEBUG evidence driver opens the same shared sheet content and scrolls the Settings Device anchor for capture; the production `?` entry and complete five-step content are covered by source-wiring tests and mutation probes.
- The implementation intentionally uses the current shipped sheet treatment and theme tokens; Liquid Glass/translucency was explicitly out of this #379 round.
