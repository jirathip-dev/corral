Issue #332 implementation report

Worktree

- Worktree: /Users/jirathip/.herdr/worktrees/corral/g332-herdr-issues-scope
- Branch: g332-herdr-issues-scope
- Baseline: origin/integration at 04b42e5a1a68697d242821487e09cba3df22aef1
- Implementation commit: 05bc61f5b59bdc006f7ab63405b3fd8e34ed3c0a
- Implementation commit subject: Scope GitHub issues to live Herdr workspaces
- Evidence commit carrying the original report: f902b277688606ff9126055a0ea5f9a507e92bd3
- The report-only metadata correction is committed and pushed separately; its final tip SHA is reported externally after push and is intentionally not embedded here (no self-referential amend).
- Pre-correction `git status --short --branch`: `## g332-herdr-issues-scope...origin/integration [ahead 2]`
- Pre-correction remote proof: `f902b277688606ff9126055a0ea5f9a507e92bd3 refs/heads/g332-herdr-issues-scope`
- No pull request was created.

Integration reconciliation (PR #339)

- Fetched `origin` and verified `origin/integration` at `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`.
- Reconciled with a normal merge of that exact integration tip into `g332-herdr-issues-scope`; no rebase or force-push was used.
- Resolved the `.report.md` merge conflict by retaining this lane's complete Issue #332 report and gate evidence.
- Audited the automatic merge in `ios/FleetNotifier/Models/Models.swift`: the #333 DiffPane terminal-state behavior and comments remain, and this lane's daemon-scoped Herdr workspace contract comment remains.
- The reconciled merge tip is reported externally after push; it is intentionally not embedded here (no self-referential tip).

Implementation scope

- `src/adapters/gh_plane.rs`: production GitHub specs now derive only from current Herdr agents with owned worktree paths, attribution, and canonical `origin` GitHub remotes. Canonical origins are grouped once and native aliases are retained. The poller refreshes specs before fetches and during sleep, clears last-known state when scope changes, and does not fetch with an empty scope. Static production repo fixtures were removed; explicit test constructors retain injected scopes.
- `src/main.rs`: removed native checkout discovery plus static fallback union from the production GitHub supervisor; it uses `GhPlane::with_herdr_scope`.
- `src/api/repo.rs`, `src/api/issues.rs`, `src/integrate/mod.rs`: live issue categories are Herdr-only, both read projections filter to those categories, and topology convergence prunes stale issue-cache categories without restart.
- `tests/http.rs`, `tests/drive.rs`, `src/api/issues.rs`, `src/integrate/mod.rs`, `src/main.rs`, `src/adapters/gh_plane.rs`: production-seam, exact-five, topology, origin, alias, cache, and mutation-sensitive fixtures.
- `clients/egui/src/protocol.rs` and `ios/FleetNotifier/Models/Models.swift`: contract comments now describe daemon-scoped Herdr workspace keys. No FleetViews presentation code was changed.
- `crates/corrald-client/tests/conformance.rs` and `tests/gh_plane.rs`: old eight-repo data is now local explicit test fixture data, not production scope.
- `docs/ARCHITECTURE.md`: documents live Herdr scope, shared projections, and stale-category pruning.

Acceptance map

1. Production poll scope: PASS. `herdr_workspace_specs` reads only source `herdr` agents with worktree paths, attributes those paths through `WorkspaceAttribution`, and reads `origin` only from those owned checkout/worktree facts. No broad checkout discovery feeds the GitHub scope.
2. Absent repositories: PASS. The exact-five HTTP and signed-drive fixtures preload `static-only` and `unrelated-local` categories but return only the five live fixture identities.
3. Static production fallback: PASS. `git grep -n -E 'TRACKED_REPOS|tracked_specs|gh_repo_specs' -- src` returned no output. Static eight-repo data exists only in explicit tests.
4. Topology changes: PASS. `herdr_scope_refreshes_after_workspace_topology_changes` changes the live store from one to two to one workspace and refreshes the poll specs; `issues_cache_prunes_when_live_herdr_membership_changes` removes a live agent and prunes its cache category in place.
5. Alias/canonical dedupe: PASS. Same-basename/different-owner fixtures remain distinct canonical specs; same canonical origin aliases are folded into one spec, and the query builder emits one repository clause per spec.
6. Cache-only read paths: PASS. `/issues` and signed `read_issues` call the shared `issues_view`; no discovery, GitHub, git subprocess, private registry, or sidecar is in those request paths. The existing adapter-dispatch-count fixture remains green.
7. Five-repo production seam: PASS. Privacy-safe labels are `fixture-alpha`, `fixture-bravo`, `fixture-charlie`, `fixture-delta`, and `fixture-echo`; unrelated labels are `static-only` and `unrelated-local`. Both `/issues` and signed `read_issues` assert exactly the five in-scope keys.
8. Client parity/measurement: PASS at the required closest client seams. R2 proves the same five scoped category identities in iOS and egui with a five-plus-unrelated fixture; the actual iOS app target build and `IssueBrowserTests` also pass. Timing below distinguishes client projection/render seams from daemon fixture timing and makes no production benchmark claim.

TDD RED evidence

- `cargo test --lib adapters::gh_plane::tests::default_constructor_waits_for_workspace_scope -- --exact --nocapture`
  - `GATE_EXIT=101`
  - Failure: assertion expected 0 repository clauses but got 8.
- `cargo test --lib api::issues::tests::issues_view_excludes_cached_categories_without_live_herdr_workspace -- --exact --nocapture`
  - `GATE_EXIT=101`
  - Failure: returned `in-scope` plus `unrelated` instead of only `in-scope`.

Scratch mutation proof

- Mutation A disabled the API projection `map.retain`.
  - Command: `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture`
  - `MUTATION_RED_EXIT=101`; public categories were 7, expected 5.
  - Restored source hash comparison: `RESTORE_CMP_EXIT=0`.
  - Restored command result: `RESTORED_GREEN_EXIT=0`; public categories 5.
- Mutation B disabled integrator cache pruning.
  - Command: `cargo test -p corrald --lib integrate::tests::issues_cache_prunes_when_live_herdr_membership_changes -- --exact --nocapture`
  - `MUTATION_RED_EXIT=101`; assertion reported the removed workspace category was not pruned.
  - Restored source hash comparison: `RESTORE_CMP_EXIT=0`.
  - Restored command result: `RESTORED_GREEN_EXIT=0`; cache categories changed from 1 to 0.
- The final committed source was checked after both restores; no mutation remained in the commit.

Review fix R1 regression protection

- Starting reviewed head: `30f280c16cd3323f807b07088c1e4eec80672fe3`.
- `GhPlane::with_config_and_herdr_scope` is available only to tests or the
  `test-utils` embedding feature. It uses `herdr_scope: Some(attribution)`
  with an empty initial spec vector. Ordinary production builds retain the
  existing production constructors; only `with_herdr_scope` supplies the live
  Herdr scope.
- Focused GREEN command: `cargo test -p corrald --test gh_plane -- --nocapture`.
  - `FOCUSED_GH_EXIT=0`.
  - 13 tests: 12 passed, 0 failed, 1 ignored. The four R1 tests all passed.
- Mutation A (run-loop refresh): in disposable `/tmp/g339-r1-scratch`,
  deleted both `self.refresh_specs()` blocks in the `SleepUntil` and `Poll`
  arms. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane herdr_scope_run_loop_refreshes_additions_and_removals -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the test timed out waiting for the query carrying
    both `fixture-owner/one` and `fixture-owner/two`.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Mutation B (ownership attribution): replaced `attribution.repo_for(&path)`
  with the path filename in `herdr_workspace_specs`. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane herdr_scope_excludes_unowned_git_checkout -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the assertion saw 2 specs instead of 1.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Mutation C (empty-scope guard): deleted the `specs.is_empty()` early
  continue in the poll arm. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane empty_live_scope_never_calls_transport -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the empty-store test saw 3 transport calls
    instead of 0 across the cadence window.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Mutation D (source filter): deleted `agent.source == "herdr"` from the
  specs fixture filter. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane herdr_scope_excludes_non_herdr_agent -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the non-Herdr exclusion assertion failed.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Scratch proof: `FINAL_SCRATCH_SOURCE_BYTE_IDENTICAL=1`,
  `SCRATCH_REMOVED=1`, and `MUTATION_CHAIN_EXIT=0`. The real worktree was
  never mutated by these probes.
- Final shared-heavy-lock gates (after formatting):
  - `cargo fmt --all --check` — `GATE_EXIT=0`.
  - `cargo clippy --workspace --all-targets -- -D warnings` — `GATE_EXIT=0`.
  - `cargo test --workspace` — `GATE_EXIT=0`; 25 suites, 703 passed, 0
    failed, 20 ignored.

Fixture measurements

- Query payload clauses: `cargo test -p corrald --lib adapters::gh_plane::tests::query_covers_all_repos_once -- --exact --nocapture`
  - `fixture_scope_measurement poll_payload_repo_clauses_before=8 poll_payload_repo_clauses_after=5`
- Cache topology pruning: `cargo test -p corrald --lib integrate::tests::issues_cache_prunes_when_live_herdr_membership_changes -- --exact --nocapture`
  - `fixture_scope_measurement cache_categories_before=1 cache_categories_after=0`
- Read projection/cache and first-open sample: `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture`
  - Scoped final sample: `cache_categories_before=7 cache_categories_after=7 public_categories=5 first_open_ms=34.303`
  - Unscoped mutation sample: `cache_categories_before=7 cache_categories_after=7 public_categories=7 first_open_ms=50.647`
  - The read path intentionally does not mutate the cache; topology convergence performs pruning. These local timing samples are classified fixture-only and are not a hardware-independent performance claim.

Exact committed pre-R1 gates

- `cargo fmt --all -- --check`
  - `GATE_EXIT=0`
- `cargo clippy --workspace --all-targets -- -D warnings`
  - `GATE_EXIT=0`
  - `Finished dev profile ... in 6.10s`
- `cargo test --workspace`
  - `GATE_EXIT=0`
  - 25 suite summaries: 699 passed, 0 failed, 20 ignored.
  - Key aggregate: `test result: ok. 306 passed; 0 failed...`
  - The default run skips only the repository's pre-existing live/real-daemon tests marked ignored; no private issue bodies were fetched or copied.
- `cargo deny --locked --workspace check`
  - `GATE_EXIT=0`
  - `advisories ok, bans ok, licenses ok, sources ok`
  - It reports pre-existing duplicate-version warnings in the locked dependency graph; this change adds no dependencies and does not alter `Cargo.lock`.
- `cargo audit --deny warnings`
  - `GATE_EXIT=0`
  - `Loaded 1235 security advisories`; `Scanning Cargo.lock for vulnerabilities (584 crate dependencies)`.
- `cargo test -p corrald-ui --lib issues`
  - `GATE_EXIT=0`
  - 10 passed, 0 failed, 190 filtered out.
- `cargo test -p corrald --test http no_fleets_json_reference_anywhere_in_src -- --exact --nocapture`
  - `GATE_EXIT=0`; 1 passed.
- Final focused committed seam commands all exited 0: live-scope constructor, topology refresh, query measurement, cache pruning, exact-five `/issues`, and exact-five signed `read_issues`.

Privacy/configlessness audit

- `git grep -n -E 'fleets\\.json|fleet_ops|fleet-ops|sidecar|gh_repo_specs|tracked_specs|TRACKED_REPOS' -- src/api src/adapters/gh_plane.rs src/main.rs` returned no output.
- Synthetic fixture labels only; no unrelated repository issue body was copied into `.report.md` or evidence.
- No private fleet registry, Fleet Ops registry, or sidecar is used as a source of GitHub scope.

R2 evidence-only AC8 closure

- Authoritative contract: `.fix-r2.md` at exact starting head `e272735cdf446351fe2d195d6e9526dda8e1032e`.
- R2 changed no production code, test code, fixtures, or generated project files. The only tracked R2 change is this report. `docs/OPERATIONS.md` remains untouched.
- The iOS and Cargo/Xcode evidence commands ran under uninterrupted ownership of `/tmp/corral-heavy-gate.lock`. The first serialized run waited for sibling native work to finish (`/tmp/g339-r2-evidence/lock-wait.log`, then `LOCK_ACQUIRED`); the corrected simulator pair was rerun under a fresh lock after fixing only the disposable probe harness.

Deterministic fixture and category contract

- Input fixture: five scoped categories `fixture-alpha`, `fixture-bravo`, `fixture-charlie`, `fixture-delta`, and `fixture-echo`, plus unrelated `static-only` and `unrelated-local`. Probe data contains no issue bodies.
- The client probes used the same 7-category/row fixture definition, supplied the 5 scoped identities to the post-filter client projections, and asserted exactly 5 presented categories/rows with the same five sorted identities. They also asserted that neither unrelated identity reached presentation.

iOS simulator evidence

- Device/runtime: `iPhone 16 (59DDC0C5-891E-4EC0-91AF-4F50DF68D793)`, Booted, iPhoneSimulator26.5 SDK; Xcode `26.6 (17F113)`.
- Current worktree build command:
  `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' /usr/bin/xcodebuild -jobs 4 build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-current-ios-dd`
  - Exit `0`; raw log `/tmp/g339-r2-evidence/ios-current-build.log`; `** BUILD SUCCEEDED **`.
  - The build emitted pre-existing `TerminalAttach.swift` `NSLock` async-context warnings; no build error occurred.
- Current worktree relevant test command:
  `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' /usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-current-ios-dd -only-testing:FleetNotifierTests/IssueBrowserTests`
  - Exit `0`; raw log `/tmp/g339-r2-evidence/ios-current-issue-tests.log`; `** TEST SUCCEEDED **`; `IssueBrowserTests` executed 9 tests, 0 failures.
- Production-shaped simulator probe commands (each run after `xcodegen generate` in its own disposable tree):
  - Baseline: `/usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-corrected-baseline-dd -only-testing:FleetNotifierTests/AC8CategoryParityTests`
  - Candidate: `/usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-corrected-candidate-dd -only-testing:FleetNotifierTests/AC8CategoryParityTests`
  - Baseline exit `0`; candidate exit `0`; each executed 1 test with 0 failures. Raw logs: `/tmp/g339-r2-evidence-corrected/baseline.log` and `candidate.log`.
  - Both final logs report:
    `AC8_IOS_PROBE fixture_input_categories=7 wire_categories=5 presented_categories=5 presented_keys=fixture-alpha,fixture-bravo,fixture-charlie,fixture-delta,fixture-echo cache_rows=7 presented_rows=5 repetitions=20 ...`
  - The probe calls production `IssuesBrowserWire` decoding, `IssuesBrowserPane.apply`, and the `IssueBrowser.rows` projection used by the SwiftUI issue browser. It is the closest executable iOS first-open seam; it is not relabeled as a live-network or physical-device benchmark.

Egui production-shaped display evidence

- Commands, run separately at both refs in disposable scratch trees with isolated Cargo targets:
  - Baseline: `CARGO_TARGET_DIR=/tmp/g339-r2-target-egui-baseline cargo test -p corrald-ui --lib ui::issues::tests::ac8_five_scoped_categories_reach_the_repo_rail -- --exact --nocapture`
  - Candidate: `CARGO_TARGET_DIR=/tmp/g339-r2-target-egui-candidate cargo test -p corrald-ui --lib ui::issues::tests::ac8_five_scoped_categories_reach_the_repo_rail -- --exact --nocapture`
  - Baseline exit `0`; candidate exit `0`; each: 1 passed, 0 failed. Raw logs: `/tmp/g339-r2-evidence/egui-baseline-ac8.log` and `egui-candidate-ac8.log`.
  - The probe rendered the actual egui repo rail and asserted the same five labels were visible, with no unrelated label. Both logs report the same five `presented_keys` and `presented_categories=5`.

Old-vs-new measurement (same host, same deterministic fixture, 20 repetitions)

- Poll payload/repository clauses:
  - Baseline `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`: `cargo test --lib adapters::gh_plane::tests::query_covers_all_repos_once -- --exact --nocapture` passed; `poll_payload_repo_clauses_before=8 poll_payload_repo_clauses_after=8`.
  - Candidate `e272735cdf446351fe2d195d6e9526dda8e1032e`: the same command passed; `poll_payload_repo_clauses_before=8 poll_payload_repo_clauses_after=5`.
  - Raw logs: `/tmp/g339-r2-evidence/poll-baseline.log` and `poll-candidate.log`.
- Read/cache fixture counts from the identical exact-five HTTP test workload:
  - Baseline: 7 cache categories before, 7 after, 0 cached rows, 7 public categories; the test's expected RED exit was `101` because pre-#332 behavior exposed the unrelated categories.
  - Candidate: 7 cache categories before, 7 after, 0 cached rows, 5 public categories; 20 repetitions had raw exit `0`.
  - Raw summary: `/tmp/g339-r2-evidence/http-repeated-measurements.log`.
- Daemon `/issues` fixture-only first-open seam, not a client benchmark:
  - Baseline: min `15.191 ms`, median `20.562 ms`, max `43.709 ms`.
  - Candidate: min `13.084 ms`, median `16.365 ms`, max `95.451 ms`.
- iOS production projection seam, 20 repetitions inside one simulator XCTest:
  - Baseline: min `0.018 ms`, median `0.018 ms`, max `0.081 ms`.
  - Candidate: min `0.018 ms`, median `0.018 ms`, max `0.084 ms`.
- egui production display seam, 20 repetitions inside one renderer test:
  - Baseline: min `0.274 ms`, median `0.285 ms`, max `53.220 ms`.
  - Candidate: min `0.703 ms`, median `0.763 ms`, max `292.929 ms`.
- These timings are local fixture/projection measurements only. They disclose min/median/max and identical workload/host conditions; they are not production latency or throughput claims.

Disposable harness correction

- The first disposable iOS probe attempt exited `65` because the temporary XCTest omitted `@testable import FleetNotifier`; no repository file was changed. After adding that import and rerunning the same baseline/candidate pair under the shared lock, both final logs above exited `0`. The temporary probe trees and harnesses remain outside the repository.

R2 scope and limits

- No live GitHub poll was run. A live end-to-end first-open would require a credentialed running Corral daemon with `read_issues` grant plus current Herdr/GitHub access; no credentials were used or copied, and no private issue content entered evidence.
- No physical iOS device run was performed. The remaining unverified gate is physical-device/live-daemon behavior; the simulator build, existing iOS issue-browser tests, and closest production iOS projection seam are verified above. No synthetic timing is presented as that missing live gate.
- Temporary baseline/candidate source copies and probe-only tests were under `/tmp/g339-r2-*`; they were not tracked or applied to the real worktree. Scope verification found the current tracked Xcode project byte-identical to `HEAD` (`804cb253a1fc380bb5763f9d70c4fc63d8fc547e5bfa017487e65e805155a3b8`).

---

# Retained #331 terminal attach lifecycle report

# Issue #331 — Terminal attach lifecycle

Implementation commit: `ef43e1e76ec027c8dc8c8b682877e1dd8d9e6045`
Branch: `g331-terminal-crash`
Base: `origin/integration` at `04b42e5a1a68697d242821487e09cba3df22aef1`

## Evidence availability and scrub method

- Physical-device probe: `xcrun devicectl list devices` exited 0 and reported zero paired device rows. No physical-device stack or physical open/dismiss/reopen recording was available.
- A local non-device `FleetNotifier` diagnostic artifact was reachable from the earlier pre-fix production-client test. It was parsed locally and reduced to exception class, termination signal, fault-thread number, app symbol, source filename, and source line. Raw report contents, identifiers, paths, and payloads were not copied into this report or committed.
- The scrubbed artifact identified `EXC_CRASH`/`SIGABRT` in the pre-fix `TerminalAttachClient.connect` at source line 58 while `testUnavailableWorktreeIsRejectedBeforeOpeningTheSocket` exercised the client. This is a proven non-device crash boundary, not a claim about the physical-device root cause.
- Remaining human gate: on a paired physical device, open the agent row, choose Terminal, wait for the first frame, dismiss the sheet, reopen Terminal, and capture a privacy-scrubbed stack/recording. Do not publish device identifiers, terminal contents, local paths, keys, signatures, tokens, or request payloads.

## Proven defect boundary

The old client created `URLSessionWebSocketTask` from the host URL before rejecting an unavailable worktree, and passed the host URL directly to the WebSocket factory. The production regression test therefore reached the crash boundary instead of only checking capability availability. The fix validates the worktree and cancellation state before opening a socket, normalizes HTTP(S) hosts to WS(S), and turns connection/protocol/handshake failures into typed errors.

This proves the reachable simulator/test-process boundary. The physical-device cause remains unknown until the human gate above is performed.

## Acceptance map

- Production lifecycle seam: `TerminalAttachLifecycleRegressionTests` calls `TerminalAttachClient.connect` and `TerminalAttachSessionModel`, not just capability predicates.
- Success/error/cancel/dismiss/retry: explicit connecting, connected, and failed states; streamed frame/cursor updates; Retry starts a new generation; Done and disappearance cancel/close; cancellation suppresses late updates.
- Failure handling: unavailable worktree, invalid host, encoding, handshake timeout, server refusal, network failure, malformed/out-of-order/binary messages, and command failures are surfaced without force unwraps, swallowed client errors, or fatal paths. Failed UI includes Retry and Done.
- Wire and cleanup: HTTP(S) to WS(S) URL normalization, signed Attach envelope preservation, handshake/opened validation, generation-scoped late-result suppression, socket cleanup, and best-effort daemon session cleanup.
- Daemon: authenticated WebSocket integration covers signed open, explicit Attach grant, resize, input, frame, close, outside-root refusal, and malformed command error/closure. Existing default-deny authorization, cwd confinement, auditing, and redaction remain in place.
- Release source manifest: `FleetViews.swift` and `TerminalAttach.swift` are manifest inputs; the approval digest was re-pinned and the self-test passed.

## Files

- `ios/FleetNotifier/UI/TerminalAttach.swift`
- `ios/FleetNotifier/UI/FleetViews.swift` (Terminal-sheet region only)
- `ios/FleetNotifierTests/FleetNotifierTests.swift`
- `ios/check-release-demo.py`
- `src/api/mod.rs` (terminal WebSocket error/cleanup handling only)
- `tests/terminal.rs`
- `Cargo.toml`
- `Cargo.lock`

No Recent-output, Diff, or Issues-scope regions were changed. No generated Xcode project file was left modified.

## RED/GREEN and mutation evidence

- Historical pre-fix production-client run: the reachable non-device diagnostic artifact recorded `EXC_CRASH`/`SIGABRT` at `TerminalAttachClient.connect` source line 58 while the unavailable-worktree regression exercised it.
- Final focused iOS lifecycle run: 7 tests, 0 failures, exit 0.
- Final iOS suite: 225 tests, 0 failures, exit 0.
- Disposable scratch mutation removed the production worktree-unavailable guard from `TerminalAttachClient.connect`. The same 7-test focused suite compiled and went RED with exit 65: `testUnavailableWorktreeIsRejectedBeforeOpeningTheSocket` observed `Terminal connection failed.` instead of `Terminal worktree is unavailable.`
- The pristine production file was copied back and byte-compared: `cmp` exit 0. The same focused suite then went GREEN with 7 tests, 0 failures, exit 0.
- The scratch copy and its dedicated simulator were deleted after verification; the real worktree remained limited to the files listed above.

## Exact gate results

Commands were run from the repository root with full logs retained outside the worktree. Heavy Cargo/Xcode runs used the shared gate lock, `CARGO_BUILD_JOBS=4` for Cargo, and `-jobs 4` for Xcode.

- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --test terminal authenticated_terminal_websocket_runs_lifecycle_and_rejects_bad_frames -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test --workspace` — exit 0; 26 result lines aggregated to 695 passed, 0 failed, 20 ignored.
- `swiftc -parse` on the changed Swift production/test files — exit 0.
- `python3 ios/check-release-demo.py --self-test` — exit 0; `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- iOS Debug build with the brief's FleetNotifier project/scheme and generic simulator destination — exit 0.
- `xcodebuild test` focused on `FleetNotifierTests/TerminalAttachLifecycleRegressionTests` — exit 0; 7 passed, 0 failed.
- `xcodebuild test` focused on `FleetNotifierTests` — exit 0; 225 passed, 0 failed.
- Anti-slop advisory: exit 1 as designed for existing diagnostics; HEAD reported 77 violations across 25 files versus 79 on the base snapshot. No new anti-slop diagnostic was reported for the terminal changes. No new production force unwrap or `try!` was added.
- `swiftlint lint --strict` was also run; exit 2 from the repository's existing strict-lint backlog plus the expanded terminal file's default style/size thresholds. This is non-binding here; build, parse, anti-slop, and test gates above are green.
- Tracked-secret scan equivalent to the repository CI `git ls-tree` check — exit 0; no tracked secret files. `gitleaks` was not installed locally, so the hosted full-tree gitleaks job remains CI-owned.

## Simulator evidence

A fresh, privacy-safe simulator was created for evidence and removed afterward. The built Debug app was installed and launched with the existing synthetic `-demoMode -corralTerminalPreview` path. Open capture, programmatic terminate, relaunch, and reopen capture all exited 0; both scaled 390x844 captures visibly showed the synthetic Terminal Preview with a rendered frame. The captures were vision-inspected and then deleted; no terminal text or path was committed or attached.

This is explicitly simulator preview evidence, not physical-device proof. `simctl terminate` is not equivalent to a user dismissing the sheet, so the required physical open/frame/dismiss/reopen behavior remains the human gate above.

## Privacy/security audit

- No credentials, key material, signatures, tokens, host/device identifiers, request bodies, raw crash reports, terminal output, or local absolute paths are present in this report or the committed diff.
- Authenticated Attach signing and replay protection are retained; daemon failures use bounded generic messages.
- Attach remains default-deny, worktrees remain root-confined, terminal actions remain audited, and no arbitrary command or new write path was introduced.

## Integration reconciliation

- Pre-reconciliation branch tip: `614b4b462112c99d0485f5a690484e383e1964ca`.
- `git fetch origin integration` verified the requested `origin/integration` pin: `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`.
- Reconciled with `git merge --no-ff --no-edit 03b61c9cfce1d851168b2d9d2beab1fb773fe99b`; no rebase or force-push was used. The only conflicts were `.report.md` and `ios/check-release-demo.py`. This file remains the #331 report.
- `FleetViews.swift` auto-merge audit retained this lane's `terminalAvailability`, optional `TerminalAttachClient`/worktree inputs, and `TerminalAttachView` wiring alongside #333's diff loaded/empty/error, Retry, cancellation, and `hasLoaded` rendering.
- `FleetNotifierTests.swift` auto-merge audit retained `ScriptedTerminalSession` and `TerminalAttachLifecycleRegressionTests` alongside #333's `DiffReliabilityTests` and deterministic drive status/cancellation fixtures.
- The requested integration tree has no separate `DiffFilesModel.swift`; #333's diff-file model coverage is in `Models.swift` (`DiffPane`/`DiffFileWire`). That coverage remains in the manifest, and `FleetViews.swift` plus `TerminalAttach.swift` remain Release inputs.
- The merged Release source digest is `3eabcbd125ba4d7a108505bc3e7bbe84db66c3c6c70d5ce04e0d8856584dd822`, with both #333 and #331 digest reasons retained in `ios/check-release-demo.py`.
- The final diff path set against the pinned integration tree was audited as exactly: `.report.md`, `Cargo.lock`, `Cargo.toml`, `ios/FleetNotifier/UI/FleetViews.swift`, `ios/FleetNotifier/UI/TerminalAttach.swift`, `ios/FleetNotifierTests/FleetNotifierTests.swift`, `ios/check-release-demo.py`, `src/api/mod.rs`, and `tests/terminal.rs`.

## Post-reconciliation gate results

All gates below ran after the merge resolution in one serialized process that acquired `/tmp/corral-heavy-gate.lock`, used `CARGO_BUILD_JOBS=4`, and used `xcodebuild -jobs 4`; the lock was released cleanly.

- `python3 ios/check-release-demo.py --self-test` — exit 0; `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --test terminal authenticated_terminal_websocket_runs_lifecycle_and_rejects_bad_frames -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test --workspace` — exit 0; 26 result lines aggregated to 695 passed, 0 failed, 20 ignored.
- `swiftc -parse ios/FleetNotifier/App/AppModel.swift ios/FleetNotifier/App/FleetStore.swift ios/FleetNotifier/Models/Models.swift ios/FleetNotifier/UI/FleetViews.swift ios/FleetNotifier/UI/TerminalAttach.swift ios/FleetNotifierTests/FleetNotifierTests.swift` — exit 0.
- `xcodebuild -jobs 4 build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd-331-reconcile build` — exit 0; `BUILD SUCCEEDED`.
- `xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=[SIMULATOR_ID_REDACTED]' -derivedDataPath /tmp/fn-dd-331-reconcile -only-testing:FleetNotifierTests/TerminalAttachLifecycleRegressionTests` — exit 0; 7 passed, 0 failed.
- `xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=[SIMULATOR_ID_REDACTED]' -derivedDataPath /tmp/fn-dd-331-reconcile -only-testing:FleetNotifierTests` — exit 0; 235 passed, 0 failed.
- Post-reconciliation anti-slop advisory — exit 1 with 77 violations across 25 files; the pinned integration control was 79 violations across 25 files. This remains pre-existing advisory output, not a blocking merge gate.
- `git diff --check` — exit 0; no whitespace errors.

## New defects / remaining work

No new functional defects were found in the verified surface. The only remaining acceptance item is the explicitly blocked physical-device stack and open/frame/dismiss/reopen gate; no physical-device claim is made here.

## Fresh R3 integration reconciliation

- Pre-reconciliation branch tip: `57a3e75d3a533836a88ca982fec8f161532ae8e9` (remote branch matched before the merge).
- Fetched `origin` and verified the exact requested `origin/integration` tip: `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`.
- `git merge-tree --write-tree origin/integration HEAD` predicted exactly one conflict, `.report.md`; all source/test paths were expected to auto-merge.
- Ran `git merge --no-edit origin/integration` normally. Actual unmerged path list was exactly `.report.md`; no rebase, amend, force-push, PR mutation, issue mutation, or direct integration write was used.
- Conflict resolution preserved the complete #332 implementation/R1/AC8 report and appended the complete incoming #331 terminal lifecycle report under `# Retained #331 terminal attach lifecycle report`. No production or test file was edited during resolution.
- The final merge tip is intentionally reported externally after commit/push rather than embedded in this report (no self-referential metadata).

## R3 gate evidence invalidation and recovery

- The corrected post-merge gate runner at PID `19201` was invalidated by orchestrator-reported critical-ENOSPC cleanup contamination while that runner was active; its partial logs under `/tmp/g339-r3-corrected-evidence` are retained only as invalidation evidence and are not counted as reconciliation gates.
- PID `19201` and all descendants were verified absent. The lock owner file still contained exactly `19201`; only that own stale owner file and `/tmp/corral-heavy-gate.lock` directory were then removed.
- The interrupted runner's partially created `target/`, probe tree, and AC8 DerivedData are being treated as contaminated regenerable artifacts. Fresh gate execution must start from clean lane-owned targets under newly acquired lock ownership.

## R3 valid gate evidence

Fresh valid execution began only after the invalidated `target/` and disposable probe artifacts were removed and a new lock owner acquired. The following results are counted; the invalidated runner outputs are not.

Rust and egui gates:

- `cargo test -p corrald --test gh_plane -- --nocapture` — exit 0; 12 passed, 0 failed, 1 ignored.
- `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture` — exit 0; 1 passed, 0 failed. The fixture measurement reported 7 cache categories before/after and 5 public categories; first-open measurement 28.978 ms.
- `cargo test -p corrald --test drive read_issues_result_carries_repos_and_audits_executed -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test -p corrald --test terminal authenticated_terminal_websocket_runs_lifecycle_and_rejects_bad_frames -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test -p corrald-ui --lib issues -- --nocapture` — exit 0; 10 passed, 0 failed.
- `cargo test -p corrald-ui --lib ac8_five_scoped_categories_reach_the_repo_rail -- --nocapture` in a disposable staged-merge tree — exit 0; 1 passed, 0 failed. The probe reported 7 fixture input/cache categories, 5 wire/presented categories, exactly `fixture-alpha,fixture-bravo,fixture-charlie,fixture-delta,fixture-echo`, 5 presented rows, 20 repetitions, and first-open min/median/max `0.523/0.531/100.396 ms`.
- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --workspace` — exit 0; 704 passed, 0 failed, 20 ignored across 26 result lines.
- `cargo deny --locked --workspace check` — exit 0; advisories, bans, licenses, and sources all ok (existing duplicate-crate warnings were reported).
- `cargo audit --deny warnings` — exit 0; 584 locked dependencies scanned.

Native simulator gates, using direct `/usr/bin/xcodebuild` with signing disabled on iPhone 16 simulator `59DDC0C5-891E-4EC0-91AF-4F50DF68D793`:

- `xcodebuild -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -sdk iphonesimulator -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r3-valid-ios-derived CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` — exit 0; `** BUILD SUCCEEDED **`.
- The same command with `test -only-testing:FleetNotifierTests/IssueBrowserTests` — exit 0; 9 tests, 0 failures.
- The same command with `test -only-testing:FleetNotifierTests/TerminalAttachLifecycleRegressionTests` — exit 0; 7 tests, 0 failures.
- A disposable staged-merge tree received only `AC8CategoryParityTests.swift`; `xcodegen generate` exited 0, and the same simulator test command with `test -only-testing:FleetNotifierTests/AC8CategoryParityTests` exited 0; 1 test, 0 failures. The probe reported 7 fixture input/cache categories, 5 wire/presented categories, exactly `fixture-alpha,fixture-bravo,fixture-charlie,fixture-delta,fixture-echo`, 5 presented rows, 20 repetitions, and first-open min/median/max `0.022/0.023/0.108 ms`.

Additional non-green diagnostics are preserved rather than hidden:

- Fresh `python3 ios/check-release-demo.py --self-test` exited 1 because the incoming #331 pin expects `3eabcbd125ba4d7a108505bc3e7bbe84db66c3c6c70d5ce04e0d8856584dd822`, while the three-way merged iOS source set computes `dd5b461eec69424636f3300def2846b6ab898ddf9210636169a1cf45e5c39506`. No release pin or production file was changed under this report-only contract.
- A pre-recovery full `FleetNotifierTests` run exited 65 with two failures in the pre-existing `GrantAdminToggleRevertTests`; the required IssueBrowser, terminal lifecycle, and AC8 selected suites above passed independently. The full-suite failure is not claimed as green and was not repaired because that would exceed the contract.

All commands and raw outputs are retained under `/tmp/g339-r3-valid-cargo-evidence`, `/tmp/g339-r3-valid-final-evidence`, `/tmp/g339-r3-valid-ios-evidence`, `/tmp/g339-r3-valid-probe-evidence`, and `/tmp/g339-r3-valid-release-check.log`.
