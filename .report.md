Issue #340 implementation report

Docs-only truth fix for docs/OPERATIONS.md (stale GET /fleets / fleet-ops
identity / issue-key claims) + bounded doc-truth regression. No production
behavior change.

Worktree

- Worktree: /Users/jirathip/.herdr/worktrees/corral/g340-operations-truth
- Branch: g340-operations-truth
- Exact base: 2c1881f6733e52cad3116dd224e731bca1f73dea (origin/integration, post-#332/#339)
- Pull request: #352 (https://github.com/jirathip-dev/corral/pull/352), opened
  at this exact head a5a35a1196e9c642353fdbf312af13b4071f3b63 (base:
  integration). No main touched.

Implementation scope (files changed in the commit)

- docs/OPERATIONS.md — corrected prose only (sections: "Fleet operations —
  configless (#237)", "Workspace/repo attribution", "Issue-linked worktrees
  (#113, slice 1)").
- docs/design/evidence/issue-340/README.md — audit table (claim -> source
  truth), gate usage, exact RED/GREEN output, scope notes.
- docs/design/evidence/issue-340/doc-truth-gate.sh — the bounded doc-truth
  regression (AC4): greps docs/OPERATIONS.md for the removed claim classes
  and fails if any reappears.
- .report.md — this report.

Audit summary (AC1/AC5: every named route/command verified against source at
the exact base)

- src/api/mod.rs route table (read: /healthz /snapshot /events /history
  /issues /v1/worktrees /v1/terminal; write: /drive /device-token
  /grants-read + auth routes) has NO GET /fleets — the route was removed
  with the Fleet Ops CLI coupling (#296, merged as #298, commit 273c22b
  deleted src/api/fleets.rs, src/fleet/cli.rs, src/fleet/switch.rs).
- src/main.rs dispatches only the `digest` subcommand; there is no
  `corrald fleet` CLI surface (the old help-text USAGE line is stale source,
  out of this docs lane's fence).
- GET /issues grouping/pruning contract: src/api/issues.rs + src/api/repo.rs
  — keys are the live Herdr `workspace.repo` categories; stale categories
  are pruned (#237 configless, #332 live-workspace scope); the signed
  `read_issues` drive arm serves the same projection.
- Issues polling: src/adapters/gh_plane.rs poll specs rebuild from current
  Herdr-owned workspaces; no fleet-identity fetch exists anywhere.
- start-worktree identity: src/api/drive.rs worktree_dispatch accepts the
  request `repo` only as a live workspace repo / cached issue category and
  derives the identity itself (name/gh_repo/worktree_dir = repo category;
  local = $CORRAL_REPO_ROOT or ~/Projects/<repo>). No fleet-ops CLI catalog.
- Still-valid fleet-ops CLI behavior preserved: herdr-fleet (list|add|
  remove|check|pause|resume|models|switch|doctor) and `herdr-fleet switch
  <name>` verified against the installed CLI (`herdr-fleet --help`, exit 0).

Acceptance map (issue #340)

1. Audit every GET /fleets / fleet-ops identity / issue-key claim in
   docs/OPERATIONS.md against source: PASS. Three stale clusters fixed
   (former lines 570, 615, 643-645) plus the same defect class in the
   adjacent "Fleet operations — configless" section (corrald fleet switch
   delegation/example/exit semantics and the removed #239 sidecar-plugin
   claim). Full-file sweep left no other current-behavior claims.
2. Issues grouping/polling prose corrected to the daemon-scoped Herdr
   repository contract: PASS. Grouping keys = live Herdr workspace.repo
   categories; poller specs rebuild from those workspaces; topology
   changes prune stale categories; refresh re-reads the last-known
   projection (no identity fetch to retry).
3. start-worktree identity prose corrected separately; still-valid
   `herdr-fleet list` behavior preserved, no daemon route invented:
   PASS. The Fleet operations section keeps the real herdr-fleet command
   list/example block (now including `herdr-fleet switch <name>`); the
   start-worktree slice is described as repo-native.
4. Bounded doc-truth regression: PASS. docs/design/evidence/issue-340/
   doc-truth-gate.sh + README.md (patterns: "GET /fleets", "corrald
   fleet", "registered fleet name", "fleet-ops CLI validated"). RED at
   base (9 matches, exit 1), GREEN at fix head (exit 0) — exact output
   recorded in the README.
5. Verify all commands/routes named in changed prose: PASS. Route table
   and CLI surfaces above; herdr-fleet subcommand set verified against
   `herdr-fleet --help`.

#324 fence

- The impl-corral-324-rev-r1 lane (Provider read-tail revision contract,
  section ~lines 497-530 at ITS head) is NOT present at this lane's base
  (its commit beff507 is not an ancestor of 2c1881f). No revision-contract
  prose exists in this diff and none was touched, renumbered, or deleted;
  the corrected hunks (former lines 528-555, 568-570, 608-647) sit below
  where the #324 section lands and merge disjointly.

Gate verification (run in this worktree at the fix head)

- `bash docs/design/evidence/issue-340/doc-truth-gate.sh` — exit 0; output:
  `doc-truth-gate: PASS - docs/OPERATIONS.md names no GET /fleets route,
  corrald fleet subcommand, or fleet-ops-CLI-validated identity.`
- RED proof (base OPERATIONS.md stashed): exit 1 with 9 matches (recorded
  verbatim in docs/design/evidence/issue-340/README.md).
- `git diff --check` — exit 0.
- Docs-only scope proof: `git diff --name-only 2c1881f..HEAD` lists only
  docs/OPERATIONS.md, docs/design/evidence/issue-340/ (2 files), and
  .report.md. No src/, tests/, clients/, ios/, crates/, or scripts/ file
  changed. No cargo/xcode gate run (repo has no docs gate; rust.yml CI
  gates code, which this lane does not touch).

Out-of-fence observation (not edited)

- docs/ARCHITECTURE.md lines ~159-197 still describe the removed
  `GET /fleets` view, `src/fleet/cli.rs`, and `corrald fleet switch` as
  current (stale since #296/#298; issue #332's ARCHITECTURE.md edit covered
  only the GET /issues paragraph). This issue's fence is docs/OPERATIONS.md
  only, so a follow-up may be warranted.

Review fix R2 (report-truth only; docs content passed R1)

- R1 FAIL: `.report.md` line 12 claimed no pull request was created, which
  became false when PR #352 was opened at this exact head
  (a5a35a1196e9c642353fdbf312af13b4071f3b63).
- Fix (this commit): delivery line corrected to reference PR #352
  (https://github.com/jirathip-dev/corral/pull/352) and the exact head.
  Report-only; no OPERATIONS.md, evidence, or production change.
- Re-verified at the new head: doc-truth gate exit 0 (`doc-truth-gate: PASS
  - docs/OPERATIONS.md names no GET /fleets route, corrald fleet subcommand,
  or fleet-ops-CLI-validated identity.`), `git diff --check` clean, and the
  changed-file set vs the exact base is still only `.report.md`,
  `docs/OPERATIONS.md`, and `docs/design/evidence/issue-340/` (2 files).
- PR #352 state: OPEN (base integration, head g340-operations-truth),
  verified via `gh pr view 352 --json number,state,headRefName,headRefOid,
  baseRefName`.
