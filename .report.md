# Issue #354 L1 report — daemon read-only cut (branch g354-l1-daemon-cut)

Queue mandate: corral-354-cut-queue.md (L1 DAEMON CUT). Full locked spec:
issue #354 comment 5505946486.

Worktree
- /Users/jirathip/.herdr/worktrees/corral/g354-l1-daemon-cut
- Branch: g354-l1-daemon-cut
- Exact base: 6dfba2249c86c3906b025b3098879ab241b691a2 (origin/integration)

## Scope executed (daemon only, per brief + orchestrator steering)

1. Mutating drive-plane removal: `prompt` / `interrupt` / `approve` /
   `kill` / `attach` / `start_worktree` / `read_issues` removed from the
   `Capability` and `DrivePayload` contracts; the removed names now parse
   as `UnknownCapability` and every signed drive naming one is refused at
   the capability boundary (`400 unknown_capability`) BEFORE the
   authorizer, before any adapter dispatch, and before the audit log.
2. Dispatch-path removal: `PendingCommand`, the approve claim validation
   seam, the step-up gate, the `start_worktree` fleet dispatch
   (`dispatch_worktree`/`worktree_dispatch`), and the `read_issues`
   fleet dispatch (`dispatch_issues`) are gone from `src/api/drive.rs`;
   only the read seams (`read_tail`, `read_diff`) dispatch.
3. Write-auth / grant-surface removal: `src/auth/step_up.rs` + `POST
   /step-up` route deleted (the gate existed only for destructive drive
   payloads); `src/approve/` module deleted (claim-check seam had no
   remaining caller; push builds the display-only claim id inline);
   `POST /grants` remains — it provisions the KEPT read capabilities —
   and removed capability names are refused by the parser, so their grant
   surface is structurally dead.
4. Terminal/attach transport removal: `src/tmux.rs`, the `/v1/terminal`
   websocket, `/v1/worktrees` listing, and `Adapter::attach` command path
   removed with the `attach` capability; `src/fleet/` worktree engine
   removed with `start_worktree`.
5. KEPT (per brief): signed device-key read auth, SSE/snapshot, bounded
   `read_tail`, `read_diff` daemon internals (client UI for diff is cut
   by L2/L3; #324 fence keeps `src/adapters/herdr.rs` read internals
   byte-touch-free — only the mutating command path inside herdr.rs was
   removed), `GET /issues` read projection (display-only now), push
   notifier (display-only state-change alerts; `approval_id_for`
   replaced by an inline `agent:hash` claim id).
6. Daemon API docs updated to the read-only surface:
   docs/OPERATIONS.md and docs/ARCHITECTURE.md ONLY (per orchestrator
   steering: README.md / QUICKSTART.md / DEVELOPING.md rewrites are L4's
   and were NOT touched — they remain at base content).

## Scope classification (reviewer aid: compile-required vs daemon-doc)

- Daemon cut (batch 1): all `src/` + `tests/` changes above, including
  suite alignment (tests/auth.rs, tests/drive.rs, tests/model.rs,
  tests/provenance.rs) and removal of tests/terminal.rs +
  tests/worktree.rs with their removed surfaces.
- Client compile ripple (batch 2): `crates/corrald-client` capability
  mirror trim (wire contract must match the daemon for the workspace
  conformance gate; its tests construct the mirror's variants) and
  `clients/egui/tests/conformance.rs` (dev-depends on the daemon lib,
  whose `Capability` enum is trimmed — compile-required). NO elective
  egui UI cut: `clients/egui/src/{drive.rs,ui/board.rs,ui/issues.rs,
  infer.rs}` were reverted to base after verification that egui lib code
  has no daemon type coupling (corrald is egui dev-dep only). L3 owns
  the egui/WASM UI cut.
- Daemon API docs (batch 3): docs/OPERATIONS.md + docs/ARCHITECTURE.md
  drive-plane/grants/security sections rewritten to the read-only
  surface (no invented routes). The #353-owned GET /fleets stale section
  in ARCHITECTURE.md was left untouched (separate docs lane).

## Fences honored

- `src/adapters/herdr.rs`: only the mutating `Adapter::drive` command
  path (Prompt/Interrupt/Approve/Kill/Attach arms) was removed; the
  `read_tail`/`read_diff` adapter internals are unmodified (g324 #324
  fence). The `read_tail` unit tests, event-stream tests, and read-seam
  code compile and pass unchanged except for tests whose subject was a
  removed command.
- No iOS/egui/WASM client changes beyond the compile-required ripple
  above. No docs/OPERATIONS.md content owned by #324/#340 was edited
  (the base has no unmerged #324 content; the #340-merged sections
  describing GET /issues remain, with the start_worktree claims removed
  because the drive no longer exists — verified against source).
- Frozen/preserved worktrees untouched; no main promotion; no PR opened
  (orchestrator dispatches review/merge).

## RED → GREEN evidence (focused gate)

- Focused probe: tests/readonly_cut.rs (4 tests; committed alone as a3dca26,
probe made dispatch-discriminating in 8172a33).

## Commits on this branch (base 6dfba2249c86c3906b025b3098879ab241b691a2)

- a3dca26 test(daemon): RED probe for #354 read-only cut (probe alone)
- 8172a33 test(daemon): make #354 cut probe dispatch-discriminating
- 192f54d cut(daemon): read-only drive plane — mutating capabilities,
  dispatch paths, write-auth/grant surfaces removed (batch 1)
- 5d0dceb cut(client): corrald-client wire mirror trim + egui conformance
  compile ripple (batch 2)
- 1cd8c85 docs(daemon): OPERATIONS + ARCHITECTURE read-only surface
  (batch 3)
- (this commit) .report.md

RED (base commit 8172a33, before the cut), real output:
/tmp/corral-354-l1-gates/red.log (GATE_EXIT=101):
- `mutating_drives_are_refused_without_dispatch_or_audit` FAILED: got
  `403 {"kind":"not_granted",...,"capability not granted: prompt"}` for
  prompt — mutating capabilities still parsed and reached the authorizer
  pre-cut.
- `signed_read_tail_still_dispatches_after_the_cut` FAILED pre-cut
  (dispatch-count discrimination).
- `sse_and_snapshot_read_plane_still_serves` ok;
  `fresh_registration_is_still_read_only_default` ok.
- Result: 2 passed; 2 failed; exit 101.

GREEN (cut head), real output:
/tmp/corral-354-l1-gates/precommit-ws.log and
/tmp/corral-354-l1-gates/final-corrald2.log:
- readonly_cut target: 5 passed; 0 failed (mutating drives now refused
  `400 unknown_capability` with zero dispatch and zero audit entries;
  signed read_tail dispatches; fresh registration read-only; SSE +
  snapshot + healthz serve).

## Full gates (real, serialized under /tmp/corral-heavy-gate.lock;
log paths under /tmp/corral-354-l1-gates/)

- Full Rust workspace (real counts): `cargo test --workspace` →
  /tmp/corral-354-l1-gates/precommit-ws.log: 655 passed, 0 failed,
  EXIT=0 (aggregated from per-target `test result:` lines; corrald lib
  268 + 16 integration targets; corrald-client + egui targets included).
- clippy: `cargo clippy --workspace --all-targets -- -D warnings` →
  /tmp/corral-354-l1-gates/bat-clippy.log: CLIPPY=0 (zero warnings).
- fmt: `cargo fmt --all --check` → /tmp/corral-354-l1-gates/bat-fmtcheck.log:
  FMT_CHECK=0 (and FMT_APPLY=0 on the final tree).
- cargo-deny: not run — no dependency additions/removals in this lane
  (pure deletion lane); the Cargo.lock is untouched (git diff shows no
  Cargo.lock change).
- cargo-audit: not run — same rationale (no dep graph change); if the
  gate demands it on any head, it can be run without source changes.
- One flake observed and re-proven green in isolation:
  `adapters::herdr::tests::accepted_then_closed_global_streams_back_off_and_recover`
  failed once under host load (backoff deltas 15-68ms vs expected
  delays), then passed in isolation (1 passed; 0 failed;
  /tmp/corral-354-l1-gates/backoff-test.log) and in every subsequent
  full-suite run. Unrelated to the cut (herdr stream backoff, untouched
  code).

## Explicit non-claims

- No iOS/egui/WASM client cut was performed or claimed (L2/L3).
- No docs rewrite of README/QUICKSTART/DEVELOPING (L4 owns; they were
  reverted per orchestrator scope boundary after an initial elective
  pass).
- read_diff daemon internals were NOT removed: the spec allows removal
  with their UI, but the client UI removal is L2/L3 and the #324 fence
  protects herdr.rs read internals; the capability stays grant-gated and
  advertised.
- cargo-deny/cargo-audit were not executed (no dependency change);
  recorded as a non-run rather than claimed green.
- The independent exact-head review and hosted CI are the orchestrator's
  dispatch, not claimed here.
- The #353 ARCHITECTURE GET /fleets docs item was not touched.

## Lane-hygiene note (for the orchestrator)

This lane's worktree was concurrently advanced by a second execution
stream inside the same session during part of this run (file edits and
gate attempts not issued by the visible turn; process ancestry traced to
this session's own hermes process). The final tree was reconciled from
that state: every change was reviewed against the brief, the elective
egui UI edits and the L4 doc rewrites made by that stream were reverted,
and the committed head was verified green by the runs above at commit
time. If a duplicate lane session exists, it should be quiesced.
