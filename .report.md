# #239 implementation report

- Checkout: `/Users/jirathip/.herdr/worktrees/corral/g239-plugin-engine`
- Branch: `g239/plugin-engine`
- Scope respected: no `ios/**` changes and no GitHub mutations.

## FINAL RESIDUAL FIX

Fixed cancellation poisoning in the per-card scheduler. The schedule task is now spawned before the initial card execution is awaited. Its initial result is cached (including failures), then the task continues on the configured interval. Concurrent callers retain race-safe `started` semantics and per-card result locking; the shared semaphore continues to bound concurrent card subprocesses.

Added `cancelled_initial_request_does_not_poison_schedule`: it aborts the first request while the fixture subprocess is running, waits for the schedule task to complete its initial run, and verifies a subsequent call returns the cached card. Existing periodic and interval-change coverage remains.

Gates:
- `cargo fmt --all --check` — passed.
- `cargo clippy --workspace --all-targets -- -D warnings` — passed.
- `RUST_TEST_THREADS=1 cargo test --workspace` — passed: corrald 301, corrald-ui 173 plus binary test, corrald-client 14, integration/conformance suites passed; documented live tests ignored.
- `cargo test plugin::tests --lib` — 5 passed, 0 failed.
- `python3 -u ios/check-release-demo.py --self-test` — `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- `git diff --check` — passed.

Commit: pending
