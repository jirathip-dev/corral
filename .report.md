# #250 implementation report

## Before / after

Before: first-entry Settings rendered a collapsed `Advanced device access`
disclosure (`audit_open: false`), hiding the approved Devices/Grants V2
surface behind an advanced-only gate.

After: the existing disclosure is named `Devices & Grants` and defaults
expanded (`audit_open: true`). The approved V2 hierarchy and behavior are
unchanged; the disclosure can still be manually collapsed after entry. The
app's user-facing audit copy now points to `Settings → Devices & Grants`.

## Regression/evidence capture

Native egui render regression:
`ui::register::tests::first_settings_entry_exposes_devices_and_grants_expanded`

Evidence bundle:
- `docs/design/evidence/issue-250/README.md`
- `docs/design/evidence/issue-250/capture.log`

The test renders the default Settings state with a registered device fixture
and verifies `Devices & Grants`, `THIS DEVICE`, and `REMOTE DEVICES` are in the
initial render. Installed-app re-verification remains with the orchestrator,
per the brief.

## Gates

- `cargo fmt --all --check` — exit 0, no output.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0; finished in 4m 05s.
- `cargo test --workspace -- > /tmp/g250-test.log 2>&1; echo "GATE_TEST=$?" >> /tmp/g250-test.log` — `GATE_TEST=0`.
  Aggregated from 24 `test result:` lines: 638 passed, 0 failed, 20 ignored.
- Focused native regression command — exit 0; 1 passed, 0 failed, 171 filtered out:
  `cargo test -p corrald-ui --lib ui::register::tests::first_settings_entry_exposes_devices_and_grants_expanded -- --exact --nocapture`

## Files changed

- `clients/egui/src/ui/register.rs`
- `clients/egui/src/app.rs`
- `docs/design/evidence/issue-250/README.md`
- `docs/design/evidence/issue-250/capture.log`

## Deviations / not verified

No installed-app screenshot was produced; the brief explicitly assigns
installed-app re-verification to the orchestrator after merge. The evidence
is the native egui render-test capture described above.
