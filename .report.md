# Lane report — g328-g330-recent-output-v3 (issues #328 + #329 + #330)

Branch: g328-g330-recent-output-v3
Base: 04b42e5a1a68697d242821487e09cba3df22aef1 (origin/integration)
Implementation commit: dc7a60c46585e204ef89fea4e3074900bf0a2e6e
Status: delivered

## PR #337 integration reconciliation

- Fetched `origin`; exact `origin/integration` was
  `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`.
- Merged that exact tip with `git merge --no-ff --no-edit`; this report was
  resolved to the existing #328/#329/#330 lane report rather than replacing
  it with the incoming #333 report.
- `ios/check-release-demo.py` retains both the #328/#329/#330 and #333
  source-set notes. The approved digest was recomputed over the merged
  23-entry `RELEASE_SOURCE_FILES` tuple:
  `b51c96c8706aa2e29f76cbaa579468a76ba2f24b92c95c751d69c24549e45b69`.
  The exact incoming tree contains no `DiffFilesModel.swift`; #333's
  integrated `DiffPane` model is in the manifest-listed
  `ios/FleetNotifier/Models/Models.swift`, so that coverage is retained.
  This lane's `FleetNotifierApp.swift`, `DemoFleet.swift`, `FleetViews.swift`,
  and `RecentOutputModel.swift` inputs remain in the same tuple. Both the
  existing `canonical_stream_golden.json` and this lane's
  `live_session_exchange_golden.json` test resources remain bundled by the
  merged Xcode project.
- Auto-merge audit: `FleetViews.swift` retains #333's Diff sheet terminal
  states, retry, and cancellation changes alongside the #328/#329/#330
  Recent-output partition, divider, bounded Harness, and empty-state paths.
  `FleetNotifierTests.swift` retains #333's Combine/transport helper and
  DiffReliabilityTests alongside the #328/#329/#330 ContextSplit and wiring
  tests. No conflict markers remain in either auto-merged file.

## Scope-correction compliance (serialization fence, 2026-09-01)

- .brief-scope-correction.md: src/main.rs is reserved for #332. My initial
  wiring attempt touched src/main.rs; it was restored to branch HEAD and
  `git diff -- src/main.rs` is EMPTY (restore proof: 0-line diff after
  `git restore --source=HEAD -- src/main.rs`).
- The structured exchange ledger was re-homed on `Store`
  (src/core/store.rs) so BOTH the recording adapter (herdr
  `handle_output_matched`) and the read_tail API path reach the SAME ledger
  WITHOUT any daemon-entrypoint wiring. Production wiring is complete with
  zero src/main.rs involvement; no `SERIALIZATION HOLD` marker is needed
  (the correction's hold is conditional on main.rs wiring being pending).
- Not touched: src/main.rs, src/adapters/gh_plane.rs, src/api/issues.rs.

## Reserved-surface audit

Owned files (fence-compliant):
- src/core/blocks.rs (canonical seam: composer-echo shape + exchange binding)
- src/core/provenance.rs (ExchangeLedger: structured assistant/tool events)
- src/core/store.rs (ledger home + accessor — shared by adapter + API)
- src/adapters/herdr.rs (output_matched recording hook — NOT the attach region)
- src/api/drive.rs (read_tail joins the exchange ledger)
- clients/egui/src/ui/board.rs (harness divider seam + cross-client test)
- ios/FleetNotifier/UI/RecentOutputModel.swift
- ios/FleetNotifier/UI/FleetViews.swift (Recent-output region only)
- ios/FleetNotifier/Demo/DemoFleet.swift (demo seed evidence)
- ios/FleetNotifier/App/FleetNotifierApp.swift (debug evidence launch args)
- ios/FleetNotifierTests/FleetNotifierTests.swift
- ios/project.yml + ios/FleetNotifier.xcodeproj (fixture bundling)
- ios/check-release-demo.py (re-pinned digest)
- tests/provenance.rs, tests/fixtures/live_session_exchange_golden.json

## Initial gate results (before PR #337 merge; real runs, logs in /tmp)

| Gate | Command | Result |
|---|---|---|
| Rust focused (blocks exchange_*) | cargo test -p corrald --lib exchange_ | 5 passed, EXIT 0 |
| Rust focused (composer echo) | cargo test -p corrald --lib composer_echo_shape_binds_recorded_prompts_exactly_once | 1 passed, EXIT 0 |
| Rust focused (adapter ledger) | cargo test -p corrald --lib blocked_question_is_recorded_into_the_structured_exchange_ledger | 1 passed, EXIT 0 |
| Daemon provenance suite | cargo test --test provenance | 13 passed, EXIT 0 |
| egui V3 suite | cargo test -p corrald-ui --lib v3_ | 11 passed, EXIT 0 |
| egui cross-client | cargo test -p corrald-ui --lib cross_client | 2 passed, EXIT 0 |
| Full workspace | cargo test --workspace | GATE_TEST=0 (all suites green) |
| fmt | cargo fmt --all --check | GATE_FMT=0 |
| clippy | cargo clippy --workspace --all-targets -- -D warnings | GATE_CLIPPY=0 |
| swiftc parse | swiftc -parse <changed swift files> | EXIT 0 |
| release demo | python3 ios/check-release-demo.py --self-test | PASS (digest re-pinned 4bc5d9d5… → 46a1f27e…) |
| iOS build | xcodebuild build (generic iOS Sim) | ** BUILD SUCCEEDED **; GATE_IOS_BUILD2=0 (`/tmp/gate-ios-build2.log`) |
| iOS tests | xcodebuild test FleetNotifierTests (iPhone 16 sim) | 228 tests, 0 failures; ** TEST SUCCEEDED **; GATE_IOS_TEST=0 (`/tmp/gate-ios-test.log`) |
| anti-slop | swift run anti-slop | Ran; GATE_ANTI_SLOP=1; 79 violations across 25 files (`/tmp/gate-anti-slop.log`). The log records no baseline-delta result. |

## PR #337 post-merge gate results (run serially under the shared heavy-gate lock)

| Gate | Exact command | Result |
|---|---|---|
| Release demo | `python3 ios/check-release-demo.py --self-test` | `release-demo check: PASS (Debug source preserved; Release boundary verified)`; RELEASE_DEMO_EXIT=0 (`/tmp/reconcile-release-demo.log`); merged digest `b51c96c8706aa2e29f76cbaa579468a76ba2f24b92c95c751d69c24549e45b69` |
| Rust fmt | `cargo fmt --all --check` | FMT_EXIT=0 (`/tmp/reconcile-fmt.log`) |
| Rust clippy | `cargo clippy --workspace --all-targets -- -D warnings` | CLIPPY_EXIT=0 (`/tmp/reconcile-clippy.log`) |
| Rust workspace | `cargo test --workspace` | 25 result lines; 705 passed, 0 failed, 20 ignored; CARGO_TEST_EXIT=0 (`/tmp/reconcile-cargo-test.log`) |
| Simulator boot | `xcrun simctl bootstatus 59DDC0C5-891E-4EC0-91AF-4F50DF68D793 -b` | SIM_BOOT_EXIT=0 (`/tmp/reconcile-sim-boot.log`) |
| iOS build | `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd-337 build` | `** BUILD SUCCEEDED **`; IOS_BUILD_EXIT=0 (`/tmp/reconcile-ios-build.log`) |
| iOS full tests | `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/fn-dd-337 -only-testing:FleetNotifierTests` | 238 tests, 0 failures; `** TEST SUCCEEDED **`; IOS_TEST_EXIT=0 (`/tmp/reconcile-ios-test.log`) |

The anti-slop result above is the recorded pre-merge advisory run; it was not
rerun as part of the PR #337 reconciliation gate chain.

## Mutation proofs (disposable scratch /tmp/g328-scratch, all RED→GREEN)

M1 (structured role source removed): deleted the exchange-event branch in
segment_canonical → `live_session_exchange_produces_canonical_conversation_blocks`
RED (question line collapses system/user/unknown/unknown/tool/system/unknown,
exit 101); pristine file restored byte-identical (filecmp) → GREEN (exit 0).

M2 (fixture reverted to all Unknown): rewrote
tests/fixtures/live_session_exchange_golden.json kinds → unknown →
`daemon_live_session_golden_fixture_matches_canonical_blocks` RED (fixture
drift, exit 101); fixture restored byte-identical → GREEN (exit 0).

M3 (composer-echo shape removed): deleted the `<label> ❯ text` strip in
strip_typed_input_prefix → `composer_echo_shape_binds_recorded_prompts_exactly_once`
RED (left 0 vs right 1 user blocks, exit 101); pristine restored
byte-identical → GREEN (exit 0).

## Per-issue acceptance map (fill at close)

## Per-issue acceptance map

### #328 — divider-only Harness cards
| AC | Evidence |
|---|---|
| 1 divider-only = presentation separators, no cards/count | harnessActivity renders isDividerBlock blocks as Divider() rows; harnessEventCount excludes them (iOS) + HarnessEntry.is_divider (egui). Tests: testHarnessEventCountExcludesDividerOnlyBlocks, testSharedDividerSeamClassifiesBlocks, egui v3_harness_divider_only_blocks_are_presentation_separators_not_events; evidence A/B ("3 outside conversation" with 4 blocks incl. divider; expanded shows 3 cards, no dash card) |
| 2 divider-only payload hides section | harnessActivity gated on eventCount > 0; testDividerOnlyHarnessPayloadHasZeroEvents; egui panel returns when zero events |
| 3 meaningful content preserved | partition unchanged (nothing dropped — kinds/order/text); tests assert real diagnostic text byte-visible; demo/egui fixture assert content |
| 4 one shared seam | RecentOutputRender.isDividerBlock consumed by BOTH conversation (RecentOutputRowView) and harness paths; wiring test asserts exactly 2 consult sites + egui mirror recent_is_divider_run |
| 5 production-wiring fixture + mutation RED | live-session golden fixture + demo seed carry divider-only + real content; iOS M4 (bypass seam/count/scroll in scratch) → RecentOutputDefectWiringTests RED (9 failures); restored byte-identical → GREEN |
| 6 #316 layout preserved | No layout redesign; only the divider branch + count inside the existing DisclosureGroup |
| 7 focused/full gates + simulator evidence | ContextSplitV3Tests + RecentOutputDefectWiringTests + fresh R1 full 245/245 suite (the original implementation evidence was 228/228; the post-#337 refresh was 238/238); evidence A/B at 390x844 |

### #329 — expanded Harness unscrollable
| AC | Evidence |
|---|---|
| 1 content inside panel/safe area, above composer | Expanded payload owns ScrollView bounded by RecentOutputLayout.harnessViewportHeight (280); evidence B shows payload ends above the pinned composer |
| 2 scroll first-to-last + reachable collapse at 390x844 | Bounded inner scroll + collapse control (DisclosureGroup label) outside the scroll, always visible; evidence B. Swipe gesture itself = human/device gate (simctl cannot inject touches — stated in evidence README) |
| 3 conversation/composer reachable, no nested-scroll trap | Conversation keeps its own bounded scroll; harness scroll is a sibling (no nesting); composer pinned outside; wiring test pins ONE scroll inside the DisclosureGroup |
| 4 soft-wrap, no horizontal overflow | Row rendering unchanged (Text soft-wraps; code lines use the existing RecentCodeLineView) |
| 5 collapsed by default, outside Conversation | harnessExpanded = false (debug arg opt-in only); evidence A; AC5 wiring unchanged |
| 6 production-view fixture + mutation RED | Demo seed long System/Unknown payloads; iOS M4 (removing the owning scroll/container constraint) → wiring test RED; restored → GREEN |
| 7 focused/full tests + evidence | RecentOutputDefectWiringTests (bounded scroll) + fresh R1 full 245/245 (the original implementation evidence was 228/228; the post-#337 refresh was 238/238); evidence B |
| 8 order/paging/autoscroll/accessibility/redaction preserved | Partition/order untouched; harness rows keep identifiedRows; divider rows accessibilityHidden; redaction runs daemon-side before blocks |

### #330 — empty Conversation despite fetched output
| AC | Evidence |
|---|---|
| 1 supported live session → User/Agent/Tool, non-empty Conversation | canonical_blocks_with_exchange joins prompt ledger (exactly-once user) + structured exchange ledger (agent questions → agent/tool); composer-echo shape (`<label> ❯ text`) binds recorded prompts; daemon live-session test asserts kinds [system,user,unknown,agent,tool,system,unknown]; iOS fixture test asserts non-empty conversation; evidence A |
| 2 canonical, structured, harness-agnostic; no marker tables | Roles from ExchangeRole (Assistant/Tool) + PromptProvenance (user); zero provider/model/harness name branches (egui test v3_no_harness_provider_or_model_name_classification_exists still green; grep audit in report) |
| 3 exactly-once user; structured roles; unprovenanced stays Unknown | bind_echoes one-to-one unchanged; exchange bind_events one-to-one (exchange_event_renders_exactly_once_per_read); unprovenanced lines stay Unknown (exchange_event_absent_keeps_line_unknown, live_session_without_structured_role_source_stays_unknown) |
| 4 iOS + egui identical daemon kinds/order; no client reclassification | Live-session golden fixture byte-asserted daemon-side, decoded identically on iOS (testLiveSessionGoldenFixtureRendersIdenticalKindsAndOrder) and egui (v3_live_session_golden_fixture_decodes_identically_across_clients); clients render blocks verbatim |
| 5 honest empty state + Harness reachable | hasNoAttributedConversation + "No attributed conversation in this window" in the loaded branch; evidence C; wiring test pins the branch; harness-only demo route |
| 6 generic privacy-safe production-path fixture | tests/fixtures/live_session_exchange_golden.json (generic fictional content, no harness/model names); proves non-empty Conversation, preserved Harness, order, divider flag, identical cross-client kinds; bundled in iOS test target + egui reads the committed file |
| 7 mutation RED: remove structured source / revert fixture | M1 (remove exchange binding) RED→GREEN; M2 (fixture all-Unknown) RED→GREEN; M3 (composer shape) RED→GREEN; iOS M5 (empty-state branch removed) RED→GREEN |
| 8 focused/full Rust/iOS/egui gates + simulator evidence | cargo test --workspace 0; fresh R1 iOS 245/245 (prior 228/228 and 238/238 records retained above); egui 11 v3 + 2 cross-client; evidence A + C |

## Remaining human/device gates
- Real physical-device interaction (expand tap, harness scroll swipe, collapse
  tap) — simctl cannot inject touches; the layout contracts are pinned by the
  wiring tests + mutation proofs.
- Live daemon + real herdr session end-to-end (production seam verified via
  adapter unit + HTTP integration tests; a live corrald + herdr smoke was not
  run in this lane).

## Docs
No doc changes required: docs/ARCHITECTURE.md, docs/corral/P4-conformance.md,
docs/QUICKSTART.md, docs/DEVELOPING.md contain no block-kind semantic
descriptions that this change contradicts (verified by grep audit); the
semantics live in code docs, which were updated. Evidence README added under
docs/design/evidence/issue-328-330/.

## Privacy audit
- Committed fixtures/evidence contain only fictional content (demo repos
  demo-atlas/demo-grove/demo-orchard, generic prose, no real paths,
  identities, tokens, or transcripts).
- No private output, paths, identities, transcripts, tokens, signatures, or
  unrelated issue bodies in committed evidence (OCR audit output not
  committed; screenshots are privacy-safe demo renders).
- Ledger stores hash+length identities only (never raw prompt/question text).

## New defects observed
- None known at close. (One test-expectation bug of my own during the lane —
  miscounted demo harness kinds — fixed before gates.)

## Review fix R1 (#330) and #331 integration refresh

- Starting head was the reviewed `c364a43db31dae8e0854e55d5fe1001f5b6093b7`.
- Fetched `origin`; the current `origin/integration` tip was
  `f1603435fe1f123ca76c041fbb434a7ce20e8ff0` (the #331 integration).
- Merged it without rebasing or force-pushing with
  `git merge --no-ff --no-edit f1603435fe1f123ca76c041fbb434a7ce20e8ff0`.
  The reconciliation merge commit is `8aa1130bcbc5469213729e356b0f7bcbaf91238a`,
  with parents `c364a43db31dae8e0854e55d5fe1001f5b6093b7` and
  `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`. The lane report remained the
  base; no #328/#329 production or test path was replaced.
- The release manifest kept the union of the #328/#329/#330 and #331 source
  inputs. Its merged 23-entry source set passes the release-demo self-test at
  digest `d86a471570a0ad08277b435f5056b82e9dc8290fac87316ff9df977dede0156c`.
  The #333 `Models.swift` coverage and this lane's Recent Output inputs remain
  present; both canonical/live golden fixtures remain bundled.
- Auto-merge audit: `FleetViews.swift` retains #331 terminal lifecycle behavior
  and the #328/#329/#330 Recent Output partition, divider, bounded Harness,
  and empty-state paths. `FleetNotifierTests.swift` retains #331 lifecycle
  coverage and the prior ContextSplit/wiring tests. No iOS file was edited in
  this review-fix round.

### F1 — shared canonical exchange identity

- `canonical_exchange_text` is the single `clean → redact → trim` identity.
  `ExchangeEvent::new`, `ExchangeEvent::matches_text`, and the read-side
  exchange candidates all use it. Exchange ledger snapshot/ordering/
  exactly-once behavior is unchanged; unprovenanced lines remain Unknown.
- The real HerdrAdapter fixture records the indented
  `"  Do you want to proceed?"`, then calls `canonical_blocks_with_exchange`
  on the trimmed read line and asserts an Agent block.
- RED: `cargo test --lib adapters::herdr::tests::blocked_question_is_recorded_into_the_structured_exchange_ledger -- --exact --nocapture`
  → EXIT=101 before the fix (`/tmp/r1_f1_red.log`). GREEN after the fix →
  1 passed, 0 failed (`/tmp/r1_f1_green.log`); final focused run → 1 passed,
  0 failed (`/tmp/r1-focused-f1.log`).
- Mutation RED: the scratch-only producer mutation restored cleaned-but-
  untrimmed hashing; the same production-path test returned EXIT=101 with
  `left: [Unknown]`, `right: [Agent]` (`/tmp/r1_f1_mutation_red.log`). The
  pristine `provenance.rs` restore was byte-identical (SHA-256
  `55ff9ce50719e675abe5ee4c0ecd3fcbecd25ce6450e6d8fb4c38a8083349608`) and
  GREEN returned 1 passed, 0 failed (`/tmp/r1_f1_mutation_green.log`).

### F2 — anchored composer echo

- The session-label branch now requires the structured `<token>-session`
  label shape before stripping a decoration. Plain leading-decoration echoes
  and the approved `orch-session ❯ ...` fixture remain unchanged; no provider,
  model, or harness name table was added.
- RED: `cargo test --lib core::blocks::tests::machine_output_prefix_does_not_unlock_composer_echo -- --exact --nocapture`
  → EXIT=101 before the fix (`/tmp/r1_f2_red.log`). GREEN → 1 passed, 0
  failed (`/tmp/r1_f2_green.log`). The positive anchored fixture also passed
  1/1 (`/tmp/r1_f2_positive.log` and `/tmp/r1-focused-f2-positive.log`).
- Mutation RED: removing only the `is_session_label` guard promoted
  `stdout > yes` to User with `prompt_request_id: "req-yes"`; the regression
  returned EXIT=101 with `left: [User]`, `right: [Unknown]`
  (`/tmp/r1_f2_mutation_red.log`). The pristine `blocks.rs` restore was
  byte-identical (SHA-256
  `c9a6dd6cfff6769a1a5a6484116cca8747cc132f51469a5fefb6a6398f928cba`) and
  GREEN returned 1 passed, 0 failed (`/tmp/r1_f2_mutation_green.log`).

### R1 gate evidence

- Focused final F1/F2: F1 1/1, F2 machine-output 1/1, and F2 anchored
  positive 1/1; all raw exits were 0.
- Existing #315/#330 daemon provenance and golden suite:
  `cargo test --test provenance -- --nocapture` → 13 passed, 0 failed,
  EXIT=0 (`/tmp/r1-preservation-provenance.log`). Canonical block suite →
  32 passed, 0 failed, EXIT=0 (`/tmp/r1-preservation-blocks.log`).
- Existing #330 cross-client tests: generic and live egui tests each 1 passed,
  0 failed, EXIT=0 (`/tmp/r1-cross-client-generic.log`,
  `/tmp/r1-cross-client-live.log`).
- Rust gates under `/tmp/corral-heavy-gate.lock`: `cargo fmt --all -- --check`
  EXIT=0; `cargo clippy --workspace --all-targets -- -D warnings` EXIT=0;
  isolated `TMUX_TMPDIR=/tmp/corral-r1-tmux-socket cargo test --workspace`
  EXIT=0 with 26 result lines, 707 passed, 0 failed, 20 ignored
  (`/tmp/r1-cargo-fmt-final.log`, `/tmp/r1-cargo-clippy-final.log`,
  `/tmp/r1-cargo-test-final.log`). A plain workspace run first hit only the
  unrelated shared-tmux sizing test (`32x10` vs `80x23`); the exact test
  passed 1/1 with a private tmux socket, and no tmux source changed.
- Affected native gates under the same lock: release-demo self-test PASS,
  EXIT=0 (`/tmp/r1-release-demo.log`); simulator bootstatus EXIT=0
  (`/tmp/r1-sim-boot.log`); iOS build `** BUILD SUCCEEDED **`, EXIT=0
  (`/tmp/r1-ios-build.log`); full `FleetNotifierTests` 245 passed, 0 failed,
  `** TEST SUCCEEDED **`, EXIT=0 (`/tmp/r1-ios-test.log`). The historical
  pre-#337 228 and post-#337 238 counts remain above with their original
  evidence; this post-#331 refresh is 245.
- Redaction boundary audit: `handle_output_matched` receives the raw matched
  line and sends it through the shared redacted identity before hashing;
  `read_tail` still redacts before TUI scrubbing, then canonical read-side
  candidates use the same idempotent identity. No raw exchange text is stored,
  and no role is inferred from prose.

## Delivery head
The orchestrator will record the exact pushed delivery head externally after
push. This report intentionally does not embed a self-referential final SHA.
