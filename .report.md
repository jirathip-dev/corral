# #239 implementation report

- Checkout: `/Users/jirathip/.herdr/worktrees/corral/g239-plugin-engine`
- Branch: `g239/plugin-engine`
- Scope respected: no `ios/**` changes and no GitHub mutations.

## FINAL RESIDUAL FIX

Fixed the concurrent first-call lost-wakeup race in `src/plugin.rs`. Card slots now use a shared `Notify` with the result check and waiter registration arranged so a concurrent caller cannot miss the initial completion notification. The scheduler task is still spawned before the initial run is awaited, so cancellation cannot poison the slot. Per-card locking, race-safe started semantics, and the four-permit concurrency bound remain in place.

Added/verified `cancelled_initial_request_does_not_poison_schedule`, which aborts the first request during a slow fixture execution and verifies a subsequent call resolves with a card. Periodic execution and interval-change coverage remain in the suite.

Gates:
- `cargo fmt --all --check` — passed.
- `cargo clippy --workspace --all-targets -- -D warnings` — passed.
- `RUST_TEST_THREADS=1 cargo test --workspace` — passed: corrald 301, corrald-ui 173 plus binary test, corrald-client 14, and all workspace integration suites passed; documented live tests ignored.
- `cargo test plugin::tests --lib` — 5 passed, 0 failed.
- `python3 -u ios/check-release-demo.py --self-test` — `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- `git diff --check` — passed.

Commit: final commit recorded by `git rev-parse HEAD` after report update.
