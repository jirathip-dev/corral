# .report.md — corral #354 L3 egui + WASM read-only cut

Worktree: `/Users/jirathip/.herdr/worktrees/corral/g354-l3-egui-cut`
Branch: `g354-l3-egui-cut` (exact base `75356cc2287d576adfbb6889945c98b5d5d6b303`,
origin/integration after L1 #355 + #351/#324 + L2 #356)
Spec: issue #354 comment 5505946486 as AMENDED by the queue mandate's 09-02
v2 amendment (`/Users/jirathip/.hermes/profiles/fleet-orch/run/corral-354-cut-queue.md`);
L2's iOS v2 board (`75356cc`, `ios/FleetNotifier`) is the reference design.

## Scope executed (clients/egui + WASM demo ONLY)

Removed (surfaces + code):
- Issues browser (top-level Issues tab, `ui/issues.rs` deleted, `GET /issues`
  client code, issue/fleet projections), action controls
  (prompt/interrupt/approve/kill/attach/start-worktree/probe intents,
  approval claims, step-up mint/sign) and read_diff/terminal surfaces
  (`ui/audit.rs`, `infer.rs`, `ui/audit` UI, audit client, grant-admin
  editor) — drive.rs keeps ONLY the signed `read_tail` capability; the
  protocol keeps snapshot / SSE / register.
- Board search + filter chips + Cards/Table split + detail-pane drive
  controls + load-earlier + Conversation/Harness partition + composer.
- Settings device/grant-admin remnants — Settings is connection-only
  (host URL, paste-token/localhost auto-register, #310 recovery block).
- Stale #20-era live evidence artifacts under `clients/egui/evidence/`
  (audit.json, live probes, old fleet-live.png) — replaced by synthetic
  demo captures.

Kept / added per the v2 mandate:
- Shared v2 read-only board (`ui/board.rs`, native + WASM): repo groups with
  status chips; blocked pinned top (cross-listed first in its repo, same as
  the iOS board); rows = name / repo / state / time-in-state / branch +
  small pane ref; attention order blocked → working → idle → unknown; orphan
  (no-repo) bucket last. No search, no filter chips. Last-known board +
  offline banner; live SSE with `Last-Event-ID` resume + snapshot fallback.
- Labels: herdr RAW tokens verbatim (working / idle / blocked / unknown —
  herdr 0.8.2 has no done; no Corral-invented wording anywhere; #319/#320
  wording banned). Colors/marks/rank/hexes live in
  `contracts/state-tokens.json` (L3-owned), mirrored by `theme.rs`.
- Recents v1: LIVE TAIL ONLY (≤200-line daemon cap, auto-scroll); desktop
  refreshes the tail revision-aware (cached source rev carried on refresh);
  no load-earlier, no partition, no composer. Browser drill-in renders demo
  recents from the fixture and explains live read_tail is desktop-only.
- app.rs reduced to tabs Board | Settings (register/recovery/SSE read loop /
  recents refresh retained); identity handling is passive (never mutates
  registration; recovery only user-initiated after a server-side
  bad_signature).
- WASM demo (#304): mobile 390×844 layout; demo data out of the box from a
  privacy-scrubbed synthetic fixture (rebuilt `assets/demo-fixture.json` +
  `demo.rs`); live daemon optional via first-open panel (localStorage
  persisted); no signed drive from the browser.
- `scripts/check-demo-privacy.py` self-test rebound from the removed
  issues-era fixture shape to the board-only fixture (identity/repo/URL/
  path mutations + positive controls).

## Judgment calls (documented deviations)

- Raw tokens are exactly the four herdr labels (working/idle/blocked/
  unknown); the wire `done` decode case stays for daemon tolerance but is
  ranked with idle and never produced by the demo fixture (mirrors L2).
- Pane refs render the FULL final two id segments (`p04:fleet`), matching
  iOS `paneReference`; the 8-char cap applies only to the row-label uuid
  fallback (a cap on pane refs rendered `p04:flee` — half-words).
- The first-open setup panel is normal UX; evidence captures pre-answer it
  by seeding localStorage through a temp same-origin bootstrap page (deleted
  with the generated `pkg/`), not by changing app code.
- Blocked agents are intentionally cross-listed (pinned section + repo
  section) — same sections model as L2's iOS BoardModel; the "9 agents"
  header counts agents, not rendered rows.

## Fences held

`git diff 75356cc --stat` touches ONLY `clients/egui/**` (src, tests,
assets/demo-fixture.json, README, evidence), `contracts/state-tokens.json`,
`scripts/check-demo-privacy.py` and the tracked `.report.md`. No daemon
src/, no crates/corrald-client, no ios/, no docs/ root changes. Frozen
worktrees (g311-tofu-glyph-fallback, g319-g320 + PR #343, g327, g324,
g215-demo-privacy-deploy) untouched. No main promotion, no TestFlight, no
issue closure, no rebase/force-push. (`.report.md` will be reconciled with
L2's at merge like the L2 lane did.)

## Commits

Single implementation commit at the final head — see `git rev-parse HEAD`
and the `git ls-remote` proof in the lane's final message (no self-SHA
inside this file).

## Gates (real runs, serialized under /tmp/corral-heavy-gate.lock)

| Gate | Command | Exit | Result |
|---|---|---|---|
| fmt | `cargo fmt --check` | 0 | clean (final log /tmp/l3-g-fmt5.log; earlier red runs /tmp/l3-g-fmt.log fixed by `cargo fmt`) |
| clippy | `cargo clippy --all-targets -- -D warnings` | 0 | 0 errors (final /tmp/l3-g-clippy4.log; 3 fix rounds: unnecessary_get_then_check, collapsible_if, useless_vec + live.rs unused var) |
| Full workspace test | `cargo test` | 0 | daemon 278+5+18+19+12+3+12+19+16+13+13+2+5+3+12, client 14, corrald-ui lib 72/72 + main 2 + conformance 3 + identity_recovery 1 (3 live probes ignored) — /tmp/l3-g-test2.log |
| deny | `cargo deny --locked --workspace check` | 0 | /tmp/l3-g-deny2.log |
| audit | `cargo audit --deny warnings` | 0 | /tmp/l3-g-audit2.log |
| Release build | `cargo build --release` | 0 | Finished release in 5m37s — /tmp/l3-g-rel.log |
| WASM build | `cd clients/egui && RUSTFLAGS="--remap-path-prefix=/Users/jirathip=/build" wasm-pack build --target web --out-dir pkg --out-name corral-web` + `cp web/index.html pkg/` | 0 | pkg ready — /tmp/l3-wasm3.log (first pass hit protocol.rs tokio/reqwest wasm breakage; `stream_events` gated native-only, mirroring `spawn_read_loop`) |
| Privacy scan | `python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json clients/egui/pkg` | 0 | "0 forbidden matches" |
| Privacy self-test | `python3 scripts/check-demo-privacy.py --self-test clients/egui/assets/demo-fixture.json` | 0 | all identity mutations rejected, positive controls accepted |
| RED/GREEN probe 1 (mutating dispatch absent) | drive.rs production code seeded with `fn start_worktree_marker() {}` → `cargo test -p corrald-ui --lib app::tests::mutating_drive_surface_is_absent_from_the_client` | 1 | RED: panicked "drive.rs production code must not contain start_worktree" (/tmp/l3-red1.log); seed reverted → GREEN 1 passed (/tmp/l3-green1.log) |
| RED/GREEN probe 2 (read path intact) | Board-arm `refresh_recent_output` line commented → same wiring test | 1 | RED: assertion "exactly one (live, uncommented) refresh call" failed (/tmp/l3-red2.log); restored → GREEN (/tmp/l3-green2.log) |

Test counts quoted from `cargo test` output lines in /tmp/l3-g-test2.log
(workspace, all suites, raw log). Focused corrald-ui runs during the fix
loop: 72/72 lib tests (e.g. /tmp/l3-test3.log: lib 72 + main 2 + conformance
3 + identity_recovery 1, exit 0).

## Synthetic evidence (390×844 per #304; no live daemon)

- `clients/egui/evidence/board-demo-390x844.png` — PNG 390×844, sha256
  3bca3e9a2ebad3bd5ca01bfbfe78b2b5e98e1eb7f731bfbbe085a93e566169ec —
  vision-verified: read-only/demo-data chips, read-plane banner, "fleet
  board · 9 agents · live"; sections blocked (1) → demo-alpha (3) →
  demo-bravo (2) → demo-charlie (2) → demo-delta (1) → no repo (1); raw
  chips `! blocked` / `O working` / `□ idle` / `? unknown`; no modal, no
  clipped rows, no half-truncated pane refs.
- `clients/egui/evidence/board-demo-desktop-1280x800.png` — PNG 1280×800,
  sha256 45f24af1cd290a8138b1f28be8af7c209ef56ebd704f387d0a17dabfec2a8d12 —
  same board full-width (vision-verified).
- `clients/egui/evidence/capture.log` — build/serve/capture commands,
  privacy results, PNG validation, vision-review notes.

## Explicit non-claims

- NOT verified: hosted CI (orchestrator dispatches rust.yml / web-pages.yml
  after push); the #[ignore]d live probes against a real corrald (need a
  scratch daemon + registry provisioning — runnable via the README recipe);
  recents drill-in and first-open setup panel interactions as screenshots
  (headless shell cannot click; covered by the board_frame/register unit
  tests); iOS/daemon surfaces (other lanes); native live-agent captures
  (synthetic-only per the brief); gh-pages deploy itself (web-pages.yml
  publishes from main after the orchestrator merges).
- The `.report.md` diff-vs-base is the L2 report; the orchestrator's merge
  reconcile resolves it (L2 did the same against #351).
