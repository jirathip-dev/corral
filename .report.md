# Corral #327 — host updater and client compatibility

This report covers the #327 implementation and the post-implementation R1
integration reconciliation. It deliberately does not embed this report's own
final commit SHA; the delivered head is recorded separately after push.

Scope

- Initial implementation base: the brief-pinned pre-R1 integration revision.
- R1 integration pin: f1603435fe1f123ca76c041fbb434a7ce20e8ff0.
- Branch: g327-host-update-compat.
- Initial pre-R1 remote audit: origin/g327-host-update-compat did not exist.
- R1 performed one local normal merge only; no PR or issue mutation occurred.
- No release publication or TestFlight upload was performed.
- No live Corral installation, daemon, app, config, key material, or device
  was used by the fixtures.

Changed implementation paths

- .github/workflows/ios-testflight.yml
- .github/workflows/release.yml
- README.md
- clients/egui/assets/demo-fixture.json
- clients/egui/src/app.rs
- clients/egui/src/model.rs
- clients/egui/src/protocol.rs
- clients/egui/src/state.rs
- clients/egui/src/ui/register.rs
- clients/egui/src/web.rs
- clients/egui/tests/identity_recovery.rs
- crates/corrald-client/src/client.rs
- crates/corrald-client/src/lib.rs
- crates/corrald-client/src/model.rs
- docs/HOST-COMPATIBILITY.md
- docs/corral/P4-conformance.md
- ios/FleetNotifier/App/FleetStore.swift
- ios/FleetNotifier/Models/Models.swift
- ios/FleetNotifier/UI/FleetViews.swift
- ios/FleetNotifierTests/FleetNotifierTests.swift
- ios/check-release-demo.py
- ios/host-compatibility.json
- scripts/check-host-compatibility.py
- scripts/install-corral.sh
- scripts/test-host-update-compat.sh
- scripts/test-update-corral-deploy.sh
- scripts/test-update-corral-path.sh
- scripts/update-corral.sh
- src/api/mod.rs
- src/core/mod.rs
- src/core/model.rs
- src/core/store.rs
- tests/http.rs
- tests/model.rs

Acceptance map

1. Host updates

scripts/update-corral.sh now treats the developer checkout as read-only input.
It fetches origin/main, archives that revision into a disposable source tree,
and builds there with CORRAL_BUILD_ID set to the fetched revision. It does not
inspect or require a clean tree, a main branch, or a clean index, and does not
pull, checkout, reset, clean, or merge the developer checkout. The completed
artifact is copied only after the build succeeds and the launchd execution
path is resolved. Binary drift is still checked when the commit does not move.

2. Preservation and rollback

scripts/install-corral.sh keeps the existing release-directory swap and adds
transaction snapshots for the existing app bundle, Corral config directory,
and daemon/update/rotation launchd plists. If setup or its health path fails,
launchd jobs are stopped best-effort, those external paths are restored, the
previous release is restored, and the prior jobs are bootstrapped best-effort.
Device-key and grant files remain inside the preserved config directory.

3. Build identity and client warnings

The daemon exposes GET /version and projects the same non-secret
BuildIdentity into every snapshot. The identity carries build_id, package
version, protocol_version, and schema_version. CI supplies the release commit
as CORRAL_BUILD_ID; local builds use a development marker.

The typed Rust client, egui desktop/web client, and iOS client decode the
additive field. The egui Settings health row and iOS Settings section show
compatible host version information, or an actionable warning for an absent,
old-schema, or incompatible-protocol identity. An old host is therefore
unknown rather than silently treated as compatible.

4. Dirty feature checkout regression

scripts/test-host-update-compat.sh creates a local bare origin, clones a dirty
feature checkout, advances only origin/main, and uses a cargo/launchd fixture.
It proves the installed artifact comes from origin/main, the cargo working
directory is isolated, the feature branch and tracked diff are unchanged, the
untracked developer file survives, and the daemon restarts once. A
non-source/release-shaped updater returns nonzero and logs release-required;
it no longer reports a successful silent skip.

5. Recovered-key grant boundary

clients/egui/tests/identity_recovery.rs now reads the grant set back after the
recovered key is registered and the remote grant mutation is sent. The test
asserts the persisted recovered key and read_tail grant, then drives the signed
read path again. Test diagnostics contain no key IDs or temporary paths.

6. Promotion gate

ios/host-compatibility.json is an explicit compatibility declaration.
scripts/check-host-compatibility.py rejects wrong protocol/schema values,
missing artifact evidence and declarations, and unverified string-only
artifact names. Release packaging embeds the declaration and the release and
TestFlight workflows run the validator before packaging or promotion.
The canonical P4 contract and docs describe /version and build_identity.

Pre-R1 implementation RED/GREEN and mutation evidence

- Initial RED: `bash scripts/test-host-update-compat.sh` exited 1 with
  `FAIL: updater did not record the isolated origin/main build` against the
  pre-fix updater behavior.
- GREEN: the same command exited 0 after implementation; its fixture reported
  isolated origin/main installation, explicit release-required failure,
  release/app/plist/config rollback, and promotion-gate pass/fail coverage.
- `bash scripts/test-update-corral-path.sh && bash
  scripts/test-update-corral-deploy.sh` exited 0. The six deploy scenarios
  covered release drift, identical binaries, binary-only drift, late drift
  checking, source-mode isolated deployment, and plist fallback.
- Compatibility mutation: changing the egui helper to return no warning for
  known incompatible identities made the focused test exit 101. The original
  model bytes were restored and the before/after SHA-256 values matched.
- Rollback mutation: removing restore_external_state from a disposable
  installer copy made the fixture exit 1 at the previous-app assertion.
  Production files were untouched.
- Promotion mutation: relaxing a disposable validator to accept an unverified
  artifact name made the fixture exit 1 at the decoy-artifact assertion.
  Production files were untouched.
- The recovered-key integration test exited 0 with one test passed and proves
  mutation plus read-back, not merely a local state update.

R3 canonical stamp repair

- `scripts/update-corral.sh` now compares the freshly built source artifact
  against a durable canonical identity: an updater-owned 0600 state file
  (`ui-artifact.sha256`) under the overrideable config root. The stamp is the
  pre-sign source artifact SHA-256, written atomically (mktemp + chmod 600 +
  mv) only after copy/sign/relaunch succeeds. An absent or corrupt stamp
  fails closed: the next cycle performs one safe deployment and then replaces
  the stamp.
- macOS `codesign` mutates the deployed executable after copy, so deployed
  bytes are no longer used as a comparison target. A failed UI copy now logs
  `release-required: UI deploy FAILED` and exits non-zero; an app-not-
  installed macOS host skips with a log line and no stamp change.
- `scripts/test-host-update-compat.sh` now models codesign faithfully: its
  stub appends bytes to the installed executable after copy. The fixture
  asserts the deployed bundle carries the signed mutation, the stamp equals
  the pre-sign source hash and is 0600, cycles 2-3 perform zero
  copy/sign/kill/relaunch and leave the running-client marker alive, and a
  failing UI copy never writes a stamp.
- Test-first RED: the new fixture against the pre-fix updater exited 1 with
  `FAIL: canonical UI stamp was not written after successful deployment`.
- GREEN: `bash scripts/test-host-update-compat.sh` exited 0 across three
  runs; the log reports identical UI cycles are no-ops, failed UI deployment
  never stamps a partial install, explicit release-required failure,
  rollback/config preservation, and promotion validation.
- Mutation RED probes, each restored byte-identical (`cmp`) and followed by a
  GREEN run:
  - Reverting the comparison to the signed deployed bytes
    (`ui_before_hash="$(file_hash "$UI_DEPLOY_PATH")"`) exited 1 with
    `FAIL: identical second cycle rewrote or relaunched the app`.
  - Disabling the fixture's faithful codesign mutation exited 1 with
    `FAIL: macOS codesign did not mutate the installed bundle`.
  - Writing the stamp before the deploy attempt exited 1 with
    `FAIL: canonical UI stamp was written for a failed deployment`.
- Operational discovery, reported for orchestrator/human handling: the
  currently installed macOS app bundle was NOT repaired or modified by this
  lane. Until the next updater/installer run deploys this fixed updater
  script, a live installed bundle may still be rewritten on each scheduled
  cycle under the old comparison behavior.

R2 updater repair

- `scripts/update-corral.sh` now resolves the deployed UI executable before
  comparing hashes: the macOS app bundle uses
  `Contents/MacOS/corrald-ui`, while Linux and other hosts use the same
  `$HOME/.local/bin/corrald-ui` locations as the installer. The production
  destinations remain the defaults and tests can override them with
  `CORRAL_MACOS_APP_DEST`, `CORRAL_LINUX_PREFIX`, and `CORRAL_OTHER_PREFIX`.
- The hash comparison now uses the freshly built isolated UI artifact versus
  that deployed executable. Copy, macOS re-signing, and client relaunch occur
  only on a hash mismatch; daemon execution-path resolution and restart logic
  remain unchanged.
- `scripts/test-host-update-compat.sh` runs three updater cycles against a
  dirty feature checkout. Its command stubs record UI copy/sign/stop/relaunch
  effects, use disposable platform destinations, and refuse any copy outside
  the test sandbox or the updater's disposable build tree. The first changed
  cycle records one installation and one relaunch; the two byte-identical
  cycles record no additional UI effects, leave the running-client marker
  alive, and preserve the branch, tracked diff, and untracked file.
- Test-first RED: `bash scripts/test-host-update-compat.sh` exited 1 with
  `FAIL: changed UI artifact was not installed at the disposable app
  destination` before the updater repair.
- GREEN: the same command exited 0 and reported that identical UI cycles are
  no-ops, explicit release-required failure remains enforced, installer
  rollback/config preservation passes, and promotion validation passes.
- Mutation RED: a scratch-only anchored replacement restored the comparison to
  `$REPO_DIR/target/release/corrald-ui`; the three-cycle test exited 1 with
  `FAIL: identical second cycle rewrote or relaunched the app`. The pristine
  updater was copied back and `cmp` proved the scratch copy byte-identical
  before cleanup.
- `bash scripts/test-update-corral-deploy.sh` exited 0 after every scenario
  was given disposable UI app/prefix destinations; all 6 daemon deployment
  scenarios passed without live installation state. The same suite exited 0
  under temporary Linux and FreeBSD `uname` overrides, exercising both
  non-macOS prefix destinations.

R3 integration reconciliation

- Fetched `origin/integration` and verified the pinned base
  `70317b566df51fc41b35fc297ff0bc824f298c30` (contains the promoted
  #331/#333/#337 batch: `e3c9e5d` recent-output attribution #328-#330,
  `89ce44f` promotion #344, `70317b5` reconcile merge). The remote branch
  was at the R3 head before reconciliation.
- Executed `git merge --no-edit origin/integration` (no rebase, no
  force-push). Content conflicts occurred only in `.report.md` and
  `ios/check-release-demo.py`; `ios/FleetNotifier/UI/FleetViews.swift`,
  `ios/FleetNotifierTests/FleetNotifierTests.swift`, and `src/core/store.rs`
  auto-merged.
- Resolution preserved both surfaces. The #327 report was retained (the
  incoming `.report.md` content is another lane's evidence) and this section
  was added afterward; `ios/check-release-demo.py` kept both sides'
  attribution comments and was re-pinned from the merged source set to
  `103001a13ebf3327290f225c9c202c597da6a8b9b51bceaf1988ea70e9d15e23`.
- The demo-privacy gate required by the reconcile contract exposed that the
  #327 fixture keys (`build_identity`, `build_id`, `version`,
  `protocol_version`) were absent from `scripts/check-demo-privacy.py`'s
  approved field set; the allowlist was extended with attribution so the
  gate passes on the merged tree (`privacy scan: 0 forbidden matches`).
- #327 markers remain present: `ui-artifact.sha256` stamp, `BuildIdentity`,
  `HostCompatibilityTests`, and the Host compatibility UI. #331 markers
  remain: `TerminalAttachLifecycleRegressionTests` and the terminal attach
  surface. #337 markers remain: `RecentOutputDefectWiringTests`, harness
  activity, and the honest empty-Conversation state.

R6 fail-closed signing repair

- `scripts/update-corral.sh` removes the `|| true` suppression on the
  macOS bundle re-sign. Before copying, the prior working binary is kept in
  a same-directory backup. If `codesign -s - --force` fails, the updater
  logs `release-required: UI signing FAILED`, restores the previous binary
  (and best-effort re-signs the restored bundle), exits non-zero, and never
  advances `ui-artifact.sha256`. Copy failure keeps its existing fail-closed
  guard with the backup removed. The stamp is still written only after
  copy + sign + relaunch all succeed (unchanged ordering).
- `scripts/test-host-update-compat.sh` gains a post-copy signing-failure
  fixture: a codesign stub that exits non-zero after the copy. It proves the
  updater exits with the explicit release-required failure, the installed
  binary is restored byte-for-byte to its prior content, the stamp does not
  advance, and the next cycle retries one safe deployment and then stamps.
- `scripts/test-update-corral-deploy.sh` stubs codesign as successful: the
  deploy scenarios model daemon deploy/stamp logic, and the disposable app
  directory is not a real signable bundle.
- Test-first RED: the new fixture against the pre-fix updater exited 1 with
  `FAIL: updater succeeded despite a post-copy signing failure`.
- GREEN: `bash scripts/test-host-update-compat.sh` exited 0 across three
  runs, including `OK post-copy signing failure rolls back and the next
  cycle retries`.
- Mutation RED: a scratch-only anchored replacement restored the exact
  suppressed line (`codesign ... 2>/dev/null || true`); the fixture exited 1
  with `FAIL: updater succeeded despite a post-copy signing failure`. The
  pristine updater was copied back and `cmp` proved the scratch copy
  byte-identical before cleanup.

R11 integration reconciliation (post-L1 read-only cut)

- Fetched `origin/integration` and verified the pinned base
  `10ba788212562497352d2f5142fbe47fb6da18a5` (L1 #355 read-only drive cut
  + #351/#324 provider revision contract + #352/#340 docs truth). The
  remote branch was at the R9 head
  `b6b96941005b3892441b7c8f1ab976e8b131a246` before reconciliation.
- Executed `git merge --no-edit origin/integration` (no rebase, no
  force-push). Content conflicts occurred in `.report.md` and
  `clients/egui/tests/identity_recovery.rs`; `clients/egui/src/app.rs`,
  `crates/corrald-client/src/client.rs`, `src/api/mod.rs`,
  `src/core/model.rs`, and `tests/model.rs` auto-merged.
- Superseded-surface handling (read-only cut supersedes them): the removed
  surfaces merged in as deletions — `scripts/corrald-grant.sh`,
  `src/approve/mod.rs`, `src/auth/step_up.rs`, `src/fleet/` (mod + worktree),
  `src/tmux.rs`, `tests/terminal.rs`, `tests/worktree.rs`. The #327
  `identity_recovery.rs` hunk that restored grants through the removed
  admin-grant API was dropped in favor of the incoming out-of-band registry
  provisioning (`auth.registry.set_grants`), preserving the recovered-key
  read_tail drive round trip without re-adding `/grants` admin surfaces.
- `.report.md` kept both evidence sets: the #327 report remains the primary
  artifact (with its preserved #319/#320, #325, and #332 sections) and the
  incoming #324 report is preserved verbatim as an appended evidence
  section.
- #327 fail-closed guards verified still present post-merge:
  `release-required: UI signing FAILED` (codesign failure never advances the
  stamp), `release-required: UI relaunch FAILED — rolled back` (startup
  certification before stamping), and the `ui-artifact.sha256` canonical
  stamp flow in `scripts/update-corral.sh`. The host-compat fixture
  (three-cycle + signing-failure + relaunch-failure scenarios) passes on the
  merged tree (see gate evidence).

R9 startup certification repair

- `scripts/update-corral.sh`'s `reinstall_ui()` now certifies startup before
  advancing the canonical stamp. After pkill + backgrounded relaunch of the
  freshly deployed binary, the updater polls the running probe (the same
  `pgrep -f` check used to detect the client) for a bounded ~6 s window
  (12 x 0.5 s). If the process is alive, it logs certification and stamps.
  If not, it rolls back to the backup copy of the prior binary (re-signing
  the macOS bundle), attempts to relaunch the restored binary, logs
  `release-required: UI relaunch FAILED — rolled back`, exits non-zero, and
  never writes the stamp. The not-running path (nothing was running) keeps
  its established deploy + stamp semantics; the daemon restart path is
  unchanged.
- `scripts/test-host-update-compat.sh` gains a faithful failing-relaunch
  fixture: with `CORRAL_TEST_RELAUNCH_FAIL=1` the kill stub clears the alive
  marker and the launcher stub exits non-zero when the deployed binary is
  the new artifact (crash on launch), while a restored previous binary
  relaunches normally. The fixture proves the updater exits with the
  release-required rollback log, the previous binary is restored, the stamp
  does not advance, the next cycle retries and fails again, and a healthy
  cycle later deploys, certifies, and stamps.
- Test-first RED: the new fixture against the pre-fix updater exited 1 with
  `FAIL: updater succeeded despite a failed UI relaunch`.
- GREEN: `bash scripts/test-host-update-compat.sh` exited 0 across three
  runs, including `OK failed UI relaunch rolls back, never stamps, and
  healthy cycles recover`.
- Mutation RED probes, each scratch-only, restored byte-identical (`cmp`)
  and followed by a GREEN run:
  - Removing the startup certification (stamp immediately after relaunch)
    exited 1 with `FAIL: updater succeeded despite a failed UI relaunch`.
  - Advancing the stamp even when the relaunch failed exited 1 with
    `FAIL: canonical stamp advanced despite failed relaunch`.

R8 integration reconciliation

- Fetched `origin/integration` and verified the pinned base
  `2c1881f6733e52cad3116dd224e731bca1f73dea` (advanced by the #339/#332
  merge). The remote branch was at the R7 head
  `238db31bd92da574f5f23f6b5be7599719c64ae5` before reconciliation.
- Executed `git merge --no-edit origin/integration` (no rebase, no
  force-push). Content conflicts occurred in `.report.md` and
  `ios/check-release-demo.py`; `clients/egui/src/protocol.rs`,
  `ios/FleetNotifier/Models/Models.swift`,
  `ios/FleetNotifierTests/FleetNotifierTests.swift`, and `tests/http.rs`
  auto-merged.
- Union resolutions kept both evidence sets and both digest pins: the #327
  report remains the primary artifact with the incoming #332 report
  preserved verbatim as an appended evidence section (its single private
  absolute worktree path redacted to `<corral worktree
  g332-herdr-issues-scope>`); `ios/check-release-demo.py` keeps both sides'
  attributions, retains the #332-introduced
  `APPROVED_TEST_SOURCE_DIGEST` structure, and re-pins both digests from the
  merged tree: release source
  `70854db83dcbfa665c54a6987bf606d4567f1ad68f36499a36443018a46ccf95` and
  focused grant test source
  `7a6325fb04baf9fd7294e9ec1291da8903327239bf5fdf473369abd668f8812f`.
- #327 markers remain present: `ui-artifact.sha256` stamp, fail-closed
  signing rollback, `BuildIdentity`, `HostCompatibilityTests`, and the
  compatibility UI. #332 markers remain: workspace-derived issue scoping
  and the AC8 parity grant-test seam. #325, #319/#320, #331, #333, and #337
  surfaces remain from the earlier reconciles.

R7 integration reconciliation

- Fetched `origin/integration` and verified the pinned base
  `58a41f0875baead7c680eef4f0e39a8096e2c091` (advanced by the #346/#325
  merge). The remote branch was at the R6 head
  `80069c4a0ba31535502e3333fe8ca157059a5e0d` before reconciliation.
- Executed `git merge --no-edit origin/integration` (no rebase, no
  force-push). The only content conflict was `.report.md`;
  `clients/egui/src/app.rs` and `clients/egui/src/state.rs` auto-merged.
  `ios/check-release-demo.py` needed no resolution (the #325 batch changed
  no release-manifest source).
- Resolution kept both evidence sets: the #327 report remains the primary
  artifact with its preserved #319/#320 section, and the incoming #325
  report is preserved verbatim as an additional appended evidence section.
- #327 markers remain present: `ui-artifact.sha256` stamp, fail-closed
  signing rollback, `BuildIdentity`, `HostCompatibilityTests`, and the
  compatibility UI. #325 markers remain: the `tail_requested_lines` cleanup
  regression tests and the corrected refresh documentation. #319/#320,
  #331, #333, and #337 surfaces remain from the earlier reconciles.

R5 integration reconciliation

- Fetched `origin/integration` and verified the pinned base
  `cf1c12ff17017021e6a222af914f843300480dce` (advanced by the #343 grouped
  #319/#320 merge). The remote branch was at the R4-approved head
  `edf0a4fd1b47f3db03a6723a14e1cb508f6d7f64` before reconciliation.
- Executed `git merge --no-edit origin/integration` (no rebase, no
  force-push). Content conflicts occurred in `.report.md`,
  `ios/check-release-demo.py`, and `scripts/check-demo-privacy.py`;
  `clients/egui/assets/demo-fixture.json`, `clients/egui/src/model.rs`,
  `ios/FleetNotifier/Models/Models.swift`,
  `ios/FleetNotifier/UI/FleetViews.swift`, and
  `ios/FleetNotifierTests/FleetNotifierTests.swift` auto-merged.
- Union resolutions kept both evidence sets: `.report.md` retains the #327
  report as the primary artifact and preserves the incoming #319/#320
  report verbatim as an appended evidence section;
  `ios/check-release-demo.py` keeps both sides' attribution comments and is
  re-pinned from the merged source set to
  `c88b5189bdf45938336e8c99c3775ef95d065690da9671fee8464a2bf767c065`;
  `scripts/check-demo-privacy.py` unions the #327 build-identity keys with
  the incoming `fixture_name` key.
- #327 markers remain present: `ui-artifact.sha256` stamp, `BuildIdentity`,
  `HostCompatibilityTests`, and the compatibility UI. #319/#320 markers
  remain: `Finished` state tokens, grouped board sections, and the
  supervising evidence surface. #331/#333/#337 surfaces remain from the R3
  reconcile.

R1 integration reconciliation

- Fetched `origin/integration` and verified the pinned base was
  `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`. The candidate was
  `618f8d2344dd9c37c39dbad40cd43acb9c8e33b3`; the common ancestor was
  `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`. The remote branch was at the
  candidate before reconciliation.
- Executed `git merge --no-ff --no-edit
  f1603435fe1f123ca76c041fbb434a7ce20e8ff0`. The normal merge commit is
  `2c5f180d67d4bba9a6e3368d4940d269e5b26403`, with the candidate first and
  integration second. Both parents are ancestors of the merge.
- The only merge conflicts were `.report.md` and `ios/check-release-demo.py`.
  The candidate report was retained and this R1 section was added afterward;
  the release checker retained both attribution comments and was re-pinned
  from the resolved source set.
- Candidate markers remain present: `BuildIdentity`, `HostCompatibilityTests`,
  and the Host compatibility UI. Integration markers remain present:
  `TerminalAttachLifecycleRegressionTests`, `TerminalAttachClient`,
  `tokio-tungstenite`, and `terminal_ws`.
- Incoming-only files `Cargo.lock`, `Cargo.toml`,
  `ios/FleetNotifier/UI/TerminalAttach.swift`, and `tests/terminal.rs` are
  byte-identical to the integration parent. Candidate compatibility paths
  outside shared overlap are byte-identical to the candidate.
- The candidate delta from the common ancestor is 35 paths; the integration
  delta is 9 paths; the final diff against the pinned integration parent is
  exactly the 35-path candidate delta. The final set is the 34 implementation
  paths listed above plus `.report.md` as the evidence artifact. There are no
  unexpected or missing paths, and the candidate report remains byte-identical
  in the merge tree.
- All 23 declared release-source inputs exist. The resolved digest is
  `01e5e14320da43f65477e860083ef391b4ef3147a46d67b9a1ab8a2d21c174e5`, and
  the checker pin matches it.
- The merge added no compatibility generation, compatibility floor, minimum
  build, or minimum version policy. No compatibility policy file changed
  relative to the candidate.

Gate evidence

- R11 reconcile rerun on the merged post-L1 tree, serialized under
  legitimate `/tmp/corral-heavy-gate.lock` ownership (mkdir shim, no
  overlap):
  - `bash scripts/test-host-update-compat.sh` exited 0 across three runs
    (three-cycle canonical-stamp regression, failed-deploy stamp refusal,
    post-copy signing-failure rollback + retry, failed-relaunch rollback +
    retry, dirty-checkout preservation, release-required failure,
    rollback/config preservation, promotion) — the #327 fail-closed guards
    still bite post-merge.
    `bash scripts/test-update-corral-path.sh` exited 0.
    `bash scripts/test-update-corral-deploy.sh` exited 0 (all 6 scenarios;
    also 0 under Linux and FreeBSD `uname` overrides).
  - `bash -n` over the changed scripts and the updater shellcheck exited 0;
    shellcheck for the changed test scripts exited 0 with the pre-existing
    SC2016 fixture diagnostic excluded.
  - `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=4 cargo clippy
    --workspace --all-targets -- -D warnings`, `cargo deny --locked
    --workspace check` (advisories/bans/licenses/sources ok), and
    `cargo audit --deny warnings` (584 crate dependencies) each exited 0.
  - `CARGO_BUILD_JOBS=4 cargo test --workspace` exited 0: 667 passed, 0
    failed, and 13 ignored across 25 result suites on the merged tree (the
    L1 cut removed the tmux/terminal/worktree suites).
  - `python3 ios/check-release-demo.py --self-test` exited 0 with
    `release-demo check: PASS`. The serialized iOS build exited 0 with
    `** BUILD SUCCEEDED **`; the full FleetNotifierTests target exited 0
    with 251 tests, 0 failures.
  - Demo privacy: `python3 scripts/check-demo-privacy.py
    clients/egui/assets/demo-fixture.json` exited 0 with
    `privacy scan: 0 forbidden matches`.

- R9 rerun on the fix tree, serialized under legitimate
  `/tmp/corral-heavy-gate.lock` ownership with no overlap:
  - `bash scripts/test-host-update-compat.sh` exited 0 across three runs
    (three-cycle canonical-stamp regression, failed-deploy stamp refusal,
    post-copy signing-failure rollback + retry, failed-relaunch rollback +
    retry, dirty-checkout preservation, release-required failure,
    rollback/config preservation, promotion).
    `bash scripts/test-update-corral-path.sh` exited 0.
    `bash scripts/test-update-corral-deploy.sh` exited 0 (all 6 scenarios;
    also 0 under Linux and FreeBSD `uname` overrides).
  - `bash -n` over the changed scripts and the updater shellcheck exited 0;
    shellcheck for the changed test scripts exited 0 with the pre-existing
    SC2016 fixture diagnostic excluded.
  - `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=4 cargo clippy
    --workspace --all-targets -- -D warnings`, `cargo deny --locked
    --workspace check` (advisories/bans/licenses/sources ok), and
    `cargo audit --deny warnings` (584 crate dependencies) each exited 0.
  - The isolated full command `env COLUMNS=80 LINES=23
    TMUX_TMPDIR=/tmp/corral-327-r9-tmux CARGO_BUILD_JOBS=4 cargo test
    --workspace` exited 0: 728 passed, 0 failed, and 20 ignored across 26
    result suites.
  - `python3 ios/check-release-demo.py --self-test` exited 0 with
    `release-demo check: PASS`. The serialized iOS build exited 0 with
    `** BUILD SUCCEEDED **`; the full FleetNotifierTests target exited 0
    with 251 tests, 0 failures. R9 changes no Swift, Xcode project, or
    manifest-listed release source.
  - Demo privacy: `python3 scripts/check-demo-privacy.py
    clients/egui/assets/demo-fixture.json` exited 0 with
    `privacy scan: 0 forbidden matches`.

- R8 reconcile rerun on the merged tree, serialized under legitimate
  `/tmp/corral-heavy-gate.lock` ownership with no overlap:
  - `bash scripts/test-host-update-compat.sh` exited 0 (three-cycle
    canonical-stamp regression, failed-deploy stamp refusal, post-copy
    signing-failure rollback + retry, dirty-checkout preservation,
    release-required failure, rollback/config preservation, promotion).
    `bash scripts/test-update-corral-path.sh` exited 0.
    `bash scripts/test-update-corral-deploy.sh` exited 0 (all 6 scenarios;
    also 0 under Linux and FreeBSD `uname` overrides).
  - `bash -n` over the changed scripts and the updater shellcheck exited 0;
    shellcheck for the changed test scripts exited 0 with the pre-existing
    SC2016 fixture diagnostic excluded.
  - `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=4 cargo clippy
    --workspace --all-targets -- -D warnings`, `cargo deny --locked
    --workspace check` (advisories/bans/licenses/sources ok), and
    `cargo audit --deny warnings` (584 crate dependencies) each exited 0.
  - The isolated full command `env COLUMNS=80 LINES=23
    TMUX_TMPDIR=/tmp/corral-327-r8-tmux CARGO_BUILD_JOBS=4 cargo test
    --workspace` exited 0: 728 passed, 0 failed, and 20 ignored across 26
    result suites on the merged tree.
  - `python3 ios/check-release-demo.py --self-test` exited 0 with
    `release-demo check: PASS`, validating both re-pinned digests (release
    source and focused grant test source). The serialized iOS build exited 0
    with `** BUILD SUCCEEDED **`; the full FleetNotifierTests target exited
    0 with 251 tests, 0 failures.
  - Demo privacy: `python3 scripts/check-demo-privacy.py
    clients/egui/assets/demo-fixture.json` exited 0 with
    `privacy scan: 0 forbidden matches`.

- R7 reconcile rerun on the merged tree, serialized under legitimate
  `/tmp/corral-heavy-gate.lock` ownership with no overlap:
  - `bash scripts/test-host-update-compat.sh` exited 0 (three-cycle
    canonical-stamp regression, failed-deploy stamp refusal, post-copy
    signing-failure rollback + retry, dirty-checkout preservation,
    release-required failure, rollback/config preservation, promotion).
    `bash scripts/test-update-corral-path.sh` exited 0.
    `bash scripts/test-update-corral-deploy.sh` exited 0 (all 6 scenarios;
    also 0 under Linux and FreeBSD `uname` overrides).
  - `bash -n` over the changed scripts and the updater shellcheck exited 0;
    shellcheck for the changed test scripts exited 0 with the pre-existing
    SC2016 fixture diagnostic excluded.
  - Merge-compile repair: the auto-merged `clients/egui/src/state.rs`
    carried four `Snapshot` test literals from the incoming #325 batch that
    predate the #327 `build_identity` field; `build_identity: None` was
    added to those four literals as a merge-resolution hunk. The first
    clippy/workspace runs exited 101 on that compile error and are
    excluded; the fixed-tree reruns below are authoritative.
  - `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=4 cargo clippy
    --workspace --all-targets -- -D warnings`, `cargo deny --locked
    --workspace check` (advisories/bans/licenses/sources ok), and
    `cargo audit --deny warnings` (584 crate dependencies) each exited 0.
  - The isolated full command `env COLUMNS=80 LINES=23
    TMUX_TMPDIR=/tmp/corral-327-r7-tmux CARGO_BUILD_JOBS=4 cargo test
    --workspace` exited 0: 719 passed, 0 failed, and 20 ignored across 26
    result suites on the merged tree.
  - `python3 ios/check-release-demo.py --self-test` exited 0 with
    `release-demo check: PASS`. The serialized iOS build exited 0 with
    `** BUILD SUCCEEDED **`; the full FleetNotifierTests target exited 0
    with 251 tests, 0 failures.
  - Demo privacy: `python3 scripts/check-demo-privacy.py
    clients/egui/assets/demo-fixture.json` exited 0 with
    `privacy scan: 0 forbidden matches`.

- R6 rerun on the fix tree, serialized under legitimate
  `/tmp/corral-heavy-gate.lock` ownership with no overlap:
  - `bash scripts/test-host-update-compat.sh` exited 0 across three runs
    (three-cycle canonical-stamp regression, failed-deploy stamp refusal,
    post-copy signing-failure rollback + retry, dirty-checkout preservation,
    release-required failure, rollback/config preservation, promotion).
    `bash scripts/test-update-corral-path.sh` exited 0.
    `bash scripts/test-update-corral-deploy.sh` exited 0 (all 6 scenarios;
    also 0 under Linux and FreeBSD `uname` overrides).
  - `bash -n` over the changed scripts and the updater shellcheck exited 0;
    shellcheck for the changed test scripts exited 0 with the pre-existing
    SC2016 fixture diagnostic excluded.
  - `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=4 cargo clippy
    --workspace --all-targets -- -D warnings`, `cargo deny --locked
    --workspace check` (advisories/bans/licenses/sources ok), and
    `cargo audit --deny warnings` (584 crate dependencies) each exited 0.
  - The isolated full command `env COLUMNS=80 LINES=23
    TMUX_TMPDIR=/tmp/corral-327-r6-tmux CARGO_BUILD_JOBS=4 cargo test
    --workspace` exited 0: 717 passed, 0 failed, and 20 ignored across 26
    result suites.
  - `python3 ios/check-release-demo.py --self-test` exited 0 with
    `release-demo check: PASS`. The serialized iOS build exited 0 with
    `** BUILD SUCCEEDED **`; the full FleetNotifierTests target exited 0
    with 251 tests, 0 failures. R6 changes no Swift, Xcode project, or
    manifest-listed release source.
  - Demo privacy: `python3 scripts/check-demo-privacy.py
    clients/egui/assets/demo-fixture.json` exited 0 with
    `privacy scan: 0 forbidden matches`.

- R5 reconcile rerun on the merged tree, serialized under legitimate
  `/tmp/corral-heavy-gate.lock` ownership with no overlap:
  - `bash scripts/test-host-update-compat.sh` exited 0 (three-cycle
    canonical-stamp regression, failed-deploy stamp refusal, dirty-checkout
    preservation, release-required failure, rollback/config preservation,
    promotion). `bash scripts/test-update-corral-path.sh` exited 0.
    `bash scripts/test-update-corral-deploy.sh` exited 0 (all 6 scenarios;
    also 0 under Linux and FreeBSD `uname` overrides).
  - `bash -n` over the changed scripts and the updater shellcheck exited 0;
    shellcheck for the changed test scripts exited 0 with the pre-existing
    SC2016 fixture diagnostic excluded.
  - `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=4 cargo clippy
    --workspace --all-targets -- -D warnings`, `cargo deny --locked
    --workspace check` (advisories/bans/licenses/sources ok), and
    `cargo audit --deny warnings` (584 crate dependencies) each exited 0.
  - The isolated full command `env COLUMNS=80 LINES=23
    TMUX_TMPDIR=/tmp/corral-327-r5-tmux CARGO_BUILD_JOBS=4 cargo test
    --workspace` exited 0: 717 passed, 0 failed, and 20 ignored across 26
    result suites on the merged tree.
  - `python3 ios/check-release-demo.py --self-test` exited 0 with
    `release-demo check: PASS` against the re-pinned merged digest.
  - The serialized iOS build exited 0 with `** BUILD SUCCEEDED **`; the full
    FleetNotifierTests target exited 0 with 251 tests, 0 failures.
  - Demo privacy: `python3 scripts/check-demo-privacy.py
    clients/egui/assets/demo-fixture.json` exited 0 with
    `privacy scan: 0 forbidden matches` on the union allowlist. The
    `clients/egui/pkg` wasm artifact is built in hosted CI only.

- Reconcile rerun on the merged tree, serialized under legitimate
  `/tmp/corral-heavy-gate.lock` ownership with no overlap:
  - `bash scripts/test-host-update-compat.sh` exited 0 (three-cycle stamp
    regression, failed-deploy stamp refusal, dirty-checkout preservation,
    release-required failure, rollback/config preservation, promotion).
    `bash scripts/test-update-corral-path.sh` exited 0.
    `bash scripts/test-update-corral-deploy.sh` exited 0 (all 6 scenarios;
    also 0 under Linux and FreeBSD `uname` overrides).
  - `bash -n` over the changed scripts and the updater shellcheck exited 0;
    shellcheck for the changed test scripts exited 0 with the pre-existing
    SC2016 fixture diagnostic excluded.
  - `cargo fmt --all -- --check`, `CARGO_BUILD_JOBS=4 cargo clippy
    --workspace --all-targets -- -D warnings`, `cargo deny --locked
    --workspace check` (advisories/bans/licenses/sources ok), and
    `cargo audit --deny warnings` (584 crate dependencies) each exited 0.
  - The isolated full command `env COLUMNS=80 LINES=23
    TMUX_TMPDIR=/tmp/corral-327-r3rc-tmux CARGO_BUILD_JOBS=4 cargo test
    --workspace` exited 0: 712 passed, 0 failed, and 20 ignored across 26
    result suites on the merged tree.
  - `python3 ios/check-release-demo.py --self-test` exited 0 with
    `release-demo check: PASS` against the re-pinned merged digest.
  - The serialized iOS build exited 0 with `** BUILD SUCCEEDED **`; the full
    FleetNotifierTests target exited 0 with 247 tests, 0 failures.
  - Demo privacy: `python3 scripts/check-demo-privacy.py
    clients/egui/assets/demo-fixture.json` exited 0 with
    `privacy scan: 0 forbidden matches` after the #327 fixture keys were
    approved. The `clients/egui/pkg` wasm build artifact does not exist in a
    source worktree; its CI check runs after the hosted wasm build.

- R3 fresh shell gates passed: `bash -n` over the four changed scripts, the
  updater shellcheck (exit 0), and shellcheck for the changed test scripts
  with the pre-existing SC2016 fixture diagnostic excluded.
- `bash scripts/test-host-update-compat.sh` exited 0 three times, covering
  the three-cycle canonical-stamp regression, failed-deploy stamp refusal,
  dirty-checkout preservation, release-required failure, rollback/config
  preservation, and promotion checks. `bash scripts/test-update-corral-path.sh`
  exited 0.
- `bash scripts/test-update-corral-deploy.sh` exited 0: all 6 daemon deploy
  scenarios passed with disposable UI destinations; the same suite exited 0
  under temporary Linux and FreeBSD `uname` overrides.
- Python syntax, host compatibility validation, workflow YAML validation,
  and `git diff --check` each exited 0. The release-source manifest command
  exited 0 and returned the pinned digest
  `01e5e14320da43f65477e860083ef391b4ef3147a46d67b9a1ab8a2d21c174e5`.
- `python3 ios/check-release-demo.py --self-test` exited 0 with
  `release-demo check: PASS (Debug source preserved; Release boundary
  verified)`.
- `cargo fmt --all -- --check` and `CARGO_BUILD_JOBS=4 cargo clippy
  --workspace --all-targets -- -D warnings` each exited 0.
- The authoritative isolated full command `env COLUMNS=80 LINES=23
  TMUX_TMPDIR=/tmp/corral-327-r3-tmux CARGO_BUILD_JOBS=4 cargo test
  --workspace` exited 0: 698 passed, 0 failed, and 20 ignored across 26
  result suites. Two earlier runs were invalidated by a stale shared tmux
  session (`duplicate session: corral-cursor-test-1`) and by a non-existent
  TMUX_TMPDIR falling back to the shared server (`(32, 10)` vs `(80, 23)` at
  `src/tmux.rs`); the detached test session was removed without touching the
  live terminal-attach session, and the rerun passed.
- The serialized iOS build exited 0 with `** BUILD SUCCEEDED **`.
  `HostCompatibilityTests` exited 0 with 2 tests, 0 failures.
  `TerminalAttachLifecycleRegressionTests` exited 0 with 7 tests, 0
  failures. The full FleetNotifierTests target exited 0 with 237 tests, 0
  failures. R3 changes no Swift, Xcode project, or manifest-listed release
  source.

The earlier post-merge chain that lost lock ownership and hit ENOSPC is
excluded entirely. The evidence below is from the corrected fresh runs only.

- `bash scripts/test-host-update-compat.sh` exited 0 and reported the host
  compatibility regression suite PASS.
- `bash scripts/test-update-corral-path.sh` exited 0. `bash
  scripts/test-update-corral-deploy.sh` exited 0 and reported all 6 sandbox
  deployment scenarios PASS.
- `bash -n scripts/update-corral.sh scripts/install-corral.sh
  scripts/test-update-corral-path.sh scripts/test-update-corral-deploy.sh
  scripts/test-host-update-compat.sh` exited 0.
- `python3 -m py_compile scripts/check-host-compatibility.py
  ios/check-release-demo.py` exited 0. `python3
  scripts/check-host-compatibility.py` exited 0. The workflow YAML validator
  exited 0 for `.github/workflows/release.yml` and
  `.github/workflows/ios-testflight.yml`.
- `swiftc -parse ios/FleetNotifier/App/AppModel.swift
  ios/FleetNotifier/App/FleetStore.swift ios/FleetNotifier/Models/Models.swift
  ios/FleetNotifier/UI/FleetViews.swift
  ios/FleetNotifier/UI/TerminalAttach.swift
  ios/FleetNotifierTests/FleetNotifierTests.swift` exited 0.
- Focused Rust gates exited 0: `CARGO_BUILD_JOBS=4 cargo test -p corrald
  --test http` (19 passed), `CARGO_BUILD_JOBS=4 cargo test -p corrald
  --test model` (13 passed), `CARGO_BUILD_JOBS=4 cargo test --test terminal`
  (1 passed), `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --lib
  host_identity_warns_on_unknown_protocol_or_old_schema` (1 passed), and
  `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --test identity_recovery`
  (1 passed).
- `cargo fmt --all -- --check` and `CARGO_BUILD_JOBS=4 cargo clippy
  --workspace --all-targets -- -D warnings` each exited 0.
- The unisolated `CARGO_BUILD_JOBS=4 cargo test --workspace` attempts exited
  101 with 301 passed, 1 failed, and 0 ignored in the first package. The
  untouched `tmux::tests::tmux_capture_reports_cursor_position` observed
  `(32, 10)` instead of `(80, 23)` at `src/tmux.rs:253`; the failure was
  host-wide terminal geometry/shared-server contamination, not a #327 path.
  The unisolated same-sha control was not used as a green claim.
- With disposable tmux servers and explicit dimensions, the current focused
  command `env COLUMNS=80 LINES=23
  TMUX_TMPDIR=/tmp/corral-327-r1-tmux-current CARGO_BUILD_JOBS=4 cargo test
  -p corrald --lib tmux::tests::tmux_capture_reports_cursor_position --
  --exact --nocapture` exited 0 with 1 passed. The exact-base control at
  `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`, using a separate disposable
  tmux directory and target, exited 0 with 1 passed.
- The authoritative isolated full command `env COLUMNS=80 LINES=23
  TMUX_TMPDIR=/tmp/corral-327-r1-tmux-current CARGO_BUILD_JOBS=4 cargo test
  --workspace` exited 0: 698 passed, 0 failed, and 20 ignored across 26
  result suites. This is the current-base result; the exact-base focused
  control passed the same tmux test.
- `python3 ios/check-release-demo.py --self-test` exited 0 with
  `release-demo check: PASS (Debug source preserved; Release boundary
  verified)`.
- The serialized iOS build exited 0 with `** BUILD SUCCEEDED **`.
  `HostCompatibilityTests` exited 0 with `** TEST SUCCEEDED **`: 2 tests,
  0 failures. `TerminalAttachLifecycleRegressionTests` exited 0 with
  `** TEST SUCCEEDED **`: 7 tests, 0 failures. The full FleetNotifier target
  exited 0 with `** TEST SUCCEEDED **`: 237 tests, 0 failures.
- `swift build --package-path ios/tools/anti-slop-swift --product anti-slop`
  exited 0. The current and exact-base anti-slop scans each exited 1 as
  advisory scans, each reporting 77 violations across 25 files and 17 rules.
  Their normalized `(rule, file)` identities are equal, so the R1 delta is
  none.

Atomicity and privacy classification

The updater, installer, and grant evidence are disposable fixtures only. They
prove file/path behavior and contract read-back; they do not prove a live
launchd install, physical-device behavior, release publication, TestFlight
availability, or live config persistence. No credential-shaped literals were
added. The configless audit found no new Fleet Ops, private registry,
fleets.json, or sidecar dependency in the changed implementation. Existing
unrelated repository references remain outside this lane.

The report itself is a delivery artifact and is not part of the implementation
scope list above. It contains no credentials, tokens, signatures, config
contents, private paths, device identities, or unrelated issue bodies.

# Preserved incoming evidence set — #319/#320 grouped status report (merged #343)

# Grouped implementation report — Corral #319 + #320 R2

Status: BLOCKED for final acceptance only because native egui PNG capture was unavailable in this CLI host. The implementation is committed locally.

Implementation commit (source and evidence parent): `c66cf43d14cb3b067936c46494da8a222016f159`
Base: `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`
Remote branch before reconciliation: `g319-g320-status-grouped` at `9dfdef368389a4f39cf5245743bbc372d4b0217c`.
Post-merge delivery head and remote equality are recorded below.
Integration reconciliation: see the post-merge reconciliation section below.

## Issue mapping

### #319 — status semantics

- `clients/egui/src/model.rs` and `ios/FleetNotifier/UI/BoardModel.swift` add parallel, pure presentation classifiers.
- Supervising requires canonical `done`, an identity-derived orchestrator role, and structured poll/watcher evidence from the existing adapter-projected reason/state-label field. Role alone and arbitrary prose do not qualify.
- Activity projections expose only generic Polling/Watcher labels and bounded counters. Raw command text is not returned to a view.
- Native iOS rows show the canonical `Finished` state plus a subordinate Polling line and an accessible `DisclosureGroup` evidence target. egui rows show the same state/activity split and the detail status panel exposes State, Activity, Evidence, and redacted command fields.

### #320 — grouped board presentation

- Both native clients use `Needs you → Working → Supervising → Finished → Idle`, omitting empty groups.
- Section headers include derived counts. Active supervising rows remain selectable even though their wire state is `done`.
- `contracts/state-tokens.json`, egui theme labels, and iOS state display labels use `Finished`; mark/rank/color/icon remain unchanged.
- Debug-only synthetic fixtures cover the grouped rows without changing live daemon, grants, drive, or lifecycle behavior.

## Changed files

- egui: `clients/egui/src/model.rs`, `clients/egui/src/ui/board.rs`, `clients/egui/src/theme.rs`, `clients/egui/src/demo.rs`, `clients/egui/assets/demo-fixture.json`, `clients/egui/README.md`
- iOS: `ios/FleetNotifier/UI/BoardModel.swift`, `ios/FleetNotifier/UI/FleetViews.swift`, `ios/FleetNotifier/Models/Models.swift`, `ios/FleetNotifier/UI/StateStyle.swift`, `ios/FleetNotifier/Demo/DemoFleet.swift`, `ios/FleetNotifierTests/FleetNotifierTests.swift`, `ios/README.md`, `ios/check-release-demo.py`
- shared/privacy: `contracts/state-tokens.json`, `scripts/check-demo-privacy.py`
- evidence: `docs/design/evidence/issue-319-320/implementation/`

## RED/GREEN evidence

- Base RED: `CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=.../target cargo test -p corrald-ui --test r2_status_presentation_red -- --exact --nocapture` exited 101 because the base had no `PresentationGroup` contract.
- egui focused GREEN: `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --lib model::tests::active_done_orchestrator_poll_is_supervising_with_safe_activity_text -- --exact --nocapture` exited 0; 1 passed, 204 filtered.
- egui full GREEN: `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --lib` exited 0; 205 passed, 0 failed.
- iOS focused GREEN: `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 test ... -only-testing:FleetNotifierTests/StatusPresentationRegressionTests` exited 0; 4 tests executed, 0 failures.

Mutation probes were run one at a time with byte/hash restoration:

- Rust active classification, inactive done, redaction, ordering, and Finished wording: all five exited 101 with the named regression test failing.
- iOS active classification, inactive done, redaction, ordering, and Finished wording: all five exited 65 with the named XCTest failing.
- Probe logs: `/tmp/corral-r2-mutation-evidence/summary.txt` and `/tmp/corral-r2-ios-mutation-evidence/summary.txt`.

## Gates

PASS:

- `cargo fmt --all -- --check` — exit 0.
- `CARGO_BUILD_JOBS=4 cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --lib` — exit 0; 205/205.
- `CARGO_BUILD_JOBS=4 cargo build -p corrald-ui --release` — exit 0.
- `xcodegen generate` — exit 0; generated project drift was restored and `git diff --exit-code -- ios/FleetNotifier.xcodeproj/project.pbxproj` exited 0.
- `xcodebuild -list -project ios/FleetNotifier.xcodeproj` — exit 0; FleetNotifier and FleetNotifierTests targets found.
- iOS Debug app build with `xcodebuild -jobs 4 ... CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` — exit 0.
- `python3 ios/check-release-demo.py --self-test` — exit 0.
- `python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json` — exit 0.
- `python3 scripts/check-demo-privacy.py --self-test clients/egui/assets/demo-fixture.json` — exit 0.
- Swift parse gate with `swiftc -parse -swift-version 5` over changed app/test files — exit 0.
- `git diff --check` — exit 0.

Expected host/sibling blockers:

- `CARGO_BUILD_JOBS=4 cargo test --workspace` — exit 101, 301 passed and 1 pre-existing `tmux::tests::tmux_capture_reports_cursor_position` failure because the sibling `g331-terminal-crash` worktree owns the detached `corral-cursor-test-1` tmux session.
- iOS full `xcodebuild ... -only-testing:FleetNotifierTests` — exit 65, 239 executed with 6 failures in pre-existing `GrantAdminToggleRevertTests` keychain/admin-token setup. The new status class passed in the focused run.
- iOS anti-slop — exit 1 with 77 violations across 25 files, all pre-existing; no new status-presentation violation was reported.

## Visual evidence

Committed:

- `docs/design/evidence/issue-319-320/implementation/ios-r2-status-390x844.png`
- Verified PNG dimensions: 1170 x 2532 px from a fresh iPhone 14 / iOS 26.5 simulator (390 x 844 pt).
- SHA-256: `de97157a738fa060c016f5ef08a6434e0b67b8b295faf13ced9941116a800c15`.
- Pixel inspection found compact status chips, `Needs you (1)`, `Working (1)`, `Supervising (1)`, `Finished (2)`, canonical Finished plus Polling on the supervising row, a native disclosure chevron, no overlay/auth/loading state, and no lifecycle Done/Review wording.

Not captured:

- Native egui release binary built and attempted with the synthetic fixture and selected supervising agent. The app's exact-owned frontmost probe remained fail-closed in the CLI lane. CoreGraphics enumerated the on-screen 1320 x 892 window, but both native ScreenCapture/screencapture paths refused image creation (`could not create image from window` / `could not create image from rect`). No HTML/prototype image was substituted or committed.

## Closure

The source/evidence commit is `c66cf43d14cb3b067936c46494da8a222016f159`; this follow-on report records implementation evidence. The final delivery head and remote equality are recorded after the post-merge gates. No issue, PR, or release state was mutated.

## Post-merge reconciliation — PR #337 integration refresh

- Pre-reconciliation target tip: `9dfdef368389a4f39cf5245743bbc372d4b0217c`.
- Fetched `origin/integration` and verified the exact requested tip:
  `e3c9e5db171f1059e6b7f79f4c00eed884965ad5`.
- Reconciled with the required normal `git merge --no-edit origin/integration`.
  No rebase or force-push was used.
- Conflict allowlist was exactly `.report.md` and
  `ios/check-release-demo.py`. The report conflict retained this lane's full
  #319/#320 evidence and adds this reconciliation section. The Release checker
  conflict retains the incoming #328/#329/#330, #333, and #331 source-set
  coverage plus this lane's #319/#320 digest reason; its digest was recomputed
  from the merged 23-file Release source set.
- Auto-merged surfaces were retained and audited: egui recent-output
  attribution in `clients/egui/src/ui/board.rs`; iOS recent-output and Diff
  behavior in `ios/FleetNotifier/UI/FleetViews.swift`,
  `ios/FleetNotifier/Demo/DemoFleet.swift`, and
  `ios/FleetNotifierTests/FleetNotifierTests.swift`; and the incoming daemon,
  provenance, project, and fixture changes. No #337 behavior was dropped.
- Reconciliation remained inside the requested scope. No #327 updater files,
  #332 grant tests, credentials, release, TestFlight, PRs, issues, or merges
  outside this local Git merge were touched.

## Post-merge gate results

- A first serialized attempt was invalidated and retained at
  `/tmp/corral-g319-g320-reconcile-final/`: the host hit `ENOSPC` while
  writing later logs, and the early Rust commands could not execute because a
  registry source directory was incomplete. It is not counted as evidence.
  `cargo fetch --locked` then exited 0 and restored the missing source.
- The authoritative rerun acquired `/tmp/corral-heavy-gate.lock` with
  `LOCK_WAITED=0s`; raw logs are under
  `/tmp/corral-g319-g320-reconcile-authoritative/`.
- `cd ios && xcodegen generate` — exit 0. The generated project corrected the
  incoming generated group/resource IDs while retaining both the #337 recent
  output fixture and this lane's status fixture; the refreshed
  `ios/FleetNotifier.xcodeproj/project.pbxproj` was staged. The post-refresh
  `git diff --exit-code -- ios/FleetNotifier.xcodeproj/project.pbxproj` exited 0.
- `cargo fmt --all -- --check` — exit 0.
- `cargo deny --locked --workspace check` — exit 0; advisories, bans, licenses,
  and sources passed.
- `cargo audit --deny warnings` — exit 0; no advisory warnings.
- `CARGO_BUILD_JOBS=4 cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `CARGO_BUILD_JOBS=4 cargo build --release` — exit 0.
- `TMUX_TMPDIR=/tmp/corral-g319-g320-reconcile-tmux CARGO_BUILD_JOBS=4 cargo test --workspace` — exit 0; 26 result lines aggregated to 714 passed, 0 failed, 20 ignored.
- Focused egui status test — exit 0; 1 passed, 206 filtered. Full egui library — exit 0; 207 passed, 0 failed.
- `python3 ios/check-release-demo.py --self-test` — exit 0; Release boundary verified. The merged 23-file source digest is
  `aa7ecb5c75b4ce6e9899c6f4cca4993343fa64d79b3ad022d96443fb30b08afd`.
- `python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json` — exit 0; 0 forbidden matches. Its `--self-test` — exit 0.
- Swift parse over the changed app/test files — exit 0.
- `xcrun simctl bootstatus ... -b` — exit 0.
- iOS build with the generic Simulator destination — exit 0; `** BUILD SUCCEEDED **`.
- Focused `FleetNotifierTests/StatusPresentationRegressionTests` — exit 0; 4 tests executed, 0 failures.
- Full `FleetNotifierTests` — exit 65; 249 tests executed with 6 failures, all in the pre-existing `GrantAdminToggleRevertTests` keychain/admin-token setup. The new status-presentation class passed; #332 grant tests were not changed.
- `git diff --check` — exit 0.

The full iOS result remains an explicit pre-existing host/keychain blocker, not
an implementation failure. No #327 updater files, #332 grant tests, live
credentials, release, TestFlight, issue state, or PR state were changed.

# Preserved incoming evidence set — #325 egui page-limit pin report (merged #346)

# Grouped implementation report — Corral #325

Status: READY FOR REVIEW. All acceptance criteria satisfied with real RED/GREEN
evidence; all repository gates green under `/tmp/corral-heavy-gate.lock`
serialization (no overlap with the live #311 lane — bounded wait of 200 s).

Base: `cf1c12ff17017021e6a222af914f843300480dce` (post-#343 integration),
branch `g325-egui-page-limit-pin`. Issue #325 open; no PR opened, no issue
closed, `main` untouched.

## Changed files

- `clients/egui/src/state.rs` — tests only: two committed regression tests
  pinning the `tail_requested_lines` cleanup contract. Production code
  (including the two pinned cleanup lines) untouched.
- `clients/egui/src/app.rs` — the stale `refresh_recent_output` doc comment
  only (lines 1824–1825): now states default 50, remembered explicit window
  up to 200, bounded by the existing daemon cap. No logic hunks; no overlap
  with the fenced #311 app.rs font work.
- `.report.md` — this report.

## Acceptance criteria

1. Committed state regression covering BOTH full-snapshot disappearance and
   delta removal of an agent with `tail_requested_lines[agent] = 200`:
   - `snapshot_disappearance_clears_the_remembered_tail_window` — seeds
     `a=200, b=200`, applies a reconnect snapshot dropping `b`, asserts the
     dropped agent's remembered window is gone while the live agent's
     survives.
   - `delta_removal_clears_the_remembered_tail_window` — seeds `a=200`,
     removes `a` via `Delta.del` (the `remove_agent` path), asserts the
     remembered window is gone.
2. Re-adding the same agent identity starts WITHOUT the stale 200-line
   expansion: both tests re-add the same identity (snapshot re-add / delta
   `upd` re-add) and assert `tail_requested_lines` contains no entry for it —
   the automatic refresh falls back to the default 50 until the operator
   explicitly re-expands (the app.rs `dispatch_drive_intents` re-insert path,
   unchanged).
3. Removing the `apply_snapshot` retain line → RED with positive test count:
   see Mutation probe 1.
4. Removing the `remove_agent` cleanup line → RED independently with positive
   test count: see Mutation probe 2.
5. Doc comment corrected: "Refreshes re-request the remembered per-agent
   window: default 50 lines, or an explicit `Load earlier` expansion up to
   200 lines, bounded by the existing daemon cap."
6. No runtime behavior, daemon/protocol surface, iOS code, pacing policy, or
   page-bounds change: state.rs diff is test-module-only; app.rs diff is the
   comment-only hunk above; the two cleanup lines are byte-identical.
7. All gates green (below).

## RED/GREEN mutation evidence

Probes ran on a scratch copy (`git archive HEAD` + the new tests carried
over, `git init` + commit), one production mutation per probe with an
asserted single-occurrence anchored replacement; pristine file restored
byte-identical (`cmp -s`) between probes.

Mutation probe 1 — delete the `apply_snapshot` retain line
(`self.tail_requested_lines.retain(...)`):
`cargo test -p corrald-ui --lib state::tests::snapshot_disappearance_clears_the_remembered_tail_window -- --exact`
→ exit 101; `test result: FAILED. 1 failed; 0 passed; 208 filtered out`;
panicked at state.rs:772:9: "a snapshot-dropped agent loses its remembered
200-line window". Restored byte-identical.

Mutation probe 2 — delete the `remove_agent` cleanup line
(`self.tail_requested_lines.remove(agent_id);`):
`cargo test -p corrald-ui --lib state::tests::delta_removal_clears_the_remembered_tail_window -- --exact`
→ exit 101; `test result: FAILED. 1 failed; 0 passed; 208 filtered out`;
panicked at state.rs:821:9: "delta removal clears the remembered 200-line
window". Restored byte-identical.

The real worktree was never mutated: `git status --short` shows only the
intended `app.rs` + `state.rs` + report diff at all times.

## Gate results (exact commands, real exits, serialized under the lock)

- `cargo fmt --all -- --check` → exit 0.
- Focused state tests: `cargo test -p corrald-ui --lib state::tests` →
  exit 0; `18 passed; 0 failed; 191 filtered out` (both new tests in the
  passing set).
- egui crate: `cargo test -p corrald-ui` → exit 0; 216 passed, 0 failed
  across all test binaries (lib 209 + integration 2/4/1).
- `cargo clippy --workspace --all-targets -- -D warnings` → exit 0, 0
  warnings (Finished dev profile in 2m 00s).
- `cargo test --workspace --all-targets` → exit 0; 716 passed, 0 failed,
  19 ignored.
- `cargo deny --locked --workspace check` → exit 0; "advisories ok, bans ok,
  licenses ok, sources ok".
- `cargo audit --deny warnings` → exit 0.

Lock discipline: `/tmp/corral-heavy-gate.lock` was held by
`/tmp/run-corral-311-heavy-gates.sh` (PID 25478); this lane waited 200 s
bounded, acquired atomically (`mkdir` + `owner.pid` + EXIT trap), ran the
entire battery single-threaded, and released. No overlap.

# Preserved incoming evidence set — #332 Herdr issues scope report (merged #339)

Issue #332 implementation report

Worktree

- Worktree: <corral worktree g332-herdr-issues-scope>
- Branch: g332-herdr-issues-scope
- Baseline: origin/integration at 04b42e5a1a68697d242821487e09cba3df22aef1
- Implementation commit: 05bc61f5b59bdc006f7ab63405b3fd8e34ed3c0a
- Implementation commit subject: Scope GitHub issues to live Herdr workspaces
- Evidence commit carrying the original report: f902b277688606ff9126055a0ea5f9a507e92bd3
- The report-only metadata correction is committed and pushed separately; its final tip SHA is reported externally after push and is intentionally not embedded here (no self-referential amend).
- Pre-correction `git status --short --branch`: `## g332-herdr-issues-scope...origin/integration [ahead 2]`
- Pre-correction remote proof: `f902b277688606ff9126055a0ea5f9a507e92bd3 refs/heads/g332-herdr-issues-scope`
- No pull request was created.

Integration reconciliation (PR #339)

- Fetched `origin` and verified `origin/integration` at `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`.
- Reconciled with a normal merge of that exact integration tip into `g332-herdr-issues-scope`; no rebase or force-push was used.
- Resolved the `.report.md` merge conflict by retaining this lane's complete Issue #332 report and gate evidence.
- Audited the automatic merge in `ios/FleetNotifier/Models/Models.swift`: the #333 DiffPane terminal-state behavior and comments remain, and this lane's daemon-scoped Herdr workspace contract comment remains.
- The reconciled merge tip is reported externally after push; it is intentionally not embedded here (no self-referential tip).

Implementation scope

- `src/adapters/gh_plane.rs`: production GitHub specs now derive only from current Herdr agents with owned worktree paths, attribution, and canonical `origin` GitHub remotes. Canonical origins are grouped once and native aliases are retained. The poller refreshes specs before fetches and during sleep, clears last-known state when scope changes, and does not fetch with an empty scope. Static production repo fixtures were removed; explicit test constructors retain injected scopes.
- `src/main.rs`: removed native checkout discovery plus static fallback union from the production GitHub supervisor; it uses `GhPlane::with_herdr_scope`.
- `src/api/repo.rs`, `src/api/issues.rs`, `src/integrate/mod.rs`: live issue categories are Herdr-only, both read projections filter to those categories, and topology convergence prunes stale issue-cache categories without restart.
- `tests/http.rs`, `tests/drive.rs`, `src/api/issues.rs`, `src/integrate/mod.rs`, `src/main.rs`, `src/adapters/gh_plane.rs`: production-seam, exact-five, topology, origin, alias, cache, and mutation-sensitive fixtures.
- `clients/egui/src/protocol.rs` and `ios/FleetNotifier/Models/Models.swift`: contract comments now describe daemon-scoped Herdr workspace keys. No FleetViews presentation code was changed.
- `crates/corrald-client/tests/conformance.rs` and `tests/gh_plane.rs`: old eight-repo data is now local explicit test fixture data, not production scope.
- `docs/ARCHITECTURE.md`: documents live Herdr scope, shared projections, and stale-category pruning.

Acceptance map

1. Production poll scope: PASS. `herdr_workspace_specs` reads only source `herdr` agents with worktree paths, attributes those paths through `WorkspaceAttribution`, and reads `origin` only from those owned checkout/worktree facts. No broad checkout discovery feeds the GitHub scope.
2. Absent repositories: PASS. The exact-five HTTP and signed-drive fixtures preload `static-only` and `unrelated-local` categories but return only the five live fixture identities.
3. Static production fallback: PASS. `git grep -n -E 'TRACKED_REPOS|tracked_specs|gh_repo_specs' -- src` returned no output. Static eight-repo data exists only in explicit tests.
4. Topology changes: PASS. `herdr_scope_refreshes_after_workspace_topology_changes` changes the live store from one to two to one workspace and refreshes the poll specs; `issues_cache_prunes_when_live_herdr_membership_changes` removes a live agent and prunes its cache category in place.
5. Alias/canonical dedupe: PASS. Same-basename/different-owner fixtures remain distinct canonical specs; same canonical origin aliases are folded into one spec, and the query builder emits one repository clause per spec.
6. Cache-only read paths: PASS. `/issues` and signed `read_issues` call the shared `issues_view`; no discovery, GitHub, git subprocess, private registry, or sidecar is in those request paths. The existing adapter-dispatch-count fixture remains green.
7. Five-repo production seam: PASS. Privacy-safe labels are `fixture-alpha`, `fixture-bravo`, `fixture-charlie`, `fixture-delta`, and `fixture-echo`; unrelated labels are `static-only` and `unrelated-local`. Both `/issues` and signed `read_issues` assert exactly the five in-scope keys.
8. Client parity/measurement: PASS at the required closest client seams. R2 proves the same five scoped category identities in iOS and egui with a five-plus-unrelated fixture; the actual iOS app target build and `IssueBrowserTests` also pass. Timing below distinguishes client projection/render seams from daemon fixture timing and makes no production benchmark claim.

TDD RED evidence

- `cargo test --lib adapters::gh_plane::tests::default_constructor_waits_for_workspace_scope -- --exact --nocapture`
  - `GATE_EXIT=101`
  - Failure: assertion expected 0 repository clauses but got 8.
- `cargo test --lib api::issues::tests::issues_view_excludes_cached_categories_without_live_herdr_workspace -- --exact --nocapture`
  - `GATE_EXIT=101`
  - Failure: returned `in-scope` plus `unrelated` instead of only `in-scope`.

Scratch mutation proof

- Mutation A disabled the API projection `map.retain`.
  - Command: `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture`
  - `MUTATION_RED_EXIT=101`; public categories were 7, expected 5.
  - Restored source hash comparison: `RESTORE_CMP_EXIT=0`.
  - Restored command result: `RESTORED_GREEN_EXIT=0`; public categories 5.
- Mutation B disabled integrator cache pruning.
  - Command: `cargo test -p corrald --lib integrate::tests::issues_cache_prunes_when_live_herdr_membership_changes -- --exact --nocapture`
  - `MUTATION_RED_EXIT=101`; assertion reported the removed workspace category was not pruned.
  - Restored source hash comparison: `RESTORE_CMP_EXIT=0`.
  - Restored command result: `RESTORED_GREEN_EXIT=0`; cache categories changed from 1 to 0.
- The final committed source was checked after both restores; no mutation remained in the commit.

Review fix R1 regression protection

- Starting reviewed head: `30f280c16cd3323f807b07088c1e4eec80672fe3`.
- `GhPlane::with_config_and_herdr_scope` is available only to tests or the
  `test-utils` embedding feature. It uses `herdr_scope: Some(attribution)`
  with an empty initial spec vector. Ordinary production builds retain the
  existing production constructors; only `with_herdr_scope` supplies the live
  Herdr scope.
- Focused GREEN command: `cargo test -p corrald --test gh_plane -- --nocapture`.
  - `FOCUSED_GH_EXIT=0`.
  - 13 tests: 12 passed, 0 failed, 1 ignored. The four R1 tests all passed.
- Mutation A (run-loop refresh): in disposable `/tmp/g339-r1-scratch`,
  deleted both `self.refresh_specs()` blocks in the `SleepUntil` and `Poll`
  arms. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane herdr_scope_run_loop_refreshes_additions_and_removals -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the test timed out waiting for the query carrying
    both `fixture-owner/one` and `fixture-owner/two`.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Mutation B (ownership attribution): replaced `attribution.repo_for(&path)`
  with the path filename in `herdr_workspace_specs`. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane herdr_scope_excludes_unowned_git_checkout -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the assertion saw 2 specs instead of 1.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Mutation C (empty-scope guard): deleted the `specs.is_empty()` early
  continue in the poll arm. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane empty_live_scope_never_calls_transport -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the empty-store test saw 3 transport calls
    instead of 0 across the cadence window.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Mutation D (source filter): deleted `agent.source == "herdr"` from the
  specs fixture filter. Command:
  `CARGO_TARGET_DIR=/tmp/g339-r1-mutant-target cargo test -p corrald --test gh_plane herdr_scope_excludes_non_herdr_agent -- --exact --nocapture`.
  - `MUTATION_RED_EXIT=101`; the non-Herdr exclusion assertion failed.
  - Restored the pristine source; `RESTORE_BYTE_IDENTICAL=1`.
- Scratch proof: `FINAL_SCRATCH_SOURCE_BYTE_IDENTICAL=1`,
  `SCRATCH_REMOVED=1`, and `MUTATION_CHAIN_EXIT=0`. The real worktree was
  never mutated by these probes.
- Final shared-heavy-lock gates (after formatting):
  - `cargo fmt --all --check` — `GATE_EXIT=0`.
  - `cargo clippy --workspace --all-targets -- -D warnings` — `GATE_EXIT=0`.
  - `cargo test --workspace` — `GATE_EXIT=0`; 25 suites, 703 passed, 0
    failed, 20 ignored.

Fixture measurements

- Query payload clauses: `cargo test -p corrald --lib adapters::gh_plane::tests::query_covers_all_repos_once -- --exact --nocapture`
  - `fixture_scope_measurement poll_payload_repo_clauses_before=8 poll_payload_repo_clauses_after=5`
- Cache topology pruning: `cargo test -p corrald --lib integrate::tests::issues_cache_prunes_when_live_herdr_membership_changes -- --exact --nocapture`
  - `fixture_scope_measurement cache_categories_before=1 cache_categories_after=0`
- Read projection/cache and first-open sample: `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture`
  - Scoped final sample: `cache_categories_before=7 cache_categories_after=7 public_categories=5 first_open_ms=34.303`
  - Unscoped mutation sample: `cache_categories_before=7 cache_categories_after=7 public_categories=7 first_open_ms=50.647`
  - The read path intentionally does not mutate the cache; topology convergence performs pruning. These local timing samples are classified fixture-only and are not a hardware-independent performance claim.

Exact committed pre-R1 gates

- `cargo fmt --all -- --check`
  - `GATE_EXIT=0`
- `cargo clippy --workspace --all-targets -- -D warnings`
  - `GATE_EXIT=0`
  - `Finished dev profile ... in 6.10s`
- `cargo test --workspace`
  - `GATE_EXIT=0`
  - 25 suite summaries: 699 passed, 0 failed, 20 ignored.
  - Key aggregate: `test result: ok. 306 passed; 0 failed...`
  - The default run skips only the repository's pre-existing live/real-daemon tests marked ignored; no private issue bodies were fetched or copied.
- `cargo deny --locked --workspace check`
  - `GATE_EXIT=0`
  - `advisories ok, bans ok, licenses ok, sources ok`
  - It reports pre-existing duplicate-version warnings in the locked dependency graph; this change adds no dependencies and does not alter `Cargo.lock`.
- `cargo audit --deny warnings`
  - `GATE_EXIT=0`
  - `Loaded 1235 security advisories`; `Scanning Cargo.lock for vulnerabilities (584 crate dependencies)`.
- `cargo test -p corrald-ui --lib issues`
  - `GATE_EXIT=0`
  - 10 passed, 0 failed, 190 filtered out.
- `cargo test -p corrald --test http no_fleets_json_reference_anywhere_in_src -- --exact --nocapture`
  - `GATE_EXIT=0`; 1 passed.
- Final focused committed seam commands all exited 0: live-scope constructor, topology refresh, query measurement, cache pruning, exact-five `/issues`, and exact-five signed `read_issues`.

Privacy/configlessness audit

- `git grep -n -E 'fleets\\.json|fleet_ops|fleet-ops|sidecar|gh_repo_specs|tracked_specs|TRACKED_REPOS' -- src/api src/adapters/gh_plane.rs src/main.rs` returned no output.
- Synthetic fixture labels only; no unrelated repository issue body was copied into `.report.md` or evidence.
- No private fleet registry, Fleet Ops registry, or sidecar is used as a source of GitHub scope.

R2 evidence-only AC8 closure

- Authoritative contract: `.fix-r2.md` at exact starting head `e272735cdf446351fe2d195d6e9526dda8e1032e`.
- R2 changed no production code, test code, fixtures, or generated project files. The only tracked R2 change is this report. `docs/OPERATIONS.md` remains untouched.
- The iOS and Cargo/Xcode evidence commands ran under uninterrupted ownership of `/tmp/corral-heavy-gate.lock`. The first serialized run waited for sibling native work to finish (`/tmp/g339-r2-evidence/lock-wait.log`, then `LOCK_ACQUIRED`); the corrected simulator pair was rerun under a fresh lock after fixing only the disposable probe harness.

Deterministic fixture and category contract

- Input fixture: five scoped categories `fixture-alpha`, `fixture-bravo`, `fixture-charlie`, `fixture-delta`, and `fixture-echo`, plus unrelated `static-only` and `unrelated-local`. Probe data contains no issue bodies.
- The client probes used the same 7-category/row fixture definition, supplied the 5 scoped identities to the post-filter client projections, and asserted exactly 5 presented categories/rows with the same five sorted identities. They also asserted that neither unrelated identity reached presentation.

iOS simulator evidence

- Device/runtime: `iPhone 16 (59DDC0C5-891E-4EC0-91AF-4F50DF68D793)`, Booted, iPhoneSimulator26.5 SDK; Xcode `26.6 (17F113)`.
- Current worktree build command:
  `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' /usr/bin/xcodebuild -jobs 4 build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-current-ios-dd`
  - Exit `0`; raw log `/tmp/g339-r2-evidence/ios-current-build.log`; `** BUILD SUCCEEDED **`.
  - The build emitted pre-existing `TerminalAttach.swift` `NSLock` async-context warnings; no build error occurred.
- Current worktree relevant test command:
  `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' /usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-current-ios-dd -only-testing:FleetNotifierTests/IssueBrowserTests`
  - Exit `0`; raw log `/tmp/g339-r2-evidence/ios-current-issue-tests.log`; `** TEST SUCCEEDED **`; `IssueBrowserTests` executed 9 tests, 0 failures.
- Production-shaped simulator probe commands (each run after `xcodegen generate` in its own disposable tree):
  - Baseline: `/usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-corrected-baseline-dd -only-testing:FleetNotifierTests/AC8CategoryParityTests`
  - Candidate: `/usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r2-corrected-candidate-dd -only-testing:FleetNotifierTests/AC8CategoryParityTests`
  - Baseline exit `0`; candidate exit `0`; each executed 1 test with 0 failures. Raw logs: `/tmp/g339-r2-evidence-corrected/baseline.log` and `candidate.log`.
  - Both final logs report:
    `AC8_IOS_PROBE fixture_input_categories=7 wire_categories=5 presented_categories=5 presented_keys=fixture-alpha,fixture-bravo,fixture-charlie,fixture-delta,fixture-echo cache_rows=7 presented_rows=5 repetitions=20 ...`
  - The probe calls production `IssuesBrowserWire` decoding, `IssuesBrowserPane.apply`, and the `IssueBrowser.rows` projection used by the SwiftUI issue browser. It is the closest executable iOS first-open seam; it is not relabeled as a live-network or physical-device benchmark.

Egui production-shaped display evidence

- Commands, run separately at both refs in disposable scratch trees with isolated Cargo targets:
  - Baseline: `CARGO_TARGET_DIR=/tmp/g339-r2-target-egui-baseline cargo test -p corrald-ui --lib ui::issues::tests::ac8_five_scoped_categories_reach_the_repo_rail -- --exact --nocapture`
  - Candidate: `CARGO_TARGET_DIR=/tmp/g339-r2-target-egui-candidate cargo test -p corrald-ui --lib ui::issues::tests::ac8_five_scoped_categories_reach_the_repo_rail -- --exact --nocapture`
  - Baseline exit `0`; candidate exit `0`; each: 1 passed, 0 failed. Raw logs: `/tmp/g339-r2-evidence/egui-baseline-ac8.log` and `egui-candidate-ac8.log`.
  - The probe rendered the actual egui repo rail and asserted the same five labels were visible, with no unrelated label. Both logs report the same five `presented_keys` and `presented_categories=5`.

Old-vs-new measurement (same host, same deterministic fixture, 20 repetitions)

- Poll payload/repository clauses:
  - Baseline `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`: `cargo test --lib adapters::gh_plane::tests::query_covers_all_repos_once -- --exact --nocapture` passed; `poll_payload_repo_clauses_before=8 poll_payload_repo_clauses_after=8`.
  - Candidate `e272735cdf446351fe2d195d6e9526dda8e1032e`: the same command passed; `poll_payload_repo_clauses_before=8 poll_payload_repo_clauses_after=5`.
  - Raw logs: `/tmp/g339-r2-evidence/poll-baseline.log` and `poll-candidate.log`.
- Read/cache fixture counts from the identical exact-five HTTP test workload:
  - Baseline: 7 cache categories before, 7 after, 0 cached rows, 7 public categories; the test's expected RED exit was `101` because pre-#332 behavior exposed the unrelated categories.
  - Candidate: 7 cache categories before, 7 after, 0 cached rows, 5 public categories; 20 repetitions had raw exit `0`.
  - Raw summary: `/tmp/g339-r2-evidence/http-repeated-measurements.log`.
- Daemon `/issues` fixture-only first-open seam, not a client benchmark:
  - Baseline: min `15.191 ms`, median `20.562 ms`, max `43.709 ms`.
  - Candidate: min `13.084 ms`, median `16.365 ms`, max `95.451 ms`.
- iOS production projection seam, 20 repetitions inside one simulator XCTest:
  - Baseline: min `0.018 ms`, median `0.018 ms`, max `0.081 ms`.
  - Candidate: min `0.018 ms`, median `0.018 ms`, max `0.084 ms`.
- egui production display seam, 20 repetitions inside one renderer test:
  - Baseline: min `0.274 ms`, median `0.285 ms`, max `53.220 ms`.
  - Candidate: min `0.703 ms`, median `0.763 ms`, max `292.929 ms`.
- These timings are local fixture/projection measurements only. They disclose min/median/max and identical workload/host conditions; they are not production latency or throughput claims.

Disposable harness correction

- The first disposable iOS probe attempt exited `65` because the temporary XCTest omitted `@testable import FleetNotifier`; no repository file was changed. After adding that import and rerunning the same baseline/candidate pair under the shared lock, both final logs above exited `0`. The temporary probe trees and harnesses remain outside the repository.

R2 scope and limits

- No live GitHub poll was run. A live end-to-end first-open would require a credentialed running Corral daemon with `read_issues` grant plus current Herdr/GitHub access; no credentials were used or copied, and no private issue content entered evidence.
- No physical iOS device run was performed. The remaining unverified gate is physical-device/live-daemon behavior; the simulator build, existing iOS issue-browser tests, and closest production iOS projection seam are verified above. No synthetic timing is presented as that missing live gate.
- Temporary baseline/candidate source copies and probe-only tests were under `/tmp/g339-r2-*`; they were not tracked or applied to the real worktree. Scope verification found the current tracked Xcode project byte-identical to `HEAD` (`804cb253a1fc380bb5763f9d70c4fc63d8fc547e5bfa017487e65e805155a3b8`).

---

# Retained #331 terminal attach lifecycle report

# Issue #331 — Terminal attach lifecycle

Implementation commit: `ef43e1e76ec027c8dc8c8b682877e1dd8d9e6045`
Branch: `g331-terminal-crash`
Base: `origin/integration` at `04b42e5a1a68697d242821487e09cba3df22aef1`

## Evidence availability and scrub method

- Physical-device probe: `xcrun devicectl list devices` exited 0 and reported zero paired device rows. No physical-device stack or physical open/dismiss/reopen recording was available.
- A local non-device `FleetNotifier` diagnostic artifact was reachable from the earlier pre-fix production-client test. It was parsed locally and reduced to exception class, termination signal, fault-thread number, app symbol, source filename, and source line. Raw report contents, identifiers, paths, and payloads were not copied into this report or committed.
- The scrubbed artifact identified `EXC_CRASH`/`SIGABRT` in the pre-fix `TerminalAttachClient.connect` at source line 58 while `testUnavailableWorktreeIsRejectedBeforeOpeningTheSocket` exercised the client. This is a proven non-device crash boundary, not a claim about the physical-device root cause.
- Remaining human gate: on a paired physical device, open the agent row, choose Terminal, wait for the first frame, dismiss the sheet, reopen Terminal, and capture a privacy-scrubbed stack/recording. Do not publish device identifiers, terminal contents, local paths, keys, signatures, tokens, or request payloads.

## Proven defect boundary

The old client created `URLSessionWebSocketTask` from the host URL before rejecting an unavailable worktree, and passed the host URL directly to the WebSocket factory. The production regression test therefore reached the crash boundary instead of only checking capability availability. The fix validates the worktree and cancellation state before opening a socket, normalizes HTTP(S) hosts to WS(S), and turns connection/protocol/handshake failures into typed errors.

This proves the reachable simulator/test-process boundary. The physical-device cause remains unknown until the human gate above is performed.

## Acceptance map

- Production lifecycle seam: `TerminalAttachLifecycleRegressionTests` calls `TerminalAttachClient.connect` and `TerminalAttachSessionModel`, not just capability predicates.
- Success/error/cancel/dismiss/retry: explicit connecting, connected, and failed states; streamed frame/cursor updates; Retry starts a new generation; Done and disappearance cancel/close; cancellation suppresses late updates.
- Failure handling: unavailable worktree, invalid host, encoding, handshake timeout, server refusal, network failure, malformed/out-of-order/binary messages, and command failures are surfaced without force unwraps, swallowed client errors, or fatal paths. Failed UI includes Retry and Done.
- Wire and cleanup: HTTP(S) to WS(S) URL normalization, signed Attach envelope preservation, handshake/opened validation, generation-scoped late-result suppression, socket cleanup, and best-effort daemon session cleanup.
- Daemon: authenticated WebSocket integration covers signed open, explicit Attach grant, resize, input, frame, close, outside-root refusal, and malformed command error/closure. Existing default-deny authorization, cwd confinement, auditing, and redaction remain in place.
- Release source manifest: `FleetViews.swift` and `TerminalAttach.swift` are manifest inputs; the approval digest was re-pinned and the self-test passed.

## Files

- `ios/FleetNotifier/UI/TerminalAttach.swift`
- `ios/FleetNotifier/UI/FleetViews.swift` (Terminal-sheet region only)
- `ios/FleetNotifierTests/FleetNotifierTests.swift`
- `ios/check-release-demo.py`
- `src/api/mod.rs` (terminal WebSocket error/cleanup handling only)
- `tests/terminal.rs`
- `Cargo.toml`
- `Cargo.lock`

No Recent-output, Diff, or Issues-scope regions were changed. No generated Xcode project file was left modified.

## RED/GREEN and mutation evidence

- Historical pre-fix production-client run: the reachable non-device diagnostic artifact recorded `EXC_CRASH`/`SIGABRT` at `TerminalAttachClient.connect` source line 58 while the unavailable-worktree regression exercised it.
- Final focused iOS lifecycle run: 7 tests, 0 failures, exit 0.
- Final iOS suite: 225 tests, 0 failures, exit 0.
- Disposable scratch mutation removed the production worktree-unavailable guard from `TerminalAttachClient.connect`. The same 7-test focused suite compiled and went RED with exit 65: `testUnavailableWorktreeIsRejectedBeforeOpeningTheSocket` observed `Terminal connection failed.` instead of `Terminal worktree is unavailable.`
- The pristine production file was copied back and byte-compared: `cmp` exit 0. The same focused suite then went GREEN with 7 tests, 0 failures, exit 0.
- The scratch copy and its dedicated simulator were deleted after verification; the real worktree remained limited to the files listed above.

## Exact gate results

Commands were run from the repository root with full logs retained outside the worktree. Heavy Cargo/Xcode runs used the shared gate lock, `CARGO_BUILD_JOBS=4` for Cargo, and `-jobs 4` for Xcode.

- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --test terminal authenticated_terminal_websocket_runs_lifecycle_and_rejects_bad_frames -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test --workspace` — exit 0; 26 result lines aggregated to 695 passed, 0 failed, 20 ignored.
- `swiftc -parse` on the changed Swift production/test files — exit 0.
- `python3 ios/check-release-demo.py --self-test` — exit 0; `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- iOS Debug build with the brief's FleetNotifier project/scheme and generic simulator destination — exit 0.
- `xcodebuild test` focused on `FleetNotifierTests/TerminalAttachLifecycleRegressionTests` — exit 0; 7 passed, 0 failed.
- `xcodebuild test` focused on `FleetNotifierTests` — exit 0; 225 passed, 0 failed.
- Anti-slop advisory: exit 1 as designed for existing diagnostics; HEAD reported 77 violations across 25 files versus 79 on the base snapshot. No new anti-slop diagnostic was reported for the terminal changes. No new production force unwrap or `try!` was added.
- `swiftlint lint --strict` was also run; exit 2 from the repository's existing strict-lint backlog plus the expanded terminal file's default style/size thresholds. This is non-binding here; build, parse, anti-slop, and test gates above are green.
- Tracked-secret scan equivalent to the repository CI `git ls-tree` check — exit 0; no tracked secret files. `gitleaks` was not installed locally, so the hosted full-tree gitleaks job remains CI-owned.

## Simulator evidence

A fresh, privacy-safe simulator was created for evidence and removed afterward. The built Debug app was installed and launched with the existing synthetic `-demoMode -corralTerminalPreview` path. Open capture, programmatic terminate, relaunch, and reopen capture all exited 0; both scaled 390x844 captures visibly showed the synthetic Terminal Preview with a rendered frame. The captures were vision-inspected and then deleted; no terminal text or path was committed or attached.

This is explicitly simulator preview evidence, not physical-device proof. `simctl terminate` is not equivalent to a user dismissing the sheet, so the required physical open/frame/dismiss/reopen behavior remains the human gate above.

## Privacy/security audit

- No credentials, key material, signatures, tokens, host/device identifiers, request bodies, raw crash reports, terminal output, or local absolute paths are present in this report or the committed diff.
- Authenticated Attach signing and replay protection are retained; daemon failures use bounded generic messages.
- Attach remains default-deny, worktrees remain root-confined, terminal actions remain audited, and no arbitrary command or new write path was introduced.

## Integration reconciliation

- Pre-reconciliation branch tip: `614b4b462112c99d0485f5a690484e383e1964ca`.
- `git fetch origin integration` verified the requested `origin/integration` pin: `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`.
- Reconciled with `git merge --no-ff --no-edit 03b61c9cfce1d851168b2d9d2beab1fb773fe99b`; no rebase or force-push was used. The only conflicts were `.report.md` and `ios/check-release-demo.py`. This file remains the #331 report.
- `FleetViews.swift` auto-merge audit retained this lane's `terminalAvailability`, optional `TerminalAttachClient`/worktree inputs, and `TerminalAttachView` wiring alongside #333's diff loaded/empty/error, Retry, cancellation, and `hasLoaded` rendering.
- `FleetNotifierTests.swift` auto-merge audit retained `ScriptedTerminalSession` and `TerminalAttachLifecycleRegressionTests` alongside #333's `DiffReliabilityTests` and deterministic drive status/cancellation fixtures.
- The requested integration tree has no separate `DiffFilesModel.swift`; #333's diff-file model coverage is in `Models.swift` (`DiffPane`/`DiffFileWire`). That coverage remains in the manifest, and `FleetViews.swift` plus `TerminalAttach.swift` remain Release inputs.
- The merged Release source digest is `3eabcbd125ba4d7a108505bc3e7bbe84db66c3c6c70d5ce04e0d8856584dd822`, with both #333 and #331 digest reasons retained in `ios/check-release-demo.py`.
- The final diff path set against the pinned integration tree was audited as exactly: `.report.md`, `Cargo.lock`, `Cargo.toml`, `ios/FleetNotifier/UI/FleetViews.swift`, `ios/FleetNotifier/UI/TerminalAttach.swift`, `ios/FleetNotifierTests/FleetNotifierTests.swift`, `ios/check-release-demo.py`, `src/api/mod.rs`, and `tests/terminal.rs`.

## Post-reconciliation gate results

All gates below ran after the merge resolution in one serialized process that acquired `/tmp/corral-heavy-gate.lock`, used `CARGO_BUILD_JOBS=4`, and used `xcodebuild -jobs 4`; the lock was released cleanly.

- `python3 ios/check-release-demo.py --self-test` — exit 0; `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --test terminal authenticated_terminal_websocket_runs_lifecycle_and_rejects_bad_frames -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test --workspace` — exit 0; 26 result lines aggregated to 695 passed, 0 failed, 20 ignored.
- `swiftc -parse ios/FleetNotifier/App/AppModel.swift ios/FleetNotifier/App/FleetStore.swift ios/FleetNotifier/Models/Models.swift ios/FleetNotifier/UI/FleetViews.swift ios/FleetNotifier/UI/TerminalAttach.swift ios/FleetNotifierTests/FleetNotifierTests.swift` — exit 0.
- `xcodebuild -jobs 4 build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd-331-reconcile build` — exit 0; `BUILD SUCCEEDED`.
- `xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=[SIMULATOR_ID_REDACTED]' -derivedDataPath /tmp/fn-dd-331-reconcile -only-testing:FleetNotifierTests/TerminalAttachLifecycleRegressionTests` — exit 0; 7 passed, 0 failed.
- `xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=[SIMULATOR_ID_REDACTED]' -derivedDataPath /tmp/fn-dd-331-reconcile -only-testing:FleetNotifierTests` — exit 0; 235 passed, 0 failed.
- Post-reconciliation anti-slop advisory — exit 1 with 77 violations across 25 files; the pinned integration control was 79 violations across 25 files. This remains pre-existing advisory output, not a blocking merge gate.
- `git diff --check` — exit 0; no whitespace errors.

## New defects / remaining work

No new functional defects were found in the verified surface. The only remaining acceptance item is the explicitly blocked physical-device stack and open/frame/dismiss/reopen gate; no physical-device claim is made here.

## Fresh R3 integration reconciliation

- Pre-reconciliation branch tip: `57a3e75d3a533836a88ca982fec8f161532ae8e9` (remote branch matched before the merge).
- Fetched `origin` and verified the exact requested `origin/integration` tip: `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`.
- `git merge-tree --write-tree origin/integration HEAD` predicted exactly one conflict, `.report.md`; all source/test paths were expected to auto-merge.
- Ran `git merge --no-edit origin/integration` normally. Actual unmerged path list was exactly `.report.md`; no rebase, amend, force-push, PR mutation, issue mutation, or direct integration write was used.
- Conflict resolution preserved the complete #332 implementation/R1/AC8 report and appended the complete incoming #331 terminal lifecycle report under `# Retained #331 terminal attach lifecycle report`. No production or test file was edited during resolution.
- The final merge tip is intentionally reported externally after commit/push rather than embedded in this report (no self-referential metadata).

## R3 gate evidence invalidation and recovery

- The corrected post-merge gate runner at PID `19201` was invalidated by orchestrator-reported critical-ENOSPC cleanup contamination while that runner was active; its partial logs under `/tmp/g339-r3-corrected-evidence` are retained only as invalidation evidence and are not counted as reconciliation gates.
- PID `19201` and all descendants were verified absent. The lock owner file still contained exactly `19201`; only that own stale owner file and `/tmp/corral-heavy-gate.lock` directory were then removed.
- The interrupted runner's partially created `target/`, probe tree, and AC8 DerivedData are being treated as contaminated regenerable artifacts. Fresh gate execution must start from clean lane-owned targets under newly acquired lock ownership.

## R3 valid gate evidence

Fresh valid execution began only after the invalidated `target/` and disposable probe artifacts were removed and a new lock owner acquired. The following results are counted; the invalidated runner outputs are not.

Rust and egui gates:

- `cargo test -p corrald --test gh_plane -- --nocapture` — exit 0; 12 passed, 0 failed, 1 ignored.
- `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture` — exit 0; 1 passed, 0 failed. The fixture measurement reported 7 cache categories before/after and 5 public categories; first-open measurement 28.978 ms.
- `cargo test -p corrald --test drive read_issues_result_carries_repos_and_audits_executed -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test -p corrald --test terminal authenticated_terminal_websocket_runs_lifecycle_and_rejects_bad_frames -- --exact --nocapture` — exit 0; 1 passed, 0 failed.
- `cargo test -p corrald-ui --lib issues -- --nocapture` — exit 0; 10 passed, 0 failed.
- `cargo test -p corrald-ui --lib ac8_five_scoped_categories_reach_the_repo_rail -- --nocapture` in a disposable staged-merge tree — exit 0; 1 passed, 0 failed. The probe reported 7 fixture input/cache categories, 5 wire/presented categories, exactly `fixture-alpha,fixture-bravo,fixture-charlie,fixture-delta,fixture-echo`, 5 presented rows, 20 repetitions, and first-open min/median/max `0.523/0.531/100.396 ms`.
- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --workspace` — exit 0; 704 passed, 0 failed, 20 ignored across 26 result lines.
- `cargo deny --locked --workspace check` — exit 0; advisories, bans, licenses, and sources all ok (existing duplicate-crate warnings were reported).
- `cargo audit --deny warnings` — exit 0; 584 locked dependencies scanned.

Native simulator gates, using direct `/usr/bin/xcodebuild` with signing disabled on iPhone 16 simulator `59DDC0C5-891E-4EC0-91AF-4F50DF68D793`:

- `xcodebuild -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -sdk iphonesimulator -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r3-valid-ios-derived CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` — exit 0; `** BUILD SUCCEEDED **`.
- The same command with `test -only-testing:FleetNotifierTests/IssueBrowserTests` — exit 0; 9 tests, 0 failures.
- The same command with `test -only-testing:FleetNotifierTests/TerminalAttachLifecycleRegressionTests` — exit 0; 7 tests, 0 failures.
- A disposable staged-merge tree received only `AC8CategoryParityTests.swift`; `xcodegen generate` exited 0, and the same simulator test command with `test -only-testing:FleetNotifierTests/AC8CategoryParityTests` exited 0; 1 test, 0 failures. The probe reported 7 fixture input/cache categories, 5 wire/presented categories, exactly `fixture-alpha,fixture-bravo,fixture-charlie,fixture-delta,fixture-echo`, 5 presented rows, 20 repetitions, and first-open min/median/max `0.022/0.023/0.108 ms`.

Additional non-green diagnostics are preserved rather than hidden:

- Before bounded closure, `python3 ios/check-release-demo.py --self-test` exited 1 because the incoming #331 pin expected `3eabcbd125ba4d7a108505bc3e7bbe84db66c3c6c70d5ce04e0d8856584dd822`, while the three-way merged iOS source set computed `dd5b461eec69424636f3300def2846b6ab898ddf9210636169a1cf45e5c39506`; the bounded closure pin correction is recorded below.
- A pre-repair full `FleetNotifierTests` run exited 65 with two failures in `GrantAdminToggleRevertTests`; the focused suites above were green, but the full-suite failure was not waived. The bounded minimal fixture seam repair and fresh full-target result are recorded below.

All commands and raw outputs are retained under `/tmp/g339-r3-valid-cargo-evidence`, `/tmp/g339-r3-valid-final-evidence`, `/tmp/g339-r3-valid-ios-evidence`, `/tmp/g339-r3-valid-probe-evidence`, and `/tmp/g339-r3-valid-release-check.log`.

## Bounded pre-review R3 closure

- The authoritative `.fix-r3.md` contract was read at delivered head `ade73cf3de2d034869c977a793f47335763ae403`. Its exception permits an additional minimal repair only if the fresh full iOS target reproduces the grant failures; no assertions may be weakened, skipped, or marked expected.
- Before the pin edit, `python3 ios/check-release-demo.py --self-test` exited `1`: the checked-in expected digest was the incoming `3eabcbd125ba4d7a108505bc3e7bbe84db66c3c6c70d5ce04e0d8856584dd822`, while the exact merged source set computed `dd5b461eec69424636f3300def2846b6ab898ddf9210636169a1cf45e5c39506`.
- Fresh full-target command (new `/tmp/g339-r3fix-full-ios-derived`, shared lock owner `99704`) exited `65`: `Executed 235 tests, with 6 failures`; all six failures were in `GrantAdminToggleRevertTests`. The failed assertions were lines 4745, 4746, 4750, and 4754 in `testFailedGrantSetKeepsToggleAtLedgerValue`, plus lines 4772 and 4774 in `testSuccessfulGrantSetAppliesToggleToView`.
- A fresh grant-only run from a separate `/tmp/g339-r3fix-grant-derived` directory reproduced the same result: `GrantAdminToggleRevertTests` executed 2 tests with 6 failures. `adminDevices` remained empty, no POST was recorded, and `grantsNotice` was `Admin token required — paste it below to manage device grants.`
- Root cause before repair: the fixture's `setUp` stored a synthetic admin token through the simulator Keychain, but the test host could not read that Keychain item; `AppModel` reads `DeviceKeyStore.adminToken()` directly and therefore returned from its admin-token guard before the mocked grant requests. The grant logic and assertions were not the failing boundary.
- Contract-faithful repair: `AppModel` now accepts an injectable admin-token loader defaulting to `DeviceKeyStore.adminToken()`, and the grant fixture supplies its synthetic token through that seam. Production behavior and Keychain storage remain unchanged; test assertions remain unchanged.
- The final repaired release pin is `321220fc4e90f3df7920c43aa2f3849c8536d27379bace4173415a27deef1827`. Under serialized owner `28241`, `python3 ios/check-release-demo.py --self-test` exited `0` with `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- Final disposable source-drift mutation appended one newline to `TerminalAttach.swift`; normal `python3 ios/check-release-demo.py` exited `1` with expected `321220fc4e90f3df7920c43aa2f3849c8536d27379bace4173415a27deef1827` and mutated digest `b6236fb706a3660f510c82e4d387066dea5cbc69a2dda71a25c74d05307a7e53`. The scratch tree was removed; the real source remained byte-identical.
- Owner-cleanup recovery proof after the timed-out first metadata runner: the live check returned `LOCK_EXISTS=0`, PID `90738` absent, `DESCENDANT_COUNT=0`, and no `owner.pid`; therefore no stale owner file or lock directory was removed. That timed-out metadata attempt is not counted.
- Fresh full repaired iOS target from `/tmp/g339-r3fix-full-ios-final-derived` exited `0`; `FleetNotifierTests.xctest` and `All tests` each executed 235 tests with 0 failures. The same run reported `GrantAdminToggleRevertTests` 2/0, `IssueBrowserTests` 9/0, and `TerminalAttachLifecycleRegressionTests` 7/0.
- An explicit focused iOS retry was not counted separately because the host returned `ENOSPC` before test execution while rebuilding a fresh DerivedData directory; the fresh full target above is the positive focused-suite evidence.
- Corrected disposable AC8 probe: egui ran 1 test with 0 failures and reported 7 input/cache categories, 5 wire/presented categories, exactly `fixture-alpha,fixture-bravo,fixture-charlie,fixture-delta,fixture-echo`, 5 presented rows, 20 repetitions, and first-open min/median/max `0.697/0.718/54.260 ms`; `xcodegen generate` exited 0; iOS ran 1 test with 0 failures and the same five keys, with min/median/max `0.020/0.021/0.173 ms`.
- Fresh `cargo deny --locked --workspace check` and `cargo audit --deny warnings` under owner `53911` both exited 0. Fresh `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` also exited 0 before the workspace run.
- Non-counted workspace retries: the first fresh attempt exited 101 at the linker with `No space left on device`; the second compiled 306 tests but exited 101 because detached fixture session `corral-cursor-test-1` caused `duplicate session`. The incomplete target and verified-detached fixture were removed before the isolated rerun.
- A final clean workspace rerun acquired owner `3492`, ran with isolated `TMUX_TMPDIR=/tmp/g339-r3fix-tmux-isolated`, and exited `0`; all 26 result lines were green, aggregating 704 passed, 0 failed, and 20 ignored. The isolated tmux server and its target were removed after the run.
- Disposable grant mutation removed only the fixture's injected token-loader argument; its scratch `GrantAdminToggleRevertTests` run exited 65 with 2 tests and 6 failures, including zero recorded POSTs and the admin-token-required notice. This proves the repair is discriminating; scratch source/DerivedData were removed.

## Bounded test-only R4 closure

- The authoritative `.fix-r4.md` contract was read at exact starting head `f4b3b5b8d2a8eda1a20b7a3ee41d91196c0c041c`. The allowed tracked paths are exactly `ios/FleetNotifierTests/FleetNotifierTests.swift`, `ios/check-release-demo.py`, and `.report.md`; `.fix-r4.md` remains an untracked lane contract and is not committed.
- `GrantAdminToggleRevertTests.setUp()` now clears `DeviceKeyStore` admin-token storage and asserts `DeviceKeyStore.adminToken()` is nil before any model is constructed. The fixture no longer stores its synthetic credential; `makeModel()` supplies the token only through the existing injected loader. The existing `tearDown()` cleanup remains intact. No production Swift/Rust, authorization, signing, audit, daemon, or gh-plane source was changed.
- Digest recomputation after the fixture edit reported `RELEASE_SOURCE_FILE_COUNT=23`, `TEST_IN_RELEASE_MANIFEST=False`, production Release-source digest `321220fc4e90f3df7920c43aa2f3849c8536d27379bace4173415a27deef1827`, and focused test-source digest `a47a139364412c7670afb12b0b60dba874435ccbbd1f9c6efbf6db1a77cf13bc`. Because XCTest source is not a Release build input, the production marker pin correctly remains unchanged; `ios/check-release-demo.py` now carries a separate focused-test-source pin so test-source drift is independently checked.

R4 grant fixture and mutation proof:

- Focused command: `/usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -sdk iphonesimulator -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r4-final-ios-dd CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -only-testing:FleetNotifierTests/GrantAdminToggleRevertTests` — exit `0`; `GrantAdminToggleRevertTests` executed 2 tests with 0 failures; `** TEST SUCCEEDED **`.
- Disposable exact-head scratch: `/tmp/g339-r4-grant-mutant` was archived from `HEAD=f4b3b5b8d2a8eda1a20b7a3ee41d91196c0c041c`, overlaid only with the repaired test file, and mutated by removing only the injected admin-token-loader argument. The same focused xcodebuild command in the scratch tree exited `65`; `GrantAdminToggleRevertTests` executed 2 tests with 6 failures. Both named grant tests failed, including the expected missing admin authorization notice and zero attempted POSTs. The repaired test file was restored in the scratch tree; the same command exited `0` with 2 tests and 0 failures. `SCRATCH_REMOVED=1`; the scratch tree and DerivedData were deleted.

R4 release-source checker proof:

- `python3 ios/check-release-demo.py --self-test` — exit `0`; `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- A normal exact-head scratch check appended one newline to `FleetNotifierTests.swift`; `python3 ios/check-release-demo.py` exited `1` with `Grant test source digest differs from the expected checkout` (expected `a47a139364412c7670afb12b0b60dba874435ccbbd1f9c6efbf6db1a77cf13bc`, mutated `788f04fbc3089f0845bf4765fbb63bd3223e73a0fa373d861415ec4a6b27a0fd`). Restoring the original test bytes and rerunning the same command exited `0` with `release-demo check: PASS (Debug source preserved; Release boundary verified)`. The scratch tree was removed. No raw credential value is retained in this report.

R4 final serialized gates:

- Focused grant xcodebuild — exit `0`; 2 passed, 0 failed.
- `python3 ios/check-release-demo.py --self-test` — exit `0`.
- `cargo test -p corrald --test gh_plane -- --nocapture` — exit `0`; 12 passed, 0 failed, 1 ignored.
- `cargo test -p corrald --lib adapters::gh_plane::tests::query_covers_all_repos_once -- --exact --nocapture` — exit `0`; 1 passed, 0 failed; `poll_payload_repo_clauses_before=8 poll_payload_repo_clauses_after=5`.
- `cargo test -p corrald --lib integrate::tests::issues_cache_prunes_when_live_herdr_membership_changes -- --exact --nocapture` — exit `0`; 1 passed, 0 failed; `cache_categories_before=1 cache_categories_after=0`.
- `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture` — exit `0`; 1 passed, 0 failed; `cache_categories_before=7 cache_categories_after=7 public_categories=5 first_open_ms=16.919`.
- `cargo test -p corrald --test drive read_issues_result_carries_repos_and_audits_executed -- --exact --nocapture` — exit `0`; 1 passed, 0 failed.
- `cargo test -p corrald-ui --lib issues -- --nocapture` — exit `0`; 10 passed, 0 failed, 190 filtered out.
- `cargo fmt --all --check` — exit `0`.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit `0`.
- `cargo deny --locked --workspace check` — exit `0`; advisories, bans, licenses, and sources passed; existing duplicate-version warnings remain documented and no lock state changed.
- `cargo audit --deny warnings` — exit `0`; 1,236 advisories loaded and 584 locked crate dependencies scanned.
- Final isolated workspace command: `CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=/tmp/g339-r4-workspace-isolated-target TMUX_TMPDIR=/tmp/g339-r4-tmux-isolated cargo test --workspace` — exit `0`; 26 result lines, 704 passed, 0 failed, 20 ignored. The lane-owned target and isolated tmux directory were removed after completion.
- Full iOS command: `/usr/bin/xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -sdk iphonesimulator -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/g339-r4-final-ios-dd CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -only-testing:FleetNotifierTests` — exit `0`; `FleetNotifierTests.xctest` and `All tests` each executed 235 tests with 0 failures; `** TEST SUCCEEDED **`. The full run included `GrantAdminToggleRevertTests` 2/0, `IssueBrowserTests` 9/0, and `TerminalAttachLifecycleRegressionTests` 7/0.

Excluded environment failures and cleanup:

- The first final-runner `cargo test --workspace` attempt exited `101` after 305 passed and 1 failed because `tmux_capture_reports_cursor_position` hit the pre-existing shared/default `duplicate session: corral-cursor-test-1` collision. It was not counted.
- The first fresh isolated retry exited `101` before test execution when its lane-owned target hit `No space left on device`; it was not counted. Only this lane's R4 targets/DerivedData were removed to recover disk space, after which the isolated run above passed.
- All R4 shared-lock owners were respected. Every R4 probe released only its own lock ownership; the final lock is absent, scratch trees are absent, and no lane-owned runner remains.
- Final scope audit before report staging showed only the two R4 source files changed; no forbidden production/docs/generated path changed. `git diff --check` exited `0`. No live provider, credential, release, TestFlight, rebase, merge, force-push, issue closure, or PR mutation was performed.
- R4 closure commit SHA is intentionally reported externally after normal push rather than embedded here, avoiding self-referential report metadata.

---

## Retained origin/integration report before R5

# Lane report — g328-g330-recent-output-v3 (issues #328 + #329 + #330)

Branch: g328-g330-recent-output-v3
Base: 04b42e5a1a68697d242821487e09cba3df22aef1 (origin/integration)
Implementation commit: dc7a60c46585e204ef89fea4e3074900bf0a2e6e
Status: delivered

## PR #337 integration reconciliation

- Fetched `origin`; exact `origin/integration` was
  `03b61c9cfce1d851168b2d9d2beab1fb773fe99b`.
- Merged that exact tip with `git merge --no-ff --no-edit`; this report was
  resolved to the existing #328/#329/#330 lane report rather than replacing
  it with the incoming #333 report.
- `ios/check-release-demo.py` retains both the #328/#329/#330 and #333
  source-set notes. The approved digest was recomputed over the merged
  23-entry `RELEASE_SOURCE_FILES` tuple:
  `b51c96c8706aa2e29f76cbaa579468a76ba2f24b92c95c751d69c24549e45b69`.
  The exact incoming tree contains no `DiffFilesModel.swift`; #333's
  integrated `DiffPane` model is in the manifest-listed
  `ios/FleetNotifier/Models/Models.swift`, so that coverage is retained.
  This lane's `FleetNotifierApp.swift`, `DemoFleet.swift`, `FleetViews.swift`,
  and `RecentOutputModel.swift` inputs remain in the same tuple. Both the
  existing `canonical_stream_golden.json` and this lane's
  `live_session_exchange_golden.json` test resources remain bundled by the
  merged Xcode project.
- Auto-merge audit: `FleetViews.swift` retains #333's Diff sheet terminal
  states, retry, and cancellation changes alongside the #328/#329/#330
  Recent-output partition, divider, bounded Harness, and empty-state paths.
  `FleetNotifierTests.swift` retains #333's Combine/transport helper and
  DiffReliabilityTests alongside the #328/#329/#330 ContextSplit and wiring
  tests. No conflict markers remain in either auto-merged file.

## Scope-correction compliance (serialization fence, 2026-09-01)

- .brief-scope-correction.md: src/main.rs is reserved for #332. My initial
  wiring attempt touched src/main.rs; it was restored to branch HEAD and
  `git diff -- src/main.rs` is EMPTY (restore proof: 0-line diff after
  `git restore --source=HEAD -- src/main.rs`).
- The structured exchange ledger was re-homed on `Store`
  (src/core/store.rs) so BOTH the recording adapter (herdr
  `handle_output_matched`) and the read_tail API path reach the SAME ledger
  WITHOUT any daemon-entrypoint wiring. Production wiring is complete with
  zero src/main.rs involvement; no `SERIALIZATION HOLD` marker is needed
  (the correction's hold is conditional on main.rs wiring being pending).
- Not touched: src/main.rs, src/adapters/gh_plane.rs, src/api/issues.rs.

## Reserved-surface audit

Owned files (fence-compliant):
- src/core/blocks.rs (canonical seam: composer-echo shape + exchange binding)
- src/core/provenance.rs (ExchangeLedger: structured assistant/tool events)
- src/core/store.rs (ledger home + accessor — shared by adapter + API)
- src/adapters/herdr.rs (output_matched recording hook — NOT the attach region)
- src/api/drive.rs (read_tail joins the exchange ledger)
- clients/egui/src/ui/board.rs (harness divider seam + cross-client test)
- ios/FleetNotifier/UI/RecentOutputModel.swift
- ios/FleetNotifier/UI/FleetViews.swift (Recent-output region only)
- ios/FleetNotifier/Demo/DemoFleet.swift (demo seed evidence)
- ios/FleetNotifier/App/FleetNotifierApp.swift (debug evidence launch args)
- ios/FleetNotifierTests/FleetNotifierTests.swift
- ios/project.yml + ios/FleetNotifier.xcodeproj (fixture bundling)
- ios/check-release-demo.py (re-pinned digest)
- tests/provenance.rs, tests/fixtures/live_session_exchange_golden.json

## Initial gate results (before PR #337 merge; real runs, logs in /tmp)

| Gate | Command | Result |
|---|---|---|
| Rust focused (blocks exchange_*) | cargo test -p corrald --lib exchange_ | 5 passed, EXIT 0 |
| Rust focused (composer echo) | cargo test -p corrald --lib composer_echo_shape_binds_recorded_prompts_exactly_once | 1 passed, EXIT 0 |
| Rust focused (adapter ledger) | cargo test -p corrald --lib blocked_question_is_recorded_into_the_structured_exchange_ledger | 1 passed, EXIT 0 |
| Daemon provenance suite | cargo test --test provenance | 13 passed, EXIT 0 |
| egui V3 suite | cargo test -p corrald-ui --lib v3_ | 11 passed, EXIT 0 |
| egui cross-client | cargo test -p corrald-ui --lib cross_client | 2 passed, EXIT 0 |
| Full workspace | cargo test --workspace | GATE_TEST=0 (all suites green) |
| fmt | cargo fmt --all --check | GATE_FMT=0 |
| clippy | cargo clippy --workspace --all-targets -- -D warnings | GATE_CLIPPY=0 |
| swiftc parse | swiftc -parse <changed swift files> | EXIT 0 |
| release demo | python3 ios/check-release-demo.py --self-test | PASS (digest re-pinned 4bc5d9d5… → 46a1f27e…) |
| iOS build | xcodebuild build (generic iOS Sim) | ** BUILD SUCCEEDED **; GATE_IOS_BUILD2=0 (`/tmp/gate-ios-build2.log`) |
| iOS tests | xcodebuild test FleetNotifierTests (iPhone 16 sim) | 228 tests, 0 failures; ** TEST SUCCEEDED **; GATE_IOS_TEST=0 (`/tmp/gate-ios-test.log`) |
| anti-slop | swift run anti-slop | Ran; GATE_ANTI_SLOP=1; 79 violations across 25 files (`/tmp/gate-anti-slop.log`). The log records no baseline-delta result. |

## PR #337 post-merge gate results (run serially under the shared heavy-gate lock)

| Gate | Exact command | Result |
|---|---|---|
| Release demo | `python3 ios/check-release-demo.py --self-test` | `release-demo check: PASS (Debug source preserved; Release boundary verified)`; RELEASE_DEMO_EXIT=0 (`/tmp/reconcile-release-demo.log`); merged digest `b51c96c8706aa2e29f76cbaa579468a76ba2f24b92c95c751d69c24549e45b69` |
| Rust fmt | `cargo fmt --all --check` | FMT_EXIT=0 (`/tmp/reconcile-fmt.log`) |
| Rust clippy | `cargo clippy --workspace --all-targets -- -D warnings` | CLIPPY_EXIT=0 (`/tmp/reconcile-clippy.log`) |
| Rust workspace | `cargo test --workspace` | 25 result lines; 705 passed, 0 failed, 20 ignored; CARGO_TEST_EXIT=0 (`/tmp/reconcile-cargo-test.log`) |
| Simulator boot | `xcrun simctl bootstatus 59DDC0C5-891E-4EC0-91AF-4F50DF68D793 -b` | SIM_BOOT_EXIT=0 (`/tmp/reconcile-sim-boot.log`) |
| iOS build | `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd-337 build` | `** BUILD SUCCEEDED **`; IOS_BUILD_EXIT=0 (`/tmp/reconcile-ios-build.log`) |
| iOS full tests | `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/fn-dd-337 -only-testing:FleetNotifierTests` | 238 tests, 0 failures; `** TEST SUCCEEDED **`; IOS_TEST_EXIT=0 (`/tmp/reconcile-ios-test.log`) |

The anti-slop result above is the recorded pre-merge advisory run; it was not
rerun as part of the PR #337 reconciliation gate chain.

## Mutation proofs (disposable scratch /tmp/g328-scratch, all RED→GREEN)

M1 (structured role source removed): deleted the exchange-event branch in
segment_canonical → `live_session_exchange_produces_canonical_conversation_blocks`
RED (question line collapses system/user/unknown/unknown/tool/system/unknown,
exit 101); pristine file restored byte-identical (filecmp) → GREEN (exit 0).

M2 (fixture reverted to all Unknown): rewrote
tests/fixtures/live_session_exchange_golden.json kinds → unknown →
`daemon_live_session_golden_fixture_matches_canonical_blocks` RED (fixture
drift, exit 101); fixture restored byte-identical → GREEN (exit 0).

M3 (composer-echo shape removed): deleted the `<label> ❯ text` strip in
strip_typed_input_prefix → `composer_echo_shape_binds_recorded_prompts_exactly_once`
RED (left 0 vs right 1 user blocks, exit 101); pristine restored
byte-identical → GREEN (exit 0).

## Per-issue acceptance map (fill at close)

## Per-issue acceptance map

### #328 — divider-only Harness cards
| AC | Evidence |
|---|---|
| 1 divider-only = presentation separators, no cards/count | harnessActivity renders isDividerBlock blocks as Divider() rows; harnessEventCount excludes them (iOS) + HarnessEntry.is_divider (egui). Tests: testHarnessEventCountExcludesDividerOnlyBlocks, testSharedDividerSeamClassifiesBlocks, egui v3_harness_divider_only_blocks_are_presentation_separators_not_events; evidence A/B ("3 outside conversation" with 4 blocks incl. divider; expanded shows 3 cards, no dash card) |
| 2 divider-only payload hides section | harnessActivity gated on eventCount > 0; testDividerOnlyHarnessPayloadHasZeroEvents; egui panel returns when zero events |
| 3 meaningful content preserved | partition unchanged (nothing dropped — kinds/order/text); tests assert real diagnostic text byte-visible; demo/egui fixture assert content |
| 4 one shared seam | RecentOutputRender.isDividerBlock consumed by BOTH conversation (RecentOutputRowView) and harness paths; wiring test asserts exactly 2 consult sites + egui mirror recent_is_divider_run |
| 5 production-wiring fixture + mutation RED | live-session golden fixture + demo seed carry divider-only + real content; iOS M4 (bypass seam/count/scroll in scratch) → RecentOutputDefectWiringTests RED (9 failures); restored byte-identical → GREEN |
| 6 #316 layout preserved | No layout redesign; only the divider branch + count inside the existing DisclosureGroup |
| 7 focused/full gates + simulator evidence | ContextSplitV3Tests + RecentOutputDefectWiringTests + fresh R1 full 245/245 suite (the original implementation evidence was 228/228; the post-#337 refresh was 238/238); evidence A/B at 390x844 |

### #329 — expanded Harness unscrollable
| AC | Evidence |
|---|---|
| 1 content inside panel/safe area, above composer | Expanded payload owns ScrollView bounded by RecentOutputLayout.harnessViewportHeight (280); evidence B shows payload ends above the pinned composer |
| 2 scroll first-to-last + reachable collapse at 390x844 | Bounded inner scroll + collapse control (DisclosureGroup label) outside the scroll, always visible; evidence B. Swipe gesture itself = human/device gate (simctl cannot inject touches — stated in evidence README) |
| 3 conversation/composer reachable, no nested-scroll trap | Conversation keeps its own bounded scroll; harness scroll is a sibling (no nesting); composer pinned outside; wiring test pins ONE scroll inside the DisclosureGroup |
| 4 soft-wrap, no horizontal overflow | Row rendering unchanged (Text soft-wraps; code lines use the existing RecentCodeLineView) |
| 5 collapsed by default, outside Conversation | harnessExpanded = false (debug arg opt-in only); evidence A; AC5 wiring unchanged |
| 6 production-view fixture + mutation RED | Demo seed long System/Unknown payloads; iOS M4 (removing the owning scroll/container constraint) → wiring test RED; restored → GREEN |
| 7 focused/full tests + evidence | RecentOutputDefectWiringTests (bounded scroll) + fresh R1 full 245/245 (the original implementation evidence was 228/228; the post-#337 refresh was 238/238); evidence B |
| 8 order/paging/autoscroll/accessibility/redaction preserved | Partition/order untouched; harness rows keep identifiedRows; divider rows accessibilityHidden; redaction runs daemon-side before blocks |

### #330 — empty Conversation despite fetched output
| AC | Evidence |
|---|---|
| 1 supported live session → User/Agent/Tool, non-empty Conversation | canonical_blocks_with_exchange joins prompt ledger (exactly-once user) + structured exchange ledger (agent questions → agent/tool); composer-echo shape (`<label> ❯ text`) binds recorded prompts; daemon live-session test asserts kinds [system,user,unknown,agent,tool,system,unknown]; iOS fixture test asserts non-empty conversation; evidence A |
| 2 canonical, structured, harness-agnostic; no marker tables | Roles from ExchangeRole (Assistant/Tool) + PromptProvenance (user); zero provider/model/harness name branches (egui test v3_no_harness_provider_or_model_name_classification_exists still green; grep audit in report) |
| 3 exactly-once user; structured roles; unprovenanced stays Unknown | bind_echoes one-to-one unchanged; exchange bind_events one-to-one (exchange_event_renders_exactly_once_per_read); unprovenanced lines stay Unknown (exchange_event_absent_keeps_line_unknown, live_session_without_structured_role_source_stays_unknown) |
| 4 iOS + egui identical daemon kinds/order; no client reclassification | Live-session golden fixture byte-asserted daemon-side, decoded identically on iOS (testLiveSessionGoldenFixtureRendersIdenticalKindsAndOrder) and egui (v3_live_session_golden_fixture_decodes_identically_across_clients); clients render blocks verbatim |
| 5 honest empty state + Harness reachable | hasNoAttributedConversation + "No attributed conversation in this window" in the loaded branch; evidence C; wiring test pins the branch; harness-only demo route |
| 6 generic privacy-safe production-path fixture | tests/fixtures/live_session_exchange_golden.json (generic fictional content, no harness/model names); proves non-empty Conversation, preserved Harness, order, divider flag, identical cross-client kinds; bundled in iOS test target + egui reads the committed file |
| 7 mutation RED: remove structured source / revert fixture | M1 (remove exchange binding) RED→GREEN; M2 (fixture all-Unknown) RED→GREEN; M3 (composer shape) RED→GREEN; iOS M5 (empty-state branch removed) RED→GREEN |
| 8 focused/full Rust/iOS/egui gates + simulator evidence | cargo test --workspace 0; fresh R1 iOS 245/245 (prior 228/228 and 238/238 records retained above); egui 11 v3 + 2 cross-client; evidence A + C |

## Remaining human/device gates
- Real physical-device interaction (expand tap, harness scroll swipe, collapse
  tap) — simctl cannot inject touches; the layout contracts are pinned by the
  wiring tests + mutation proofs.
- Live daemon + real herdr session end-to-end (production seam verified via
  adapter unit + HTTP integration tests; a live corrald + herdr smoke was not
  run in this lane).

## Docs
No doc changes required: docs/ARCHITECTURE.md, docs/corral/P4-conformance.md,
docs/QUICKSTART.md, docs/DEVELOPING.md contain no block-kind semantic
descriptions that this change contradicts (verified by grep audit); the
semantics live in code docs, which were updated. Evidence README added under
docs/design/evidence/issue-328-330/.

## Privacy audit
- Committed fixtures/evidence contain only fictional content (demo repos
  demo-atlas/demo-grove/demo-orchard, generic prose, no real paths,
  identities, tokens, or transcripts).
- No private output, paths, identities, transcripts, tokens, signatures, or
  unrelated issue bodies in committed evidence (OCR audit output not
  committed; screenshots are privacy-safe demo renders).
- Ledger stores hash+length identities only (never raw prompt/question text).

## New defects observed
- None known at close. (One test-expectation bug of my own during the lane —
  miscounted demo harness kinds — fixed before gates.)

## Review fix R1 (#330) and #331 integration refresh

- Starting head was the reviewed `c364a43db31dae8e0854e55d5fe1001f5b6093b7`.
- Fetched `origin`; the current `origin/integration` tip was
  `f1603435fe1f123ca76c041fbb434a7ce20e8ff0` (the #331 integration).
- Merged it without rebasing or force-pushing with
  `git merge --no-ff --no-edit f1603435fe1f123ca76c041fbb434a7ce20e8ff0`.
  The reconciliation merge commit is `8aa1130bcbc5469213729e356b0f7bcbaf91238a`,
  with parents `c364a43db31dae8e0854e55d5fe1001f5b6093b7` and
  `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`. The lane report remained the
  base; no #328/#329 production or test path was replaced.
- The release manifest kept the union of the #328/#329/#330 and #331 source
  inputs. Its merged 23-entry source set passes the release-demo self-test at
  digest `d86a471570a0ad08277b435f5056b82e9dc8290fac87316ff9df977dede0156c`.
  The #333 `Models.swift` coverage and this lane's Recent Output inputs remain
  present; both canonical/live golden fixtures remain bundled.
- Auto-merge audit: `FleetViews.swift` retains #331 terminal lifecycle behavior
  and the #328/#329/#330 Recent Output partition, divider, bounded Harness,
  and empty-state paths. `FleetNotifierTests.swift` retains #331 lifecycle
  coverage and the prior ContextSplit/wiring tests. No iOS file was edited in
  this review-fix round.

### F1 — shared canonical exchange identity

- `canonical_exchange_text` is the single `clean → redact → trim` identity.
  `ExchangeEvent::new`, `ExchangeEvent::matches_text`, and the read-side
  exchange candidates all use it. Exchange ledger snapshot/ordering/
  exactly-once behavior is unchanged; unprovenanced lines remain Unknown.
- The real HerdrAdapter fixture records the indented
  `"  Do you want to proceed?"`, then calls `canonical_blocks_with_exchange`
  on the trimmed read line and asserts an Agent block.
- RED: `cargo test --lib adapters::herdr::tests::blocked_question_is_recorded_into_the_structured_exchange_ledger -- --exact --nocapture`
  → EXIT=101 before the fix (`/tmp/r1_f1_red.log`). GREEN after the fix →
  1 passed, 0 failed (`/tmp/r1_f1_green.log`); final focused run → 1 passed,
  0 failed (`/tmp/r1-focused-f1.log`).
- Mutation RED: the scratch-only producer mutation restored cleaned-but-
  untrimmed hashing; the same production-path test returned EXIT=101 with
  `left: [Unknown]`, `right: [Agent]` (`/tmp/r1_f1_mutation_red.log`). The
  pristine `provenance.rs` restore was byte-identical (SHA-256
  `55ff9ce50719e675abe5ee4c0ecd3fcbecd25ce6450e6d8fb4c38a8083349608`) and
  GREEN returned 1 passed, 0 failed (`/tmp/r1_f1_mutation_green.log`).

### F2 — anchored composer echo

- The session-label branch now requires the structured `<token>-session`
  label shape before stripping a decoration. Plain leading-decoration echoes
  and the approved `orch-session ❯ ...` fixture remain unchanged; no provider,
  model, or harness name table was added.
- RED: `cargo test --lib core::blocks::tests::machine_output_prefix_does_not_unlock_composer_echo -- --exact --nocapture`
  → EXIT=101 before the fix (`/tmp/r1_f2_red.log`). GREEN → 1 passed, 0
  failed (`/tmp/r1_f2_green.log`). The positive anchored fixture also passed
  1/1 (`/tmp/r1_f2_positive.log` and `/tmp/r1-focused-f2-positive.log`).
- Mutation RED: removing only the `is_session_label` guard promoted
  `stdout > yes` to User with `prompt_request_id: "req-yes"`; the regression
  returned EXIT=101 with `left: [User]`, `right: [Unknown]`
  (`/tmp/r1_f2_mutation_red.log`). The pristine `blocks.rs` restore was
  byte-identical (SHA-256
  `c9a6dd6cfff6769a1a5a6484116cca8747cc132f51469a5fefb6a6398f928cba`) and
  GREEN returned 1 passed, 0 failed (`/tmp/r1_f2_mutation_green.log`).

### R1 gate evidence

- Focused final F1/F2: F1 1/1, F2 machine-output 1/1, and F2 anchored
  positive 1/1; all raw exits were 0.
- Existing #315/#330 daemon provenance and golden suite:
  `cargo test --test provenance -- --nocapture` → 13 passed, 0 failed,
  EXIT=0 (`/tmp/r1-preservation-provenance.log`). Canonical block suite →
  32 passed, 0 failed, EXIT=0 (`/tmp/r1-preservation-blocks.log`).
- Existing #330 cross-client tests: generic and live egui tests each 1 passed,
  0 failed, EXIT=0 (`/tmp/r1-cross-client-generic.log`,
  `/tmp/r1-cross-client-live.log`).
- Rust gates under `/tmp/corral-heavy-gate.lock`: `cargo fmt --all -- --check`
  EXIT=0; `cargo clippy --workspace --all-targets -- -D warnings` EXIT=0;
  isolated `TMUX_TMPDIR=/tmp/corral-r1-tmux-socket cargo test --workspace`
  EXIT=0 with 26 result lines, 707 passed, 0 failed, 20 ignored
  (`/tmp/r1-cargo-fmt-final.log`, `/tmp/r1-cargo-clippy-final.log`,
  `/tmp/r1-cargo-test-final.log`). A plain workspace run first hit only the
  unrelated shared-tmux sizing test (`32x10` vs `80x23`); the exact test
  passed 1/1 with a private tmux socket, and no tmux source changed.
- Affected native gates under the same lock: release-demo self-test PASS,
  EXIT=0 (`/tmp/r1-release-demo.log`); simulator bootstatus EXIT=0
  (`/tmp/r1-sim-boot.log`); iOS build `** BUILD SUCCEEDED **`, EXIT=0
  (`/tmp/r1-ios-build.log`); full `FleetNotifierTests` 245 passed, 0 failed,
  `** TEST SUCCEEDED **`, EXIT=0 (`/tmp/r1-ios-test.log`). The historical
  pre-#337 228 and post-#337 238 counts remain above with their original
  evidence; this post-#331 refresh is 245.
- Redaction boundary audit: `handle_output_matched` receives the raw matched
  line and sends it through the shared redacted identity before hashing;
  `read_tail` still redacts before TUI scrubbing, then canonical read-side
  candidates use the same idempotent identity. No raw exchange text is stored,
  and no role is inferred from prose.

## Review fix R2 — shared read-tail exchange identity

- The R2 fix started at the exact delivered head `71dd05a3c4c4e2aeb3d5c6e0396970edb62308e4`; no merge or rebase was performed. The only implementation/test files changed are `src/adapters/herdr.rs`, `src/core/blocks.rs`, and `src/core/provenance.rs`. `src/main.rs` remains unchanged.
- `canonical_exchange_text` now applies the same ordered identity used by the read-tail path: redaction, TUI-furniture scrubbing, unsupported-glyph replacement, and whitespace normalization/outer trimming. `ExchangeEvent::new`, `ExchangeEvent::matches_text`, and read-side candidates use this one helper.
- `handle_output_matched` still keeps the redacted `waiting_on` snapshot, but passes the raw matched line to the identity-only `ExchangeEvent::new`; the constructor stores only hash/length identity, so no raw exchange text is persisted.
- The real HerdrAdapter regression exercises `handle_output_matched`, `bounded_redacted_tail`, and `canonical_blocks_with_exchange` together. It covers literal control, rail-wrapped Agent question, unsupported-glyph/icon-prefixed Agent question, redacted content, and asymmetric internal whitespace. Every case binds one Agent block after the fix:
  `cargo test --lib adapters::herdr::tests::exchange_identity_matches_real_read_tail_transforms -- --exact --nocapture` → 1 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-focused-adapter.log`).
- Composer attribution remains narrow: `stdout > yes` is System, `build-session > yes` is User, and `corral-wQ-1 ❯ yes` is Unknown/non-user. The committed checks are
  `cargo test --lib core::blocks::tests::machine_output_prefix_does_not_unlock_composer_echo -- --exact --nocapture` and
  `cargo test --lib core::blocks::tests::composer_echo_contract_preserves_build_and_unsupported_shapes -- --exact --nocapture`; each passed 1/1, EXIT=0 (`/tmp/corral-r2-serialized-focused-composer-system.log`, `/tmp/corral-r2-serialized-focused-composer-contract.log`).

### R2 mutation evidence — serialized under the shared heavy-gate lock

- The four probes each neutralized exactly one production identity transform in a disposable archived scratch tree and ran the real-adapter regression. All four correctly returned RED `EXIT=101` with the expected attribution mismatch, then the pristine sources were restored byte-identically; the mutation wrapper returned EXIT=0 (`/tmp/corral-r2-serialized-mutations.log`).
  - Rail scrub removed: `left: [System]`, `right: [Agent]`, test EXIT=101 (`/tmp/corral-r2-serialized-mutation-rail.log`).
  - Unsupported-glyph scrub removed: `left: [Unknown]`, `right: [Agent]`, test EXIT=101 (`/tmp/corral-r2-serialized-mutation-unsupported.log`).
  - Redaction removed: `left: [Unknown]`, `right: [Agent]`, test EXIT=101 (`/tmp/corral-r2-serialized-mutation-redaction.log`).
  - Whitespace normalization removed: `left: [Unknown]`, `right: [Agent]`, test EXIT=101 (`/tmp/corral-r2-serialized-mutation-whitespace.log`).
- The scratch restoration comparison returned `SCRATCH_RESTORATION_CMP_EXIT=0`; the scratch tree and target were removed after the probes.

### R2 gate evidence — serialized rerun only

The complete rerun acquired `/tmp/corral-heavy-gate.lock` atomically before any affected gate and released it through its cleanup path. Each command wrote a full raw log and an immediate exit marker:

- `cargo fmt --all -- --check` → EXIT=0 (`/tmp/corral-r2-serialized-fmt.log`).
- Focused adapter regression → 1 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-focused-adapter.log`).
- Prior R1 adapter regression `cargo test --lib adapters::herdr::tests::blocked_question_is_recorded_into_the_structured_exchange_ledger -- --exact --nocapture` → 1 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-focused-r1-f1.log`).
- Composer System, User, and Unknown contract tests → 1 passed each, EXIT=0 (`/tmp/corral-r2-serialized-focused-composer-system.log`, `/tmp/corral-r2-serialized-focused-composer-contract.log`).
- `cargo test --test provenance -- --nocapture` → 13 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-focused-provenance.log`).
- `cargo test --lib core::blocks::tests -- --nocapture` → 33 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-focused-blocks.log`).
- `cargo test -p corrald-ui --lib v3_` → 11 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-egui-v3.log`).
- `cargo test -p corrald-ui --lib cross_client` → 2 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-egui-cross.log`).
- `cargo clippy --workspace --all-targets -- -D warnings` → EXIT=0 (`/tmp/corral-r2-serialized-clippy.log`).
- `TMUX_TMPDIR=/tmp/corral-r2-serialized-tmux-socket cargo test -p corrald --lib tmux::tests::tmux_capture_reports_cursor_position -- --exact --nocapture` → 1 passed, 0 failed, EXIT=0 (`/tmp/corral-r2-serialized-tmux.log`).
- `TMUX_TMPDIR=/tmp/corral-r2-serialized-tmux-socket cargo test --workspace` → 26 result lines, 709 passed, 0 failed, 20 ignored, EXIT=0 (`/tmp/corral-r2-serialized-workspace.log`).
- `python3 ios/check-release-demo.py --self-test` → `release-demo check: PASS (Debug source preserved; Release boundary verified)`, EXIT=0 (`/tmp/corral-r2-serialized-release-demo.log`).
- `xcrun simctl bootstatus 59DDC0C5-891E-4EC0-91AF-4F50DF68D793 -b` → EXIT=0 (`/tmp/corral-r2-serialized-sim-boot.log`).
- Focused iOS parity `FleetNotifierTests/ContextSplitV3Tests/testLiveSessionGoldenFixtureRendersIdenticalKindsAndOrder` → 1 test, 0 failures, `** TEST SUCCEEDED **`, EXIT=0 (`/tmp/corral-r2-serialized-ios-parity.log`).
- The required direct iOS build command → `** BUILD SUCCEEDED **`, EXIT=0 (`/tmp/corral-r2-serialized-ios-build.log`).
- The required direct full `FleetNotifierTests` command → 245 tests, 0 failures, `** TEST SUCCEEDED **`, EXIT=0 (`/tmp/corral-r2-serialized-ios-full-tests.log`).

- The tracked-secret-file check from `.github/workflows/secret-scan.yml` exited 0 with no findings. The local `gitleaks` executable is unavailable, so the full-tree gitleaks scan could not be run in this lane.

No iOS source or fixture files were changed in R2; the serialized release/parity/full-suite checks confirm the preserved #328/#329/#330 and #333 native surfaces. Physical-device interaction and live authenticated daemon traffic remain the same human/device gates documented above.

## Delivery head
The orchestrator will record the exact pushed delivery head externally after
push. This report intentionally does not embed a self-referential final SHA.

## R5 pre-review integration reconcile

- Preflight verified branch `g332-herdr-issues-scope`, clean tracked tree, and
  `origin/integration=70317b566df51fc41b35fc297ff0bc824f298c30` after
  `git fetch origin integration`.
- The normal `git merge origin/integration` completed as merge commit
  `311ea3e8edbdf29e0d1242bc7b81b24807ee16cf`. Its first parent is the R4 head
  `252940bda66a6bad14384780e8f2f14b4a228bba`; its second parent is the exact
  integration tip `70317b566df51fc41b35fc297ff0bc824f298c30`.
- Conflict decisions were limited to the two reported files. The `.report.md`
  whole-file collision was disjoint intent: the complete R4 report was kept
  and the complete incoming integration report was appended under
  `Retained origin/integration report before R5`, preserving both evidence
  sets. The `ios/check-release-demo.py` source-rationale/pin collision was
  also disjoint intent: incoming #328/#329/#330/#333/#331 rationale and the
  R3/R4 grant rationale were retained, while the approved digest was
  recomputed over the merged 23-file Release source set. The focused test
  source remains separately pinned because it is outside that manifest.
- The auto-merged `ios/FleetNotifierTests/FleetNotifierTests.swift` retains
  both the R4 storage-clear/assertion plus injected-loader fixture and the
  incoming #337/#331/#333 regression tests. No production behavior was edited
  during reconciliation.
- Merged digest verification: Release source digest
  `c9c7a5ff1241ac7532ed00dbeba4d6c759085070965c1ede2babd525167b0bbd` and
  focused test-source digest
  `2ca9975c1fee1859a7ab49d6b04b077981ffbd1c9a726f3f9c4bf12a81f508e4`.

### R5 serialized gate evidence

All native/Cargo work ran one command at a time under legitimate atomic
ownership of `/tmp/corral-heavy-gate.lock`; no sibling process was killed or
overlapped. The battery acquired the lock as PID `57777`, and the mutation
runner waited for an active sibling owner before acquiring it as PID `86672`.
Each raw command wrote its own log and exit marker:

- `python3 ios/check-release-demo.py --self-test` → `release-demo check:
  PASS (Debug source preserved; Release boundary verified)`, EXIT=0
  (`/tmp/g339-r5-final-gate-evidence/release_self_test.log`).
- Focused `/usr/bin/xcodebuild -jobs 4 test -project
  ios/FleetNotifier.xcodeproj -scheme FleetNotifier -sdk iphonesimulator
  -destination platform=iOS\ Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793
  -derivedDataPath /tmp/g339-r5-ios-dd CODE_SIGNING_ALLOWED=NO
  CODE_SIGNING_REQUIRED=NO -only-testing:FleetNotifierTests/GrantAdminToggleRevertTests`
  → 2 tests, 0 failures, `** TEST SUCCEEDED **`, EXIT=0
  (`/tmp/g339-r5-final-gate-evidence/grant_focused.log`).
- The same xcodebuild command filtered to `-only-testing:FleetNotifierTests`
  → 245 tests, 0 failures, `** TEST SUCCEEDED **`, EXIT=0
  (`/tmp/g339-r5-final-gate-evidence/ios_full_tests.log`).
- `cargo test -p corrald --test gh_plane -- --nocapture` → 12 passed,
  0 failed, 1 ignored, EXIT=0 (`gh_plane.log`).
- `cargo test -p corrald --lib adapters::gh_plane::tests::query_covers_all_repos_once -- --exact --nocapture`
  → 1 passed, 0 failed, EXIT=0; `poll_payload_repo_clauses_before=8
  poll_payload_repo_clauses_after=5` (`query_dedupe.log`).
- `cargo test -p corrald --lib integrate::tests::issues_cache_prunes_when_live_herdr_membership_changes -- --exact --nocapture`
  → 1 passed, 0 failed, EXIT=0; `cache_categories_before=1
  cache_categories_after=0` (`stale_cache_prune.log`).
- `cargo test -p corrald --test http issues_endpoint_returns_exactly_current_herdr_workspace_repos -- --exact --nocapture`
  → 1 passed, 0 failed, EXIT=0; `public_categories=5`
  (`http_exact_five.log`).
- `cargo test -p corrald --test drive read_issues_result_carries_repos_and_audits_executed -- --exact --nocapture`
  → 1 passed, 0 failed, EXIT=0 (`signed_read_issues_exact_five.log`).
- `cargo test -p corrald-ui --lib issues -- --nocapture` → 10 passed,
  0 failed, EXIT=0 (`egui_issues.log`).
- `cargo fmt --all --check` → EXIT=0 (`fmt_check.log`).
- `cargo clippy --workspace --all-targets -- -D warnings` → EXIT=0
  (`clippy.log`).
- `cargo test --workspace` with isolated `CARGO_TARGET_DIR` and `TMUX_TMPDIR`
  → 26 result lines, 718 passed, 0 failed, 20 ignored, EXIT=0
  (`workspace_tests.log`).
- `cargo deny --locked --workspace check` → EXIT=0 (`cargo_deny.log`).
- `cargo audit --deny warnings` → EXIT=0 (`cargo_audit.log`).
- `python3 scripts/check-demo-privacy.py
  clients/egui/assets/demo-fixture.json` → `privacy scan: 0 forbidden
  matches`, EXIT=0 (`demo_privacy.log`).

The battery released the lock cleanly. Its only shell warnings were the
non-fatal `(eval):1: can't change option: zle` environment warning.

### R5 R4 discrimination recheck

At exact merge head `311ea3e8edbdf29e0d1242bc7b81b24807ee16cf`, the disposable
scratch mutation removed only the injected admin-token loader argument from
`ios/FleetNotifierTests/FleetNotifierTests.swift`; the production worktree
was not edited. The focused mutation command returned EXIT=65: both
`GrantAdminToggleRevertTests` cases failed, with 2 tests and 6 failures. The
loader was restored byte-identically, and the same focused command returned
EXIT=0 with 2 tests and 0 failures and `** TEST SUCCEEDED **`.
Evidence logs are `/tmp/g339-r5-grant-mutation-evidence/mutation_red.log` and
`/tmp/g339-r5-grant-mutation-evidence/restored_green.log`. The scratch tree
was removed, the lock was released by its owner, and the production test
source hash was unchanged (`REPO_TEST_UNCHANGED=1`).

### R5 scope and verification closeout

- Conflict markers are absent and `git diff --cached --check` passed before
  the merge commit; the merged checker and test source were parsed/compiled
  by the focused and full iOS runs.
- R5-owned report work is confined to `.report.md`; the conflict-resolution
  checker work is confined to `ios/check-release-demo.py`. The integration
  merge itself retains the incoming production/test/project surfaces without
  drive-by edits.
- No live app, physical-device, authenticated daemon, or credentialed provider
  evidence was produced or claimed. Those human/device gates remain not
  verified.
- The archived contract attachments remain outside the worktree at
  `/tmp/g339-fix-r4-contract.md` and `/tmp/g339-reconcile-r5-contract.md`.
- Final local status was clean before this evidence append; the final report
  child commit and pushed head are recorded externally after normal delivery.

---

## Retained origin/integration report at R6

# Grouped implementation report — Corral #319 + #320 R2

Status: BLOCKED for final acceptance only because native egui PNG capture was unavailable in this CLI host. The implementation is committed locally.

Implementation commit (source and evidence parent): `c66cf43d14cb3b067936c46494da8a222016f159`
Base: `f1603435fe1f123ca76c041fbb434a7ce20e8ff0`
Remote branch before reconciliation: `g319-g320-status-grouped` at `9dfdef368389a4f39cf5245743bbc372d4b0217c`.
Post-merge delivery head and remote equality are recorded below.
Integration reconciliation: see the post-merge reconciliation section below.

## Issue mapping

### #319 — status semantics

- `clients/egui/src/model.rs` and `ios/FleetNotifier/UI/BoardModel.swift` add parallel, pure presentation classifiers.
- Supervising requires canonical `done`, an identity-derived orchestrator role, and structured poll/watcher evidence from the existing adapter-projected reason/state-label field. Role alone and arbitrary prose do not qualify.
- Activity projections expose only generic Polling/Watcher labels and bounded counters. Raw command text is not returned to a view.
- Native iOS rows show the canonical `Finished` state plus a subordinate Polling line and an accessible `DisclosureGroup` evidence target. egui rows show the same state/activity split and the detail status panel exposes State, Activity, Evidence, and redacted command fields.

### #320 — grouped board presentation

- Both native clients use `Needs you → Working → Supervising → Finished → Idle`, omitting empty groups.
- Section headers include derived counts. Active supervising rows remain selectable even though their wire state is `done`.
- `contracts/state-tokens.json`, egui theme labels, and iOS state display labels use `Finished`; mark/rank/color/icon remain unchanged.
- Debug-only synthetic fixtures cover the grouped rows without changing live daemon, grants, drive, or lifecycle behavior.

## Changed files

- egui: `clients/egui/src/model.rs`, `clients/egui/src/ui/board.rs`, `clients/egui/src/theme.rs`, `clients/egui/src/demo.rs`, `clients/egui/assets/demo-fixture.json`, `clients/egui/README.md`
- iOS: `ios/FleetNotifier/UI/BoardModel.swift`, `ios/FleetNotifier/UI/FleetViews.swift`, `ios/FleetNotifier/Models/Models.swift`, `ios/FleetNotifier/UI/StateStyle.swift`, `ios/FleetNotifier/Demo/DemoFleet.swift`, `ios/FleetNotifierTests/FleetNotifierTests.swift`, `ios/README.md`, `ios/check-release-demo.py`
- shared/privacy: `contracts/state-tokens.json`, `scripts/check-demo-privacy.py`
- evidence: `docs/design/evidence/issue-319-320/implementation/`

## RED/GREEN evidence

- Base RED: `CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=.../target cargo test -p corrald-ui --test r2_status_presentation_red -- --exact --nocapture` exited 101 because the base had no `PresentationGroup` contract.
- egui focused GREEN: `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --lib model::tests::active_done_orchestrator_poll_is_supervising_with_safe_activity_text -- --exact --nocapture` exited 0; 1 passed, 204 filtered.
- egui full GREEN: `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --lib` exited 0; 205 passed, 0 failed.
- iOS focused GREEN: `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 test ... -only-testing:FleetNotifierTests/StatusPresentationRegressionTests` exited 0; 4 tests executed, 0 failures.

Mutation probes were run one at a time with byte/hash restoration:

- Rust active classification, inactive done, redaction, ordering, and Finished wording: all five exited 101 with the named regression test failing.
- iOS active classification, inactive done, redaction, ordering, and Finished wording: all five exited 65 with the named XCTest failing.
- Probe logs: `/tmp/corral-r2-mutation-evidence/summary.txt` and `/tmp/corral-r2-ios-mutation-evidence/summary.txt`.

## Gates

PASS:

- `cargo fmt --all -- --check` — exit 0.
- `CARGO_BUILD_JOBS=4 cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `CARGO_BUILD_JOBS=4 cargo test -p corrald-ui --lib` — exit 0; 205/205.
- `CARGO_BUILD_JOBS=4 cargo build -p corrald-ui --release` — exit 0.
- `xcodegen generate` — exit 0; generated project drift was restored and `git diff --exit-code -- ios/FleetNotifier.xcodeproj/project.pbxproj` exited 0.
- `xcodebuild -list -project ios/FleetNotifier.xcodeproj` — exit 0; FleetNotifier and FleetNotifierTests targets found.
- iOS Debug app build with `xcodebuild -jobs 4 ... CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` — exit 0.
- `python3 ios/check-release-demo.py --self-test` — exit 0.
- `python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json` — exit 0.
- `python3 scripts/check-demo-privacy.py --self-test clients/egui/assets/demo-fixture.json` — exit 0.
- Swift parse gate with `swiftc -parse -swift-version 5` over changed app/test files — exit 0.
- `git diff --check` — exit 0.

Expected host/sibling blockers:

- `CARGO_BUILD_JOBS=4 cargo test --workspace` — exit 101, 301 passed and 1 pre-existing `tmux::tests::tmux_capture_reports_cursor_position` failure because the sibling `g331-terminal-crash` worktree owns the detached `corral-cursor-test-1` tmux session.
- iOS full `xcodebuild ... -only-testing:FleetNotifierTests` — exit 65, 239 executed with 6 failures in pre-existing `GrantAdminToggleRevertTests` keychain/admin-token setup. The new status class passed in the focused run.
- iOS anti-slop — exit 1 with 77 violations across 25 files, all pre-existing; no new status-presentation violation was reported.

## Visual evidence

Committed:

- `docs/design/evidence/issue-319-320/implementation/ios-r2-status-390x844.png`
- Verified PNG dimensions: 1170 x 2532 px from a fresh iPhone 14 / iOS 26.5 simulator (390 x 844 pt).
- SHA-256: `de97157a738fa060c016f5ef08a6434e0b67b8b295faf13ced9941116a800c15`.
- Pixel inspection found compact status chips, `Needs you (1)`, `Working (1)`, `Supervising (1)`, `Finished (2)`, canonical Finished plus Polling on the supervising row, a native disclosure chevron, no overlay/auth/loading state, and no lifecycle Done/Review wording.

Not captured:

- Native egui release binary built and attempted with the synthetic fixture and selected supervising agent. The app's exact-owned frontmost probe remained fail-closed in the CLI lane. CoreGraphics enumerated the on-screen 1320 x 892 window, but both native ScreenCapture/screencapture paths refused image creation (`could not create image from window` / `could not create image from rect`). No HTML/prototype image was substituted or committed.

## Closure

The source/evidence commit is `c66cf43d14cb3b067936c46494da8a222016f159`; this follow-on report records implementation evidence. The final delivery head and remote equality are recorded after the post-merge gates. No issue, PR, or release state was mutated.

## Post-merge reconciliation — PR #337 integration refresh

- Pre-reconciliation target tip: `9dfdef368389a4f39cf5245743bbc372d4b0217c`.
- Fetched `origin/integration` and verified the exact requested tip:
  `e3c9e5db171f1059e6b7f79f4c00eed884965ad5`.
- Reconciled with the required normal `git merge --no-edit origin/integration`.
  No rebase or force-push was used.
- Conflict allowlist was exactly `.report.md` and
  `ios/check-release-demo.py`. The report conflict retained this lane's full
  #319/#320 evidence and adds this reconciliation section. The Release checker
  conflict retains the incoming #328/#329/#330, #333, and #331 source-set
  coverage plus this lane's #319/#320 digest reason; its digest was recomputed
  from the merged 23-file Release source set.
- Auto-merged surfaces were retained and audited: egui recent-output
  attribution in `clients/egui/src/ui/board.rs`; iOS recent-output and Diff
  behavior in `ios/FleetNotifier/UI/FleetViews.swift`,
  `ios/FleetNotifier/Demo/DemoFleet.swift`, and
  `ios/FleetNotifierTests/FleetNotifierTests.swift`; and the incoming daemon,
  provenance, project, and fixture changes. No #337 behavior was dropped.
- Reconciliation remained inside the requested scope. No #327 updater files,
  #332 grant tests, credentials, release, TestFlight, PRs, issues, or merges
  outside this local Git merge were touched.

## Post-merge gate results

- A first serialized attempt was invalidated and retained at
  `/tmp/corral-g319-g320-reconcile-final/`: the host hit `ENOSPC` while
  writing later logs, and the early Rust commands could not execute because a
  registry source directory was incomplete. It is not counted as evidence.
  `cargo fetch --locked` then exited 0 and restored the missing source.
- The authoritative rerun acquired `/tmp/corral-heavy-gate.lock` with
  `LOCK_WAITED=0s`; raw logs are under
  `/tmp/corral-g319-g320-reconcile-authoritative/`.
- `cd ios && xcodegen generate` — exit 0. The generated project corrected the
  incoming generated group/resource IDs while retaining both the #337 recent
  output fixture and this lane's status fixture; the refreshed
  `ios/FleetNotifier.xcodeproj/project.pbxproj` was staged. The post-refresh
  `git diff --exit-code -- ios/FleetNotifier.xcodeproj/project.pbxproj` exited 0.
- `cargo fmt --all -- --check` — exit 0.
- `cargo deny --locked --workspace check` — exit 0; advisories, bans, licenses,
  and sources passed.
- `cargo audit --deny warnings` — exit 0; no advisory warnings.
- `CARGO_BUILD_JOBS=4 cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `CARGO_BUILD_JOBS=4 cargo build --release` — exit 0.
- `TMUX_TMPDIR=/tmp/corral-g319-g320-reconcile-tmux CARGO_BUILD_JOBS=4 cargo test --workspace` — exit 0; 26 result lines aggregated to 714 passed, 0 failed, 20 ignored.
- Focused egui status test — exit 0; 1 passed, 206 filtered. Full egui library — exit 0; 207 passed, 0 failed.
- `python3 ios/check-release-demo.py --self-test` — exit 0; Release boundary verified. The merged 23-file source digest is
  `aa7ecb5c75b4ce6e9899c6f4cca4993343fa64d79b3ad022d96443fb30b08afd`.
- `python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json` — exit 0; 0 forbidden matches. Its `--self-test` — exit 0.
- Swift parse over the changed app/test files — exit 0.
- `xcrun simctl bootstatus ... -b` — exit 0.
- iOS build with the generic Simulator destination — exit 0; `** BUILD SUCCEEDED **`.
- Focused `FleetNotifierTests/StatusPresentationRegressionTests` — exit 0; 4 tests executed, 0 failures.
- Full `FleetNotifierTests` — exit 65; 249 tests executed with 6 failures, all in the pre-existing `GrantAdminToggleRevertTests` keychain/admin-token setup. The new status-presentation class passed; #332 grant tests were not changed.
- `git diff --check` — exit 0.

The full iOS result remains an explicit pre-existing host/keychain blocker, not
an implementation failure. No #327 updater files, #332 grant tests, live
credentials, release, TestFlight, issue state, or PR state were changed.

## R6 integration reconciliation and final rerun

- R6 started at `96947d29e234719c949fc5e1d705abb486a4aae5` on
  `g332-herdr-issues-scope`. `git fetch origin integration` verified
  `origin/integration=cf1c12ff17017021e6a222af914f843300480dce`.
- The required normal `git merge origin/integration` produced conflicts only
  in `.report.md` and `ios/check-release-demo.py`. The report resolution keeps
  both complete evidence bodies. The checker resolution keeps the R4/R5
  focused-test and Release-source digest checks, plus the #319/#320 attribution
  comments. The merged pins are Release `b71e14332091e51f89d1e2e9f98d826aeb4bf5f07a021f99b5611c262e34d8e3`
  and focused-test `6b4bc2095b1a397fac656d0bae4cc83a86544c5891a71dc5758261b292834b8b`.
- The resolved merge was concluded normally as
  `236652727b1558f2b709d6e1fa3e951193d08068`, with parents
  `96947d29e234719c949fc5e1d705abb486a4aae5` and
  `cf1c12ff17017021e6a222af914f843300480dce`.
- The R4 tokenless fixture remains present: setup clears persisted admin
  storage, asserts it is empty before model construction, and supplies the
  admin token only through the injected loader. R6 also seeds one stale
  recorded URLProtocol request before the existing reset and asserts the
  request list is empty after reset; this is test-only fixture discrimination.

### Authoritative R6 lock-owned battery

The final-tree battery was `/tmp/g339-r6-final-gates-r2.py`. It waited for
unrelated owners, acquired `/tmp/corral-heavy-gate.lock` as PID `33542`, ran
one heavy command at a time, and released the lock. The wrapper exited `0`.
Raw logs are under `/tmp/g339-r6-final-gate-evidence-r2/`.

- `python3 ios/check-release-demo.py --self-test` — exit 0;
  `release-demo check: PASS (Debug source preserved; Release boundary verified)`.
- Focused `/usr/bin/xcodebuild ... -only-testing:FleetNotifierTests/GrantAdminToggleRevertTests` — exit 0; 2 tests, 0 failures.
- Full `/usr/bin/xcodebuild ... -only-testing:FleetNotifierTests` — exit 0;
  249 tests, 0 failures; `** TEST SUCCEEDED **`.
- `env CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=/tmp/g339-r6-cargo-target-r2 TMUX_TMPDIR=/tmp/g339-r6-tmux-isolated-r2 cargo fmt --all --check` — exit 0.
- `env CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=/tmp/g339-r6-cargo-target-r2 TMUX_TMPDIR=/tmp/g339-r6-tmux-isolated-r2 cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `env CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=/tmp/g339-r6-cargo-target-r2 TMUX_TMPDIR=/tmp/g339-r6-tmux-isolated-r2 cargo test --workspace` — exit 0; 26 result lines, 723 passed, 0 failed, 20 ignored.
- `cargo deny --locked --workspace check` — exit 0.
- `cargo audit --deny warnings` — exit 0.
- `python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json` — exit 0; 0 forbidden matches.

### R6 discrimination evidence

- `TOKENLESS_FIXTURE_PRESENT=1`; pristine focused-test source SHA was
  `6b4bc2095b1a397fac656d0bae4cc83a86544c5891a71dc5758261b292834b8b`.
- Cleanup mutation: removed only `GrantAdminStubURLProtocol.reset()` from the
  grant fixture setup. Focused xcodebuild exit 65; 2 tests, 2 failures, both
  on `#332 R6: grant fixtures must start without recorded requests`.
  Mutated SHA was
  `96e7b44ef9fdb363744d753099e93316b674fa257d338687ae17a4d9aead1aac`.
  Scratch removed (`CLEANUP_SCRATCH_REMOVED=1`).
- Loader mutation: removed only the injected `adminTokenLoader` argument.
  Focused xcodebuild exit 65; 2 tests, 6 failures. Mutated SHA was
  `ab4343df8bc45ab37dba80e4c19fa2a46ea2c98dc162f9464555c1638e7fd11f`.
  Scratch removed (`LOADER_SCRATCH_REMOVED=1`).
- Restored pristine focused scratch: xcodebuild exit 0; 2 tests, 0 failures;
  `RESTORED_GREEN_EXIT=0`.

### R6 excluded attempts

- The first R6 battery at `/tmp/g339-r6-final-gates.py` passed all release,
  focused/full iOS, Rust, deny/audit, and privacy commands, but its initial
  admin-token cleanup mutation stayed green because simulator Keychain
  readback did not expose the seeded value. Its wrapper exit was 1 and it is
  not counted as final evidence. The deterministic fixture assertion above
  was added, the focused-test digest was repinned, and the complete final-tree
  battery was rerun.
- `/tmp/g339-r6-cleanup-digest.py` never acquired the shared lock before its
  bounded wait expired; it ran no mutation command and is excluded. No sibling
  process or lock was removed or interrupted.

The final R6 child scope is `.report.md`,
`ios/FleetNotifierTests/FleetNotifierTests.swift`, and
`ios/check-release-demo.py`; the remaining merge-tree paths came from the
required `origin/integration` merge. `git diff --check` passed for the merge
resolution and no production behavior was changed by the R6 child edits.

---

## Retained origin/integration report at R7 (#325)

# Grouped implementation report — Corral #325

Status: READY FOR REVIEW. All acceptance criteria satisfied with real RED/GREEN
evidence; all repository gates green under `/tmp/corral-heavy-gate.lock`
serialization (no overlap with the live #311 lane — bounded wait of 200 s).

Base: `cf1c12ff17017021e6a222af914f843300480dce` (post-#343 integration),
branch `g325-egui-page-limit-pin`. Issue #325 open; no PR opened, no issue
closed, `main` untouched.

## Changed files

- `clients/egui/src/state.rs` — tests only: two committed regression tests
  pinning the `tail_requested_lines` cleanup contract. Production code
  (including the two pinned cleanup lines) untouched.
- `clients/egui/src/app.rs` — the stale `refresh_recent_output` doc comment
  only (lines 1824–1825): now states default 50, remembered explicit window
  up to 200, bounded by the existing daemon cap. No logic hunks; no overlap
  with the fenced #311 app.rs font work.
- `.report.md` — this report.

## Acceptance criteria

1. Committed state regression covering BOTH full-snapshot disappearance and
   delta removal of an agent with `tail_requested_lines[agent] = 200`:
   - `snapshot_disappearance_clears_the_remembered_tail_window` — seeds
     `a=200, b=200`, applies a reconnect snapshot dropping `b`, asserts the
     dropped agent's remembered window is gone while the live agent's
     survives.
   - `delta_removal_clears_the_remembered_tail_window` — seeds `a=200`,
     removes `a` via `Delta.del` (the `remove_agent` path), asserts the
     remembered window is gone.
2. Re-adding the same agent identity starts WITHOUT the stale 200-line
   expansion: both tests re-add the same identity (snapshot re-add / delta
   `upd` re-add) and assert `tail_requested_lines` contains no entry for it —
   the automatic refresh falls back to the default 50 until the operator
   explicitly re-expands (the app.rs `dispatch_drive_intents` re-insert path,
   unchanged).
3. Removing the `apply_snapshot` retain line → RED with positive test count:
   see Mutation probe 1.
4. Removing the `remove_agent` cleanup line → RED independently with positive
   test count: see Mutation probe 2.
5. Doc comment corrected: "Refreshes re-request the remembered per-agent
   window: default 50 lines, or an explicit `Load earlier` expansion up to
   200 lines, bounded by the existing daemon cap."
6. No runtime behavior, daemon/protocol surface, iOS code, pacing policy, or
   page-bounds change: state.rs diff is test-module-only; app.rs diff is the
   comment-only hunk above; the two cleanup lines are byte-identical.
7. All gates green (below).

## RED/GREEN mutation evidence

Probes ran on a scratch copy (`git archive HEAD` + the new tests carried
over, `git init` + commit), one production mutation per probe with an
asserted single-occurrence anchored replacement; pristine file restored
byte-identically (`cmp -s`) between probes.

Mutation probe 1 — delete the `apply_snapshot` retain line
(`self.tail_requested_lines.retain(...)`):
`cargo test -p corrald-ui --lib state::tests::snapshot_disappearance_clears_the_remembered_tail_window -- --exact`
→ exit 101; `test result: FAILED. 1 failed; 0 passed; 208 filtered out`;
panicked at state.rs:772:9: "a snapshot-dropped agent loses its remembered
200-line window". Restored byte-identical.

Mutation probe 2 — delete the `remove_agent` cleanup line
(`self.tail_requested_lines.remove(agent_id);`):
`cargo test -p corrald-ui --lib state::tests::delta_removal_clears_the_remembered_tail_window -- --exact`
→ exit 101; `test result: FAILED. 1 failed; 0 passed; 208 filtered out`;
panicked at state.rs:821:9: "delta removal clears the remembered 200-line
window". Restored byte-identical.

The real worktree was never mutated: `git status --short` shows only the
intended `app.rs` + `state.rs` + report diff at all times.

## Gate results (exact commands, real exits, serialized under the lock)

- `cargo fmt --all -- --check` → exit 0.
- Focused state tests: `cargo test -p corrald-ui --lib state::tests` →
  exit 0; `18 passed; 0 failed; 191 filtered out` (both new tests in the
  passing set).
- egui crate: `cargo test -p corrald-ui` → exit 0; 216 passed, 0 failed
  across all test binaries (lib 209 + integration 2/4/1).
- `cargo clippy --workspace --all-targets -- -D warnings` → exit 0, 0
  warnings (Finished dev profile in 2m 00s).
- `cargo test --workspace --all-targets` → exit 0; 716 passed, 0 failed,
  19 ignored.
- `cargo deny --locked --workspace check` → exit 0; "advisories ok, bans ok,
  licenses ok, sources ok".
- `cargo audit --deny warnings` → exit 0.

Lock discipline: `/tmp/corral-heavy-gate.lock` was held by
`/tmp/run-corral-311-heavy-gates.sh` (PID 25478); this lane waited 200 s
bounded, acquired atomically (`mkdir` + `owner.pid` + EXIT trap), ran the
entire battery single-threaded, and released. No overlap.

---

## R7 integration reconciliation and final rerun

- R7 started from `f1f40b59853234272b03da7a138734c5a0760492` and fetched the
  exact requested `origin/integration` tip
  `58a41f0875baead7c680eef4f0e39a8096e2c091`.
- The required normal `git merge --no-edit origin/integration` produced only
  the allowed `.report.md` conflict. The report resolution retains the
  complete R6 evidence above and the complete incoming #325 report. The
  auto-merged egui `clients/egui/src/app.rs` and `clients/egui/src/state.rs`
  changes are retained; no other conflict path occurred.
- `ios/check-release-demo.py` auto-merged unchanged from R6, retaining the
  Release-source digest check and the independent focused-test source digest
  check. The incoming #325 checker did not contain the focused-test check, so
  no R6 digest feature was dropped.
- The R7 merge tip is intentionally reported externally after commit/push and
  is not embedded here, avoiding self-referential metadata.

### R7 gate evidence

- The authoritative R7 battery was `/tmp/g339-r7-final-gates.py`, with raw
  per-command logs under `/tmp/g339-r7-final-gate-evidence/`. It waited for
  sibling owners, acquired `/tmp/corral-heavy-gate.lock` as PID `41582`, ran
  each native/Cargo command one at a time, and released the lock cleanly
  (`LOCK_RELEASED`). The wrapper exited `0`; its only outer-shell warning was
  the non-fatal `(eval):1: can't change option: zle` environment message.
- `xcrun simctl bootstatus 59DDC0C5-891E-4EC0-91AF-4F50DF68D793 -b` — exit
  `0` (`sim_boot.log`).
- `python3 ios/check-release-demo.py --self-test` — exit `0`; `release-demo
  check: PASS (Debug source preserved; Release boundary verified)`
  (`release_self_test.log`). The merged checker retains Release digest
  `b71e14332091e51f89d1e2e9f98d826aeb4bf5f07a021f99b5611c262e34d8e3` and
  focused-test digest
  `6b4bc2095b1a397fac656d0bae4cc83a86544c5891a71dc5758261b292834b8b`.
- Focused `/usr/bin/xcodebuild -jobs 4 test -project
  ios/FleetNotifier.xcodeproj -scheme FleetNotifier -sdk iphonesimulator
  -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793'
  -derivedDataPath /tmp/g339-r7-ios-dd CODE_SIGNING_ALLOWED=NO
  CODE_SIGNING_REQUIRED=NO -only-testing:FleetNotifierTests/GrantAdminToggleRevertTests`
  — exit `0`; `2` tests, `0` failures, `** TEST SUCCEEDED **`
  (`grant_focused.log`).
- Full `/usr/bin/xcodebuild -jobs 4 test -project
  ios/FleetNotifier.xcodeproj -scheme FleetNotifier -sdk iphonesimulator
  -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793'
  -derivedDataPath /tmp/g339-r7-ios-dd CODE_SIGNING_ALLOWED=NO
  CODE_SIGNING_REQUIRED=NO -only-testing:FleetNotifierTests` — exit `0`;
  `249` tests, `0` failures, `** TEST SUCCEEDED **`
  (`ios_full_tests.log`).
- `env CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=/tmp/g339-r7-cargo-target
  TMUX_TMPDIR=/tmp/g339-r7-tmux-isolated cargo fmt --all --check` — exit `0`
  (`cargo_fmt.log`).
- `env CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=/tmp/g339-r7-cargo-target
  TMUX_TMPDIR=/tmp/g339-r7-tmux-isolated cargo clippy --workspace
  --all-targets -- -D warnings` — exit `0` (`cargo_clippy.log`).
- `env CARGO_BUILD_JOBS=4 CARGO_TARGET_DIR=/tmp/g339-r7-cargo-target
  TMUX_TMPDIR=/tmp/g339-r7-tmux-isolated cargo test --workspace` — exit `0`;
  `26` result lines aggregated to `725 passed, 0 failed, 20 ignored`
  (`workspace_tests.log`). The isolated `TMUX_TMPDIR` prevented shared-session
  contamination.
- `cargo deny --locked --workspace check` — exit `0`; advisories, bans,
  licenses, and sources passed. Existing duplicate-version warnings remain
  in the raw log (`cargo_deny.log`); lock state was not changed.
- `cargo audit --deny warnings` — exit `0`; `584` locked crate dependencies
  scanned (`cargo_audit.log`).
- `python3 scripts/check-demo-privacy.py
  clients/egui/assets/demo-fixture.json` — exit `0`; `privacy scan: 0
  forbidden matches` (`demo_privacy.log`).

### R7 R4 discrimination recheck

- `TOKENLESS_FIXTURE_PRESENT=1`; the pristine focused-test source SHA was
  `6b4bc2095b1a397fac656d0bae4cc83a86544c5891a71dc5758261b292834b8b`.
- Cleanup mutation removed only `GrantAdminStubURLProtocol.reset()` from the
  grant fixture setup. The focused xcodebuild exited `65`; `2` tests executed
  with `2` failures, both on `#332 R6: grant fixtures must start without
  recorded requests`. Mutated SHA:
  `96e7b44ef9fdb363744d753099e93316b674fa257d338687ae17a4d9aead1aac`.
  Scratch removed (`CLEANUP_SCRATCH_REMOVED=1`); raw log
  `cleanup_red.log`.
- Loader mutation removed only the injected `adminTokenLoader` argument. The
  focused xcodebuild exited `65`; `2` tests executed with `6` failures,
  including the admin-token-required notice and zero grant POSTs. Mutated
  SHA: `ab4343df8bc45ab37dba80e4c19fa2a46ea2c98dc162f9464555c1638e7fd11f`.
  Scratch removed (`LOADER_SCRATCH_REMOVED=1`); raw log `loader_red.log`.
- Restored pristine focused scratch: xcodebuild exited `0`; `2` tests,
  `0` failures, `** TEST SUCCEEDED **` (`RESTORED_GREEN_EXIT=0`, raw log
  `mutation_restored_green.log`). The R7 Cargo target, iOS DerivedData,
  isolated tmux directory, mutation trees, and lock were absent after cleanup.

R7 raw evidence is retained outside the repository under
`/tmp/g339-r7-final-gate-evidence/`. No physical-device or live credentialed
Corral daemon evidence was produced or claimed.

# Preserved incoming evidence set — #324 provider revision report (merged #351, post-L1 #355)

# Implementation report — Corral #324: provider read-tail revision contract + honest upstream blocker surfacing

Status: READY FOR REVIEW. Contract implemented in the herdr adapter with a
defined provider revision contract, end-to-end propagation through
`read_tail_since_with_rev`, simulated-provider regression/probe evidence, and
the live Herdr 0.8.2 blocker recorded honestly (re-verified read-only at this
base). All repository gates green under `/tmp/corral-heavy-gate.lock`
serialization (no overlap with the live #339 lane's runs).

Base: `58a41f0875baead7c680eef4f0e39a8096e2c091` (current origin/integration),
branch `g324-provider-rev-contract`. Issue #324 open; no PR opened, no issue
closed, `main` untouched.

## Changed files

- `src/adapters/herdr.rs` — production: module-doc provider revision contract
  (#324 section), one shared `read_tail_rpc` helper behind all three read_tail
  seams (`read_tail`, `read_tail_since`, and the new
  `read_tail_since_with_rev` override), and the pure `contract_tail`
  classifier (legacy / unchanged / changed). Tests: 4 `contract_tail` unit
  tests, 5 `read_tail_since_with_rev` socket tests against a
  contract-honoring mock provider, 3 mock-socket helpers (incl. a
  request-aware one and the byte-counting probe server), and the
  `probe_read_tail_bytes_first_unchanged_changed` measured probe.
- `docs/OPERATIONS.md` — "Provider read-tail revision contract (#324)"
  subsection under the Recent output read path (#167); scoped strictly to the
  revision contract (no GET /fleets content — #340 owns that separately).
- `docs/corral/P4-conformance.md` — normative wire shapes: read_tail payload
  gains `since_rev`, result gains `source_rev` + unchanged semantics.
- `docs/design/evidence/issue-324/` — README.md (contract, blocker, measured
  table), probe.sh (reproducible probe runner), probe.log (real run),
  live-herdr-0.8.2-probe.md (read-only live socket re-verification).
- `.report.md` — this report.

## Provider revision contract (defined, per AC1)

`agent.read` responses carry `read.revision`: a monotonic
per-agent/output-source revision of the `recent_unwrapped` window — NOT the
fleet snapshot revision (the drive envelope's `rev`). Semantics:
`revision` 0/absent = legacy provider (bounded full-page fallback, no
provider revision); request `rev` == provider `revision` = UNCHANGED (provider
returns empty `text`; adapter returns an explicit empty result with the same
revision — no page re-transferred); any other case (first read, changed
output, wrap/restart where the provider counter reset below the cache) =
bounded window + the provider's current revision. Wire: request
`{"kind":"read_tail","lines","since_rev"}`; response result
`{"lines","blocks","source_rev"}`. Documented in `docs/OPERATIONS.md`,
`docs/corral/P4-conformance.md`, and the herdr.rs module docs.

## Acceptance criteria

1. Provider-side revision contract defined in the daemon protocol docs —
   DONE (`docs/OPERATIONS.md` + `docs/corral/P4-conformance.md` + module
   docs): monotonic per-agent/output-source revision, explicit
   unchanged/empty semantics, legacy fallback.
2. Adapter contract when the provider supplies a meaningful nonzero revision
   — DONE and socket-tested: (a) no cached rev → bounded output + provider
   revision (`read_tail_since_with_rev_first_read_returns_window_and_provider_rev`);
   (b) current cached rev → explicit empty result without re-transferring the
   page (`read_tail_since_with_rev_unchanged_returns_explicit_empty_without_page`
   asserts the request carried `rev=59` and the response is empty, and the
   probe measures 62 response bytes vs 20260 for the full page);
   (c) advanced output → changed bounded data + strictly newer revision
   (`read_tail_since_with_rev_changed_returns_new_window_and_newer_rev`).
3. End-to-end propagation through `read_tail_since_with_rev`, never the
   fleet snapshot revision — DONE: the override returns the provider
   `read.revision` only; RED mutation M3 proves a fleet-rev substitution is
   caught (below).
4. Older providers degrade safely — DONE and tested: absent `revision` and
   `revision: 0` (the live herdr 0.8.2 response) both return the bounded full
   page with no provider revision; `rev` forwarding is harmless for legacy
   providers (`read_tail_since_with_rev_legacy_provider_falls_back_to_bounded_full_page`).
   No unbounded or hidden-agent fetch: the RPC still sends one bounded
   `lines`-clamped request (1..=200 / 32 KiB unchanged).
5. D5 no-prefetch, D9 redact-before-scrub, line clamp, 32 KiB cap intact —
   DONE: the shared helper keeps the existing `recent_unwrapped` +
   `lines.clamp(1, READ_TAIL_MAX_LINES)` request and
   `bounded_redacted_tail` (redact → scrub → bounds); `contract_tail_keeps_redaction_and_bounds`
   pins it.
6. Regression tests + RED mutations — DONE (below).
7. Measured long-running-pane probe — DONE with a simulated
   contract-honoring provider (live upstream does not support revisions);
   the live blocker is documented honestly (below).

## Honest upstream blocker

The live Herdr 0.8.2 provider does NOT support revisions. Re-verified
read-only at this base against the live socket
(`~/.config/herdr/herdr.sock`, `agent.read`, target `orch-hermes-brain`,
`source: recent_unwrapped`): no `rev`, `rev = 1`, and `rev = 999999` all
return `read.revision: 0` with byte-identical 929-byte bodies — see
`docs/design/evidence/issue-324/live-herdr-0.8.2-probe.md` (read-only JSON-RPC;
herdr rejects integer request ids, so string ids were used). This matches the
issue's own #314 verification. No live-provider incremental behavior is
claimed: against Herdr 0.8.2 the adapter keeps the bounded full-page fallback
and `source_rev` echoes the client's cached revision (pre-existing behavior).

## Measured probe (simulated provider, REAL adapter path)

`docs/design/evidence/issue-324/probe.sh` runs the committed probe test
(`adapters::herdr::tests::probe_read_tail_bytes_first_unchanged_changed`):
the real `HerdrAdapter::read_tail_since_with_rev` performs first/unchanged/
changed reads against a mock socket whose fixture provider implements the
contract (200-line ≈ 18 KiB long-running-pane window). Raw wire bytes
(incl. newline) from `probe.log`:

- first (no cached rev): request 105 B, response 20260 B, 200 lines, rev 59
- unchanged (cached rev 59): request 114 B, response 62 B, 0 lines, rev 59
- changed (cached rev 59): request 114 B, response 20260 B, 200 lines, rev 60

The unchanged read transfers 62 bytes instead of the full page (~99.7%
saving) and returns the explicit empty result; the changed read returns the
window with strictly newer revision 60.

## RED/GREEN mutation evidence

Probes ran on a scratch copy (`git archive HEAD` + this lane's herdr.rs
carried over, `git init` + commit), one production mutation per probe with an
asserted single-occurrence anchored replacement; pristine file restored
byte-identical (`cmp -s`, `RESTORE*_BYTE_IDENTICAL=1`) between probes. Log:
`/tmp/corral-324-mutations.log` and `/tmp/corral-324-m3.log`.

- M1 IGNORE_REV_DECISION (delete the unchanged arm of `contract_tail`) →
  `cargo test -p corrald --lib contract_tail_unchanged_only_for_equal_nonzero_provider_revision -- --nocapture`
  exit 101; 0 passed, 1 failed; panicked at src/adapters/herdr.rs:4606:
  `assertion 'left == right' failed: equal revision -> explicit empty result`,
  `left: (["one"], Some(59))`, `right: ([], Some(59))`.
- M2 IGNORE_REV_WIRE (never forward the cached `rev` param) →
  `cargo test -p corrald --lib read_tail_since_with_rev_unchanged_returns_explicit_empty_without_page -- --nocapture`
  exit 101; 0 passed, 1 failed; panicked at src/adapters/herdr.rs:4701:
  `assertion 'left == right' failed: cached revision is forwarded`,
  `left: Null`, `right: 59`.
- M3 SUBSTITUTE_FLEET_REV (use the store snapshot rev as the provider
  revision instead of `read.revision`) → exit 101; 0 passed, 1 failed;
  panicked at src/adapters/herdr.rs:4677: `left: None`, `right: Some(59)`
  (the "provider revision is propagated" assertion). The first mutation
  form of this probe failed to COMPILE (MutexGuard held across an await);
  it was corrected to clone the store handle first so the test executes and
  bites on its assertion — the compile-error form is not used as evidence.
- Pristine restores: `PRISTINE_FOCUSED` exit 0 (5 passed), `PRISTINE_CONTRACT`
  exit 0 (4 passed); `PRISTINE_FIRST_READ` exit 0.

The real worktree was never mutated during probes: `git status --short`
showed only the intended diff throughout.

## Gate results (exact commands, real exits, serialized under the lock)

Battery `/tmp/run-corral-324-heavy-gates.sh` (mkdir + owner.pid + EXIT trap,
bounded 1800 s wait; `LOCK_ACQUIRED owner=... waited=N` logged; no overlap —
the lock was held by the #339 lane's script at lane start and released before
this lane's acquisitions). Log: `/tmp/corral-324-heavy-gates.log` (second,
post-fmt run) and `/tmp/corral-324-workspace-nff.log`.

- `cargo fmt --all -- --check` → exit 0 (after `cargo fmt --all` fixed this
  lane's new test formatting; fmt touched only src/adapters/herdr.rs).
- Focused revision tests: `cargo test -p corrald --lib read_tail_since_with_rev`
  → exit 0, 5 passed / 0 failed / 316 filtered.
- Focused contract tests: `cargo test -p corrald --lib contract_tail` →
  exit 0, 4 passed / 0 failed.
- Probe: `cargo test -p corrald --lib probe_read_tail_bytes_first_unchanged_changed -- --nocapture`
  → exit 0, 1 passed.
- herdr adapter module: `cargo test -p corrald --lib adapters::herdr` →
  exit 0, 88 passed / 0 failed / 233 filtered.
- `cargo clippy --workspace --all-targets -- -D warnings` → exit 0, 0 warnings.
- `cargo test --workspace --all-targets` → exit 0; 726 passed / 0 failed /
  19 ignored across all five test binaries (corrald lib+main, corrald-client,
  corrald-ui lib+main). The gate runs under tmux isolation
  (`COLUMNS=80 LINES=23 TMUX_TMPDIR=/tmp/corral-324-tmux-iso`, dir created
  first) because this host's long-lived SHARED tmux server carries a stale
  server environment that breaks `tmux::tests::tmux_capture_reports_cursor_position`
  (pane cursor never reaches (80,23); panics at src/tmux.rs:253). On a fresh
  isolated server the same test passes (verified focused: exit 0 in 3.30s).
  The isolation recipe (fresh `TMUX_TMPDIR` that EXISTS — tmux silently falls
  back to the default socket dir when the target dir is missing — plus
  `COLUMNS`/`LINES`) is the one the #327 lane validated on this host
  (`/tmp/corral-327-r1-isolated-tmux-gates/tmux_*_focused.log`, both exit 0);
  `src/tmux.rs` is byte-identical to base (`git diff 58a41f0 -- src/tmux.rs`
  = 0 lines) and was not modified (terminal lifecycle is fenced). The #339
  lane hit the same shared-server failure class this morning; orphaned
  `corral-cursor-test-1` fixtures leaked by failing runs (incl. one from this
  lane's own isolation probe) were killed — other lanes' tmux sessions were
  never touched. CI (ubuntu, fresh runner) is unaffected.
- `cargo deny --locked --workspace check` → exit 0 (advisories ok, bans ok,
  licenses ok, sources ok; 40 existing duplicate-version warnings).
- `cargo audit --deny warnings` → exit 0.
- Privacy/diff gates: tracked-secret CI check reproduced verbatim from
  `.github/workflows/secret-scan.yml` (git ls-tree awk) → exit 0, no tracked
  secret files. gitleaks is not installed on this host (CI-only); no
  secret-shaped spans exist in the diff (evidence README/logs contain no
  tokens; the probe fixture text is synthetic). Anti-slop delta: the
  anti-slop CLI is not installed on this host and the diff contains no
  Swift/TS files (Rust + docs only); not runnable locally — advisory lints
  for future iOS review rounds only.

## Scope and hygiene notes

- Only the fenced files changed (see Changed files). No updater scripts, egui
  fonts, iOS code, GitHub polling scope, or terminal lifecycle code touched;
  no live socket mutation (the live herdr probe was read-only); no live app
  touch.
- `docs/OPERATIONS.md` edit is scoped to the revision-contract subsection
  only; the #340 docs fix owns the GET /fleets cleanup and was not touched.
- Environment hygiene performed and disclosed: killed orphaned
  `corral-cursor-test-1` tmux fixture sessions (leaked by failing runs of the
  tmux cursor test, incl. one from this lane's own isolation run) so the
  shared tmux server stays clean for other lanes; other lanes' tmux sessions
  (`corral-herdr-*`) were never touched.

## R2 integration reconcile (merge 2c1881f, per .reconcile-r2.md)

- Delivery head `258913f` was based on `58a41f0`; origin/integration had
  advanced to `2c1881f6733e52cad3116dd224e731bca1f73dea` (PR #339, the #332
  Herdr-issues-scope merge; 16 files, code-only). PR #351 reported
  CONFLICTING.
- `git fetch origin integration` then `git merge origin/integration` (normal
  merge, no rebase, no force-push). The ONLY conflict was `.report.md`
  (integration carried the #332 lane's report); resolved by retaining this
  lane's complete #324 report and evidence (the #332 lane's own reconcile
  precedent), then appending this R2 section. No production conflict: none
  of #324's changed paths (src/adapters/herdr.rs, docs/OPERATIONS.md,
  docs/corral/P4-conformance.md, docs/design/evidence/issue-324/) overlap
  with the #332/#339 diff.
- R2 gate rerun on the merged tree under `/tmp/corral-heavy-gate.lock`
  (owner logged, no overlap; `/tmp/corral-324-r2-gates.log`):
  - `cargo fmt --all -- --check` → exit 0.
  - `cargo test -p corrald --lib read_tail_since_with_rev` → exit 0, 5 passed.
  - `cargo test -p corrald --lib contract_tail` → exit 0, 4 passed.
  - probe test → exit 0, 1 passed.
  - `cargo test --workspace --all-targets` (tmux-isolated, per the R1
    environment note) → exit 0, 735 passed / 0 failed / 19 ignored (the
    merged tree adds the #332/#339 tests).
  - `cargo clippy --workspace --all-targets -- -D warnings` → exit 0.
  - `cargo deny --locked --workspace check` → exit 0.
  - `cargo audit --deny warnings` → exit 0.
- Merge committed and pushed normally; clean local==remote exact head
  verified after push (see external report for the new SHA and PR #351
  mergeable state).
## R3 integration reconcile (merge e5a6ba5 — L1 read-only daemon cut #354/#355, per .reconcile-r3.md)

- origin/integration advanced to `e5a6ba5a802903f34229bb6fcb285b5566a61c1c` (L1
  daemon cut #355 merged; removes mutating drive dispatch, read_diff+UI,
  step-up auth, /grants admin surface, fleet/worktree/terminal modules,
  grant CLI, src/tmux.rs, and their tests).
- Normal merge, no rebase/force-push. Conflicts: `src/adapters/herdr.rs` and
  `.report.md` only (docs/OPERATIONS.md auto-merged; this lane's "Provider
  read-tail revision contract (#324)" subsection survived intact).
- herdr.rs resolution (diff3: ours = my read-tail tests + base test block,
  base = the block, theirs = L1 deleted it): KEPT this lane's #324 read-tail
  tests and helpers (contract_tail_*, read_tail_since_with_rev_*, probe,
  mock_socket_serve_read/rev_contract/probe) — they test read_tail code that
  SURVIVES the L1 cut; DROPPED the L1-removed pre-existing test block
  (drive/attach/kill/migration-control tests whose production code L1
  deleted). None of this lane's #324 changes touched removed surfaces
  (grant/step-up/read_diff/terminal): the lane's contract work is
  read_tail-only, and no read_diff provider-rev contract work existed to
  re-scope — the module docs and protocol docs reference read_tail only.
  Production hunks (read_tail_rpc, contract_tail, module docs) merged
  unchanged.
- .report.md resolution: retained this lane's complete #324 report + R1/R2
  evidence (the #332/#354 lanes' own reconcile precedent); the L1 lane's
  report remains in integration history.
- R3 gate rerun on the merged tree under the flock shim
  (`flock /tmp/corral-heavy-gate.lock /tmp/run-corral-324-r3-gates.sh` —
  no overlap; `/tmp/corral-324-r3-gates.log`):
  - `cargo fmt --all -- --check` → exit 0.
  - `cargo test -p corrald --lib read_tail_since_with_rev` → exit 0, 5 passed.
  - `cargo test -p corrald --lib contract_tail` → exit 0, 4 passed.
  - probe test → exit 0, 1 passed.
  - `cargo test --workspace --all-targets` → exit 0, 664 passed / 0 failed /
    13 ignored (post-L1 test count; the L1 cut removed the tmux cursor test
    with src/tmux.rs, so no tmux isolation is needed on this tree).
  - `cargo clippy --workspace --all-targets -- -D warnings` → exit 0.
  - `cargo deny --locked --workspace check` → exit 0.
  - `cargo audit --deny warnings` → exit 0.
- Merge committed and pushed normally; clean local==remote exact head
  verified after push (see external report for the new SHA).
