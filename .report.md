# Issue #290 implementation report

## Changes

- Added a 50-line default first-open `read_tail` page while retaining the daemon's 1–200 line clamp.
- Added optional `since_rev` to the drive and adapter contracts, preserving decoding of older commands.
- Forwarded `since_rev` to herdr `agent.read` requests when supplied.
- Added an egui incremental tail intent and changed the egui load-earlier action to request the full bounded page.
- Updated iOS read-tail canonical payload/action identity to use the 50-line first page.
- Re-pinned the iOS release-source digest for the changed release-listed source.

## Response-size measurement

Measured one live `herdr agent read` response for `orch-corral` using:

    herdr agent read orch-corral --source recent-unwrapped --lines 200 --format text

The command succeeded with 140 newline-delimited lines and 15,949 bytes on stdout (measured by redirecting stdout to a temporary file and running `wc -c`/`wc -l`). This is below the 32 KiB daemon response cap for that pane.

## Verification

All commands were run from this worktree:

- `cargo fmt --all --check` — exit 0
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0
- `cargo test --workspace` — exit 0
- `python3 ios/check-release-demo.py --self-test` — exit 0
- `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd build` — exit 0
- `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/fn-dd -only-testing:FleetNotifierTests` — exit 0
- `git diff --check` — exit 0

The requested 100k-line host-local performance assertion is now covered by `adapters::herdr::tests::hundred_thousand_lines_stay_bounded_quickly`: it constructs 100,000 lines, runs the bounded path, asserts `<300ms`, `<=200` lines, and the byte cap. Focused result: 1 passed in 0.03s.

## Review fix cycle 1 evidence

- Base comparison: a detached `cf766b3` worktree ran `GrantAdminToggleRevertTests`; both tests passed (2 executed, 0 failures). The previously reported two failures were not reproducible in this focused base run.
- `cargo test --workspace` after the perf test and default-page correction: passed (all non-ignored tests passed).

Known remaining gap: the production egui/iOS models do not yet extract and persist the daemon's `source_rev`, so they cannot automatically populate `since_rev` on a later refresh. The adapter/API seam now carries an optional `source_rev`, but the live herdr CLI has no `--since_rev` option for a direct CLI reproduction; therefore a real same-revision empty/delta probe could not be performed. This is intentionally reported rather than claimed as verified.

Review Fix Cycle 1 status: the 100k-line bounded-path test and base GrantAdminToggleRevertTests evidence are complete; the end-to-end cached source-revision chain and same-revision live probe remain incomplete.

