# G79 report — iOS live-stream lifecycle (#79)

Branch g79/ios-livestream off main @ 94e0239. Implementer of record:
the orchestrator — the mandated delegate chain is exhausted and was
re-verified live this session (commandcode deepseek: weekly limit,
resets 2026-08-24; opencode-go: "Insufficient balance" in-pane; dsh: no
coding profile). Cross-model independence preserved via the separate
opus reviewer.

## Changes (issue's four fixes)

1. AppModel.register() calls startLive() after success
   (App/AppModel.swift, after mode = .live).
2. RootView gains .task { if model.mode == .live { model.startLive() } }
   (App/FleetNotifierApp.swift) — cold launch connects without a
   scenePhase transition.
3. connectionState-on-first-frame: ALREADY PRESENT on current main —
   apply() sets .connected on every applied frame (FleetStore.swift:72
   and :157). The issue's defect-2 first half was written against
   build-3 source; verified and documented rather than re-fixed.
4. CorraldClient.decode -> DecodeOutcome { event / ignored /
   failed(reason) } (Network/CorraldClient.swift); FleetStore.connect
   routes .failed to noteDecodeFailure -> connectionState =
   .error("stream frame undecodable — <decoder error>") + a log line;
   .error renders via the existing connection banner
   (UI/FleetViews.swift:571). Heartbeat .message frames stay silently
   ignored so keepalives can never masquerade as errors.

## Idempotence verification (read, not assumed)

startLive() guards `guard let hostURL else { return }` (hostURL set
before the call in register()); FleetStore.connect() guards
`guard streamTask == nil else { return }`. The three call sites
(register, RootView task, .active transition) therefore compose to
at most one live stream; no new flags added.

## Gates

- ios: xcodegen generate (no project delta); xcodebuild test on
  iPhone 17 Pro Max simulator: "Executed 83 tests, with 0 failures"
  (suite 81 -> 83: +testDecodeSurfacesFailuresAndIgnoresMessages,
  +testDecodeFailureResolvesSpinnerToVisibleError; one existing decode
  test updated for the new DecodeOutcome shape).
- cargo test -p corrald-client: 14 lib + 5 canonical-bytes tests green.
  CORRECTION (review F3): the R1-R10 conformance tests themselves are
  `#[ignore]`d without a live corrald (run with --ignored against a
  daemon) and were NOT run by this gate. They are unaffected — no Rust
  changed on this branch — and running them rides the device-verification
  round against the live daemon.
- cargo fmt --check clean; cargo clippy --all-targets -D warnings clean
  (no Rust touched — proven).

## Scope discipline

Only the four files + tests. No daemon changes, no ATS/project.yml
changes (#80's lane), no broad refactor.

## NEEDS DEVICE VERIFICATION

The simulator masked this bug. This fix is NOT claimed working on
hardware until Guy confirms on his physical device (registered
dev_09d54804..., daemon reachable via Tailscale Serve) that registering
populates the board with live agents. #80 lands next so ONE TestFlight
build carries both changes.

Local HEAD: see git log (1 commit).

IMPLEMENTATION COMPLETE
