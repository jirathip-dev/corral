# #207 implementation report

## Change

The native GitHub plane now hydrates `IssuesCache` for every emitted `GhRepoState`, keyed by its native `repo` identity. The obsolete optional `issue_repo`/`issues_key` seam was removed from the event/spec contracts. `/issues` documentation now describes the native identity contract. The two existing cache tests were updated to prove both native identity and hydration for a non-fleet tracked repository.

No egui files, Fleet Ops/plugin code, or request-path subprocesses were changed.

## Regression evidence

RED on the pre-fix implementation, after changing the test contract but before changing `handle_gh`:

```text
cargo test -p corrald issues_cache_is_keyed_by_native_repo_identity
RED_EXIT=101
thread 'integrate::tests::issues_cache_is_keyed_by_native_repo_identity' ... FAILED
native repo identity carries the fetched issue
failures:
    integrate::tests::issues_cache_is_keyed_by_native_repo_identity
```

GREEN after the fix:

```text
cargo test -p corrald issues_cache_is_keyed_by_native_repo_identity
1 passed; 0 failed

cargo test -p corrald tracked_non_fleet_gh_fact_publishes_issues
1 passed; 0 failed
```

## Required gates

- `cargo fmt --all --check` — exit 0, no output.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0; finished `dev` profile in 3m 24s.
- `cargo test --workspace -- > /tmp/g207-test.log 2>&1; echo "GATE_TEST=$?" >> /tmp/g207-test.log` — `GATE_TEST=0`; aggregate: 635 passed, 0 failed, 20 ignored across 24 suites.

The first full-test invocation was blocked by host disk exhaustion while writing its temporary aggregation script (`No space left on device`). I removed only this lane's `/tmp/g207-corral-target` (5.6G), reran the exact required test command successfully, and recorded the final result above.

## Files

- `src/integrate/mod.rs`: publish all GH issue facts under `state.repo`; update regression tests.
- `src/core/events.rs`: remove obsolete `issue_repo` identity field.
- `src/adapters/gh_plane.rs`: remove obsolete `issues_key` plumbing and stale tests.
- `src/api/issues.rs`: document native cache keys.

## Round 2 reviewer-fix evidence

### Production poll seam

`src/main.rs::gh_repo_specs` now uses the same `LiveRepoSourceDiscovery` as the git plane at each plane generation. It reads each discovered checkout's `origin` with git2 and accepts GitHub HTTPS, SCP-style SSH, and `ssh://` URLs. Static `TRACKED_REPOS` remains the fallback/union. No `/issues` request performs discovery or subprocess work.

### RED

Against the r1 implementation, with the new production-seam test and the spec builder temporarily restored to static-only behavior:

```text
cargo test -p corrald --bin corrald live_origin_repo_reaches_issue_cache_through_production_seam
RED_EXIT=101
thread 'tests::live_origin_repo_reaches_issue_cache_through_production_seam' ... FAILED
live origin repo is included in production gh specs
```

### GREEN

```text
cargo test -p corrald --bin corrald live_origin_repo_reaches_issue_cache_through_production_seam
exit 0
1 passed; 0 failed; 2 filtered out
```

The test creates a git fixture with `origin = git@github.com:example/live-only.git`, proves it enters production specs despite being absent from `TRACKED_REPOS`, then sends its `GhRepoState` through the production integrator event channel and verifies a non-empty issue-cache entry.

### Round-2 gates

- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --workspace -- > /tmp/g207-test-r2.log 2>&1; echo "GATE_TEST=$?" >> /tmp/g207-test-r2.log` — `GATE_TEST=0`; aggregate: 636 passed, 0 failed, 20 ignored across 24 suites.

Item 4 was left untouched: no `clients/egui/**` files changed. No GitHub mutations were made.

## Round 3 reviewer-fix evidence

### RED

Against the 5025cc0 r2 implementation, the new tests were run with the production builder temporarily restored to remote-basename keys and no alias grouping:

```text
cargo test -p corrald --bin corrald live_origin_repo_reaches_issue_cache_through_production_seam
ALIAS_RED=101

cargo test -p corrald --bin corrald same_remote_basename_groups_specs_without_native_key_collisions
COLLISION_RED=101
```

The alias test failed because `synergy-costing` was not represented by the production spec key. The collision test failed because the basename-keyed implementation did not preserve native aliases. The fan-out regression also passes against the final implementation and is covered by `one_query_fans_out_issue_state_to_native_aliases`.

### GREEN

```text
cargo test -p corrald --bin corrald live_origin_repo_reaches_issue_cache_through_production_seam
ALIAS_GREEN=0: 1 passed; 0 failed; 3 filtered out

cargo test -p corrald --bin corrald same_remote_basename_groups_specs_without_native_key_collisions
COLLISION_GREEN=0: 1 passed; 0 failed; 3 filtered out

cargo test -p corrald one_query_fans_out_issue_state_to_native_aliases
FANOUT_GREEN=0: 1 passed; 0 failed; 286 filtered out
```

The alias fixture uses directory `synergy-costing` with origin `synergy-services-cooling-tower/synergy-apps.git`; the cache assertion uses `synergy-costing`. The collision fixture uses two different native directories sharing remote basename `sendmeter`; production construction yields one spec per canonical owner/name slug, with native aliases retained.

### Round-3 gates

- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --workspace -- > /tmp/g207-test-r3.log 2>&1; echo "GATE_TEST=$?" >> /tmp/g207-test-r3.log` — `GATE_TEST=0`; aggregate: 638 passed, 0 failed, 20 ignored across 24 suites.

No `clients/` or `ios/` files changed. No issue, PR, comment, or merge operations were performed.

## Round 4 reviewer-fix evidence

### Global alias ownership

`gh_repo_specs` now builds discovered specs in sorted checkout-path order, groups canonical GitHub slugs, and claims each native alias globally. A discovered checkout therefore wins over a conflicting static fallback alias; fallback specs with no remaining aliases are omitted. Static aliases that do not conflict remain available, and same-slug fallback aliases merge into the discovered spec without creating a second poller.

### RED

Against the r3 implementation, the production regression using static `sendmeter` plus discovered directory `sendmeter` (origin `jirathip-dev/sendmeter`) failed because both canonical specs owned the same native alias:

```text
cargo test -p corrald --bin corrald discovered_alias_replaces_conflicting_static_fallback
RED_EXIT=101
```

### GREEN

```text
cargo test -p corrald --bin corrald live_origin_repo_reaches_issue_cache_through_production_seam
1 passed; 0 failed

cargo test -p corrald --bin corrald same_remote_basename_groups_specs_without_native_key_collisions
1 passed; 0 failed

cargo test -p corrald --bin corrald discovered_alias_replaces_conflicting_static_fallback
1 passed; 0 failed

cargo test -p corrald one_query_fans_out_issue_state_to_native_aliases
1 passed; 0 failed
```

The new precedence regression covers both real live collision shapes: `sendmeter` resolves solely to `jirathip-dev/sendmeter`, and `synergy-services-website` resolves solely to `synergy-services-cooling-tower/synergy-services-website`. The existing different-owner test asserts both native directory keys once; no duplicate alias producer remains.

### Round-4 gates

- `cargo fmt --all --check` — exit 0.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo test --workspace > /tmp/g207-test-r4.log 2>&1; ...` — `GATE_TEST=0`; 644 passed, 0 failed, 20 ignored across the workspace suites.

The required merge of `origin/main` at `a40aa29` was retained. No `clients/` or `ios/` files changed. No GitHub mutations were performed.
