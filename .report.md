# Corral #373 — iOS recents: block-per-run rendering (old style), always-expanded default, collapsible, themed

Branch: g373-ios-recents-blocks · Base (exact): 8a4467af76f6dc068f88f19fe5fb6cc376a99032 · Worktree: /Users/jirathip/.herdr/worktrees/corral/g373-ios-recents-blocks · Lane date: 2026-09-04.

## What changed (5 files — all inside the #373 fence)

- ios/FleetNotifier/UI/RecentOutputModel.swift — the #361 continuous-rail model is REPLACED by the #373 block-per-run display model:
  a role change starts a block (You / Assistant / Tool run / Status); consecutive same-role canonical blocks merge into ONE run, so same-tool
  calls share one block with a compact line per call and a growing live tail appends INTO the current semantic block (stable ordinal ids);
  divider-only/empty material drops before runs form; tool text splits into call lines (shell `$` echoes, plus a bare first-line tool verb) vs
  output lines (interior blanks kept); a TRAILING call-only tool run gets the muted inline "waiting for output…" row (never its own block);
  `lineCap` = 20 with `cappedLineCount`; `BlockRowKind`/`BlockRow`/`ToolKind`/`DisplayBlock`; `RecentOutputRender` keeps divider
  classification and gains the public per-line syntax segments.
- ios/FleetNotifier/UI/FleetViews.swift (recents-sheet region only — every diff hunk starts at/after line 1351; board/#371 code untouched):
  `RecentOutputSheet` renders `RecentOutputModel.displayBlocks` in a ScrollView; `RecentsSheetSession` (@StateObject, destroyed on dismissal =
  session-scoped collapse/reveal, defaults ALL EXPANDED); `RecentRunBlockView` = rounded surface0 chrome + thin left role accent, ICON-ONLY
  SF Symbol header chip + chevron (no role words visible), whole-header tap toggles collapse (chevron rotates, collapsed shows a one-line
  preview), bodies gated on `!isCollapsed`, output runs render inside the #372 output-panel token (tailBackground = mantle dark / BASE on
  Latte — the accepted recess rule), code/diff output lines color through the theme's ANSI-slot segment colors, per-block 20-line cap with an
  inline "Show all (+N lines)" reveal, waiting line styled muted italic; auto-scroll keys on the LAST block's text (collapsing above never
  jumps); VStack (not Lazy) + a collapse-change bottom re-anchor fix a stale-offset blank; DEBUG `-corralDemoRecentsBlocksEvidence` driver +
  large-detent override for the synthetic captures.
- ios/FleetNotifier/Demo/DemoFleet.swift — recents fixture rebuilt to the block-per-run evidence shape (10 role runs in the order Status →
  You → Assistant → read_file tool (bare call, doc icon) → Assistant → giant >20-line vitest run (cap + Show all) → Assistant → git-diff run
  (ANSI +/−/@@ marks) → Assistant → trailing call-only `rg` run (waiting line). Fully fictional demo-atlas content, ≤200 lines.
- ios/FleetNotifierTests/FleetNotifierTests.swift — the #361 rail-era classes (RecentOutputTailModelTests, RecentRailModelTests) and the
  rail wiring/theme pins were REPLACED by: RecentBlockModelTests (role-boundary start, same-role/same-tool grouping, call/output split incl.
  bare calls + the locked icon vocabulary, blank-line spacing, legacy-lines fallback, divider drop, waiting-line scope, 20-line cap, live
  append into the open block, phase, divider/a11y pins), RecentsBlockSessionTests (default-expanded, per-block toggle, one-shot reveal,
  reset), an updated demo-fixture pin, and block-renderer source-wiring pins (whole-header toggle, collapse gate, chevron rotation, cap +
  Show all call sites, theme tokens incl. the recess token; the removed-surfaces list now also bans the REJECTED #361 rail vocabulary).
- ios/check-release-demo.py — release-source digest + test-source digest re-pinned with dated 2026-09-04 (#373) comments.

## Gates (exact commands + real results)

1. Build (final state): `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd build`
   → BUILD SUCCEEDED, exit 0 (log /tmp/g373-build6.log; later source edits re-verified by the test-action build below).
2. Full iOS suite (final state, iPhone 16 sim 59DDC0C5): `... xcodebuild test ... -only-testing:FleetNotifierTests`
   → Executed 177 tests, with 0 failures (0 unexpected) — TEST_EXIT=0 (log /tmp/g373-tests-final2.log).
   Focused recents classes on the byte-identical scratch copy: 42 tests, 0 failures, exit 0 (/tmp/g373-scratch-final.log).
3. RED/GREEN mutation probes — all run in the disposable scratch copy /tmp/g373-scratch (git archive of the base + the lane files; the real
   worktree was never mutated; each probe restored the pristine file and proved byte-identity with `cmp -s`; scratch git-clean before every
   probe). One RED run per probe, then GREEN on the restored file; final GREEN battery on the pristine scratch = 42/42. Logs:
   /tmp/g373-probes/*.log (each with EXIT= and the failing-test summary).

| # | Mutation (scratch only) | RED test(s) | Result (log) |
|---|---|---|---|
| P1 | run merge ignores role changes (merges across kinds) | testRoleBoundaryStartsANewBlock | RED 1 test/2 failures → GREEN after restore |
| P2 | every canonical block becomes its own run (no same-kind merging) | testConsecutiveSameToolCallsShareOneBlockWithCompactLines; testLiveAppendGrowsTheOpenBlockNotANewOne (re-probed P2b after the fixture was made discriminating) | both RED (3 failures each) → GREEN after restore |
| P3 | lineCap 20 → 1 000 (cap removed) | testLineCapHidesRowsBeyondTwentyWithShowAllCount | RED (2 failures) → GREEN |
| P4 | waiting-row append disabled | testWaitingLineAppendsToTrailingCallOnlyToolRun | RED (2 failures) → GREEN |
| P5 | waiting attached to EVERY call-only run, not just the trailing one | testWaitingLineOnlyOnTheTrailingRun (trailing-behavior companion stayed GREEN under the mutation — the scope guard) | RED (1 failure) → GREEN |
| P6 | bare first-line tool verbs no longer classified as calls | testBareFirstLineToolCallIsClassifiedAsCallWithDocIcon | RED (2 failures) → GREEN |
| P7 | session seeds one collapsed id (default-expanded broken) | testSessionDefaultsToEverythingExpanded | RED (1 failure) → GREEN |
| P8 | toggle can collapse but never re-expand | testToggleCollapsesAndExpandsPerBlock | RED (1 failure) → GREEN |
| P9 | sheet bypasses the display model (empty block list) | testRecentsSheetCallsTheLiveReadTailDrive | RED → GREEN |
| P10 | block body rendered without the collapse gate | testRecentsBlocksRenderThroughTheBlockModel | RED (1 failure) → GREEN |

4. Release/privacy checks: `python3 ios/check-release-demo.py --self-test` → PASS (digests re-pinned first); Release simulator build
   (Configuration Release, CODE_SIGNING_ALLOWED=NO) → BUILD SUCCEEDED, exit 0 (/tmp/g373-release-build.log); binary inspection
   `python3 ios/check-release-demo.py --binary <Release executable>` → PASS ("Debug source preserved; Release boundary verified") —
   the Release binary carries the re-pinned source-digest marker and no demo entrypoint strings.
5. anti-slop (advisory): `swift run --package-path ios/tools/anti-slop-swift anti-slop ios/FleetNotifier ios/FleetNotifierTests`
   → 24 violations — identical count AND identical (file, rule) identities to the base commit (24): this lane adds ZERO new violations
   (/tmp/aslop-final.log vs /tmp/aslop-base.log identity delta = {}).

## Evidence (390x844 synthetic, Mocha + Latte) — /tmp/g373-evidence/

Five frames from one deterministic launch (`-corralDemoDetail -corralDemoRecentsBlocksEvidence`, DEBUG demo fleet, iPhone 16 sim
1179x2556 @3x, downscaled via `sips -z 844 390` to exactly 390x844 — 0.18% aspect distortion noted). README.md + capture.log + sha256.txt
in that directory; the frames were verified with per-phase OCR/vision reads and pairwise pixel deltas:
- phase-1 mocha expanded: block-per-run with the >20-line run capped at 20 + "Show all +4 lines", ANSI-remapped git diff (red −, green +, @@
  hunk), muted italic "waiting for output…" on the trailing rg run.
- phase-2 mocha collapsed: all 10 blocks collapsed — icon-only header map (info/person/sparkles/doc/terminal/magnifier), role accents
  (blue/mauve/peach/overlay), preview lines, rotated chevrons.
- phase-3 mocha show-all: the capped block's hidden rows now render and the control is gone (compare phase 1).
- phase-4 latte expanded: same bottom view on Latte — surface0 cards with output panels recessed toward BASE (panel visibly lighter than the
  card), diff marks in Latte ANSI slots (red #d20f39 / green #40a02b — the two of the three documented <3.0:1 exception slots exercised;
  the third is bright-black, documented in AppTheme's comment), waiting line present.
- phase-5 latte collapsed: parity collapsed headers.
ANSI remap context: the wire tail is escape-stripped daemon-side, so the remap is exercised through the same per-flavor ANSI-slot syntax-mark
pipeline (#372 lock; Latte exception ratios 2.96/2.31/2.83:1 documented in AppTheme and the design round's contrast.py).
Live-append-into-open-block is model-proved (P2 RED/GREEN), not a static frame. The collapsed/show-all states were driven by the DEBUG
driver because simctl cannot inject taps (established #364/#372 synthetic pattern, honest note in the evidence README).

## Structure summary + explicit non-claims

- No live daemon, no physical-device interaction evidence; no pull-gesture/drag evidence; Frappé/Macchiato not captured (gate asks
  Mocha + Latte); board/#371 code, daemon, egui, crates/, docs/ untouched; no TestFlight, no main promotion, no issue closure, no PR
  (orchestrator opens it; this lane pushed the branch — exact SHA below).
- The issue's LATER spec additions (09-03: intentional translucency, then the native Liquid Glass revision for recents + Settings sheets)
  are NOT part of this brief's scope or evidence gates (the fence is recents UI/model/tests only and the binding evidence list omits
  translucency); they postdate the lane's locked spec — flagged for the orchestrator as a follow-up decision. egui mirror remains
  undecided/parked (#376 posture comments).
- Host/environment honesty: CoreSimulator on this host was wedged under heavy multi-lane load (three other lanes' xcodebuilds + load avg
  300+); the shared CoreSimulatorService was restarted once mid-lane (other lanes' idle booted sims were shut down by that restart) and the
  lane's own simulator was rebooted; all gate runs above are from after the recovery.

## Pushed branch

Implementation commit 6e2fd6bc06fd5e68bed0edc07dfb10283cd0b79a; branch head after the report SHA note: 68fdd10acbc5f1eb9d5c3a4f960c791005f41182, pushed to origin/g373-ios-recents-blocks and verified local == remote via `git ls-remote origin g373-ios-recents-blocks`.
