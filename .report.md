# Corral #296 implementation report

Commit: `5684c93` (updated locally after the original partial commit)

## Removal inventory

- Removed `src/plugin.rs`, `src/api/plugin.rs`, plugin UI, plugin manifest,
  plugin fixture/docs, and plugin status test.
- Removed `/fleets` and the Fleet Ops identity/provider seam from `AppState`.
- Removed `src/fleet/cli.rs` and `src/fleet/switch.rs`.
- Removed `corrald fleet switch` dispatch/helpers.
- `/issues` now uses native live workspace repository categories only.
- Removed obsolete Fleet Ops projection assertions from `tests/http.rs` and
  retained native-category/read-plane coverage.
- `tests/http.rs` includes a poisoned-PATH read-plane regression covering
  `/healthz`, `/snapshot`, `/events`, and `/issues`.

Proof from repository root (all zero source-file hits):

```
CliFleetOpsProvider source_files=0
FleetOpsProvider source_files=0
CORRALD_FLEET_OPS source_files=0
fleet_ops_command source_files=0
herdr-fleet source_files=0
```

## Verification

- `cargo fmt --all --check` — PASS.
- `cargo clippy --workspace --all-targets -- -D warnings` — PASS.
- `cargo test --workspace` — PASS; all workspace tests passed, including the
  native worktree dispatch and poisoned-PATH HTTP regression.
- `xcodebuild test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/fn-dd-test` — PASS; 206 tests, 0 failures.
- `xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd` — PASS.
- `python3 ios/check-release-demo.py --self-test` — PASS.
- `cargo build --release -p corrald-ui` — completed and produced
  `target/release/corrald-ui`; copied to `/tmp/corrald-ui-g296`.

## Release timing/process evidence

Measured with `/usr/bin/time -p sh -c 'cargo build --release'` and a detached
worktree at base `759ac98`:

| revision | real | user | sys | observed matching processes |
|---|---:|---:|---:|---:|
| base `759ac98` | 265.00s | 1137.29s | 77.80s | 2 |
| head `ee1c486` | 0.98s | 0.21s | 0.20s | 2 |

The head build was incremental because dependencies/artifacts were already
available. These are build timings, not a UI startup latency claim.

## Gaps / limits

- The release UI binary was rebuilt and copied to `/tmp/corrald-ui-g296`.
  Running its GUI `--help` probe did not complete within the 420-second tool
  limit, so native board visual/runtime evidence with poisoned PATH was not
  captured. The source-level poisoned-PATH HTTP regression passed.
- No digest repin was needed: no release-demo source manifest entry changed.
- No separate screenshot or pull-to-refresh runtime capture was required by
  the source changes; the full iOS test suite passed.
