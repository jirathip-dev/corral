# #239 implementation report

- Checkout: `/Users/jirathip/.herdr/worktrees/corral/g239-plugin-engine`
- Branch: `g239/plugin-engine`
- Scope respected: no `ios/**` changes and no GitHub mutations.

## REVIEW FIX CYCLE 1

Implemented daemon card scheduling:
- `GET /plugins` now uses cached scheduled card results rather than running every card synchronously on every request.
- Each manifest card runs immediately on first pane/API access, then on its own `interval_sec` timer (default 60 seconds; values are clamped to at least one second).
- A per-card async result lock prevents overlapping executions of the same card.
- A shared four-permit semaphore bounds concurrent card subprocesses.
- Card keys include ID, argv, JSON mode, and interval; manifest interval/command edits create a fresh schedule on the next request.
- Existing 30-second subprocess timeout, output cap, and kill-on-drop behavior remain in force.
- Added `scheduled_card_runs_again_after_interval`, which executes a one-second fixture card twice and asserts an interval change produces a new schedule key.

Tests and gates:
- `cargo test plugin::tests --lib` — 4 passed; 0 failed.
- `cargo fmt --all --check` — passed.
- `cargo clippy --workspace --all-targets -- -D warnings` — passed.
- `cargo test --workspace` — passed with `RUST_TEST_THREADS=1`: corrald 300 passed, all workspace suites passed; live tests ignored as documented.
- `cargo test -p corrald --test git_plane` — 3 passed, 1 ignored. This was rerun after the prior environmental exhaustion/timing issue.
- `git diff --check` — passed.

`python3 ios/check-release-demo.py --self-test` was attempted but terminated by the environment while compiling the temporary Swift fixture (`SIGTERM`); no iOS files changed and this lane does not own `ios/**`. Disk availability at the retry was 9.7 GiB (95% used).

Visual evidence and live client-to-daemon audit verification remain outside this daemon scheduler fix and were not re-captured in this cycle.
