# Corral #311 — epaint glyph resolver closure

Status: implementation complete; final commit and normal push are recorded below.

## Scope and base

- Worktree: `/Users/jirathip/.herdr/worktrees/corral/g311-tofu-glyph-fallback`
- Current `origin/integration` fetched at `cf1c12ff17017021e6a222af914f843300480dce`; the branch was merged without rewriting integration history.
- The branch was merged normally with `git merge --no-edit origin/integration`; the merge commit is `47b1f0e5e1886ed18e40455f80eae1d74deda484`.
- New implementation commit before that merge: `409e9ea4d8e08230ff2cea5a72d55022b96e6753`.
- No rebase, reset, force-push, PR creation, PR merge, issue close, deployment, or release was performed.

## Implementation

`clients/egui/src/app.rs` now registers `NotoSansMath-Regular.otf` and inserts it
immediately after epaint's primary `Ubuntu-Light` face in the proportional
fallback family. This is a resolver anchor: NotoSansMath supplies U+25FB, the
replacement character epaint uses to identify its replacement face, but none of
the required fixture scalars. The existing NotoEmoji, Hack, and
NotoSansSymbols2 faces remain the actual owners of the emoji, ASCII/sparse
block, and symbol glyphs. Primary Ubuntu typography is unchanged.

The old test exclusions for U+2705 (`✅`) and U+26A0 (`⚠`) were removed. Only
U+FE0F, the variation selector in `⚠️`, remains excluded from scalar coverage.
The test drives `transcript_font_definitions()` through `configure_fonts()`,
`Context::run_ui`, and `FontsView::has_glyph`, then checks the laid-out fixture
for real UV/advance data. The exact fixture is:

    tool ✓ ✗ ✅ ⚠️ ▸ ● ⏺ ░

No board, iOS, updater, grants, protocol, Recent-output, or status-grouping
source was changed by this lane.

## Font provenance

- Artifact: `clients/egui/assets/NotoSansMath-Regular.otf`
- Source URL (pinned upstream bytes):
  `https://github.com/notofonts/noto-fonts/blob/ffebf8c1ee449e544955a7e813c54f9b73848eac/unhinted/otf/NotoSansMath/NotoSansMath-Regular.otf`
- Upstream raw URL used for identity verification:
  `https://raw.githubusercontent.com/notofonts/noto-fonts/ffebf8c1ee449e544955a7e813c54f9b73848eac/unhinted/otf/NotoSansMath/NotoSansMath-Regular.otf`
- Font version: 2.001 (font metadata)
- Byte size: 264,540 bytes
- SHA-256: `37dd8bede91f4cc96445dc5d275685ee161e9c3b3ba5cea670a86885b4321fb2`
- `cmp` against a fresh download from the pinned raw URL: identical.
- License: SIL Open Font License 1.1; complete license text is
  `clients/egui/assets/OFL.txt`, and the provenance file records the license
  and upstream identity.

## Production resolver proof

Exact focused command:

    CARGO_BUILD_JOBS=1 cargo test -p corrald-ui --lib app::font_tests::transcript_fixture_has_glyph_coverage -- --exact --nocapture

Result: exit 0; 1 test passed, 0 failed, 207 filtered out, with a positive
executed count. The
run exercises the production configured definitions and `FontsView::has_glyph`
for every non-variation-selector scalar in the exact fixture, and verifies
that each fixture character survives layout with UV/advance data.

Mutation proof was run against the same app tree before the integration refresh;
`git diff --exit-code 409e9ea 47b1f0e5e1886ed18e40455f80eae1d74deda484 -- clients/egui/src/app.rs`
returned 0, so the tested production file is byte-identical at the merged head:

- Removing the replacement-anchor family insertion: exit 101; the focused
  test reported `missing configured glyph ✅`.
- Replacing the symbols registration with the existing fallback face: exit 101;
  the focused test reported `missing configured glyph ✓`.
- Replacing the ASCII registration with the existing fallback face: exit 101;
  the focused test reported `missing configured glyph ░`.
- Restoring the pristine app source byte-identically: exit 0; 1 test passed,
  0 failed.
- Mutation logs: `/tmp/corral-311-live-replacement.log`,
  `/tmp/corral-311-live-symbols.log`, `/tmp/corral-311-live-ascii.log`, and
  `/tmp/corral-311-live-pristine.log`.

## Gates

All cargo gates below were run serially under the legitimate
`/tmp/corral-heavy-gate.lock` with an owner PID and no cargo overlap. Full logs
are in `/tmp/corral-311-postmerge-gate-*.log`.

    cargo fmt --all --check
    PASS (exit 0)

    CARGO_BUILD_JOBS=1 cargo test -p corrald-ui --lib app::font_tests::transcript_fixture_has_glyph_coverage -- --exact --nocapture
    PASS (exit 0; positive executed count)

    cargo clippy --workspace --all-targets -- -D warnings
    PASS (exit 0)

    cargo test --workspace --all-targets
    EXIT 101: 310 tests passed, 1 failed, 0 ignored; the exact run hit the pre-existing shared tmux session
    `corral-cursor-test-1` and failed with `duplicate session: corral-cursor-test-1`.
    This was not a source assertion or compile failure; the session belongs to
    another active Corral lane and was not killed.

    cargo build --release -p corrald-ui
    PASS (exit 0)

    cargo deny --locked --workspace check
    PASS (exit 0)

    cargo audit --deny warnings
    PASS (exit 0)

The same workspace test command was rerun under the same heavy lock with an
isolated temporary tmux socket, without changing sources:

    TMUX_TMPDIR=/tmp/corral-311-postmerge-tmux cargo test --workspace --all-targets

Result: exit 0; 23 test binaries, 715 passed, 0 failed, 19 ignored. This
controlled rerun includes the 208-test corrald-ui suite and removes only the
unrelated global tmux-session collision.

Non-cargo gates:

    python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json
    PASS (exit 0; privacy scan: 0 forbidden matches)

    python3 scripts/check-demo-privacy.py --self-test clients/egui/assets/demo-fixture.json
    PASS (exit 0)

    git diff --check
    PASS (exit 0)

## Native evidence

No native PNG is claimed. The brief's separate GUI-session gate remains
unverified because this headless lane does not have the required Accessibility
and Screen Recording/TCC permissions. No synthetic screenshot was substituted.

## Delivery

The report was replaced from the unrelated prior report with this #311 report.
The final delivery SHA and the local/remote exact-head proof are recorded in
the final lane message after the normal push; no PR was opened or merged.

## R8 review fix — dual-family coverage contract

R8 started from the pushed implementation/report head
`a15be09bc6af45ec640d40e9acb89a6f28385b3b`. The production font definitions,
font assets, and fallback registrations were not changed. The only source
change is inside the existing `#[cfg(test)] mod font_tests` block in
`clients/egui/src/app.rs`.

`transcript_fixture_has_glyph_coverage` now shares one layout and resolver
assertion for both `FontId::proportional(12.0)` and
`FontId::monospace(12.0)`. For each family it uses `Context::run_ui`, lays out
the exact fixture, checks UV/advance data, and calls `FontsView::has_glyph` for
every non-whitespace fixture scalar. Only U+FE0F is excluded:

    tool ✓ ✗ ✅ ⚠️ ▸ ● ⏺ ░

The test calls `configure_fonts` and then, only in the test module, mirrors the
configured proportional family list into the monospace family before the direct
monospace check. This avoids epaint's built-in `Hack` face being selected as the
monospace replacement face while keeping the same production-registered
transcript fallback faces and making the rendering family explicit. No runtime
or asset behavior changes.

### R8 focused and gate evidence

All commands below were run serially by `/tmp/run-corral-311-r8-gates.sh` while
that process owned `/tmp/corral-heavy-gate.lock`; each command had its own raw
log in `/tmp/corral-311-r8-gates/`.

    cargo fmt --all --check
    PASS (exit 0)

    env CARGO_BUILD_JOBS=1 cargo test -p corrald-ui --lib app::font_tests::transcript_fixture_has_glyph_coverage -- --exact --nocapture
    PASS (exit 0; 1 passed, 0 failed, 207 filtered out)

    env CARGO_BUILD_JOBS=1 cargo test -p corrald-ui --lib
    PASS (exit 0; 208 passed, 0 failed, 0 ignored)

    cargo clippy --workspace --all-targets -- -D warnings
    PASS (exit 0)

    cargo test --workspace --all-targets
    EXIT 101: 310 passed, 1 failed, 0 ignored. The unrelated shared tmux
    cursor test observed `(32, 10)` instead of `(80, 23)` and failed at
    `tmux::tests::tmux_capture_reports_cursor_position`.

    env TMUX_TMPDIR=/tmp/corral-311-r8-tmux cargo test --workspace --all-targets
    PASS (exit 0; 23 test binaries, 715 passed, 0 failed, 19 ignored). The
    isolated control included the 208-test corrald-ui suite.

    cargo deny --locked --workspace check
    PASS (exit 0)

    cargo audit --deny warnings
    PASS (exit 0)

    git diff --check
    PASS (exit 0)

### R8 mutation evidence

The three accepted R7 registration mutations were rerun against the R8 test
source. Each inner focused cargo command returned a real RED exit and named the
load-bearing scalar:

- Replacement-anchor insertion removed: exit 101; `missing configured
  proportional glyph ✅`.
- Symbols registration replaced with the ASCII face: exit 101; `missing
  configured proportional glyph ✓`.
- ASCII registration replaced with the fallback face: exit 101; `missing
  configured proportional glyph ░`.

In this exact registered font set, U+25B8 (`▸`) remains supplied by
`NotoSansSymbols2-Regular.ttf` after the ASCII face is replaced, so the faithful
ASCII-only mutation names U+2591 (`░`) first. No second mutation was combined to
manufacture a `▸` failure. The raw three-probe summary is
`/tmp/corral-311-r8-mutations-summary.log`.

The monospace-path mutation replaced the test-only shared-family mirror with a
`Hack`-only monospace chain. Its focused command returned exit 101 with
`missing configured monospace glyph t`, proving the monospace assertion itself
bites. The source was restored byte-identically; the direct post-run `cmp`
check returned exit 0. Raw mutation output is
`/tmp/corral-311-r8-mutation-monospace-summary.log` and the focused pristine
run is `/tmp/corral-311-r8-gates/focused.log`.

The first combined mutation wrapper had a literal-`\\n` quoting error at its
monospace mutation site; its three completed registration sub-probes remain
valid above. The corrected monospace wrapper checked restoration before its
EXIT trap, so its aggregate wrapper exit was 1 despite the inner RED and the
direct post-trap restoration check both being valid. No wrapper result was
used as a green claim.

R8 did not add assets, fonts, production code, or native evidence. The existing
native PNG non-claim remains unchanged: required Accessibility and Screen
Recording/TCC permissions are unavailable in this headless lane, and no
synthetic PNG is substituted.

## Retained integration report — Corral #325

# Grouped implementation report — Corral #325

Status: READY FOR REVIEW. All acceptance criteria satisfied with real RED/GREEN
evidence; all repository gates green under `/tmp/corral-heavy-gate.lock`
serialization (no overlap with the live #311 lane — bounded wait of 200 s).

Base: `cf1c12ff17017021e6a222af914f843300480dce` (post-#343 integration),
branch `g325-egui-page-limit-pin`. Issue #325 open; no PR opened, no issue
closed, `main` untouched.

## Changed files

- `clients/egui/src/state.rs` — tests only: two committed regression tests
  pinning the `tail_requested_lines` cleanup contract. Production code
  (including the two pinned cleanup lines) untouched.
- `clients/egui/src/app.rs` — the stale `refresh_recent_output` doc comment
  only (lines 1824–1825): now states default 50, remembered explicit window
  up to 200, bounded by the existing daemon cap. No logic hunks; no overlap
  with the fenced #311 app.rs font work.
- `.report.md` — this report.

## Acceptance criteria

1. Committed state regression covering BOTH full-snapshot disappearance and
   delta removal of an agent with `tail_requested_lines[agent] = 200`:
   - `snapshot_disappearance_clears_the_remembered_tail_window` — seeds
     `a=200, b=200`, applies a reconnect snapshot dropping `b`, asserts the
     dropped agent's remembered window is gone while the live agent's
     survives.
   - `delta_removal_clears_the_remembered_tail_window` — seeds `a=200`,
     removes `a` via `Delta.del` (the `remove_agent` path), asserts the
     remembered window is gone.
2. Re-adding the same agent identity starts WITHOUT the stale 200-line
   expansion: both tests re-add the same identity (snapshot re-add / delta
   `upd` re-add) and assert `tail_requested_lines` contains no entry for it —
   the automatic refresh falls back to the default 50 until the operator
   explicitly re-expands (the app.rs `dispatch_drive_intents` re-insert path,
   unchanged).
3. Removing the `apply_snapshot` retain line → RED with positive test count:
   see Mutation probe 1.
4. Removing the `remove_agent` cleanup line → RED independently with positive
   test count: see Mutation probe 2.
5. Doc comment corrected: "Refreshes re-request the remembered per-agent
   window: default 50 lines, or an explicit `Load earlier` expansion up to
   200 lines, bounded by the existing daemon cap."
6. No runtime behavior, daemon/protocol surface, iOS code, pacing policy, or
   page-bounds change: state.rs diff is test-module-only; app.rs diff is the
   comment-only hunk above; the two cleanup lines are byte-identical.
7. All gates green (below).

## RED/GREEN mutation evidence

Probes ran on a scratch copy (`git archive HEAD` + the new tests carried
over, `git init` + commit), one production mutation per probe with an
asserted single-occurrence anchored replacement; pristine file restored
byte-identical (`cmp -s`) between probes.

Mutation probe 1 — delete the `apply_snapshot` retain line
(`self.tail_requested_lines.retain(...)`):
`cargo test -p corrald-ui --lib state::tests::snapshot_disappearance_clears_the_remembered_tail_window -- --exact`
→ exit 101; `test result: FAILED. 1 failed; 0 passed; 208 filtered out`;
panicked at state.rs:772:9: "a snapshot-dropped agent loses its remembered
200-line window". Restored byte-identical.

Mutation probe 2 — delete the `remove_agent` cleanup line
(`self.tail_requested_lines.remove(agent_id);`):
`cargo test -p corrald-ui --lib state::tests::delta_removal_clears_the_remembered_tail_window -- --exact`
→ exit 101; `test result: FAILED. 1 failed; 0 passed; 208 filtered out`;
panicked at state.rs:821:9: "delta removal clears the remembered 200-line
window". Restored byte-identical.

The real worktree was never mutated: `git status --short` shows only the
intended `app.rs` + `state.rs` + report diff at all times.

## Gate results (exact commands, real exits, serialized under the lock)

- `cargo fmt --all -- --check` → exit 0.
- Focused state tests: `cargo test -p corrald-ui --lib state::tests` →
  exit 0; `18 passed; 0 failed; 191 filtered out` (both new tests in the
  passing set).
- egui crate: `cargo test -p corrald-ui` → exit 0; 216 passed, 0 failed
  across all test binaries (lib 209 + integration 2/4/1).
- `cargo clippy --workspace --all-targets -- -D warnings` → exit 0, 0
  warnings (Finished dev profile in 2m 00s).
- `cargo test --workspace --all-targets` → exit 0; 716 passed, 0 failed,
  19 ignored.
- `cargo deny --locked --workspace check` → exit 0; "advisories ok, bans ok,
  licenses ok, sources ok".
- `cargo audit --deny warnings` → exit 0.

Lock discipline: `/tmp/corral-heavy-gate.lock` was held by
`/tmp/run-corral-311-heavy-gates.sh` (PID 25478); this lane waited 200 s
bounded, acquired atomically (`mkdir` + `owner.pid` + EXIT trap), ran the
entire battery single-threaded, and released. No overlap.

## R9 exact-tip integration reconcile

- Starting pushed lane head: `ff469ef7b9d4cdc173066b1afffa04dc9ba98eb4`.
- Ran `git fetch origin integration`; verified
  `origin/integration` exactly equals `58a41f0875baead7c680eef4f0e39a8096e2c091`.
- Ran the required normal merge command: `git merge origin/integration`.
  The merge was divergent and produced merge commit
  `0154aed93a645a5b1d402856daa4e871eb9de816` with parents
  `ff469ef7b9d4cdc173066b1afffa04dc9ba98eb4` and
  `58a41f0875baead7c680eef4f0e39a8096e2c091`.
- The only unmerged path was the allowed tracked report `.report.md`.
  Its resolution retains the complete #311/R8 report body and the complete
  incoming #325 report body verbatim under the retained-history heading above.
  `clients/egui/src/app.rs` auto-merged without edits: the R8 production
  resolver and dual-family `transcript_fixture_has_glyph_coverage` test remain,
  and the incoming `refresh_recent_output` behavior/documentation remains.
  `clients/egui/src/state.rs` auto-merged with both incoming tail-window
  regression tests present. No other conflict path was resolved.
- Source audit markers after the merge: `refresh_recent_output`,
  `transcript_fixture_has_glyph_coverage`,
  `snapshot_disappearance_clears_the_remembered_tail_window`, and
  `delta_removal_clears_the_remembered_tail_window` were all present.

### R9 merged-tree gate battery

All commands below ran one at a time under the legitimate
`/tmp/corral-heavy-gate.lock`; raw logs and exit markers are retained at
`/tmp/corral-311-r9-gates.Fnzwls/`. The runner recorded `OWNER_PID=75318` and
`DONE=1`.

- `cargo fmt --all --check` — exit `0`.
- `env CARGO_BUILD_JOBS=1 cargo test -p corrald-ui --lib app::font_tests::transcript_fixture_has_glyph_coverage -- --exact --nocapture` — exit `0`; `1 passed, 0 failed, 0 ignored, 209 filtered out`.
- `env CARGO_BUILD_JOBS=1 cargo test -p corrald-ui --lib` — exit `0`; `210 passed, 0 failed, 0 ignored`.
- `env CARGO_BUILD_JOBS=1 cargo clippy --workspace --all-targets -- -D warnings` — exit `0`.
- `env CARGO_BUILD_JOBS=1 cargo test --workspace --all-targets` — exit `101`; `310 passed, 1 failed, 0 ignored`. The sole failure was the environmental shared-tmux test `tmux::tests::tmux_capture_reports_cursor_position` at `src/tmux.rs:253`, with `(32, 10)` versus `(80, 23)`.
- `env COLUMNS=80 LINES=23 TMUX_TMPDIR=/tmp/corral-311-r9-gates.Fnzwls/tmux CARGO_BUILD_JOBS=4 cargo test --workspace --all-targets` — exit `0`; 23 result lines aggregated to `717 passed, 0 failed, 19 ignored`.
- `cargo deny --locked --workspace check` — exit `0`.
- `cargo audit --deny warnings` — exit `0`.

The isolated workspace control is the authoritative workspace result for this
lane; it removes only the shared default-tmux contamination and does not alter
source or test configuration.

Native PNG evidence remains unavailable because Accessibility and Screen
Recording/TCC permissions were unavailable; no native PNG claim is made.
