# .report.md — corral #354 L2 iOS client read-only cut

Worktree: `/Users/jirathip/.herdr/worktrees/corral/g354-l2-ios-cut`
Branch: `g354-l2-ios-cut` (base `e5a6ba5a802903f34229bb6fcb285b5566a61c1c`, exact
L1 integration head)
Spec: issue #354 full locked spec comment 5505946486 + amendments
(5506287551, 5506327139, 5506434511) + queue mandate
(`/Users/jirathip/.hermes/profiles/fleet-orch/run/corral-354-cut-queue.md` L2
section, which carries the AMENDED v2 board vocabulary).

## Scope executed (iOS FleetNotifier only; daemon/egui/WASM untouched)

Removed (surfaces + code):
- Issues browser view (navigation, toolbar entry, IssueChip/IssueInference/
  IssueBrowser/IssueFilter projections, `read_issues` drive, workspace
  `issues` join decode types), Terminal view + terminal attach client +
  worktree browser (file deleted), Diff view (read_diff drive + diff wire
  types), and ALL action controls — answer/prompt/interrupt/kill/attach/
  start-worktree/probe actions (RowAction/AgentActionAvailability/primary
  action, approval claims, canned replies, step-up/Face ID, destructive
  pattern mirror), including lock-screen notification reply actions.
- Devices & Grants admin UI (admin token store, GET/POST /grants client
  calls, grant toggles/revoke, step-up mint) — the daemon's /grants admin
  surface was removed by L1.
- Board search + repo filter chips (BoardFilter, FleetSearchable), the
  #319/#320 status-group hierarchy (Needs-you/Supervising/Finished wording
  superseded by the 09-02 amendment), the V3 Conversation/Harness partition
  + Session-status metadata + load-earlier paging + prompt composer, and
  the demo/evidence launch routes that targeted them.

Kept / added per the v2 mandate:
- Board (home): repo groups with status chips; blocked agents pinned on top
  (promotion — also first in their repo); every repo agent listed in
  attention order blocked → working → idle → unknown (wire `done` ranks
  with idle as its herdr fallback) so an idle (finished-fallback) agent
  stays in its repo until replaced — last-done-per-repo retention. Orphan
  (no-repo) bucket sorts last. Row = agent name, repo, state, time-in-
  state, branch + small pane ref. No search, no filter chips.
- Labels: herdr RAW tokens verbatim — working / idle / blocked / unknown
  (no Corral-invented wording). Colors/marks/hexes stay bound to
  `contracts/state-tokens.json` (iOS-side StateStyle mirrors colors/marks
  and diverges ONLY in label/rank, documented in StateStyle.swift; the
  shared contract file itself is untouched — egui/L3 owns its side).
- Recents v1: tap row → bottom sheet, LIVE TAIL ONLY (≤200-line daemon
  cap), auto-scroll + auto-refresh while open, loading/empty/error+Retry
  states; no load-earlier, no partition, no composer.
- Notifications: state-change pushes start (→working) / blocked / done
  (active→idle, ONCE per episode, deduped until the agent starts again);
  content title `agent · repo`, body `state · branch`; tap deep-links to
  that agent row with recents open; Settings = global on/off only; no
  badge/catch-up on foreground. DEBUG keeps the local UNUserNotification
  fallback while SSE is connected (PushBridge); signed `/device-token`
  APNs enrollment retained.
- Settings: connection + notification pairing only (device/grant admin
  UI removed; grants shown read-only and provisioned out-of-band by the
  host).
- Offline: board keeps the last-known fleet with a "daemon offline"
  banner when the stream is down; live SSE + pull-to-refresh + foreground
  refresh retained; registration + signed grants refresh retained.

## Judgment calls (documented deviations)

- The .brief.md text "grouped labels kept Working / Supervising /
  Finished (per #319/#320)" predates the spec amendments; the binding
  queue mandate (line 15) says "per spec AMENDED 09-02 (v2): herdr RAW
  status labels verbatim … #319/#320 closed not-planned" — raw labels
  implemented; PR #343's earlier wording swept from iOS only.
- Recents v1 = live tail only per amendment 3 (no "Load earlier"); the
  locked-spec "Load earlier" line is superseded.
- Board retention interpretation: the daemon keeps idle panes in the
  snapshot (herdr panes fall back to idle, never done); rows render all
  snapshot agents per repo in attention order, so a finished agent stays
  until the daemon deletes/replaces it — no ghost/retention cache added
  to the read model (would fight the revision gate).
- Notifications transition source: herdr 0.8.2 has no `done`, so the
  episode-end push fires on active→idle deltas (v2 semantics); a
  transitional wire `done` is treated as the same episode end. Real APNs
  delivery is the daemon-side provisioning checkpoint (Guy .p8 export +
  CORRAL_APNS_* env) — see non-claims.
- `AgentState.done` decode case retained for wire safety; it is ranked
  with idle and never produced by the demo fixture.
- `Contracts/state-tokens.json` NOT edited (shared with egui, L3-owned);
  iOS divergence confined to StateStyle.swift with rationale.

## Fences held

`git diff e5a6ba5..HEAD --stat` touches ONLY `ios/**` (FleetNotifier app,
tests, project.yml, generated project, README, release/privacy tooling)
plus the pre-existing tracked `.report.md`. No daemon src/, crates/
corrald-client, clients/egui, WASM, docs/ root changes. Preserved frozen
worktrees/branches untouched. No main promotion, no TestFlight, no issue
closure, no rebase/force-push.

## Commits (local == remote head `751a2ea…`)

- fe87784 iOS (#354 L2): cut Issues/Terminal/Diff/actions + grant-admin surfaces
- df9d4d8 iOS (#354 L2): store + notifications — episode transitions and state-change pushes
- a144b0e iOS (#354 L2): read-only board, recents sheet, settings — views and fixtures
- 2761e9f iOS (#354 L2): rebuild the test suite + docs for the read-only client
- 7097114 iOS (#354 L2): fix tailSourceRev decode for small Int64-coded revisions
- 751a2ea iOS (#354 L2): release-gate alignment — DEBUG-only demo hooks + checker tables

Final head: `751a2ea0…` (full: see `git rev-parse HEAD` below / ls-remote).

## Gates (real runs at head 751a2ea, serialized under /tmp/corral-heavy-gate.lock)

| Gate | Command | Exit | Result |
|---|---|---|---|
| App build | `HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd-l2 build` | 0 | BUILD SUCCEEDED (log /tmp/l2-build4.log) |
| iOS full suite | same env + `xcodebuild test … -destination 'platform=iOS Simulator,id=59DDC0C5-…' -only-testing:FleetNotifierTests` | 0 | **Executed 99 tests, with 0 failures** — "** TEST SUCCEEDED **" (log /tmp/l2-ios-final.log; prior full runs /tmp/l2-ios-full4.log) |
| anti-slop (advisory) | `swift run --package-path ios/tools/anti-slop-swift anti-slop ios/FleetNotifier ios/FleetNotifierTests` | 1 (advisory; CI continue-on-error) | 25 findings: 21 in FleetNotifierTests, 4 in production — all RETAINED original sites (AppModel URL force-unwrap #166 F13 + silent grants catch #101, LocalNotifier auth catch D16, RecentOutputModel `copy() as!` date formatter); zero new-code violations; pre-cut documented baseline 77 → 25 over the smaller source set (log /tmp/l2-antislop-final.log) |
| Release build | `xcodebuild -configuration Release -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build` (fresh derived data) | 0 | BUILD SUCCEEDED; digest marker phase ran (log /tmp/l2-release2-build.log) |
| Release/privacy check | `python3 ios/check-release-demo.py --binary <Release-dir>/FleetNotifier` | 0 | PASS (Debug source preserved; Release boundary verified; per-slice digest markers OK) — log /tmp/l2-release2-check.log |
| Release self-test | `python3 ios/check-release-demo.py --self-test` | 0 | PASS (log /tmp/l2-selftest2.log) |
| Focused+full counts | suite run above | 0 | 99/99 (one test module; the whole iOS test target) |

Test bundle `FleetNotifierTests` + `StateStyleTests` were rebuilt for the
cut (action/issue/diff/terminal/admin classes removed; board/transitions/
payload/recents/wiring tests added).

## Simulator evidence (390x844, SYNTHETIC demo only — no physical device)

iPhone 16 sim (59DDC0C5-891E-4EC0-91AF-4F50DF68D793), Debug app built from
HEAD, launched with `-demoMode` / `-corralDemoDetail`; raw 1179x2556
screenshots downscaled to 390x844 with `sips -z 844 390` (0.18% aspect
distortion, stated in the evidence files):

- `ios/evidence/l2-board-readonly-390x844.png` — sha256
  e61120f3bb2fa5dc788bf35d6402be688f3ece5325611c4407764e78204e91ec
  (vision-verified: sections `blocked (2)` pinned top + repo sections
  demo-atlas(3)/demo-garden(2)/demo-ledger(2); rows show raw
  blocked/working/idle/unknown chips, name, repo · branch, pane refs,
  tool, PR/dirty; NO search field, NO filter chips, NO action buttons).
- `ios/evidence/l2-recents-sheet-390x844.png` — sha256
  ab2377faf1ff6a3f4a9e6f4bd113f456f0ba9f290fb37be8928b0946e7a1cc64
  (vision-verified: dark recents bottom sheet for `demo-output`, one
  unpartitioned live tail stream — assistant/diagnostic/unknown/tool diff
  blocks with +/- coloring — Done button only; NO composer, NO
  Conversation/Harness partition headers, NO actions).

## Explicit non-claims

- NOT verified: physical-device behavior, touch injection, pull-gesture
  feel, real APNs delivery (requires Guy's .p8 export + daemon-side
  `CORRAL_APNS_TEAM_ID/KEY_ID/AUTH_KEY_PATH` provisioning — checkpoint
  not satisfiable in-lane; the client keeps the signed /device-token
  enrollment and the DEBUG local bridge); daemon-side push content for the
  v2 transitions (daemon fenced to L1 and out of L2); hosted CI
  (orchestrator dispatches ios.yml/rust.yml after push); TestFlight /
  App Store; egui/WASM surfaces (L3).
- Demo evidence is synthetic; no live herdr daemon was contacted for the
  captures (board = seeded fixture; recents = seeded read_tail fixture).

## Notes

- The xcodebuild shim path required `HERDR_XCODEBUILD_DIRECT=1
  HERMES_SIM_TASK_ACTIVE=1` + a hand-booted simulator (documented pitfall).
- Gate serialization used the host's `flock` mkdir shim at
  /tmp/corral-heavy-gate.lock (no other corral lane gates ran
  concurrently; no other heavy xcodebuild/cargo processes observed).

## R1 reconcile — merge integration 10ba788 (#351 #324) into g354-l2-ios-cut

PR #356 reviewed PASS at 330c3b8, then the integration base moved
(e5a6ba5 → 10ba788212562497352d2f5142fbe47fb6da18a5 after the #351
read_tail provider-revision-contract merge). Reconcile per
`.reconcile-r1.md`:

- `git merge origin/integration` produced exactly ONE conflict —
  `.report.md` (both lanes keep their living report there); resolved to
  the L2 lane report (`--ours`), everything else (#351's
  `src/adapters/herdr.rs`, `docs/OPERATIONS.md`, `docs/corral/
  P4-conformance.md`, `docs/design/evidence/issue-324/*`) merged clean.
  Merge commit `28255fc170e278e3a454c0c197f9a2323ca25f5e`; final
  branch-vs-integration diff is ios/** + .report.md only (no daemon/
  crate/docs drift from this lane).
- Gate battery re-run at merge head 28255fc under the gate lock
  (`flock /tmp/corral-heavy-gate.lock bash /tmp/l2-reconcile-gates.sh`,
  no -c flag):

| Gate | Command (in the chained script) | Exit | Result / log |
|---|---|---|---|
| App build | xcodebuild build … generic iOS Simulator, /tmp/fn-dd-l2 | 0 | BUILD SUCCEEDED — /tmp/l2-r1-build.log |
| iOS full suite | xcodebuild test … -only-testing:FleetNotifierTests | 0 | **Executed 99 tests, 0 failures** — TEST SUCCEEDED — /tmp/l2-r1-test.log |
| Release build | xcodebuild -configuration Release -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO | 0 | /tmp/l2-r1-release-build.log |
| Release/privacy | python3 ios/check-release-demo.py --binary <release>/FleetNotifier | 0 | PASS — /tmp/l2-r1-release-check.log |
| Release self-test | python3 ios/check-release-demo.py --self-test | 0 | PASS — /tmp/l2-r1-selftest.log |

No source changes were needed for the reconcile (zero ios/** overlap with
#351); the merge commit itself is the only new commit besides this
report update.

