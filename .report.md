# .report.md — corral #354 L1 daemon cut (read-only daemon)

Worktree: `/Users/jirathip/.herdr/worktrees/corral/g354-l1-daemon-cut`
Spec: locked spec comment `5505946486` on jirathip-dev/corral#354 (L1 =
daemon-only; clients read-only witnesses, no L2/L3 surface work here
beyond keeping the workspace compiling).

## What was removed

- **Mutating drive dispatch** (`src/api/drive.rs`): the
  `prompt` / `approve` / `interrupt` / `kill` / `attach` /
  `start_worktree` / `read_issues` arms, the approval-claim seam, the
  step-up gate, the worktree route, and every helper only they used.
  Kept: `read_tail`, `read_diff` (signed, granted, replay-claimed,
  audited) and the typed error mapping.
- **Capability contract** (`src/drive/mod.rs`): `Capability` is now a
  closed two-variant set `{ReadTail, ReadDiff}`; `FromStr` refuses every
  legacy name; `DrivePayload` trimmed to the read payloads. Removed
  names fail as `unknown_capability` BEFORE the authorizer, dispatch,
  and audit.
- **Approval plane**: `src/approve/mod.rs` deleted (claim identity,
  prompt-hash validation, choice validation). The one kept consumer
  (`approval_id_for` format in `src/push/mod.rs` history rendering)
  inlines the `"agent:hash"` format.
- **Step-up**: `src/auth/step_up.rs` deleted, `POST /step-up` route
  removed from `src/auth/http.rs`, AuthPlane doc/mentions updated.
- **Terminal/attach transport**: `src/tmux.rs` deleted; the
  `/v1/terminal` socket route was not present in this tree (attach was
  drive-plane only); herdr adapter attach/kill/prompt/approve adapter
  RPCs and their terminal_ref handling removed.
- **Fleet/worktree module**: `src/fleet/` (`mod.rs`, `worktree.rs`)
  deleted; `dispatch_worktree` route gone from `src/api/mod.rs`; the
  admin worktree projection rows removed.
- **Tests**: `tests/terminal.rs`, `tests/worktree.rs` deleted;
  `tests/drive.rs`, `tests/auth.rs`, `tests/provenance.rs`,
  `tests/model.rs` rewritten to the read-only surface.

## Kept (GREEN probes all pass)

- Signed device-key auth: `/host-key`, `/register`, `/grants`
  (read + admin write for the READ capability set), `/audit`.
- Read drive: `read_tail` / `read_diff` — default-deny, grant-gated,
  replay-idempotent, audited.
- SSE plane: `/snapshot`, `/events` (resume + deltas).
- L1 spec note honored: `read_diff` kept because its daemon read path
  remains reachable from the kept client surface.

## Probe discipline (RED before GREEN)

`tests/readonly_cut.rs` (new) is the focused gate, 4 tests:

1. `mutating_drives_are_refused_without_dispatch_or_audit` — signs real
   envelopes for every removed capability; expects `400
   unknown_capability` with identical bytes for registered+granted and
   UNREGISTERED keys (proves the refusal is pre-authorizer), zero audit
   growth, zero adapter dispatches.
2. `signed_read_tail_still_dispatches_after_the_cut` — GREEN probe.
3. `fresh_registration_is_still_read_only_default` — GREEN probe.
4. `sse_and_snapshot_read_plane_still_serves` — GREEN probe.

**RED evidence (pre-cut base, `/tmp/corral-354-red-fixed.log`):**
`test result: FAILED. 3 passed; 1 failed` — the RIP probe failed with
`prompt: mutating drive must be refused at the capability boundary (got
200 OK … "error_kind":"not_implemented") — left: 200, right: 400`, i.e.
the old daemon accepted the envelope past parse/auth and hit the
dispatch arm. The three GREEN probes passed at base and after.

**GREEN (post-cut):** all 4 pass.

## Client layer (workspace constraint, kept compiling — L2/L3 will cut UI)

- `crates/corrald-client`: wire types mirrored to the read set
  (`Capability`, payloads, `StepUpRequired` error variant removed);
  ignored live conformance scenarios R3/R4/R6–R9 rewritten or retargeted
  (`prompt` → `read_tail`); unit tests retargeted. Non-ignored suite
  stays green without a daemon.
- `clients/egui`: grant-editor closed set `GRANT_CAPABILITIES` /
  `RECOVERY_GRANT_CAPS` now `["read_tail","read_diff"]`; mutating UI
  controls (prompt composer, interrupt/kill + confirm, approve choices,
  issue start-worktree action) removed with their tests; capability
  descriptions trimmed with a legacy fallback; demo seed ledger trimmed.

## Docs

- `README.md` — tagline, features, security bullets, architecture line
  rewritten to read-only.
- `docs/ARCHITECTURE.md`, `docs/OPERATIONS.md` — drive plane sections
  rewritten (kept from earlier in this lane).
- `docs/DEVELOPING.md` — module map (approve/, step_up.rs, tmux, fleet/
  removed), conformance scenario list, "how to add a capability" now
  gated on the closed read set.
- `docs/QUICKSTART.md` — grant examples and app-surface list to
  `read_tail,read_diff`.
- `docs/corral/P4-conformance.md` — endpoints table (step-up removed),
  wire shapes to the closed set, scenarios renumbered; superseded
  mutating scenarios listed as removed.

## Gate evidence (all real runs, lock `/tmp/corral-heavy-gate.lock`)

| Gate | Command | Exit | Result |
|---|---|---|---|
| fmt | `cargo fmt --all --check` | 0 | clean |
| clippy | `cargo clippy --workspace --all-targets -- -D warnings` | 0 | no warnings |
| tests | `cargo test --workspace` | 0 | **655 passed; 0 failed; 13 ignored** |
| deny | `cargo deny --locked --workspace check` | 0 | advisories/bans/licenses/sources ok |
| audit | `cargo audit --deny warnings` | 0 | no advisories |

Focused suites (logs in `/tmp/corral-354-*.log`): `readonly_cut` 4/4,
`provenance` 20/20, `drive` 13/13, `auth` 19/19, corrald-ui +
corrald-client 234/234.

## Notes / judgment calls

- `StepUpRequired` removed from `DriveApiError` (route gone) and from
  the client error mirror — both sides of the wire moved together.
- iOS `ios/FleetNotifier` untouched (L1 is daemon-only; its wire
  contract only loses surfaces, nothing it calls changed shape).
- `docs/corral/P1–P4-brief.md`, `DECISIONS.md`, `G35-registry.md` are
  historical records and were NOT rewritten (only the normative
  conformance contract was).
- The `approval_id` format string lives on ONLY inside push history
  rendering (inline format), documented in the push module.
- Ignored tests: 13 (live-daemon conformance + live herdr), unchanged
  in count from the pre-cut baseline minus the 6 removed mutating
  scenarios, +3 re-scoped read scenarios.
