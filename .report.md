# .report.md — corral #354 queue L4: docs + demo read-only rewrite

Worktree: `/Users/jirathip/.herdr/worktrees/corral/g354-l4-docs-demo`
Branch: `g354-l4-docs-demo` (exact base `2e375204c90ffa2a18f8464e77e1b2294a0cb031`
= origin/integration after L1 #355 + #351/#324 + L2 #356 + L3 #357)
Spec: queue mandate `/Users/jirathip/.hermes/profiles/fleet-orch/run/corral-354-cut-queue.md`
L4 bullet + the L4 brief in `.brief.md`. Docs-only lane: NO code changed.

## Scope executed (docs/ + root README + demo references ONLY)

Rewrote every top-level doc that still presented the PRE-CUT surface as
current behavior, to the merged read-only reality:

- `README.md` — product description is now the read-only monitor: removed
  the "Steer the fleet" tagline, the prompt/approve/interrupt/kill/attach
  feature copy, the mutating diagram, the "Approvals that can't go wrong"
  bullet, the step-up/signed-writes security claims, and the Issues/Terminal/
  Diff references; kept honest install/usage (register → out-of-band grant →
  signed recents) and the live WASM demo link (bundled synthetic fixture).
- `docs/OPERATIONS.md` — device lifecycle + grants are out-of-band only;
  iOS FleetNotifier section rewritten to the L2 client (board, recents
  read_tail, state-change notifications with the APNs provisioning
  checkpoint, no diff page); removed the "Worktree diff read path (#232)"
  client how-to and grant-for-diff instructions; board-tabs and GET /issues
  sections now state no bundled client renders Issues after L2/L3; added a
  "Read-only API reference" table (routes verified against src at base);
  troubleshooting rows de-diffed.
- `docs/ARCHITECTURE.md` — full rewrite of the stale parts: intro now leads
  with the read-only cut + herdr RAW status vocabulary (working / idle /
  blocked / unknown, plus wire-`done` ranked/rendered with idle, #319/#320
  wording gone) + recents v1 live-tail
  + no push in egui; REMOVED the #353 stale "Side readers: src/fleet/,
  corrald fleet switch, GET /fleets" block and the "fleet-ops CLI validated
  names" / "GET /fleets view" attribution claims (resolves issue #353);
  fixed the gh-plane Issues-tab claims, the egui client description
  (Board | Settings, connection-only), the security-model write-plane
  wording, and the layout map.
- `docs/QUICKSTART.md` — rewritten end-to-end for the read-only daemon:
  register (empty grants) → out-of-band `registry.json` grant → signed
  read_tail drive; removed POST /grants admin flow, `scripts/corrald-grant.sh`
  usage, action-control bullets (prompt/interrupt/kill/attach/approve),
  Face ID step-up, Issues/CI chips board copy, done-in-order board copy.
- `docs/DEVELOPING.md` — module map aligned to the current tree (no
  src/approve, no src/auth/step_up.rs, src/push present, tests include
  readonly_cut.rs/provenance.rs/fixtures), conformance-scenario paragraph to
  the surviving arms (R1/R2/R5/R10/R11 + read-only probes; approve/step-up
  arms removed), "How to add a capability" rewritten for the closed
  signed-read set (no POST /grants, no approve seam, no fleet-level
  dispatch_worktree), state-token wording (RAW tokens; wire-done ranked/
  rendered with idle as finished).
- `docs/ios-showcase.md` — capture-route truth after the L2 client cut:
  DemoFleet exposes `-demoMode` (board) and `-corralDemoDetail` (recents)
  only; the showcase script's pre-cut issues/issue-detail entries are a
  pipeline change (reported, not edited).
- `docs/design/evidence/issue-354/` — this lane's evidence dir with
  `doc-truth-gate.sh` + `README.md` (RED/GREEN proof, inventory, privacy
  scan output).

Demo fixture + `scripts/check-demo-privacy.py` are L3-owned and were NOT
rewritten (only referenced). Docs referencing the demo (README showcase,
QUICKSTART WASM note, ios-showcase) were checked against the L3 fixture
reality (synthetic read-only board, 390×844 evidence present).

## Judgment calls (documented deviations)

- `read_diff` STILL parses and dispatches in the daemon at this exact base
  (verified: `src/drive/mod.rs` Capability::ReadDiff, `src/api/drive.rs`
  ReadDiff dispatch, herdr adapter `read_diff`, registry grant parse). The
  docs therefore say read_diff is a daemon-RETAINED read with no client UI
  (iOS Diff UI removed L2, egui wire-decode only L3) rather than claiming
  it was deleted — a removal claim would be false at this base.
- `docs/corral/P4-conformance.md` (phase-brief area, frozen pre-cut wire
  record) still enumerates removed endpoints (/step-up, admin /grants,
  /fleets) as normative and is cited by the ios/ + clients/egui READMEs as
  the wire contract. Not edited (phase history, outside the L4 doc set);
  the rewritten docs no longer point at it as the current contract.
  Recommend a follow-up to re-scope or annotate it.
- `src/main.rs` `--help` still prints a dead `USAGE: corrald fleet switch
  <name> [--pane <id>]` line (code, out of docs fence; dispatch rejects any
  such subcommand). Noted, not edited.

## Gates (real runs at this lane head; base = 2e37520)

Doc-truth gate (new, issue-354) — RED at base (24 pattern hits: README
7 hits on lines 12/16/28/30/5, OPERATIONS 2, ARCHITECTURE 7,
QUICKSTART 7, DEVELOPING 1 — full verbatim output in the evidence
README), GREEN at head:

    $ git stash push -- README.md docs/OPERATIONS.md docs/ARCHITECTURE.md \
        docs/QUICKSTART.md docs/DEVELOPING.md docs/ios-showcase.md
    Saved working directory and index state On g354-l4-docs-demo: l4-docs-red-proof
    $ bash docs/design/evidence/issue-354/doc-truth-gate.sh; echo exit=$?
    ... (matches listed in docs/design/evidence/issue-354/README.md)
    doc-truth-gate: FAIL - a rewritten doc reintroduces removed mutating/fleet surface (matches above).
    exit=1
    $ git stash pop
    On branch g354-l4-docs-demo
    ... (docs restored)
    $ bash docs/design/evidence/issue-354/doc-truth-gate.sh; echo exit=$?
    doc-truth-gate: PASS - README + docs/ carry no removed mutating-drive, grant-admin, step-up, grant-CLI, Issues/Terminal/Diff, or GET /fleets claims as current behavior.
    exit=0

Existing issue-340 gate still passes:

    $ bash docs/design/evidence/issue-340/doc-truth-gate.sh; echo exit=$?
    doc-truth-gate: PASS - docs/OPERATIONS.md names no GET /fleets route, corrald fleet subcommand, or fleet-ops-CLI-validated identity.
    exit=0

Privacy scan (L3 scanner, fixture untouched):

    $ python3 scripts/check-demo-privacy.py clients/egui/assets/demo-fixture.json
    privacy scan: 0 forbidden matches
    exit=0
    $ python3 scripts/check-demo-privacy.py --self-test clients/egui/assets/demo-fixture.json
    privacy self-test: all identity mutations rejected, positive controls accepted
    exit=0

Docs-only smoke: `git diff --check` clean; every relative markdown
link/image in the six changed docs resolves (checked with a local python
link resolver; no build references to removed files).

NOT run (explicit non-claims): no cargo/xcode tests or builds — docs-only
lane, brief says workspace tests need not rerun; no live-daemon verification
of any quoted curl output (register/grants-read/drive examples keep their
pre-existing "verified" provenance from earlier lanes; the two spots I
touched either quote the daemon's real note string verbatim or elide with
`...`); no hosted CI run (orchestrator dispatches); no Pages/TestFlight
verification of the demo links.

## Files changed (docs-only proof)

    git diff --name-only 2e375204c90ffa2a18f8464e77e1b2294a0cb031..HEAD
    README.md
    docs/ARCHITECTURE.md
    docs/DEVELOPING.md
    docs/OPERATIONS.md
    docs/QUICKSTART.md
    docs/design/evidence/issue-354/README.md
    docs/design/evidence/issue-354/doc-truth-gate.sh
    docs/ios-showcase.md
    .report.md

No src/, crates/, ios/, clients/egui, .github/, scripts/, or asset changes.
`.brief.md` stays untracked by design.

## Review fix R1 (PR #359 finding at 9300844)

Reviewer finding (verified at source): OPERATIONS.md's "Read-only since
#354" bullet grouped the worktree-diff page with the daemon-side removals
("removed from the daemon and both clients") — WRONG: L1 removed the
mutating capabilities and the CLIENT diff UI; the daemon RETAINS the
signed `read_diff` read path at base 2e37520 (src/api/drive.rs:4 "signed
read_tail and read_diff are the only...", drive.rs:293 read_diff
request/response + Adapter::read_diff dispatch; src/drive/mod.rs ReadDiff
capability + bounds).

Fix (this commit): reworded OPERATIONS.md's FleetNotifier bullet to state
the worktree-diff page was removed from BOTH CLIENTS (iOS L2, egui L3)
while the daemon RETAINS the signed `read_diff` read path (bounded
changed-files/diff page; no client dispatches it). Rescanned ALL rewritten
docs (README, OPERATIONS, ARCHITECTURE, QUICKSTART, DEVELOPING,
ios-showcase + this evidence dir + .report.md) for any other
"read_diff removed from the daemon" claim: every remaining mention now
reads "daemon-retained"/"retained daemon-side"/"RETAINS" (verified by the
rg output below); no other equivalent claim existed.

Re-ran gates at the fix head:

    $ bash docs/design/evidence/issue-340/doc-truth-gate.sh; echo exit=$?
    doc-truth-gate: PASS - docs/OPERATIONS.md names no GET /fleets route, corrald fleet subcommand, or fleet-ops-CLI-validated identity.
    exit=0
    $ bash docs/design/evidence/issue-354/doc-truth-gate.sh; echo exit=$?
    doc-truth-gate: PASS - README + docs/ carry no removed mutating-drive, grant-admin, step-up, grant-CLI, Issues/Terminal/Diff, or GET /fleets claims as current behavior.
    exit=0
    $ git diff --check   # clean
    $ rg -n 'GET /fleets|corrald fleet|fleet-ops CLI validated|registered fleet name|prompt, interrupt, approve|signed drive: prompt|Steer the fleet|Approve / Deny|Face ID|step-up token|grant editor|device-grant editor|Issues tab renders|corrald-grant.sh --' README.md docs/OPERATIONS.md docs/ARCHITECTURE.md docs/QUICKSTART.md docs/DEVELOPING.md docs/ios-showcase.md
    (no hits — exit 1)
    $ rg -n -i 'worktree-diff page|read_diff' README.md docs/OPERATIONS.md docs/ARCHITECTURE.md docs/QUICKSTART.md docs/DEVELOPING.md docs/ios-showcase.md | rg -i 'daemon|removed|retain'
    (every hit says daemon-retained / RETAINS / removed from BOTH CLIENTS)

## Review fix R2 (PR #359 finding at b01d8d0)

Reviewer finding (verified against the repo's own evidence): 4 rewritten-doc
sites claimed the herdr 0.8.2 wire never emits `done` (README, ARCHITECTURE,
OPERATIONS, DEVELOPING; a 5th same-class site in QUICKSTART said 0.8.2
delivers no `done`) — contradicted by the checked-in #324 live probe
(docs/design/evidence/issue-324/live-herdr-0.8.2-probe.md:8 records
agent_status `done` from the live 0.8.2 socket) and by code at base
(src/core/model.rs:52 `"done" => Self::Done` parses; contracts/
state-tokens.json still carries `done` rank 2; iOS StateStyle renders a
`done` chip ranked with idle).

Fix (this commit): reworded all 5 sites to the truthful statement — the
herdr 0.8.2 wire CAN carry `done` (recorded in the #324 live probe); the
read-only board treats `done` as finished, ranked and rendered WITH `idle`
(idle-equivalent), so finished panes never read as active/working;
#319/#320's invented grouped wording (Working/Supervising/Finished) stays
banned. Updated the summary lines in this report + the issue-354 evidence
README to the same wording, and the v2-attention-order inventory row
(blocked → working → idle(=done) → unknown).

Rescan of the six rewritten product docs (README.md, docs/OPERATIONS.md,
docs/ARCHITECTURE.md, docs/QUICKSTART.md, docs/DEVELOPING.md,
docs/ios-showcase.md — the same file scope as the doc-truth gates; this
report + the evidence README document the R2 finding and therefore quote
the removed phrasing) for claims that 0.8.2 never emits `done`, or that
finished panes "fall back to idle":

    $ rg -n -i 'has no .?done|no .?done|no `done`|falls? back to idle|fall.?s? to idle|finished panes fall|herdr 0\.8\.2 has no|0\.8\.2 delivers no|never emits .?done' README.md docs/OPERATIONS.md docs/ARCHITECTURE.md docs/QUICKSTART.md docs/DEVELOPING.md docs/ios-showcase.md
    (no hits — exit 1)

Gates re-run at the fix head:

    $ bash docs/design/evidence/issue-340/doc-truth-gate.sh; echo exit=$?
    doc-truth-gate: PASS - docs/OPERATIONS.md names no GET /fleets route, corrald fleet subcommand, or fleet-ops-CLI-validated identity.
    exit=0
    $ bash docs/design/evidence/issue-354/doc-truth-gate.sh; echo exit=$?
    doc-truth-gate: PASS - README + docs/ carry no removed mutating-drive, grant-admin, step-up, grant-CLI, Issues/Terminal/Diff, or GET /fleets claims as current behavior.
    exit=0
    $ git diff --check   # clean

## Out-of-fence observations (not edited)

1. `docs/corral/P4-conformance.md` — pre-cut normative wire record still
   listing /step-up, admin /grants, /fleets; referenced by client READMEs.
2. `scripts/ios-showcase.py` — capture ALLOWLIST still names the pre-cut
   issues/issue-detail DemoFleet routes (pipeline code, TestFlight-gated;
   must be trimmed before the next capture).
3. `src/main.rs` help text — dead `corrald fleet switch` USAGE line.
4. Live Pages demo content is published from `main` only; the #354 cut
   queue holds main promotion, so the deployed WASM/iOS showcase reflects
   an older revision until the cut outcome is approved.
