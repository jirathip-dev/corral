# #288 git-plane serving implementation report

## Checkout

- Worktree: `/Users/jirathip/.herdr/worktrees/corral/g288-git-plane-serving`
- Branch: `g288/git-plane-serving`
- Base/head before this change: `bcaf54f`
- Scope: git-plane scheduling/probing and regression coverage only. No `clients/egui/**` or `ios/**` files changed.

## Changed

- Enforced the 200 ms per-worktree probe budget at both debounced watcher probes and safety-net sweep probes.
- Added an explicit over-budget result. The probe is discarded as a unit, the worktree is marked stale, cached complete Git facts remain published, and a later round can retry it.
- Added an observable warning with `skipped=true`, elapsed time, worktree, and budget fields.
- Kept one pending task per worktree and bounded queued-event follow-up work to one coalesced rerun, preventing an unbounded backlog.
- Preserved the shared four-command Git semaphore and serialized topology rescans; test-only delay applies to `git status` only.
- Added regression coverage for over-budget retry, coalescing, and high-volume Git load with contiguous SSE revisions and bounded Git concurrency.
- Optional `degraded: git_plane_backlog` snapshot flag was not added; the existing additive stale/deferred behavior was sufficient for this lane.

## Changed files

- `src/adapters/git_plane.rs`
- `.report.md`

## Gates

All commands were run in this worktree after the final source changes.

### `cargo fmt --all --check`

Exit 0; no output.

### `cargo clippy --workspace --all-targets -- -D warnings`

Exit 0:

```text
Checking corrald v0.1.0
Checking corrald-ui v0.1.0
Checking corrald-client v0.1.0
Finished `dev` profile [unoptimized + debuginfo]
```

### `cargo test --workspace`

Exit 0. All non-ignored workspace tests passed. Final aggregate results included:

```text
corrald lib: 297 passed; 0 failed
corrald integration suites: all passed (live tests ignored)
corrald-client: 14 passed; 0 failed
corrald-ui: 172 unit tests passed; 1 main test passed; integration suites passed
Doc-tests: passed (ignored doctests remain ignored)
```

### Timing regression

Command:

```text
cargo test --lib adapters::git_plane::tests::high_volume_git_work_is_bounded_and_preserves_sse_revisions -- --nocapture
```

Exit 0. Real output:

```text
git wave first complete snapshot latency_ms=682
test adapters::git_plane::tests::high_volume_git_work_is_bounded_and_preserves_sse_revisions ... ok
...
test result: ok. 1 passed; 0 failed
```

The test uses 9 worktrees (8 linked worktrees plus the main checkout), delayed Git status probes, the shared four-command budget, and a live Store SSE subscriber. It asserts Git concurrency does not exceed four and that SSE revisions remain contiguous while the wave drains. The measured first complete snapshot latency was 682 ms, below the one-second acceptance bound for this simulated wave.

Additional focused commands also passed:

```text
cargo test --lib adapters::git_plane::tests::queued_probe_events_coalesce_to_one_follow_up -- --nocapture
# 1 passed; 0 failed; finished in 1.80s

cargo test --lib adapters::git_plane::tests::over_budget_probe_is_skipped_and_retried_later -- --nocapture
# 1 passed; 0 failed; finished in 0.54s
```

### `python3 ios/check-release-demo.py --self-test`

Exit 0:

```text
release-demo check: PASS (Debug source preserved; Release boundary verified)
```

No iOS source or release manifest digest changed, so no same-commit digest re-pin was needed.

### Diff hygiene

```text
git diff --check
# exit 0
```

## Not verified

- No pre-fix timing run was captured; the regression test is a post-fix measurement.
- The timing regression exercises the live Store/SSE serving path but does not start a real TCP daemon or issue repeated `/healthz` and `/snapshot` HTTP requests.
- The ignored live acceptance tests requiring the real Herdr repository/daemon were not run.
- No GitHub mutations, PR creation, or push were performed.

## REVIEW FIX CYCLE 1 status

This section records the reviewer-requested follow-up state at the interrupted
handoff. The review-fix implementation was not completed in this commit:

- RED-on-base evidence captured: detached `/tmp/g288-base` at `bcaf54f` was
  removed after running the timing test; it failed with
  `SSE delta remains live during git load: Elapsed(())`.
- The post-fix timing run printed `git wave first complete snapshot latency_ms=682`
  and passed, but the test did not yet contain the required `<1000ms` assertion.
- The production scheduler retry test and additive snapshot backlog wiring were
  not completed in the interrupted cycle.
- No existing load or timeout weakening was retained by this follow-up; the
  source tree was restored to commit `7325c9f` before this report-only commit.
