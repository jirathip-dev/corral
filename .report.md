# .report.md — corral #371: iOS board v2 — status sections w/ colored repo subgroups + working-motion chips

Worktree: `/Users/jirathip/.herdr/worktrees/corral/g371-ios-board-v2`
Branch: `g371-ios-board-v2` (exact base `ed33eea690548b2a71266004fa900711d29fc796` =
integration AFTER the #372 theming foundation merged)
Issue: https://github.com/jirathip-dev/corral/issues/371 (FULL LOCKED SPEC +
binding comments incl. DESIGN APPROVED 09-03: mauve UI accent, teal reserved
for working, fnv1a32%8 repo hues ported as a FUNCTION, Other=gray, Latte
label-ink mixes, Reduce Motion = static teal dot)
Report lineage: this file REPLACES the inherited #372 report (zero #371
content). Fences respected: iOS board UI + tests ONLY — no egui, no daemon
src/, no crates/, no root docs/; recents/rail (#373) untouched.

## What landed (delta over the #372 theming base)

1. `ios/FleetNotifier/UI/BoardModel.swift` — the status projection gained
   always-open REPO SUBGROUPS inside EVERY section (uniform, incl. Blocked):
   named repos alphabetical first, then the synthetic `Other` subgroup
   (repo == nil / "" / literal "Other") LAST. `RepoSubgroup` = repo + agents
   (+ count header); `StatusSection` now holds subgroups and its `header`
   shows the TOTAL across them (rescopes with a repo filter). Bucketing is a
   stable partition of the section's ts-desc/id order; Done still renders
   only when herdr reports it, positioned after Idle before Unknown.
2. `ios/FleetNotifier/UI/FleetViews.swift` — board-v2 renderer:
   - `boardSections` renders per status section: subgroup header bands
     (2 pt hue rail + hue dot + repo label ink + count) then agent rows —
     no DisclosureGroup / isExpanded / chevron anywhere (always open).
   - `AgentRow` state chip is now a TINTED chip (fill = mix(state 17 %,
     base), border = mix(state 34 %, base)) resolved through the ONE shared
     `ThemeStore.stateToken(for:)` mapping `stateColor(for:)` also uses —
     no parallel switch (the #372 R1 rule).
   - `WorkingMotion` (pure math: 1.2 s cycle, 160 ms stagger, 3 × 4 pt
     squares, opacity 0.34→1 / scale 0.78→1 peaking at 42 %) +
     `WorkingMotionGlyph` inside the working chip; Reduce Motion swaps the
     squares for the STATIC teal dot (squares removed, never a spinner —
     no ProgressView anywhere on the board).
   - `RepoLabelChip` on every row's workspace line (hue dot + repo name
     chip, `Other` gray chip for orphans); subgroup bands + row chips +
     filter chips all resolve hues from the SAME `theme.repoHue(for:among:)`
     fnv1a32%8 function over the filter-chip repo set.
   - DEBUG filter-evidence driver (`runFilterSequence`) converted from
     `try? Task.sleep` to the #372 `themePause` cancellation-abort pacing —
     the old form raced on this build (all four markers landed within
     0.2 s; observed + fixed so the recorded filter phases are real).
3. `ios/FleetNotifier/UI/AppTheme.swift` — the #371 chip surface tokens:
   repo label ink = mix(hue X %, text) per flavor (Latte 29 %, Frappé 76 %,
   Macchiato/Mocha 100 %; `Other` ink = subtext1 verbatim — design lock),
   `repoBand` = hue 9 % over mantle, `repoChipFill/Border` = hue 15 %/38 %
   over base, `stateChipFill/Border` = state 17 %/34 % over base. Mix
   quantization is `.toNearestOrEven` (half-even) — matching the approved
   prototype's CSS color-mix render, pixel-sampled (Mocha blue-15 %-over-
   base = #2e344d, NOT #2e354d; see RED note below).
4. `ios/FleetNotifier/Demo/DemoFleet.swift` — reseeded for the board-v2
   evidence shape: 8 agents = blocked demo-garden + demo-orbit, working
   demo-atlas + demo-orbit + orphan (→ Other), idle demo-ledger, one done
   demo-garden row, unknown demo-atlas. Doc comment states what the first
   844 pt viewport shows and that lower sections sit below the fold
   (board scrolls; simctl cannot inject scroll).
5. `ios/FleetNotifier/App/FleetNotifierApp.swift` — DEBUG-only
   `-corralDemoReduceMotion` launch argument forces the Reduce-Motion
   provider (simctl cannot toggle the system setting) for the static-dot
   evidence frame.
6. Tests: `BoardModelBoardV2Tests` (was BoardModelReadOnlyTests — subgroup
   alpha + Other-last + totals + blocked-uniformity + filter-keeps-sections
   + orphan folds), `WorkingMotionTests` (staggered shared curve,
   out-of-phase-at-rest, cycle bounds/peak), `BoardV2WiringTests` (source
   pins: glyph dot/squares + shared state color, tinted chip via the single
   mapping, subgroup/row hue consumption, no DisclosureGroup/chevron/
   ProgressView), `BoardV2ChipMixTests` in ThemeTests.swift (per-flavor ink
   + state/repo surface hex vectors), DemoSeedTests/RepoFilterChipProjec-
   tionTests/ReadOnlySurfaceWiringTests updated to the subgroup shapes.
7. `ios/check-release-demo.py` — BOTH digests re-pinned with dated #371
   comments (Release source set changed: AppTheme/BoardModel/FleetViews/
   DemoFleet/FleetNotifierApp; test source changed: FleetNotifierTests).

## Gates (real, serialized under /tmp/corral-heavy-gate.lock via flock)

- Focused board-v2 tests (BoardModelBoardV2Tests, WorkingMotionTests,
  RepoFilterChipProjectionTests, DemoSeedTests, BoardV2WiringTests,
  ReadOnlySurfaceWiringTests, BoardV2ChipMixTests): PASS — 39 executed /
  0 failures — /tmp/g371-focused2.log (FOCUSED2_EXIT=0).
- FULL iOS suite: PASS — "Executed 167 tests, with 0 failures
  (0 unexpected)" — /tmp/g371-final-full.log (FINAL_FULL_EXIT=0).
- App build (Debug, iOS Simulator, clean derived data): PASS — 0 errors —
  /tmp/g371-final-build.log (FINAL_BUILD_EXIT=0).
- Release build + privacy checks: PASS —
  `python3 ios/check-release-demo.py --self-test` AND `--binary
  /tmp/fn-rel-final/Build/Products/Release-iphonesimulator/.../FleetNotifier`
  both "release-demo check: PASS (Debug source preserved; Release boundary
  verified)" — /tmp/g371-final-rel{1,2}.log (all exits 0). Digests:
  APPROVED_RELEASE_SOURCE_DIGEST =
  414b4b8760e712449864d602c113cc030478710d1f1b8b7b20310ab18f5c848f,
  APPROVED_TEST_SOURCE_DIGEST =
  c39c0714436003d47cf7813802954a294f996b32e05fbf684672dfa1bdc54ac4.
- anti-slop (advisory): 24 pre-existing violations across 22 files — the
  SAME baseline as #372; ZERO flags on any lane-added line (scripted
  overlap check vs the ed33eea diff) — /tmp/g371-final-antislop.log.

## RED/GREEN discrimination proof (new board-v2 tests)

Natural RED first: the FIRST focused run failed 1 test —
`BoardV2ChipMixTests.testRepoChipSurfacesMatchTheDesignTints` — actual
`#2e354d` vs expected `#2e344d` (Mocha blue 15 %-over-base, g channel). Root cause: Swift `.rounded()` (half away from zero) vs the
approved prototype's browser color-mix quantization. Verified the design
truth by PIXEL-SAMPLING the approved screenshot
(~/design-output/corral/371-372-board-theming/screenshots/mocha-board-full-
390x844.png): 836 px of #2e344d vs 1 stray px of #2e354d. Fix:
`.rounded(.toNearestOrEven)` + quantization-lock comment in mixedHex.
GREEN after fix: BoardV2ChipMixTests 4/4 (log /tmp/g371-chipmix.log).

Scripted mutation probes (each on a byte-identical scratch copy
/tmp/g371-scratch, git-committed, mutated, rebuilt, focused class run —
logs /tmp/g371-mut{1,2,3,4a,4b}-red.log):

- M1 subgroup order DESC (BoardModel `sorted().reversed()`):
  BoardModelBoardV2Tests → 11 executed, 5 failures (alpha + Other-last
  assertions) — RED.
- M2 motion stagger flattened to 0.0 (FleetViews WorkingMotion):
  WorkingMotionTests → 3 executed, 1 failure (out-of-phase-at-rest) — RED.
- M3 Done dropped from the locked section order (BoardModel):
  BoardModelBoardV2Tests → 9 executed, 6 failures (done section + demo
  order tests; the suite's Array-index crash on the missing section also
  SIGTRAP'd the test host — crash report FleetNotifier-2026-09-03-222220
  .ips) — RED.
- M4 shared state mapping working: .teal → .red (AppTheme stateToken —
  the #372 R1 probe replay): BoardV2ChipMixTests → 4 executed, 4 failures;
  StateStyleTests → 4 executed, 8 failures (both accessors through the
  single mapping) — RED.

GREEN at the real head on the SAME focused classes: 39/39 (g371-focused2)
+ full 167/167 (g371-final-full). Scratch removed after the probes; real
worktree untouched by the probes (byte-identical pre/post checks).

## Evidence (390x844 synthetic — no live daemon, no physical device)

Host sim = iPhone 16 (393x852 pt = 1179x2556 @3x; NO 390x844 device exists
on this host — honest note, same as #372). Raw shots downscaled with
`sips -z 844 390` into /tmp/g371-evidence/390x844/ (0.18 % aspect
distortion). Capture: DEBUG builds of the COMMITTED head source; timed
frames for the plain demo, marker-polled frames for the recorded drivers
(simctl cannot inject taps/scroll — recorded drivers are the established
synthetic stand-in).

- phase-1-board-mocha-all-sections-subgroups-chips.png — Mocha board v2:
  mauve "All 8" chip selected; repo chips (demo-atlas/demo-garden/... hue
  dots + counts); blocked (2) → demo-garden + demo-orbit subgroup bands;
  working (3) → demo-atlas + demo-orbit bands; working rows carry the
  three-square breathing chip in the teal tinted state chip (vision-
  inspected: "three small squares").
- phase-2-board-reduce-motion-static-dot.png — same board launched with
  `-demoMode -corralDemoReduceMotion`: working chips show the STATIC TEAL
  DOT (single circle), no squares anywhere (vision-inspected).
- phase-1-all / phase-2-filtered-atlas / phase-3-back-to-all /
  phase-4-done.png — `-corralDemoFilterEvidence` sequence:
  phase-2-filtered-atlas shows the mauve-selected demo-atlas chip with the
  status sections KEPT (working (1) + unknown (1)), each holding ONLY the
  demo-atlas subgroup — the chips-filter-keeps-sections acceptance —
  content complete in-frame (vision-inspected).
- Honest scope notes: the idle/done/unknown sections + the gray Other
  subgroup sit BELOW the 844 pt fold on the All board (the list scrolls;
  simctl cannot inject scroll) — they are covered by the unit tests +
  source-wiring pins (BoardModelBoardV2Tests, DemoSeedTests,
  BoardV2WiringTests) and are NOT claimed as viewport evidence; the
  DemoFleet shape comment states this. Reduce-Motion behavior beyond the
  static dot is unit- + source-wiring-tested (the sim cannot toggle the
  system setting; the DEBUG launch override stands in).

## Explicit non-claims

- No live daemon, no physical device, no pull-gesture evidence, no
  TestFlight, no App Review, no PR opened / issue closed / main promotion
  (orchestrator-owned). 390x844 frames are DOWNSCALES of the host's iPhone
  16 sim. Latte/Frappé/Macchiato board frames were not separately captured
  (Mocha is the briefed minimum; per-flavor token correctness is pinned by
  BoardV2ChipMixTests + the #372 theme suites). The animation's live
  motion is verified by its pure-math tests + wiring pins only — a still
  frame cannot prove motion (the squares are visibly staggered in every
  frame, consistent with the locked math).
