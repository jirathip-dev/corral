# Corral #376 — Remove egui entirely: iOS-only product + static demo snapshot (Guy decision 09-03, REOPENED)

Branch: g376-remove-egui · Base (exact): b66657340096393206ae9a90150a8d8fa909f61d (integration AFTER #373) · Worktree: /Users/jirathip/.herdr/worktrees/corral/g376-remove-egui · Lane date: 2026-09-04.

This report REPLACES the inherited #373 report (base-squash artifact). #376 = Rust/CI/docs deletion lane; daemon + iOS code untouched.

## What changed

DELETED (tracked files, 49 files total incl. clients/egui):
- clients/egui/ entirely — source, tests/conformance (lib/conformance/identity_recovery/live), evidence screenshots, web/, assets/demo-fixture.json, README (26 files).
- .github/workflows/web-pages.yml — the "Web demo Pages" wasm publish workflow (wasm-pack → gh-pages root demo).
- scripts/install-corral-ui.sh (desktop Corral.app installer), scripts/test-icon-packaging.sh (its test), scripts/test-design-gate-egui-integration.sh (desktop evidence verifier), tools/icon/check-desktop-entry.py (desktop-entry validator).
- The two board demo PNGs were NOT deleted: moved byte-identical to docs/demo/ (sha 5724bcbd…/3425a45d…).

WORKSPACE:
- Cargo.toml: members/default-members = ["crates/corrald-client"] / [".", "crates/corrald-client"] (corrald-ui removed; comment updated).
- Cargo.lock: regenerated — 584 → 356 package records; removed = corrald-ui + 204 egui/wasm/wgpu transitive packages (eframe, egui*, epaint*, emath, wgpu*, winit, naga, wayland/x11 stack, wasm-bindgen family, image/png/tiff…); ADDED = none; CHANGED = ONLY wnaf 0.14.0 → 0.14.1 (upstream yank 2026-09-03; `cargo update -p wnaf --precise 0.14.1`). `git diff -- Cargo.lock` shows only those removals + the wnaf bump (29 insertions / 2553 deletions, no unrelated pins).
- deny.toml: dropped the two now-unmatched license exceptions — `option-ext` (MPL-2.0) and `epaint_default_fonts` (OFL-1.1/Ubuntu-font-1.0) — both crates left the graph with egui; cargo-deny fails hard on unmatched exceptions (`unused-license-exception = "deny"`). webpki-root-certs exception kept (still in lock via rustls-platform-verifier).

CI/WORKFLOWS:
- rust.yml: egui/wgpu apt deps (wayland/x11/GL) removed — "Install Linux system deps (keyring)" keeps only libkeyutils-dev; stale egui comments updated.
- release.yml: corrald-ui build/test/copy/tar/notes steps removed; bundle = corrald + scripts + docs (no desktop icons); release notes now state the iOS-only product and that FleetNotifier ships via TestFlight, never in the macOS bundle.
- ios-testflight.yml / ios.yml: untouched (iOS showcase Pages publishing kept).

SCRIPTS (desktop-UI lifecycle → daemon-only, all `bash -n` clean):
- scripts/install-corral.sh: header/env/usage, self-test, uninstall, required-bundle list, messages — Corral.app/corrald-ui/install-corral-ui.sh payload machinery removed.
- scripts/setup-corrald.sh: header, --from-release detection, install_desktop_client + call, "Next:" hints removed.
- scripts/update-corral.sh: header safety rule, UI mtime tracking, reinstall_ui(), relaunch block removed.
- scripts/test-daemon-launchd-env.sh: desktop-installer/corrald-ui stubs removed (test passes, see gates).
- scripts/design-gate-content-identity.py: rewritten — single current-product scope (iOS FleetNotifier sources/tests/Xcode project/release wiring + capture generator); #205/#206 egui file lists, the eframe/wgpu Cargo.lock renderer fingerprint and --issue arg removed (renderer records no longer exist in the lock; 205 scope already referenced a deleted prototype at base).
- scripts/design-gate-evidence.sh: implementation_identity() single scope call; conformance "Implementation identity scope" prose updated (both ios/egui branches collapsed).
- scripts/test-design-gate-evidence.sh: lockfile-fingerprint battery replaced with a current-scope absence battery (scope non-empty, includes iOS sources, excludes clients/egui and evidence dirs).
- tools/icon/check-assets.py: deleted-source pins/checks/mutations removed (clients/egui/src/main.rs, install-corral-ui.sh, test-icon-packaging.sh, check-desktop-entry.py, setup-corrald.sh syntax pin, corrald-ui --require-build + egui/packager/unembedded-build self-tests). One NON-removal hunk: refreshed the ios project.pbxproj pin 4e51ae80… (was bd5d8ab9…) — the pin was already stale at base (ios/ untouched by #376, `git diff HEAD -- ios/` empty; #371-#373 xcodegen additions never refreshed it). Icon checker + self-tests pass with the refresh.

TESTS (forced minimal fix, documented loudly):
- tests/http.rs::no_fleets_json_reference_anywhere_in_src walked root/clients which no longer exists → read_dir NotFound panic. Probe now scans only existing dirs (src, crates, clients-if-present). This is the only daemon-side change; src/ itself untouched.

DOCS (README + 5 live guides rewritten; historical archives docs/design/evidence/*, docs/corral/*, docs/design/* NOT touched):
- README.md — full rewrite per the issue's SCOPE ADD (09-03): short/plain, iOS-only product line + one-line architecture, DAEMON SETUP (cargo build --release -p corrald; launchd com.corral.corrald port 8474 via scripts/setup-corrald.sh OR one manual command; verify /healthz → ok; herdr socket ~/.config/herdr/herdr.sock) + CONNECT iOS (TestFlight install, Settings gear → Host, register/pair with daemon token, enable Notifications with APNs provisioning note, out-of-band read_tail grants, Tailscale Serve note). Demo section: iOS showcase Pages + docs/demo static snapshot. Docs index + MIT/Apache license kept.
- docs/QUICKSTART.md — desktop UI section removed (iOS renumbered 8), auto-register note + UI config dir removed.
- docs/OPERATIONS.md — setup section rewritten daemon-only; install-corral-ui.sh/Corral.app/desktop-client paragraphs, Keychain how-to (corrald-ui), auto-register sentence, UI troubleshooting rows removed; history sentences rewritten without the removed surface (read_diff, GET /issues, Fleet-ops section).
- docs/ARCHITECTURE.md — intro client list, Issues-UI history, read_tail/read_diff history, Clients bullet (clients/egui removed; FleetNotifier = only product client), Layout tree updated.
- docs/DEVELOPING.md — workspace layout + module map, icon/packaging section trimmed (retained icons disclosed), quality-gates list (corrald-ui live test line removed), design-gate evidence section (desktop harness removal noted; ios capture path kept), coverage scope text, supply-chain section (two members; 356 lock records vs 584; epaint/option-ext exception text dropped), state-token contract (iOS only), capability recipe clients list.
- docs/ios-showcase.md — "leaves the egui/WASM root alone" → "leaves the rest of the Pages root alone".

STATIC DEMO (scope item 4):
- docs/demo/index.html (new, 80 lines, no JS) + docs/demo/board-demo-390x844.png + board-demo-desktop-1280x800.png — byte-identical copies of the #354 L3 evidence captures (the brief's "existing 390x844/desktop board PNGs"). Verified: served over a loopback HTTP server → /index.html 200 text/html, both PNGs 200 image/png; HTML references both images. Privacy: scripts/check-demo-privacy.py over the three files (tesseract OCR available) → 0 forbidden matches, exit 0; PNG pixels vision-reviewed — all synthetic (demo-alpha/bravo/charlie/delta repos, p0x:role panes), no private identifiers. In-pixel branch labels (g354-l3-egui-cut, g304-wasm-mobile) are fictional fixture content from the frozen captures, not doc claims.

EVIDENCE (issue-354 pattern):
- docs/design/evidence/issue-376/doc-truth-gate.sh — scans README + docs/{OPERATIONS,ARCHITECTURE,QUICKSTART,DEVELOPING,ios-showcase}.md for removed-surface tokens; RED at base (exit 1, matches every removed claim), GREEN at head (exit 0, zero matches).
- docs/design/evidence/issue-376/README.md — inventory, RED/GREEN transcripts, removal probes, gate tables, disclosures.

## Gates (real runs, serialized under /tmp/corral-heavy-gate.lock via flock; logs below)

Rust workspace (worktree HEAD b6665734 + this diff; wnaf 0.14.1 in lock):
| Gate | Command | Result |
|---|---|---|
| fmt | cargo fmt --check | exit 0 |
| build | cargo build --release | exit 0 — Finished release profile [optimized] in 2m 40s |
| test | cargo test --workspace | exit 0 — all suites green: 5+18+19+12+3+12+19+16+13+13+2+5+3+12+14+5 = 171 tests, 10 ignored |
| clippy | cargo clippy --all-targets -- -D warnings | exit 0 |
| deny | cargo deny --locked --workspace check | exit 0 — advisories ok, bans ok, licenses ok, sources ok (wnaf 0.14.1 not yanked) |
| audit | cargo audit --deny warnings | exit 0 — 356 crate dependencies scanned, 0 findings |

Logs: /tmp/376-cargo-build.log, /tmp/376-cargo-test2.log, /tmp/376-cargo-gates.log (clippy/deny/audit), /tmp/376-ios-gates2.log (iOS battery), /tmp/376-design-gate-test.log (design-gate hermetic suite).

iOS verification (read-only on ios/; committed FleetNotifier.xcodeproj used — no xcodegen regen, see disclosures):
- Focused: xcodebuild … -only-testing:FleetNotifierTests/StateStyleTests -only-testing:FleetNotifierTests/ThemeTests test → ** TEST SUCCEEDED ** (4 tests, 0 failures), exit 0.
- Full: xcodebuild … test → ** TEST SUCCEEDED ** — 177 tests, 0 failures (FleetNotifierTests.xctest), exit 0.
- Release app build: xcodebuild -configuration Release -sdk iphonesimulator … build → ** BUILD SUCCEEDED **, exit 0.
- check-release-demo.py --self-test → "release-demo check: PASS (Debug source preserved; Release boundary verified)", exit 0.
- check-release-demo.py --binary <Release app binary> → PASS (inspected), exit 0.
- `git diff HEAD -- ios/` = empty on this branch.

Doc-truth greps (the gate above + these commands, all zero-hit on README + the five docs):
- grep -rniE 'egui|corrald-ui|clients/egui|eframe|wgpu|epaint|emath' README.md docs/{OPERATIONS,ARCHITECTURE,QUICKSTART,DEVELOPING,ios-showcase}.md → no matches.
- grep -niE 'wasm-pack|wasm demo|WASM demo|wasm build|WASM build|web demo|Web demo|live web demo|\.wasm' same files → no matches (one deliberate technical exception: DEVELOPING calls the remaining rustls-platform-verifier dependency "wasm32-only" — a dependency target triple, not a Corral web product; disclosed in the evidence README).

Removal probes (fail if the client returns):
- test -d clients/egui → absent (exit 1); git ls-files clients/egui → 0 files; git ls-files | grep -cE 'corrald-ui|install-corral-ui' → 0; grep -c corrald-ui Cargo.toml .github/workflows/*.yml → 0 in every file; cargo metadata --no-deps | grep -c 'clients/egui' → 0.

Static demo: see above (loopback HTTP 200s; privacy scan 0 matches).

Design-gate hermetic suite (test-design-gate-evidence.sh, kept local-only developer check): ran under the lock → "OK: design-gate evidence validation, complete-PNG contract, bounded cleanup, trust boundary, capture seams, and failure paths", DG_EXIT=0 (Real-Chrome structural regression self-skips on this host — no Chrome installed; log /tmp/376-design-gate-test.log).

Shell/icon checks: scripts/test-daemon-launchd-env.sh OK (exit 0); test-corral-log-rotate.sh OK; test-update-corral-path.sh OK; test-update-corral-deploy.sh OK (all 6 sandbox scenarios); tools/icon/check-assets.py + --self-test OK (15 negative cases); python py_compile of edited tools clean.

## Non-claims / could not verify

- Hosted CI and the fresh exact-head review and the integration merge are the orchestrator's (hosted CI runs after the PR opens; this lane pushed the branch only).
- cargo llvm-cov coverage gate (85/82 + 40/35) NOT run locally — the brief's gates are build/clippy/test/deny; coverage runs in hosted rust.yml (rust.yml scope unchanged by this lane).
- cargo audit advisories reflect the local advisory DB at run time.
- The egui evidence archives (docs/design/evidence/issue-206/207/209/…, docs/corral/*, docs/design/* prototypes) are intentionally untouched historical records; their conformance manifests pin pre-removal script hashes and are not re-verifiable on the new tree.
- gh-pages root still serves the last deployed wasm demo until someone removes/republishes it — this lane makes no GitHub mutation beyond the branch push (out-of-band follow-up; web-pages.yml is gone so nothing refreshes it).
- Residual kept mentions (by design, disclosed in evidence README): dormant desktop-surface code paths/comments in scripts/design-gate-evidence.sh + scripts/test-design-gate-evidence.sh (mock-tested capture tool); tools/icon/from-user-png.py still regenerates the retained icon outputs (corral-icon-256/macos kept as checked-in assets with no consumer — docs mark them "kept from pre-#376 desktop packaging"); scripts/check-demo-privacy.py kept as the generic privacy scanner; deny.toml "wasm32-only" comment; iOS source comments/fixtures mentioning the removed client (StateStyle.swift "egui mirror" comments from #371/#372, FleetNotifierTests branch-name fixtures) — iOS files are outside this lane's fence (read-only).

## Disclosures (loud)

1. tests/http.rs probe fix (above) — the only daemon-side change; forced by the deletion.
2. tools/icon/check-assets.py project.pbxproj pin refresh — pre-existing staleness at base (ios/ diff empty; #371-#373 project additions never refreshed the pin); refreshed so the icon gate passes.
3. Host xcodegen drift (pre-existing, NOT this lane): xcodegen 2.45.4 on this host regenerates ios/FleetNotifier.xcodeproj/project.pbxproj differently from the base-committed file (66 lines: PBXGroup order + resource IDs). ios/ was restored byte-identical after verification (sha 4e51ae80… == base). iOS gates therefore ran against the committed project; the drift check inside ios.yml would fail on this host regardless of #376 and belongs to the iOS toolchain.
4. The iOS test runs executed against the committed project and completed green (177 tests) — nothing iOS broke.
