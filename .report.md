# Corral #402 — explicit GitHub release repository (no-checkout gh calls)

Branch: fix-402-release-cli-repo
Worktree: /Users/jirathip/.herdr/worktrees/corral/issue-402-release-cli-repo
Exact base: a06ad5a7200494f560f83d7c0b7ff1d1e23a6aba (origin/integration)
Implementation head: e2c28a87312a1c2622dee2d80bfdbc9059757498
Lane date: 2026-09-05

This report replaces the inherited #379 report (stale integration artifact). No issue, PR, merge, or release mutation was performed; the branch was pushed only.

## Failure reproduction

Hosted release run 33954882283 failed in the final `release` job (build-macos and build-linux pass and upload their four assets). The job has no `actions/checkout`, so `gh release create` cannot infer a repository:

```
fatal: not a git repository (or any of the parent directories): .git
```

Local reproduction with the real `gh` 2.86.0 in a disposable no-`.git` directory (isolated HOME, no auth, no API contact — repository inference fails first), using the OLD command shapes:

- `gh release view v0.4.1 --json tagName` → exit 1: `failed to run git: fatal: not a git repository (or any of the parent directories): .git`
- `gh release create v0.4.1` → exit 1: same error

## What changed

- .github/workflows/release.yml (only production file, only the two gh invocations)
  - `gh release create` gains `--repo "$GITHUB_REPOSITORY"` (line 323).
  - `gh release view` gains `--repo "$GITHUB_REPOSITORY"` on the same line as the ref (line 328).
  - All existing arguments, asset paths, title/notes flags, `--json`/`--jq`, and `set -euo pipefail` preserved. No checkout step, dependency, runner, signing, or bundle change. No credential/log change (`GH_TOKEN` env untouched; nothing echoes it).

Head diff vs base: `.github/workflows/release.yml | 3 ++-` (2 insertions, 1 deletion).

## Gates (raw exits)

- YAML parse (python3 yaml.safe_load, jobs build-macos/build-linux/release present): exit 0.
- actionlint 1.7.12 on base file (scratch copy of a06ad5a): exit 1, exactly one finding — pre-existing `SC2034:warning:15:1: i appears unused` at release.yml:166 (build-linux smoke loop, untouched by this diff).
- actionlint 1.7.12 on head file: exit 1, log byte-identical to base log (`diff` clean) — no new finding introduced.
- Disposable no-`.git` shell simulation (`bash /tmp/i402-sim-run.sh`, log `/tmp/i402-sim.log`): exit 0, SIM_PASS. The two exact gh invocations were extracted from the head workflow and run against a fake `gh` argv recorder in a no-`.git` dir with `GITHUB_REPOSITORY=jirathip-dev/corral`; recorder shows both calls carry `--repo jirathip-dev/corral` (create keeps all four assets + `--title` + `--notes-file`; view keeps `--json tagName,assets` + `--jq`). No live release API was called.
- `git diff --check`: exit 0.
- `bash -n scripts/*.sh` (release build-linux "Installer unit tests" syntax gate): exit 0.
- `bash scripts/test-install-corral-linux.sh` (G4 hermetic installer suite, release pipeline's installer gate, runs on macOS): exit 0, `OK: install-corral Linux/systemd scenarios passed (21 logs ...)`. Log: /tmp/i402-installer-suite.log.

## Commits

- e2c28a87312a1c2622dee2d80bfdbc9059757498 — release: pass --repo to gh release create/view (no checkout) [.github/workflows/release.yml, +2/-1]
- (report commit follows with this file)

## Residual verification ceiling

- Hosted `release` job success on a real tag push (v0.4.2 or next) is not verified here; the fix matches the job's no-checkout design and the failure mode is reproduced and eliminated locally.
- No live `gh release` API call was made in the lane; the create/view success path beyond repo inference is exercised only through the argv-recording shim.
- Issue/PR state untouched; merge to integration belongs to the orchestrator.
