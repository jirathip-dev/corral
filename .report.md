BLOCKED — code and repository gates are green; authenticated live-device/daemon audit correlation was not reachable in this lane.

Implementation

- Implementation commit: cebc4bbbacd0f3b31d7011c79f95850c50a23b3c
- Branch: g333-diff-reliability
- Base: origin/integration at 04b42e5a1a68697d242821487e09cba3df22aef1.
- The evidence report is delivered as a follow-up commit; the final branch
  head is verified after that commit and push below.

Acceptance map

- Valid loaded page: DiffPane.apply records the loaded state and clears loading/error; covered by testSuccessfulDiffPageFoldsIntoLoadedPane.
- Valid empty page: hasLoaded distinguishes a valid zero-file/zero-line page from idle; the sheet renders "No changes in this worktree."; covered by testValidEmptyDiffPageLeavesLoadingAndShowsEmptyPane and testOffsetZeroRetryReplacesPreviouslyLoadedLines.
- ok:true missing result: folds contract_error instead of returning silently; covered by testOkTrueMissingDiffResultFoldsContractError.
- ok:true malformed/undecodable result: folds decode_error; covered by testOkTrueMalformedDiffPageFoldsDecodeError. An unparseable transport response is also classified decode_error.
- Typed refusal: folds the pane with the daemon error kind and HTTP status; covered by testTypedHTTPRefusalFoldsIntoDiffPane. Local missing-grant refusal also folds into the pane.
- Network/encoding failure: existing offline parity coverage remains green; read_diff network, timeout, and encoding paths now fold pane failures.
- Timeout: read_diff shares the existing 12-second Recent-output budget through a cancellation-safe race and folds "Diff timed out." with kind timeout.
- Dismiss/reopen: AgentDiffSheet Done and onDisappear call cancelReadDiff; the selected task/key is cancelled/detached, loading is cleared, and late responses are rejected. Request ownership is checked during deferred cleanup so an old task cannot remove a reopened request's in-flight key.
- Retry: the sheet has an in-sheet Retry action; page-zero retries replace stale lines and the retry race is covered by testRetryStartsFreshDiffRequestAfterFailure.
- Read-only/bounded behavior: read_diff grants, the existing grant/capability gate, the existing 128-file/200-line payload bounds, redaction, and worktree targeting were preserved.

Changed files

- ios/FleetNotifier/App/AppModel.swift
- ios/FleetNotifier/App/FleetStore.swift
- ios/FleetNotifier/Models/Models.swift
- ios/FleetNotifier/UI/FleetViews.swift (Diff sheet region only)
- ios/FleetNotifierTests/FleetNotifierTests.swift
- ios/check-release-demo.py (required Release source digest repin after manifest-listed Swift edits)

TDD and mutation evidence

- Disposable missing-result mutant: removed the pane fold from the ok:true missing-result branch in /tmp/corral-g333-mutation-missing. Exact focused test exited 65 and emitted TEST FAILED with an unfulfilled "diff pane reaches a terminal state" expectation. Restored AppModel.swift with RESTORE_CMP_EXIT=0; cmp reported MUTATION_RESTORE_BYTE_IDENTICAL=1; the exact focused test then emitted TEST SUCCEEDED and MUTATION_GREEN_EXIT=0.
- Disposable timeout mutant: changed the shared read timeout condition to read_tail only in /tmp/corral-g333-mutation-timeout. Exact focused timeout test exited 65 after 15.342 seconds with XCTest's asynchronous wait failure. Restored the pristine file with RESTORE_CMP_EXIT=0; cmp reported TIMEOUT_MUTATION_RESTORE_BYTE_IDENTICAL=1; the exact focused test then emitted TEST SUCCEEDED and MUTATION_TIMEOUT_GREEN_EXIT=0.
- These are behavioral RED/GREEN proofs, not source-spelling checks. No private audit identity, request content, or credential was copied into the repository.

Exact gates

1. Focused tests first

Command:
HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' -derivedDataPath /tmp/fn-dd-333 -only-testing:FleetNotifierTests/DiffReliabilityTests

Final output: FOCUSED_FINAL_EXIT=0. DiffReliabilityTests executed 10 tests with 0 failures in 12.099 seconds. The timeout test completed in about 12.045 seconds.

2. Swift parse

Command:
swiftc -parse ios/FleetNotifier/App/AppModel.swift ios/FleetNotifier/App/FleetStore.swift ios/FleetNotifier/Models/Models.swift ios/FleetNotifier/UI/FleetViews.swift ios/FleetNotifierTests/FleetNotifierTests.swift

Output: PARSE_FINAL_EXIT=0.

3. Release self-test, serialized under /tmp/corral-heavy-gate.lock

Command:
python3 ios/check-release-demo.py --self-test

Output: release-demo check: PASS (Debug source preserved; Release boundary verified); RELEASE_SERIAL_EXIT=0.

An earlier self-test invocation was started without the shared lock before the gate-serialization correction and exited 0; this overlap is recorded rather than hidden. No other lane was interrupted. The exact command was rerun after the correction with atomic mkdir acquisition and trap cleanup, and passed.

4. Simulator boot

- xcrun simctl list devices available found iPhone 16, UDID 59DDC0C5-891E-4EC0-91AF-4F50DF68D793, already Booted.
- xcrun simctl boot returned 149 because the simulator was already booted.
- xcrun simctl bootstatus 59DDC0C5-891E-4EC0-91AF-4F50DF68D793 -b returned SIM_BOOTSTATUS_EXIT=0.

5. Exact build command from the brief, serialized under the shared lock

Command:
HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 build -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/fn-dd-333 build

Output: BUILD_FINAL_EXIT=0; both build actions emitted BUILD SUCCEEDED. The only warnings were pre-existing TerminalAttach async-lock warnings and the standard AppIntents metadata warning; no new compile errors occurred.

6. Full FleetNotifier test target, serialized under the shared lock

Command:
HERDR_XCODEBUILD_DIRECT=1 HERMES_SIM_TASK_ACTIVE=1 xcodebuild -jobs 4 test -project ios/FleetNotifier.xcodeproj -scheme FleetNotifier -destination "platform=iOS Simulator,id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793" -derivedDataPath /tmp/fn-dd-333 -only-testing:FleetNotifierTests

Output: FULL_FINAL_EXIT=0; All tests executed 228 tests with 0 failures in 14.810 seconds; TEST SUCCEEDED. The logged connection/decode errors are intentional exercised failure fixtures and their tests passed.

7. Anti-slop advisory

Command:
swift run --package-path ios/tools/anti-slop-swift anti-slop ios/FleetNotifier ios/FleetNotifierTests

Raw output: ANTI_SLOP_FINAL_EXIT=1 and [anti-slop] 79 violations across 25 files (17 rules). A base-tree run also reported 79 violations. Programmatic diagnostic comparison reported BASE_DIAGNOSTICS 79, HEAD_DIAGNOSTICS 79, NEW_DIAGNOSTICS 0, RESOLVED_DIAGNOSTICS 0. The advisory was not bypassed with baseline-only edits.

Evidence classification

- XCTest ran in the real FleetNotifier app-target simulator bundle using a deterministic URLProtocol transport. It provides privacy-safe behavioral evidence for loaded, empty, contract/decode error, typed refusal, timeout, retry, cancellation, reopen, and stale-late-response states. It is not daemon audit evidence.
- The iPhone 16 simulator was booted and the app target built successfully. No authenticated live-device UI capture was claimed.
- A local corrald listener was reachable: /healthz returned HTTP 200 and /snapshot returned HTTP 200. /audit returned HTTP 401. No registration/admin credentials were supplied to this lane, so no live read_diff request was issued and no request ID could be correlated to daemon audit output. This is the remaining blocker.
- No screenshot was retained or described as proof of the Diff sheet; a board/demo or unit-test result would not prove that rendered surface.

Privacy/security/scope audit

- Diff and secret-pattern scan over the committed diff found 0 private-key blocks, 0 bearer values, 0 credential assignments, and 0 connection strings.
- No API key, token, password, secret, credential, private audit/session identity, or request content was added to the report or source.
- git diff --check passed.
- The project file and project.yml were unchanged; no source-list regeneration was needed.
- Final implementation scope is limited to the six files listed above;
  .report.md is the committed lane evidence artifact.

New defects / unresolved items

- No new test or build defect observed.
- Existing advisory baseline remains at 79 violations across 25 files; there is no new diagnostic delta.
- Remaining issue is environmental evidence only: the required authenticated live/simulator-to-daemon audit correlation could not be performed without credentials, so delivery remains BLOCKED despite green code gates.
