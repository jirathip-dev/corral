# Corral #296 egui residual client removal

## Removal inventory

- `clients/egui/src/protocol.rs`: removed plugin card/action/view types, plugin GET/POST requests, and retired fleet-identity request plus ApplyMsg variants.
- `clients/egui/src/state.rs`: removed plugin and retired fleet-identity state and reducers.
- `clients/egui/src/app.rs`: removed connect-time plugin/fleet refresh hooks, handlers, refresh methods, fields, and obsolete tests; Issues refresh remains the sole issue read path.
- `clients/egui/src/ui/issues.rs`: Issues groups now use the daemon's native issue repository key directly as the validated worktree target; removed the retired second identity catalog and alias-resolution client path.
- `clients/egui/src/web.rs`, `demo.rs`, `assets/demo-fixture.json`: removed web/demo fleet-identity request/state/fixture data.
- `clients/egui/src/model.rs`, `keys.rs`, `README.md`: removed retired identity models/comments and updated the protocol documentation.

The current Issues UI remains functional: `/issues` is hydrated by native repository identity, and its start-worktree action targets that same key. No replacement plugin or registry abstraction was added.

## Zero-residue grep

Command:

```text
python3 - <<'PY'
from pathlib import Path
terms=['PluginView','PluginCard','PluginAction','fetch_plugin','execute_plugin_action','refresh_plugin','plugin_loading','plugin_confirm','plugin_result','set_plugin','/plugins','/fleets','fetch_fleets']
root=Path('.')
for term in terms:
 hits=[]
 for base in (root/'src',root/'clients'):
  for p in base.rglob('*'):
   if p.is_file() and 'docs/design/evidence' not in str(p):
    try: text=p.read_text()
    except: continue
    if term in text: hits.append(str(p))
 print(f'{term}: {len(hits)}')
PY
```

Output:

```text
PluginView: 0
PluginCard: 0
PluginAction: 0
fetch_plugin: 0
execute_plugin_action: 0
refresh_plugin: 0
plugin_loading: 0
plugin_confirm: 0
plugin_result: 0
set_plugin: 0
/plugins: 0
/fleets: 0
fetch_fleets: 0
```

## Initial-connection proof

`cargo test -p corrald-ui --lib app::tests::initial_connection_requests_only_live_read_routes -- --exact --nocapture`

```text
running 1 test
test app::tests::initial_connection_requests_only_live_read_routes ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 163 filtered out
```

The test starts a loopback axum HTTP recorder, runs the real egui read-loop connection, drains the app's apply queue, and asserts the observed requests include `GET /events` and `GET /issues` while excluding all retired plugin and fleet routes.

`cargo test -p corrald-ui --lib app::tests::live_connected_orders_issue_and_registry_refreshes_by_identity_generation -- --exact --nocapture`

```text
running 1 test
test app::tests::live_connected_orders_issue_and_registry_refreshes_by_identity_generation ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 163 filtered out
```

This preserves the connected issue read-model behavior. The orchestrator must perform installed-app runtime verification after merge as specified by the brief.

## Release strings gate

Command:

```text
cargo build --release -p corrald-ui && bin=target/release/corrald-ui; for needle in 'GET /plugins' '/plugins/action' 'GET /fleets' 'Fleet Ops'; do if strings "$bin" | grep -F "$needle" >/dev/null; then printf 'FOUND %s\\n' "$needle"; else printf 'ABSENT %s\\n' "$needle"; fi; done
```

Output:

```text
Finished `release` profile [optimized] target(s) in 3m 12s
ABSENT GET /plugins
ABSENT /plugins/action
ABSENT GET /fleets
ABSENT Fleet Ops
```

## Gates

- `cargo fmt --all && cargo fmt --all --check` — exit 0, no output.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0; finished in 1m 20s.
- `cargo test --workspace` — exit 0; aggregate: 633 passed, 0 failed, 20 ignored across 24 suites.
- Focused connection trace test above — exit 0.
- Connected issue refresh regression test above — exit 0.
- `cargo build --release -p corrald-ui` — exit 0; finished in 1m 31s.
- Release strings gate — exit 0; all four retired-route markers absent.
