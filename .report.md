# Corral integration merge record — #547 + #548 (2026-09-15)

This file is the per-lane scratch report location in this repo. For this integration pass it
records the merge, because two concurrently-reviewed lanes each replaced it on their own branch.

Merged into `integration` (base `ed24e6f`, the accepted integration head):

| Issue | Lane branch | Exact reviewed head | Merge commit | Independent review |
| --- | --- | --- | --- | --- |
| #547 | `g547-foreground-reconnect` | `6b34b390d1af33ba4a2d4e4d4df525f32ad08b19` | `eafebfb` | PASS — `rev547-fg-reconnect-r1` (fleet-rev, claude-opus-5, read-only) |
| #548 | `g548-herd-rail-zone` | `c5ca6919eea07fc1c9b2b41e79a9ee5dfdf339dc` | this merge commit | PASS — `rev548-herd-rail-r1` (fleet-rev, claude-opus-5, read-only) |

Lane reports preserved in-tree (the branch copies were the ones that conflicted here):

- #547 implementation report: `docs/evidence/issue-547/report-round2.md` (round-1 blocker report:
  `docs/evidence/issue-547/report-round1.md`).
- #548 implementation report: `docs/evidence/issue-548/lane-report.md`.

Merge resolution performed by the orchestrator:

1. `.report.md` — this record replaced the two conflicting lane copies; both lane reports are
   preserved at the paths above.
2. `ios/check-release-demo.py` — both lanes re-pinned `APPROVED_RELEASE_SOURCE_DIGEST` for their
   own branch (branch-local by design). The merged tree unions the #547 app-source delta
   (AppModel/FleetStore/HostStreamCoordinator/FleetViews) with the #548 UI delta (HerdView), so the
   release digest was recomputed over THIS merged tree with the canonical algorithm
   (`ios/release_source_manifest.release_source_digest`) →
   `d8997fa8b255ffdf4f2042179517bb0d84dec9ddd083276be2e2150c513e247e`; the test-source digest is
   unchanged (`f8e9b422…`, only #547 touched that file). The checker was not weakened: the only
   non-comment change is the digest literal.

Open human/physical gates (both issues stay OPEN — owner device evidence):

- #547: physical-iPhone before/after per-stage timings against a loaded Bazzite host and the Mac
  host (protocol: `docs/evidence/issue-547/device-protocol.md`). No device attached to this host.
- #548: physical-iPhone check of the empty-rail (a) and 1-blocked (b) states.

No `main`/release/TestFlight promotion, no PR was created, no daemon change.

---

# Issue #550 — minimal rustls advisory remediation

Worktree: `/Users/jirathip/.herdr/worktrees/corral/fix-rustls-advisory`.
Branch: `g549-rustls-advisory-fix`.
Pinned base: `d365a38116ddd90a79914f7f8a4ed4147aa69039`.
The preceding integration record is preserved byte-for-byte as this file's prefix.

## Change and resolver scope

Ran `cargo update -p rustls --precise 0.23.45`, raw exit **0**,
log `/tmp/g550-update.log`. Its version-update line was:

```text
    Updating rustls v0.23.43 -> v0.23.45
```

| Package | Base | Delivered | Other changes |
| --- | --- | --- | --- |
| rustls | 0.23.43 | 0.23.45 | crates.io checksum for the patched release |

No transitive package version changes, additions, or removals. Parsing both
lockfiles with Python `tomllib` and comparing every package record yielded:

```text
package_deltas = [('rustls', '0.23.43', '0.23.45', ['checksum', 'version'])]
```

The resolver initially also changed tempfile 3.27.0's edge from the already-locked
getrandom 0.4.3 to the already-locked 0.3.4. That unrelated edge change was restored
to its base value before any GREEN gate. The installed tempfile manifest allows
`>=0.3.0, <0.5`; `cargo tree --locked -p rustls` and the locked deny gate both
accepted the restored edge. The delivered diff therefore contains only rustls's
version/checksum, not this incidental resolver churn. No Cargo.toml or policy
change was needed; `deny.toml` retains `ignore = []`.

`cargo tree --locked -p rustls` exited **0**, log `/tmp/g550-tree.log`:

```text
rustls v0.23.45
```

Filtered `git diff Cargo.lock` version lines captured before committing:

```diff
 name = "rustls"
-version = "0.23.43"
+version = "0.23.45"
```

## RED before changing the lockfile

Both commands ran at the pinned base, before the update, with no tracked changes:

```sh
cargo deny --locked --workspace check > /tmp/g550-deny.log 2>&1; echo DENY_EXIT=$?
cargo audit > /tmp/g550-audit.log 2>&1; echo AUDIT_EXIT=$?
```

Raw results: `DENY_EXIT=1`, `AUDIT_EXIT=1`. Their complete logs were copied to
`/tmp/g550-base-deny.log` and `/tmp/g550-base-audit.log` before the GREEN runs
reused the brief's filenames. Neither failure was an advisory-DB/network failure.

Verbatim base deny lines:

```text
error[vulnerability]: TLS 1.3 handshake messages incorrectly accepted across encryption level boundaries
    ├ ID: RUSTSEC-2026-0285
    ├ Advisory: https://rustsec.org/advisories/RUSTSEC-2026-0285
advisories FAILED, bans ok, licenses ok, sources ok
```

Verbatim base audit lines:

```text
Crate:     rustls
Version:   0.23.43
Title:     TLS 1.3 handshake messages incorrectly accepted across encryption level boundaries
Date:      2026-09-14
ID:        RUSTSEC-2026-0285
URL:       https://rustsec.org/advisories/RUSTSEC-2026-0285
Severity:  5.3 (medium)
Solution:  Upgrade to >=0.23.45

error: 1 vulnerability found!
```

## GREEN gate evidence

Runner: native macOS Cargo/rustc **1.97.1**, cargo-deny **0.20.2**,
cargo-audit **0.22.2**, from the named worktree root. No repository justfile,
AGENTS.md, or CLAUDE.md exists at this root; direct commands below are the brief's
entry points. No code-structure search was needed or performed; ast-grep was
loaded but not invoked. Exact metadata/log searches used Hermes file tools.

Precommit verification completed successfully on the final dependency/source
bytes. The same complete command set is the final committed-head gate before
push; `/tmp/g550-final-gates.log` records that invocation's HEAD and raw exits.
It does not use test-output pipelines. Cargo commands run sequentially, never
concurrently within this lane. No gate flags were relaxed.

| Exact command | Raw exit | Log |
| --- | ---: | --- |
| `cargo deny --locked --workspace check` | 0 | `/tmp/g550-deny.log` |
| `cargo audit` | 0 | `/tmp/g550-audit.log` |
| `cargo fmt --check` | 0 | `/tmp/g550-fmt.log` |
| `cargo clippy --all-targets -- -D warnings` | 0 | `/tmp/g550-clippy.log` |
| `cargo test --workspace` | 0 | `/tmp/g550-test.log` |
| `cargo build --release` | 0 | `/tmp/g550-release.log` |
| `git diff --check` | 0 | `/tmp/g550-diffcheck.log` |

Exact shell commands (each raw status is captured immediately):

```sh
cargo deny --locked --workspace check > /tmp/g550-deny.log 2>&1; echo DENY_EXIT=$?
cargo audit > /tmp/g550-audit.log 2>&1; echo AUDIT_EXIT=$?
cargo fmt --check > /tmp/g550-fmt.log 2>&1; echo FMT_EXIT=$?
cargo clippy --all-targets -- -D warnings > /tmp/g550-clippy.log 2>&1; echo CLIPPY_EXIT=$?
cargo test --workspace > /tmp/g550-test.log 2>&1; echo TEST_EXIT=$?
cargo build --release > /tmp/g550-release.log 2>&1; echo RELEASE_EXIT=$?
git diff --check > /tmp/g550-diffcheck.log 2>&1; echo DIFFCHECK_EXIT=$?
```

Workspace test summaries aggregate to **479 passed, 0 failed, 10 ignored** across
21 test-result summaries (including doc tests). Both corrald and corrald-client
compiled and passed. The release build completed successfully for both packages.
The fmt and diff-check logs are empty, with raw exits 0.

Verbatim GREEN deny summary:

```text
advisories ok, bans ok, licenses ok, sources ok
```

Verbatim GREEN audit output:

```text
    Fetching advisory database from `https://github.com/RustSec/advisory-db.git`
      Loaded 1246 security advisories (from /Users/jirathip/.cargo/advisory-db)
    Updating crates.io index
    Scanning Cargo.lock for vulnerabilities (356 crate dependencies)
```

`RUSTSEC-2026-0285` occurs twice in each base log (ID and URL), and zero times in
each GREEN log. Absence is a literal-string count, not a fabricated "no advisory"
line. Existing cargo-deny duplicate/unencountered-license warnings remain allowed
by the unchanged policy; they did not cause a nonzero exit. This lane uses the
brief's exact `cargo audit`, not a claim of running `cargo audit --deny warnings`.

## Scope and hash proof

The only implementation file changed is `Cargo.lock` (2 insertions, 2 deletions).
The sole additional tracked change is this required `.report.md` append.

The final committed scope command is:

```sh
git diff --name-only d365a38116ddd90a79914f7f8a4ed4147aa69039..HEAD
```

Required result, verified in `/tmp/g550-scope-final.log` before push:

```text
.report.md
Cargo.lock
```

`git diff --stat d365a38116ddd90a79914f7f8a4ed4147aa69039..HEAD -- src/ crates/ Cargo.toml deny.toml`
is empty. The broader precommit source diff, including tests, workflows, and
rust-toolchain.toml, also exited 0 with no output.

Git tree/blob identifiers are identical at base and delivered HEAD:

| Path | Base and HEAD object ID |
| --- | --- |
| src | `91d2b59c4b6b14c43f44ada6b0d95525667d142a` |
| crates | `bc877be2094428c7e60026c3c7d5ab33909c59ee` |
| Cargo.toml | `b54faebe06e92ab0a00149403e8c8c20d725c019` |
| deny.toml | `09ce0a7b8b1d3e2b37ac8ac9b87a0b30608c71e7` |

Commands: `git rev-parse <base>:<path> HEAD:<path>` for each row; the final scope
receipt rechecks these after committing. Cargo.lock SHA-256 on the tested bytes:
`2ba277f3ff41e16622ec1ce64938370ea050d303955d75e3d5fec1533682b99b`.
The final scope receipt compares the committed and working lockfile to this hash
and checks that the package-record delta is exactly the rustls version/checksum.

Precommit package/hash/count receipt: `/tmp/g550-scope-precommit.log`, raw exit 0.
Postcommit scope/diff receipt: `/tmp/g550-scope-final.log`.

## Delivery boundary and honest non-claims

Only the named branch is authorized for `git push -u origin g549-rustls-advisory-fix`.
The final delivery receipt `/tmp/g550-delivery.log` records local HEAD,
`git rev-parse origin/g549-rustls-advisory-fix`, and
`git ls-remote --heads origin refs/heads/g549-rustls-advisory-fix` readback.
No force push, reconciliation, PR creation/comment/modification, merge, issue
mutation, main action, deployment, or TestFlight action belongs to this lane.
Dependabot PR #525 was not folded in. Swift/iOS sources were untouched.

No hosted Rust CI result is claimed. Contrary to the brief's shorthand "PRs
only", the checked-in Rust workflow also runs on main pushes and a schedule;
it does not run on a push to this branch alone. Other push-triggered workflows
may run, so this report does not claim that no hosted workflow of any kind was
triggered. The orchestrator owns hosted Rust verification at the integration
head via PR #549. Local gates are macOS, not hosted Linux. Coverage, ignored/live
integration tests, live outbound TLS, and deployed daemon behavior were not
separately exercised or claimed. There is no local gate blocker.

The pre-existing untracked `.hermes-context.md` is a supplied harness/gate file,
not an implementation change; it is preserved and not committed. Apart from this
permitted gate file, the tracked worktree must be clean at delivery. No source,
manifest, or advisory-suppression exception was needed.
