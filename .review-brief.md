# ADVERSARIAL REVIEW — branch g62/transcript-readers (#62, local-only)

Independent read-only reviewer. The implementer was the orchestrator (a
different model from you); both deepseek routes were down — judge the work
on its merits, harder if anything. Scope: `git diff origin/main...HEAD`
(one commit) + `.report.md`. The branch is LOCAL (not pushed — the user
merges later); your verdict file is the parked review of record.

## HARD RULES

READ-ONLY; you MAY run bounded test commands if needed
(`CARGO_TARGET_DIR=/Users/jirathip/Projects/corral/target cargo test --lib transcript`,
`... --lib redact`) but no builds beyond that, ONE cargo command at a
time, and NEVER touch live stores (`~/.local/share/opencode/opencode.db`,
`~/.claude`, `~/.codex`) or the live registry. Write `.review-findings.md`
INCREMENTALLY; end with `REVIEW COMPLETE`.

## PRIORITY TARGET: the `core/redact.rs` change

This diff modifies shared SECURITY code (the D-083 redaction used by push
and drive paths) to fix a measured O(n²) (35s on a 64KiB unbroken blob).
Attack the memoization's correctness argument to destruction:

- Can any input match `high_entropy_at` from an interior position of a run
  whose full scan failed? (The claim: suffixes are shorter with a subset
  of character classes.) Consider runs ENTERED mid-way via `i = end` after
  a prefix/env match — is `entropy_cleared_until` ever stale across span
  edges? Construct adversarial inputs (token-glued runs, span-edge
  boundaries, UTF-8 multibyte neighbors).
- Does the fix change ANY existing redaction outcome? The 23 pre-existing
  tests pass — find an input they don't cover where behavior differs.
- Is the regression test's 1.5s debug bound sane for CI?

## Transcript module surfaces

1. JSONL backwards pager: off-by-ones at chunk boundaries (line exactly at
   `lower`, file not ending in newline, CRLF, empty lines, cursor==0,
   cursor==len, cursor mid-line — what happens and is it safe?); the
   short-page path when a range is all-skipped; can the walk ever loop
   forever or drop a line straddling the scan-cap boundary?
2. Opencode: SQL injection via session_id/cursor id (the quoting rule —
   is doubling `'` complete against all inputs incl. NUL, newline?);
   LEFT JOIN row multiplication when a message has MULTIPLE text parts
   (does one message emit N entries? is that the intended shape?);
   cursor stability when two messages share time_created.
3. Redaction-before-boundary: any path where Entry.text escapes without
   redact() (role? — roles are attacker-influenced strings too, judge).
4. Deviations flagged in the report (db_path parameter, ts: Option) —
   defensible?
5. Tests: do they pin the claims? Name the gaps.

## Output

`.review-findings.md`: verdict APPROVE / REQUEST-CHANGES, numbered
findings with severity + file:line + concrete failure scenario. End with
`REVIEW COMPLETE`.
