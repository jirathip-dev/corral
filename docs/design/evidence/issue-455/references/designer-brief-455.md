# Corral #455 — authorized designer prototype run

Guy explicitly authorized designer to produce this prototype. English only. Role=designer; execute artifacts, not merely a plan. Read https://github.com/jirathip-dev/corral/issues/455 and its comments plus #456–460 for child boundaries. Scope is design-only, NOT Corral implementation. #455 filing-only status is superseded for prototype creation by this dispatch; all implementation children remain UNROUTED. Fleet stays paused.

## Workspace and authoritative sources
Write only under /Users/jirathip/design-output/corral/455-immersive-herd/. Preserve all other dirty/untracked design-output projects. No product edits, repo commits, pushes, PRs, issue creation, profile/config/credential edits, fleet spawns/resumes, release or main changes in this run. Deliver locally for conductor verification; archive promotion is separately gated here.
Source checkout /Users/jirathip/Projects/corral is behind main and lacks Herd in its checked-out tree. Read exact CURRENT source using git show 86d1287a9e5c611ec6bef677c95544c53e71b982:ios/FleetNotifier/UI/Herd/HerdView.swift (and RanchEnvironment.swift, HerdModel.swift, HerdArt.swift, FleetViews.swift, AppTheme.swift, AppModel.swift). Do not checkout/reset/edit that repo. Existing accepted proof: /Users/jirathip/design-output/corral/442-herd-view/; also 427-filter-header and 371-372-board-theming. Read README/manifests and inspect visual references before changing anything. Use accepted V1 panorama + R2 ranch environment + original V1 horse art and corrected grazing anatomy. No alternate ranch or redesigned horses. Existing #448 horse gait and #449 ordering are not implementation tasks in this run.

## Owner-selected choices — verbatim scope
- Floating top HUD: scope + Settings, compact bottom paddock navigation.
- Settings controls the saved mode and applies immediately; remove top switch; retain Open Board recovery action.
- Full gentle ambience: shared wind, swaying leaves/grass, drifting clouds and slow night-sky motion.
Full-screen means ranch scene reaches screen edges behind safe areas, not hiding status bar/home indicator or putting controls under the notch. Native iOS shipping is procedural SwiftUI Canvas/Path with distinct exportable layers, not flattened scene rasters or video. Browser prototype may use faithful SVG/Canvas/CSS equivalents as disposable design proof; do not misrepresent as native rendered evidence.

## Two variants within locked layout
A: compact single-row glass scope/settings HUD with concise status. B: more readable two-level status/scope cluster, still floating top HUD with bottom navigation. Different hierarchy/density, not palette-only. Recommend one. Do not reopen panorama/map choice. Match ranch Day/Night control treatment and filter sheet; Board and general Settings retain app Catppuccin theming. Day/Night/Auto environment independent of app theme.
Proposed edge defaults, label clearly as awaiting approval: no saved mode -> Board; unknown value -> Board. Settings choice saves+applies. Open Board recovery is temporary and does not silently change saved preference; foreground alone doesn't reverse recovery; cold reload restores preference. Explicit Settings selection wins immediately.

## Required deliverables
1. index.html mobile comparison gallery linking functional A/B variants. Each has Day/Night, filters, Settings Board/Herd persistence simulation, readable host/repo scope, status counts, blocked front rail, bottom paddock controls, Recent Output placeholder interaction explicitly synthetic, offline/empty/dense states. No real host/user/secrets/data. Use coherent fictional fixture set and deterministic counts.
2. Gentle native-feasible foliage/wind/cloud/slow star-field motion. Existing source already has grass sway, tiny cloud oscillation and twinkle; tree canopy static. Preserve scene/horse art; visibly improve environmental motion without storms/shooting stars/audio. Reduced Motion freezes intentionally; background/obscured scene pauses unnecessary work; no gratuitous animation. Motion must be observable at phone scale.
3. Labeled PNGs at 390x844 for A/B Day/Night, open filter and Settings; additional small/large-phone/Dynamic-Type-equivalent and offline/empty proof where useful. Short real-time MP4/WebM motion clips for day foliage/clouds and night drift. Show time compression only if explicitly labeled; also provide real-time proof.
4. README with recommendation, acceptance matrix mapping #455 and child scopes to evidence, proposed defaults, limitations and unverified native/device claims. SHA-256 manifest plus reproducible verifier/render scripts under tools/. Keep scripts as files; don't rely on inline python -c/-e approvals.
5. Browser verification: real loaded pages, console errors, safe-area geometry, controls >=44px, contrast over day/night, filter/reset and mode persistence interaction, reduced-motion/pause behavior, no clipped labels. Use browser/Playwright tools available. Inspect key rendered images before declaring done. No claims based solely on source strings.
6. Phone gallery URL only if actually reachable. Current tailscale wrapper points to missing /Applications/Tailscale.app/Contents/MacOS/Tailscale; don't reinstall/change shared Serve/network config. You may verify existing localhost :8777 and existing served gallery if possible, else report link delivery blocked and provide local paths/PNGs. Do not fabricate live URL reachability.

## Finish contract
Run to completion with real verification evidence. If model/provider unavailable, STOP and report; no silently substituting another model. Do not spawn other models or delegates. Report exact artifact paths, real verification results, failures/limitations, recommended variant. Stop at PROTOTYPE — awaiting Guy approval. Do not mark any implementation child complete or route it. No product commits/push/release. Parent receives terminal completion and verifies before presenting.
