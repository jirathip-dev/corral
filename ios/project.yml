name: FleetNotifier
options:
  bundleIdPrefix: com.corral
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  groupSortPosition: top
settings:
  base:
    SWIFT_VERSION: "5.9"
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    GENERATE_INFOPLIST_FILE: YES
    CODE_SIGN_STYLE: Automatic
    CURRENT_PROJECT_VERSION: 1
    MARKETING_VERSION: "0.1.0"
targets:
  FleetNotifier:
    type: application
    platform: iOS
    sources:
      - FleetNotifier
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.corral.fleetnotifier
        PRODUCT_NAME: FleetNotifier
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_CFBundleDisplayName: Fleet Notifier
        INFOPLIST_KEY_NSLocalNetworkUsageDescription: "Connects to your corrald daemon on the local network (Tailscale) to show the fleet and send signed commands."
        INFOPLIST_KEY_NSAppTransportSecurity_NSAllowsLocalNetworking: YES
        INFOPLIST_KEY_NSFaceIDUsageDescription: "Face ID step-up is required before destructive commands are sent to agents."
        INFOPLIST_KEY_NSUserNotificationsUsageDescription: "Fleet Notifier shows a local notification when an agent blocks, so you can answer from the lock screen."
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_GENERATE_ASSET_SYMBOLS: YES
  FleetNotifierTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - FleetNotifierTests
    dependencies:
      - target: FleetNotifier
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.corral.fleetnotifier.tests
        GENERATE_INFOPLIST_FILE: YES
schemes:
  FleetNotifier:
    build:
      targets:
        FleetNotifier: all
        FleetNotifierTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - FleetNotifierTests
    archive:
      config: Release
